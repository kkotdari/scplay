# scplay 작업 규약

## 도록(카탈로그) 뽑기
"도록 뽑아줘"는 아래 한 줄이다. 파일명·조건은 스크립트가 정한다 — 손으로 바꾸지 않는다.

    node scripts/doc-catalog.mjs --out <scratch>/dorok

결과: `1. terran_units_blue.png` `2. protoss_units_blue.png` `3. zerg_units_blue.png`
`4. terran_bldgs_blue.png` `5. protoss_bldgs_blue.png` `6. zerg_bldgs_blue.png`
`7. extra.png` `list.txt`, 그리고 상위 디렉터리의 `도록.zip`. zip을 사용자에게 보낸다.
조건(4방위 45·135·225·315 · narrow · 흰 배경 · 임자색 #2b62e8 · 폭 660 · dpr 3)도 스크립트 안에 있다.
그림은 **GL 붓**(`DocIcon9` → gl9 메시, 지도가 그리는 그 그림)이 기본이다(2026-09) — 키값·마주 봄 판정 같은 2D 전용 어긋남이 도록에
안 실린다. 옛 2D 면 그림(ShapeIcon SVG)은 `--2d`(폰·`#gl=0` 이 아직 그 길이라 검토용). 앱 도록(scplayer GalleryScreen)도 DocIcon9 다.

## 도록의 칸(2026-09)
★ **칸을 세우는 일은 scplay 가 한다**(`docCellsOf9(kind, t, yaw)`) — 앱(GalleryScreen)은 배치만 한다.
칸은 넷이고 **있는 것만** 선다: 대기 · **이동**(유닛)/**활성**(건물) · 공격 · 액션.
· **공격 칸에 트레이서를 겹친다**(`DocTracer9 overlay`) — 따로 세우면 총알만 덩그러니 남아 무엇이 쏘는지 안 보인다.
· **액션 칸이 변신을 맡는다** — 시즈 모드는 탱크의, 럴커 버로우는 럴커의 액션 칸이다(요청: 별도 목록 없앰).
· ⚠ **값은 지도의 값을 그대로 쓴다** — 앱이 제 숫자를 들면 조용히 어긋난다(실측: 도는 걸음을 초당 4칸으로
  베껴 두어 지도(17.6칸)의 5분의 1로 끊겼고, 코어 디스크가 **쉬는 중에도** 돌아 '업그레이드 중'이라는 신호가
  못 됐다). 늘 도는 것 2.2바퀴/초 · 일할 때만 도는 것(포지·코어·머신샵) 1.6 · 불빛 깜빡임 0.9초의 3분의 2 ·
  성큰 사격 1.34초. **활성은 켜 둔 한 컷이 아니라 깜빡임이다.**
· ⚠ **창(배율)은 칸마다 따로 잰다** — 한 창으로 묶으면 가장 큰 몸에 맞춰져 알·고치가 점이 된다. 칸 **안에서는**
  못 박는다(그 칸이 그릴 수 있는 종류 × 여덟 각의 합집합) — 돌려도 컷이 바뀌어도 배율이 안 흔들린다.
· ⚠⚠ **도록 표(SHAPE_GALLERY)에서 항목을 지우지 마라 — 숨겨라**(`hidden: true`). 광택 표(`glossOf9`)가 그
  표로 종족을 찾으므로, 지우면 그 모델이 제 종족 광택을 잃고 '그 밖' 값으로 그려진다(터렛을 가를 때 적어 둔
  그 함정이다). 지금 숨긴 둘: `tanksiege` · `sunkenfire`.
· ⚠ **갈라 놓은 별본은 `docBldKind9` 에 적어라**(sunkenrear·sunkentongue·turretbase·turrethead) — 표 밖이라
  이 문이 거짓을 내면 아이콘이 **유닛 길**로 굽고, 유닛 길에는 딸림 부품이 없어 혓바닥·포탑이 통째로 빠진다
  (지적: "성큰 … 공격 애니메이션도 안 나오고").
· ★★ **트레이서는 칸의 자가 아니라 모델의 자로 그린다**(2026-09, 지적: "모델의 좌표랑 안 맞네 —
  실제 재생기에서처럼 보이길 원했던 건데, 회전하면 트레이서도 그에 맞게 회전되고") — 지도가 쓰는
  자 셋을 그대로 가져온다: **자리**는 총구 앵커표(`muzzlePoint` · 건물은 `BLD_MUZZLE`, 머리가 도는
  종류는 `spinMuzzle9`)가 낸 16-상자 점을 칸에 모델을 앉히는 식(원점 8·12 · 창을 칸에 맞추는 배수 k)에
  태운 것, **방향**은 몸의 요잉(겨누는 포탑이 있으면 그 각), **배율**은 `k·16 ÷ 그 종류의 지도 타일 수`.
  · ⚠ 여기서는 **MODEL_NORM 을 안 곱한다** — 도록은 창을 잉크 상자에 맞추므로 그리기 배수가 k 에 이미
    들어 있다(지도는 판 배수를 따로 곱한다 — 자가 다르다).
  · ⚠⚠ **`tilePx` 에 타일 px 를 넣지 마라** — 붓은 `tz9 = zoom · tilePx/8` 로 갈래표의 굵기를 키우므로
    둘 다 넣으면 **제곱**이 된다(실측: 마린 줄기가 칸을 통째로 덮었다). `zoom = 타일px/8 · tilePx = 8`.
  · 배율을 8 로 못 박아 두면 모델이 칸을 꽉 채운 도록에서 줄기만 지도 크기로 남아 **안 보인다**
    (지적: "파뱃 … 안 나오는 게 아니라 작아서 안 보인 거였어").
· ★ **별본은 제 본체 바로 뒤에 선다**(2026-09, 요청) — 애드온은 제 본 건물 뒤, 시즈 모드는 탱크 뒤,
  짐 든 일꾼은 그 일꾼 뒤다. 한때 "혼자 못 서는 것들이 목록을 토막 낸다"며 끝으로 몰았던 손질을
  되물렸다 — 붙는 자리가 곧 그 모델의 뜻이라 옆에 있어야 읽힌다.
· ★ **전차포는 선이 아니라 셋이다**(`NO_BEAM_FX` + `MUZZLE_BURST_FX9`) — ① 총구에서 터지는 타원 불꽃
  ② **포물선을 나는 에너지포**(`FX_IMPACT.tankshell` · 가로로 납작 · 나는 몫 `u^1.45` 가속)
  ③ 착탄 자리의 같은 그림, 더 넓게(스플래시). 타원은 `FX_IMPACT.flat` 이다.
  · 나는 시간은 **쿨다운의 12%·상한 0.16초** — 원작은 쏘는 것과 맞는 것이 거의 같은 순간이다.
  · ⚠ 박자는 **진짜 쿨다운(fxCdRaw)** 이다 — NO_BEAM 자리의 `fxCd` 는 '번쩍임 주기'(≤0.32초)라
    그것으로 재면 전차포가 기관총처럼 연사한다(커세어 flare 는 그 번쩍임이 맞다).
  ⚠ 도록의 hit op 은 `splash: true` 와 `dist: 0` 을 꼭 준다 — splash 가 없으면 붓이 **파편**으로 그리고,
    dist 를 비우면 기본값(상자의 0.71배)만큼 **옆으로 밀려** 난다.
· ★ **추진체 불은 이동 칸의 것**(`POSE_KINDS.thrust`) — 다리로 걷지 않는 몸(나는 것·호버·SCV·프로브)은
  걸음 컷을 지어내지 말고 이 칸을 켠다: 움직이면 자세 1, 빌더는 **그 자세에서만** 불꽃을 낸다.
  ⚠ 짐 든 별본(scvMin·probeGas…)도 표에 적어야 한다 — 그리는 종류가 갈리므로 본체만 적으면
    자원을 나르는 동안 불이 꺼진다(가장 많이 움직이는 때다).
· ★ **준비 동작은 공격 칸이 아니라 액션 칸이다**(2026-09, 지적: "고스트 공격 모션은 총을 들고 퉁퉁 앞뒤
  반동 주는 거여야 하고 총을 꺼내는 건 기타 액션으로 가야 해") — 고스트의 옛 공격 한 바퀴는 '반쯤 →
  겨눔 → 반쯤 → 등에 멤'이라 **쏠 때마다 총을 넣었다 꺼냈다** 했다. 원작도 그렇지 않고, 그것은 사격이
  아니라 준비 동작이다. 공격 칸은 겨눈 채 반동만 치고(쿨다운 앞 18% 만 뒤로 밀림), 꺼내는 길은
  `DOC_ACT_POSE9`(자세 컷 몇 장을 차례로 도는 액션)로 옮겼다.
  · ⚠ **반동을 '든 몫'에 묶지 마라** — 고스트는 `kick = 0.18 · at` 이라 겨누기만 하면 총이 **늘 밀린
    자리에 서 있었다**. 곧 반동이 아니라 자세의 일부였다. 퉁퉁 치려면 같은 겨눔 안에 밀린 컷과
    안 밀린 컷이 **둘 다** 있어야 한다(겨눔 2 = 0 · 쏜 순간 POSE_ATK_R = 0.34 · 평균이 옛 값이다).
  · ⚠ 그 대신 지도에서 '등에 멤 → 겨눔'이 교전에 들어설 때 **한 번** 튄다 — 쏠 때마다 튀던 것을 교전마다
    한 번으로 줄인 것이고, 그 한 번까지 매끄럽게 하려면 '방금 교전에 들어섰나'를 엔진이 알아야 한다.
· ★ **'두 줄기'와 '두 칸'은 딴 이야기다**(2026-09, 지적: "줄기가 두 개인 거 말한 건 아니고 — 그건
  또 그거대로 표현되어야 하지만") — 칸을 가르는 자는 **지상·대공 무기가 다른가**이고, 줄기가 둘인
  것은 **그 무기의 발사관이 좌우 한 쌍인가**(미사일: 골리앗·레이스·발키리·스카우트 대공)다. 지도는
  이미 둘로 그리는데(engine9 의 `twin9`) 도록만 한 줄이라 무기의 꼴이 달랐다.
  · 자도 지도의 것을 그대로 쓴다: **두 발 전체의 폭이 몸 폭 하나**(바깥 가장자리끼리 = 잉크 폭)이고,
    잔상이 몸보다 굵으면 0 으로 죄어 한 줄로 겹친다(작게 그릴 때는 어차피 한 획으로 읽힌다).
  · ⚠ 빼는 값은 **실제로 그려지는 굵기**(w × 배율 × 3.4)여야 한다 — 갈래표의 w 는 상대 값이라 그대로
    빼면 단위가 섞인다(지도에서 한 번 물렸던 그 자리다).
· ★ **지상·대공이 다른 무기면 공격 칸이 둘이다**(2026-09, 요청: "공격 트레이서가 두 종류 이상
  (골리앗 스카우트 배틀 등)인 경우 나눠서 두 셀로 표시") — 골리앗·스카우트는 총구 스파크 ↔ 미사일,
  레이스는 레이저 ↔ 미사일로 **아예 다른 무기**인데, 도록은 표(ATTACK_FX) 한 줄만 보고 늘 지상
  것만 그렸다. 그 갈래가 **그리는 자리에 인라인으로 박혀** 있어서다 — `attackFxOf9(이름, air)`
  공용 문으로 빼서 지도와 도록이 같은 수를 본다(`docAtkFx9` 가 칸 목록을 낸다).
  · ⚠ **다를 때만 가른다** — 배틀크루저는 두 무기가 같은 그림(레이저)이라 두 칸이 똑같아 값이 없다.
    가르고 싶으면 무기 갈래를 먼저 둘로 만들어야 한다(도록이 아니라 표의 일이다).
  · ⚠ 못 때리는 표적은 칸이 안 선다 — `weaponVs(프로필, air)` 가 없으면 그 쪽 무기가 없는 것이다
    (발키리·커세어는 대공 한 칸 · 파이어뱃·질럿은 지상 한 칸).
  · 칸마다 제 갈래를 `DocCell9.fx` 로 싣고 `DocTracer9 fx` 가 그것을 받는다(안 주면 종류의 기본).
· ★ **혼자 안 서는 몸은 제 주인의 칸에 든다**(2026-09, 요청: "인터셉터는 캐리어 공격에 포함시키고
  목록에선 제거 · 스캐럽 마인도 목록에선 제거 · 벌처는 마인 심기를 기타 액션으로") — 캐리어·리버는
  **제 몸으로 안 쏜다**(내보낸 것이 쏜다), 벌처의 마인은 무기가 아니라 심는 것이다. 목록에 따로
  서면 '이게 뭐지'가 되고, 주인의 칸에 들면 곧 그 뜻이다.
  · 공격 칸이 대신 그린다: `DOC_ATK_KIND9`(캐리어 → 인터셉터 · 리버 → 스캐럽) — 트레이서도
    **그 몸의 것**이라야 한다(캐리어의 burst 가 아니라 인터셉터의 gun).
  · 액션 칸: `DOC_ACT_KIND9`(벌처 → 마인) · 몸은 그대로 두고 **한 발만** 겹치는 것은 `DOC_ACT_FX9`
    (배틀크루저 → 야마토포. 요청: "배틀은 야마토를 말한 거였는데 그건 기타 액션에 넣어 줘").
    ⚠ 야마토는 지도에서 **캐스트**(표적 자리의 빛덩이)라 갈래표에 없었다 — 도록이 한 발을 그리려면
      제 갈래가 있어야 해서 같은 색 결로 `FX_BEAM.yamato` 를 새로 적었다.
  · ⚠ 목록에서 빼는 것은 **`hidden: true`** 다(지우지 마라 — 광택 표가 이 표로 종족을 찾는다).
    `doc-catalog` 는 정규식이 `race: "…" }` 로 끝나는 줄만 잡아 **저절로** 빠지고, `doc-sheet` 는
    `!g.hidden` 을 손으로 걸어야 한다.
· 성큰의 사격은 **선이 아니라 `erupt` op** 한 장이다(표적 발밑에서 솟는 주황 가시) — FX_BEAM 의 "spike"(럴커
  가시)를 빌려 쓰면 **작은 삼각형**이 된다(지적).

## 도록의 액션은 '두 그림 갈아 끼우기'가 아니라 애니메이션이다
★★ **지도가 여러 판으로 그리는 동작은 도록도 여러 판으로 그린다**(2026-09, 요청: "도록 팝업에서
탱크 변신 장면을 재생기와 똑같이 애니메이션으로 보여 줘 — **앞으로 다른 액션도 모두 애니메이션으로
올려야 돼**") — 시즈 모드의 액션 칸은 합본 `tank` 와 `tanksiege` 를 1.1초마다 갈아 끼우고 있었다.
그것은 변신이 아니라 **깜빡임**이고, 지도가 보여 주는 세 마디(돌기 → 포신 갈아 끼우기 → 쳐올리기)가
통째로 빠진다. 한 칸이 한 판만 그릴 수 있던 것이 그 까닭이었다(`attach` 한 자리뿐).
· **`DocCell9.parts`** 가 그 자다 — 몸과 **같은 자리·같은 창**에 판을 차례로 얹고 각자 제 자세·요잉을
  쥔다. 시즈 전환은 지도와 똑같이 **차체 `tankbody` + 버팀다리 홑판 둘 + 전환 포탑 `tankturretxf`** 다.
  길은 셋 다 열어 두었다: `gl9.GlIconReq9.parts`(GL) · `ShapeIcon.parts`(2D 폴백) · `DocIcon9.parts`.
· **시계도 지도의 것을 그대로 쓴다**(`docSiegeCell9`) — 창 `SIEGE_XF_SEC`(1.5초) · 앞 35%
  (`SIEGE_TURN_U9`)는 도는 몫 · 나머지가 기계 마디(engine9 `xfMech9`) · 포탑 자세는 창 전체의
  smoothstep(engine9 `gunU9`). **새 숫자를 지어내지 마라** — 지어내면 도록이 재생기와 딴 속도로 돈다.
· 한 바퀴는 **접기 → 접힌 채 → 펴기 → 편 채**다(머무는 몫 0.8초) — 머무는 몫이 있어야 두 끝 모습이
  눈에 남는다. 그 두 몫에서는 지도와 같은 **정착 판**(`tankgun` · `tanksiegegun`)으로 갈아탄다.
· ⚠ **도는 각만 도록의 것이다** — 지도는 참값 각에서 −45(`SIEGE_FACE_DEG9`)로 돌지만 도록엔 참값이
  없고 요잉은 손가락이 쥐고 있다. 그래서 '제자리에서 90도'로 보인다(그 마디가 **있다**는 것이 뜻이고,
  어느 절대각이냐는 도록에서 뜻이 없다).
· ⚠⚠ **칸의 창은 겹판까지 아울러 재야 한다** — 앱(GalleryScreen `boxOf`)이 `c.kind`·`c.attach` 만
  모으고 있었다. 빼면 창이 차체에만 맞춰져 **뻗은 버팀다리와 뒤로 나온 시즈 포신이 잘린다**.
  `doc-sheet --anim` 도 같은 손이 필요하다(둘 다 고쳤다).
· 🔎 보는 자: `node scripts/doc-sheet.mjs --anim --kinds tank`(GL) · `--2d`(폴백) — 여덟 시각의 칸이
  한 줄로 서므로 마디가 빠진 자리가 눈에 바로 걸린다.
· ★ **다음 액션도 이 길이다**: 지도에서 그 동작이 무슨 판을 어떤 자세로 그리는지 먼저 찾고(engine9),
  그 판·그 자세를 `parts` 로 그대로 옮긴 뒤 시계만 도록의 한 바퀴에 맞춘다.

## 손으로 얹은 흰 반사는 이제 셰이더와 싸운다
★ **광택은 셰이더의 일이 되었다 — 덧칠로 그려 둔 반사는 걷어라**(2026-09, 요청: "사베 하얗게 덧댄
반사 느낌 제거") — 사이언스 베슬의 구에는 `shinePath3` 한 장이 흰색 0.28 로 깔려 있었다. 캔버스가
유일한 붓이던 때는 그것이 '둥글다'는 유일한 신호였지만, 지금은 셰이더가 윤기·봉우리·얼룩을 제 각으로
낸다. 곧 **요잉을 돌려도 안 따라 도는 흰 자국**이 그 위에 한 장 더 얹히는 꼴이라 스티커로 읽힌다.
· **어두운 그늘은 남긴다** — 그 쪽은 반사가 아니라 몸의 그늘이라 셰이더가 내는 몫과 겹치지 않는다.
· 같은 꼴(몸 위에 얹은 흰 `topFace`·`shinePath3`)이 남아 있으면 다 이 손이다.

## 한 줄로 꿴 부품은 한 값으로 옮겨라
★★ **실린더·노즐·테·불꽃은 한 축이다**(2026-09, 요청 둘: "드랍십 스러스터 위치 위로 살짝 올리기
(불꽃도 위치 맞추고)" → "드랍십 실린더 조금 올리고 **실린더-스러스터-화염 전부 중심이 맞게** 해 줘
안쪽 스러스터-화염도") — 첫 요청의 괄호가 곧 이 자리의 함정이고, 둘째 요청이 그 뿌리를 짚었다.
· 드랍십의 노즐 원뿔은 `tz` 축에 있는데 **테(`nozzleRim9`)와 불꽃(`thrustFlame`)만 `tz + tr·0.36`**
  으로 그 위에 떠 있었다(포드 0.24 · 안쪽 0.17 모델칸). 그래서 불이 아가리에서 나오는 것이 아니라
  **아가리 윗입술에 얹힌** 꼴이었다 — 노즐을 올리면 그 어긋남이 따라 올라갈 뿐 안 사라진다.
· 고침은 **축을 하나로 두는 것**이다: `TH_UP9` 한 값이 포드 실린더(`POD_Z`)와 안쪽 노즐의 높이를
  함께 올리고, 테·불꽃의 치우침은 걷어 넷 다 제 `tz` 에 앉힌다. 위아래로 옮길 일이 있으면 그 한
  값만 고친다.
· ★ 이 꼴을 찾는 자는 **부품마다 다른 z 식**이다 — 같은 뭉치인데 `tz`·`tz + a`·`tz + b` 로 갈려
  있으면 그 차가 곧 '언젠가 드러날 어긋남'이다.
· ⚠ 정규화는 **움직인 몫과 견주어** 판단한다 — dship 은 top 자로 0.680 → 0.689(+1.3%)인데 표에
  박힌 값이 0.684 라 이미 그만큼 낡아 있었다. 그 폭보다 작은 흔들림으로 표를 갈면 크기만 들썩인다.
★ **한 호 위에 앉은 것 중 일부만 옮길 때는 호를 줄이지 말고 평행이동을 얹어라**(2026-09, 요청:
"캐리어 아래 두 잎 x축 살짝 안쪽으로 이동(딸린 부품들도 같이)") — 캐리어 잎 셋은 반지름 `LEAF_R`
의 한 호 위에 120도씩 앉는다(위 90 · 아래 210·330). 그 반지름을 줄이면 셋이 **다 같이** 모이므로
(그것이 앞선 "잎끼리 더 가운데로" 였다) 아래 둘만 옮기려면 자리에 `x + inX` 한 항을 더해야 한다.
· **꼴이 안 바뀐다** — 등뼈에 더하는 상수라 접선이 그대로고, 그래서 단면 축(u·v)·말림(skewV)·
  등마루가 한 톨도 안 흔들린다. 곧 자리만 옮긴다.
· ⚠ **딸린 것을 전부 태워라**(요청의 괄호가 그 자리다) — 덮개 판 넷·그 위의 임자색 데칼·앞
  테두리의 창이 다 이 호에서 자리를 낸다. 창은 판의 `at` 을 지나 저절로 따라오지만, **데칼은
  제 손으로 점을 찍어** 안 따라온다. 그리고 **깊이 키**(`leafKey`·덮개 키)가 `depthNow(cos·R, y)`
  로 재므로 그 x 도 함께 옮겨야 앞뒤가 옛 자로 남지 않는다.
· ⚠⚠ **`Math.sign(cos θ)` 로 좌우를 가르지 마라** — `cos(π/2)` 는 6.1e−17 이라 부호가 **+1** 이고,
  그러면 가운데 잎(θ 90)이 한쪽으로 밀린다. 작은 문(|cos| < 0.05)으로 '가운데는 0' 을 못 박는다.
· 정규화는 **안 갈았다** — 잉크폭 9.62 → 9.31(−3.2%)이라 재측정 배수가 0.619 → 0.634 로 오르는데,
  표에 박힌 값은 그전부터 0.605(옛 측정 0.619보다 2.3% 아래로 눌러 둔 값)다. 그 값을 올리면
  캐리어가 2.4% 커질 뿐이고, 요청은 '잎을 안으로' 였다. 잉크 중심도 top 9.37 → 9.36 으로 안 움직였다.

## 꺾인 줄기의 각은 마디 길이가 정한다
★★ **튀는 폭만 보면 짧은 사거리에서 도로 날카로워진다**(2026-09, 지적: "아콘 트레이서 줄기 너무
심하게 꺾임 · 꺾임각이 90도보다 크게 둔각으로") — 안쪽 각을 정하는 것은 폭 하나가 아니라 **마디
길이에 견준 폭**이다. 좌우로 번갈아 튀므로 이웃 두 마디의 어긋남은 최대 2·amp 이고, 안쪽 각
θ 를 지키려면 `2·amp ≤ 마디 × tan((180−θ)/2)` — θ = 100도면 **amp ≤ 마디 × 0.42** 다.
· 그래서 그 죔은 **붓에 둔다**(갈래표의 `zigAmp`·`zigW` 는 상한일 뿐이고 이 기하가 늘 이긴다).
  표 값만 손보면 len 이 작아질 때 마디도 함께 작아져 같은 각으로 되돌아간다.
· 마디 수를 줄이면(9 → 5) 같은 각에서 **더 크게** 튈 수 있다 — 마디가 길어지기 때문이다.

## 연기는 '흐려지는 띠'가 아니라 덩이다
★ **뭉게뭉게는 줄기 위에 흩어 얹은 반투명 원이다**(2026-09, 요청: "파뱃 화염방사 연기 자연스럽게
(반투명 회색 연기 뭉게뭉게로) + 열기 느껴지는 글로우 추가 앞쪽 자연스럽게 호 형태로 처리" ·
"디바우러 트레이서도 연기 느낌 나게 뭉게뭉게 하게") — 그러데이션 끝을 회색으로 빼는 것만으로는
**매끈한 띠가 흐려질 뿐**이라 연기로 안 읽힌다. 갈래표의 `billow`(덩이 수)·`billowFrom`(줄기의 어느
몫부터)·`billowK`(크기)·`billowA`(짙기)·`billowCol` 이 그 자다.
· **앞으로 갈수록 크게·옆으로 더 흩어** 놓으면 끝이 저절로 호로 퍼진다 — 호를 따로 그릴 일이 없다.
· 씨앗은 **번쩍임 위상**에서 뽑는다(한 발 동안 무늬가 몇 번 갈려 연기가 살아 움직인다).
· 화염은 앞쪽 55%만(`billowFrom` 0.45 — 뿌리는 불이다) · 디바우러의 산은 **통째로** 구름이다(0.04).
· ⚠ **이름이 겹친다** — 미사일 연기의 속색이 이미 `smoke` 라, 이 갈래는 `billow` 로 적었다.
★ **열기 번짐(`heat`)은 불 둘레에 도는 것이지 불 자체가 아니다** — 반지름 2.4배·짙기 0.7 로 뒀더니
도록 칸에서 불줄기가 안 보이고 주황 얼룩만 남았다(실측). 1.9배·0.45 로 낮춰 몸 **아래**에 깐다.

## 모델 손질 루프
1. `src/components/replay/ReplayMotionPlayer.tsx` 수정 → `npx tsc --noEmit -p tsconfig.json`
2. `node scripts/model-shot.mjs --kinds <k> --rots 0,45,90,180 --mode top --cell 300 --lit [--zoom 0.5] --out <scratch>/x.png` 로 눈으로 확인
3. 정규화 재측정 — 유닛 `node scripts/model-norm.mjs --kinds <k>`(맨 위 표의 필요배수) → MODEL_NORM,
   건물 `node scripts/bld-norm.mjs --kinds <k>` → BLD_NORM
4. `npx vite build` → `node scripts/model-depth-check.mjs` · 클래스를 새로 붙였으면 `node scripts/css-guard.mjs`(규칙 없는 scr-* 이름이면 실패) (✔ 새로 어긴 모델 없음 이어야 한다; 새 빌더는 partKey 키)
   · **모델을 고쳤으면 `node scripts/tier-table.mjs`로 부품 등급표를 다시 뽑는다**(`--check`로 어긋남 검사).
   · **`node scripts/model-gl.mjs`** — 156종을 GL 로 굽어 **빈 그림이 없나** 보고(5초) 눈으로 볼 시트를 낸다.
     ★ 눈으로 확인할 때는 이것을 쓴다 — `model-shot` 은 **2D 캔버스 붓**이라 앱이 그리는 그림이 아니다.
   · **`node scripts/model-mesh.mjs --check`** — **덮임 100%** 관문이다(아래 '메시 층'의 ★★). 100% 아래면
     새 헬퍼·새 손 면이 `meshPut9` 로 3D 를 안 적었다는 뜻이고, 그 면은 GL 에서 **그냥 빠진다**.
     어느 줄인지는 `node scripts/miss-sites.mjs`.
   · 총구를 가진 모델을 고쳤으면 **`node scripts/muzzle-table.mjs`로 총구 앵커표도 다시 뽑는다**(`--check`).
     앵커의 임자는 빌더다 — 총열·포신·아가리를 짜는 줄에서 `markMuzzle9(x,y,z)`(공용 저그 얼굴은 무른 표식)로 끝점을
     적으면 표(muzzleTable.gen.ts)가 그 값을 싣고 엔진(MUZZLE_ANCHOR)이 손 표 위에 덮어쓴다. 눈으로 볼 때는
     `node scripts/muzzle-sheet.mjs --kinds <k> --rots 0,90,270 --out <scratch>/x.png`(십자가 총구 위에 앉아야 한다).
     그 표(tierTable.gen.ts)는 모델 기하만의 함수라 빌드 시각에 굽는다 — 안 그러면 폰이 들어올 때마다
     종류마다 여덟 방위를 다시 구워 로딩에서 2~3초를 쓴다(실측). 낡으면 틀린 등급이 실린다.
5. 커밋 → 브랜치 푸시

## 배포
**사용자가 "배포"라고 할 때만** 한다. 그 전엔 feature 브랜치 푸시까지만.
배포 = ① scplay `git push origin HEAD:main` → ② scplayer의 `package-lock.json`에서 `node_modules/scplay`의
`resolved` 해시를 그 main 커밋으로 올리는 커밋("재생기 갱신: …")을 브랜치와 main에 푸시.
scplayer 쪽 소스를 만졌으면 그쪽에서 `npx tsc --noEmit -p tsconfig.json`만이 아니라 **`npx vite build`까지** 돌린다(CSS 문법 오류는 tsc가 못 잡는다). scplayer는 락 고정이다(vercel installCommand `npm ci`) — 락을 안 올리면 scplay main을 밀어도 앱에 안 실린다.
⚠ **해시를 보고할 때는 늘 40자리 전체다**(되풀이된 지적: "scplay 해시는 40자리로 줘야지 또 까묵었냐구") —
`git rev-parse HEAD` 의 값 그대로. 짧은 해시는 락에 박는 값이 아니라 눈으로 못 맞춰 본다. scplay·scplayer 둘 다.
옛 방식(빈 "배포 트리거" 커밋 · installCommand의 HEAD 덮어쓰기)은 걷었다.
락을 통째로 다시 만들 일이 있으면 **node_modules를 치운 채** `npm install --package-lock-only`로 만든다 — 설치된 트리에서
뽑으면 이 기계 플랫폼의 선택 패키지만 실려 Vercel의 `npm ci`가 거부한다(esbuild·rollup 바이너리·fsevents).

## 프레임 엔진·워커 구조(2026-09)
- `deriveWorld9`(파생 자료)·`createEngine9`(시각 t → 프레임: unitOps·fxOps·DOM 기록·안개)는 **`engine9.ts`**의
  순수 함수다(엔진이 이행적으로 참조하는 표·헬퍼 151개를 함께 옮겼다 — React·DOM 없음, 워커는 이 모듈만 든다).
  `ReplayMotionPlayer.tsx`는 붓·UI만 남았고 필요한 이름을 engine9에서 import한다(다른 파일이 쓰던 것은 re-export).
  옮긴 기준은 `scratchpad/split_engine.mjs`(TS API로 이행 참조를 닫음)였다. 컴포넌트는 화면 입력(EngineView9)만 건넨다.
- 두 일꾼: **설계 일꾼**(`frameWorker.ts`, 동적 `import("./frameWorker?worker&inline")`, 라이브러리 빌드는
  `inlineDynamicImports`로 한 파일)이 주인(메인 재생 상태)의 명령(`cmd`: 재생/정지·기준 시각·배속, **바뀔 때만**)과
  시점(`view`: 상자·기울기·색·품질·**시야 사각형**)을 받아 제 벽시계로 앞으로 설계도를 지어 둔다(벽시계 3초·24MB 한도(폰 1.5초·4MB)).
  **그림 일꾼**(메인의 붓)은 받은 설계도 중 t 이하 가장 늦은 장을 골라 그때 푼다. 단방향. 메인 엔진 대비 길은 없다 —
  워커가 못 서면 마지막 프레임을 든 채 `SCR_DIAG.worker`(#diag "워커" 줄: on/준비중/off · got/used/missed ·
  짓기 ms · op 수·KB · 앞 s·장·MB · 시야 · ⚠오류)에 까닭이 적힌다.
- 설계도는 `framePack.ts`로 **float32 배열 + 문자열 표**로 싸서 transfer로 넘긴다(구조화 복제 없음). 안개 판 셋은
  바뀐 장에만 싣고, 메인은 그 시각 이하 가장 늦은 안개 판을 붙인다.
- 컬링: 메인이 보이는 사각형에 앞뒤 한 화면씩 여유(3×3)를 붙여 `view.cull`로 보낸다. 보이는 것이 그 안에 있는 동안은
  다시 안 보낸다(작은 팬은 설계도를 안 버린다). 1.2배 이하는 지도 전체. 밖의 개체는 미니맵 점만 남는다.
- 세계의 주인은 워커다: 참값(truth)은 메인이 `postTruth9`로 **transfer**해 넘기고(메인엔 껍데기), 개체 표(entData)는
  워커가 참값에서 만든다. 화면(UI)이 읽는 파생 자료(건물 행·캐스트·핵·가스·생산·업글)는 워커가 `worldui`로 한 번
  보내고, 걷기(entWalks)는 추적을 켤 때 `want walks`(임자별)로 청한다. 메인은 deriveWorld9를 안 부른다. 넘긴 뒤
  메인의 트랙 배열은 비운다. 걷기는 참값 키를 가리키는 창(`WalkView`, `posAtW`)이라 복사가 없다(폰 메모리).
- 참값 자리 형식(`openbwTracks.ts`): 키는 `kt`(초 Float32)·`kxy`(픽셀 Int16×2)·`kh`(방향 바이트)·`kst`(상태)로 나눠 들고
  접근자 `kT/kX/kY/kH/kS`로만 읽는다. 체력·인터셉터·표적은 평평한 형식 배열 `Ticks`([초,값,…], 표적은 Float64)이고
  `tkN/tkT/tkV/tkAt/tkLast/tkSlice`로 읽는다. 자리를 바꾸면 접근자만 고친다. 검사: `node scripts/openbw-tracks-check.mjs`.
- **판(스프라이트) 굽기 길은 걷었다(2026-09)** — 유닛·건물 몸은 어느 기기에서나 GL 붓이 메시로 그린다(아래 "메시 층·WebGL").
  함께 걷은 것: `unitSprite`·`buildingSprite(Bake)`·판 보관함·바이트 예산·LRU·대타(같은 종류의 다른 크기)·프레임당 굽기 몫·
  굽기 일꾼(`bakeWorker.ts`)·색별 물들이기(`tintedOf9`)·그림자 판(`shadowPlate`)·판 크기 사다리·판 갈림 진단. 약 1900줄이다.
  **캔버스에 남은 판은 크립 얼룩 한 가지**(`creepPlate9`, 무늬 셋 × 보기 둘 = 여섯 장·크기 못 박음·색 하나라 구울 때 물들여 합친다):
  크립은 모델이 아니라 땅이라 지형 마스크로 파내야(destination-out) 하고 그것은 2D 캔버스의 일이다. 효과 래스터(`FX_RASTER_CACHE`)도 남는다.
  모델 빌더·표·헬퍼와 크립 래스터(`rasterBld9`)는 **`bake9.ts`**에 있다(DOM 없음 — 캔버스는 `BAKE_ENV9`의 손으로만). 빌더를 고치면
  bake9.ts를 고친다(model-depth-check·doc-catalog도 그 파일을 읽는다). 모듈 전역 깃발(pose·head·lit·spin·lod·pitchFlat)은
  세터(`poseSet9`·`pitchFlatSet9`…)로만 세운다.
  ⚠ bake9.ts의 **문 차례**는 원본 그대로여야 한다 — 파생 빌더 등록문(`SHAPE_BUILDERS.x = …`)이 `SHAPE_FACES` 표보다 앞에 있어야 한다.
- 기기 프로필은 `DEV9` 한 표(폰/PC: 크립 굽기 상한·굽는 판 한도·불티·효과 래스터·그림자 최소 배율·시야 여유·앞 한도(폰 1.5s/6MB·
  PC 3s/24MB)·요잉 8칸·**GL 메시 상한**(폰 240 · PC 600)). 새 문턱은 표에 더하고 자리에서는 `DEV9.x`만 읽는다. 기기 판정은 `smallDevice9` 하나.
  기기는 진입 벤치로 **단**을 오른다(`DEV9.tiers` — PC 네 줄 PC_TIERS9 · **폰 세 줄 PHONE_TIERS9**). `#tier=N` 강제 · 단은 **오르기만** 한다
  (부풀린 첫 값이 내린 단은 짐이지 기기가 아니다) · 유휴 재기(crowdRecheck9)가 더 작은 값을 내면 그 자리에서 오른다.
  **표가 스스로 문턱과 덮어쓸 값을 든다**: `upMs`(2D 벤치 ms 이하) · `up3Ms`(입체 벤치) · `minMem`(deviceMemory GB) ·
  그리고 그 단이 덮어쓰는 DEV9 칸들(`aheadSec·aheadMB·glMeshMax·glBloom·glow·yaw8Always·shadowGroundMinZoom·decalBakeMax·hitShardK·dieShards`).
  0단은 **프로필 기본값과 같아야** 한다(단이 안 오른 기기는 종전 그대로). 자리에서는 다들 `DEV9.x` 를 프레임마다 읽으므로 갈아 끼우면 곧 먹는다.
  ⚠ **모듈 적재 때 베껴 두면 단이 영영 안 먹는다**(2026-09, 지적: "모델 모양대로 그림자 사영되는 거 좋았는데 왜 지금 보니
    타원형으로 바뀌었지?") — `const SHADOW_GROUND_MIN_ZOOM = DEV9.shadowGroundMinZoom` 처럼 한 번 베낀 사본이 셋 있었다
    (그림자 최소 배율 · 크립 굽기 상한 · 효과 래스터 예산). 그래서 폰 표가 적어 둔 접지 그림자 배율이 **한 번도 먹은 적이 없고**,
    낮은 배율의 지상 유닛은 늘 캔버스 타원을 받았다. 지금은 셋 다 자리에서 읽는 함수다(`shadowGroundMinZoom9()` …).
    새 문턱을 더할 때 **`const X = DEV9.y` 꼴로 베끼지 마라** — 표가 덮어쓰는 칸이면 그 순간 죽은 값이 된다.
  · **폰 세 단**(2026-09, 요청: "요즘 폰도 성능차이가 심해서 모바일용 벤치를 별도로 분리하되 3단계로 나누고 … 최고단계에선 광택 글로우도 넣고"):
    0 낮음(벤치 미달 — 종전 폰 값 그대로 · 글로우 끔 · 낮은 배율 죄기) · 1 보통(2D ≤30ms — 광택 켬 · **번짐(블룸) 켬** · **접지 그림자 2배율부터** · 앞 2.5s/10MB · 메시 300벌 · 파편 0.8/16)
    · 2 높음(2D ≤13ms **이고** 입체 ≤28ms — 요잉 16칸 · 접지 그림자 1배율부터 · 손짓 중 그림자 안 접음 · 앞 4s/16MB · 메시 360벌).
    ⚠ 폰 문턱은 PC 자의 비율로 못 나눈다(그러면 0단만 나온다) — 따로 적은 숫자다. ⚠ `minMem` 은 폰에 안 쓴다: iOS 사파리는
    deviceMemory 를 아예 안 내므로 메모리를 문턱으로 삼으면 정작 가장 빠른 폰이 0단에 남는다. 그래서 메모리에 닿는 값(메시 상한)만 조심히 올린다.
    ⚠ 터치 기기 제외 문(touchPc9)은 **PC 프로필에만** 건다 — 폰은 다 터치라, 그 문을 그대로 두면 표가 세 줄이어도 단이 영영 안 오른다.
    결(grain)은 폰의 어느 단에서도 안 켠다(굽기 +25~30%). 품질 알림은 폰도 단을 읽는다(높음/보통/낮음·미달이면 매우 낮음).
  · **문턱은 GL 몫만큼 늦췄다**(2026-09, 지적: "폰이나 PC 매우낮음인데 여유가 있어보여서 혹시 지금 벤치 기준이
    너무 높진 않아?") — 옛 값(PC 16.8·8.4 · 폰 24·9)은 **한 장이 판을 굽고 찍는 일**이던 때 정한 것이다. 몸을 GPU 가
    그리는 지금 실제 몫은 1.24~1.76배 줄었는데(위 GL 실측) **문턱을 재는 벤치는 여전히 판 찍기**라, 같은 기기가 같은
    ms 를 내며 그만큼 놀고 있었다. 보수적으로 1.45배 늦춘다: PC 24·12 · 폰 30·13(실측 헤드리스 PC 벤치 11ms —
    보통 → **높음**으로 오르고 p50 은 66.7ms 그대로였다).
    ⚠ 벤치 자체를 GL 로 바꾸지는 않는다 — 벤치는 '기기가 얼마나 빠른가'의 눈금이고 헤드리스의 GL 은 소프트웨어라
      진입에서 재면 기기가 아니라 드라이버를 잰다.
    ⚠ **입체 문턱(up3Ms)은 한 번 더 어긋나 있었다**: `benchDevice9(true)` 는 2D 판보다 화소를 **2.87배** 칠하는데
      문턱이 16 으로 2D(9)보다 오히려 낮아, 화소당으로는 4.3배 엄격했다(2단에 오르려면 2D 기준 5.6ms). live3d
      문턱에서 한 번 고쳤던 그 어긋남과 같은 것이다 — 28 로 늦췄다. **깊은 벤치에 문턱을 줄 때는 늘 2.87 을 곱해 견주라.**
  · **죽은 `glow`·`brush`(결) 스위치는 걷었다**(2026-09, 요청: "죽은 글로우 코드 걷어내고") — 그것을 쓰던
    `glowBake9`(2D 광택 겹)는 판 굽기 길을 걷으며 부르는 자리가 사라져 **한 번도 안 돌았는데** 표에는 켜진 것처럼
    적혀 있어 "켜 있는데 안 보인다"는 혼동을 두 번 냈다. 함께 지운 것: `GLOW9`·`BRUSH9` 표 · `GLOW_HI9` ·
    `brushOn9/brushSet9` · `glowOn9/glowSet9` · `brushSeed9` · `grainAxes9` · `faceGrain9`, 그 셋만 읽던
    shapeOblique 의 결 자 표(`FACE_GRAIN9` — 낯마다 적던 몫이라 굽기도 그만큼 준다), model-shot 의 `--noglow`.
    합쳐 **671줄** · 번들 1484 → 1470KB. 모델 광택은 이제 GL 셰이더가 낸다(`DEV9.glSpec`·`#glspec`).
  · **손짓 중 실시간 원근**(live3d)은 **PC 3단(매우 높음)에서 표가 켠다**(2026-09, 요청: "PC 매우높음에서 3D 제스쳐중
    모델도 시점 변화 가능하려나 지금은 지도만 실시간인데"). 모델이 안 따라오던 까닭은 둘이었다:
    ㉠ 판정 문턱(입체 벤치 26ms · 손짓 한 장 중앙값 24ms)이 **판 굽기 시절** 값이라 GL 이 번 몫만큼 저절로 꺼졌다 —
       표가 켜 주는 단은 이제 문턱을 안 묻는다(`DEV9.live3d`).
    ㉡ **밀림 기준(PitchGeom9.sox)을 손짓 시작 값에 얼려** 두었다 — 자리는 따라오고 기울기만 얼어 있어서 "지도만
       실시간"으로 보였다. 얼린 까닭은 밀림 각이 **판 열쇠**(6도 칸)였기 때문인데(끌면 판 폭풍: 한 장 23 → 240ms),
       판 길을 걷은 뒤 그 각은 **카메라 유니폼**이고 메시 열쇠에 안 든다. 그래서 실시간 원근을 켠 손짓에서는 안 얼린다.
    같은 까닭으로 그 단에서는 vq 칸을 **2도**로 쪼갠다(그 밖은 종전 유닛 6도·건물 12도) — 끄는 동안 기울기가 계단으로 안 튄다.
    접기(한 장이 33ms 넘으면 이 손짓 동안 실시간 원근을 끈다)는 그 단에서 **연달아 세 장**일 때만 건다 — 한 장 규칙은
    메시를 처음 짓는 첫 프레임에 걸려 손짓마다 접힌 채 돌았다. 접히면 워커에 새 원점을 안 보내므로 기울기가 그 자리에서 멎는다.
    검산: `node scripts/perf-check.mjs --wide --3d --drag --diag [--hash tier=3]` → `[손짓] 9ms 배킹×1 원근 live 밀림 따라옴`.
    실측(헤드리스·소프트웨어 GL·3D 766기): 손짓 한 장 7 → 9ms, rAF p50 16.7 → 33.3ms — 헤드리스는 GL 채우기까지 CPU 라
    이 값은 상한이다(실기 GPU 는 훨씬 싸다). 무거운 자리는 위 접기가 받는다.
  · **GL 로 바꾼 값**(2026-09 실측, perf-check 폰 프로필 390px·유닛 600·조임 없음): 판 굽기 때(27cc1c7) p50 **83ms**·건물 판 74.6MB
    → GL 메시(HEAD) p50 **67ms**·캔버스 6장 31.9MB(gl9 판 7.3MB 포함). CPU 4배 조임에서는 383ms → 217ms. 헤드리스는 소프트웨어 GL 이라
    삼각형 채우기까지 CPU 가 지는데도 **더 빠르다** — 실기(GPU)에서는 차이가 더 벌어진다. 번짐을 켜도 폰 프로필 p50 은 같았다(66.6 → 66.6).
- `#diag`는 요약 한 줄, `#diag=draw|bake|fog|load|gest|mem|worker|truth|brush|view|all`(쉼표로 여럿)로 용도를 가른다 — draw(화면·덜어내기·프레임) · bake(굽기·굽기일꾼·판갈림·캔버스) · fog(안개) · load(로딩·지도판·입체) · gest(손짓·원점). 한 주제가 한 줄, 줄 머리에 주제 이름. `mem`에 메모리 어림(memEst9) 줄.
- 워커는 짓기 시간(ms)에 맞춰 프레임 간격을 벌린다(초당 30장 기본, 최소 8장). 메인은 2초 안의 프레임이면 낡아도 든다.
- **붓은 React 밖에서**(4번): 시계 틱이 살아 있는 시각(`tLiveRef9`)을 한 걸음(벽시계 ≤80ms) 올리고 `paintFnRef9`로
  설계도를 골라(`frameAt9`: 앞·뒤 장 보간) `unitPaintRef`로 유닛 캔버스를 곧장 칠한다. UnitLayer는 `opsSrc/fxSrc`(ref)를
  props보다 먼저 읽고 `driven`이 참이면 effect에서 안 칠한다. React 상태 t는 `REACT_STEP_MS9`(100ms)마다만 올린다 —
  시간 표시·DOM 효과·미니맵·안개는 그 박자다. 탐색으로 t가 밖에서 바뀌면 렌더가 tLive를 맞춘다.
- 엔진은 늘 **자세히** 낸다(요잉 16칸·모든 자세·탱크 차체+포탑). 낮은 배율 간이화는 붓(UnitLayer)의
  `detailAt`·`yawAt`·`moveAt`가 한다. 배율·팬 자체는 프레임에 안 실린다(시야 사각형만).
- 붓 계측(`SPRITE_PERF`)이 세는 것도 그만큼 줄었다: **찍기**(캔버스에 남은 판 블릿 = 크립·효과) · **직접**(GL 이 못 맡아 면을 곧장 그린 몸 —
  0이라야 한다) · 최악 프레임 · 화면 캔버스 무게. 메시 무게·벌 수는 `gl9.stat`(#diag=draw 의 GL 줄)이 따로 낸다.
- 계측: `node scripts/perf-check.mjs [--msgsize]`(vite 번들이 기본) — `--3d`(조종부의 2D/3D 단추를 눌러 입체로 재기 · `--wide` 와 함께 —
  폰 폭에서는 재생기가 3D 를 막는다) · `--drag`(끄는 부하 — 끄는 자리는 `.scr-motion-map` 가운데를 찾아 쓴다) ·
  `[손짓]` 줄(한 장 ms · 배킹 몫 · 실시간 원근 live/접힘/off · 밀림 기준 따라옴/얼림 · 단, `SCR_DIAG.gest`) ·
  `[워커] on got/used/missed`로 워커가 쓰였는지,
  `--msgsize`로 장당 바이트·op 수를 본다. `--wide --cpu 1`이 PC 판(굽기 일꾼 `[굽기일꾼]` 줄이 on이어야 한다). 워커 번들의 `process.env.NODE_ENV`는 vite.config의 define이 박는다(사파리).
  esbuild 도구 번들(model-shot 등, perf-check `--esbuild`)에는 워커가 없어 유닛 프레임이 안 그려진다.

## 모델 z 좌표 손질(2026-09)
- 평면 카메라의 높이 누름(`TOP_Z_PRESS9` 0.8)은 **모델 z 좌표 자체로 옮겨 굳혔다**(`scripts/model-z-scale.mjs --k 0.8`): 빌더·헬퍼의
  z 자리 숫자를 접어 쓴다(리터럴은 값 자체, z 전용 지역 상수는 선언, x·y와 나눠 쓰는 상수는 `NAMEz9` 쌍둥이, 지역 함수는
  z-return 접기 또는 선형 인자 접기). 못 접는 식은 감싸지 않고 `--report` 에 적는다. 카메라는 이제 정직하다(누름 1.0).
  앞으로 모델 높이를 바꿀 때는 카메라가 아니라 좌표를 고친다. 순수 기하 커널(spirePillar 등, KERNEL_FN)은 본문을 안 건드린다.
- 검산: `node scripts/model-faces-snap.mjs --out ref.json --zk 0.8`(원본 + 꼭짓점 z배수 = 정답) · `--out new.json`(고친 소스) ·
  `--diff ref.json new.json`(종류별 어긋난 면·버그급). Node 헤드리스, 전 종류 15초.
- 잉크 중심표(`UNIT_INK_CY9`·`BLD_INK_MID9`)는 `node scripts/ink-center.mjs --emit` 으로 다시 뽑아 engine9 에 붙인다(캔버스 래스터라 브라우저).
- **코드모드가 틀린 자리는 손으로 고쳤다(2026-09, 118종 버그급 → 잔 조각 몇 개)**. 원리: 코드모드는 z **입력**을 접는데 정답은 **꼭짓점** z를
  접는 것이라, 회전·경로 매개변수·접선에서 z를 만드는 자리는 접을 수 없다. 그런 자리는 설계 좌표 그대로 두고 **꼭짓점으로 나가는 자리에서
  `Z8`(bake9, 0.8)을 곱한다** — 경로 람다의 return z, 관절 풀이(jointBetween)는 s·w 를 `/Z8` 로 되돌려 풀고 결과 z 에 `*Z8`, 부품 회전
  (뮤탈 날개 P·포탑 포드 롤·오버로드 bz)은 설계 자로 돌린 뒤 z 에 `*Z8`. 코드모드가 못 본 z 후보: `[x, z]`/`[y, z]` 쌍 표(가디언 다리·
  벌처 hullPlan·옵저버토리 기둥 키), 기본 매개변수(`tankTrack h = 2.6`), 이름이 z 답지 않은 지역 헬퍼 인자(forgeDome9·rampVent·
  sunkenFoot arc), `MODEL_Z_OFF9` 표(withModelZOff 는 modelZK 를 곱하므로 표 값 자체를 ×0.8), 화면 원으로 설계한 것(포지 바퀴 —
  RIM 은 화면 자라 접지 않는다). 남은 잔차(관 단면·구 껍질의 뒷면 판정, 평균 0.02~0.03 모델칸)는 받아들였다.
- **뒤늦게 드러난 잔차 하나**(2026-09, 지적: "넥서스 발판 이상해짐 뒤쪽만 정상임"): 넥서스 모퉁이 받침(cornerPad9)이
  높이·바닥 기준을 **화면 자 상수**(PLINTH_Z9 0.55)로 쓰고, 본체 반폭을 잴 때 피라미드 높이를 **6.4**(제 절두체는
  5.12)로 나누고 있었다. 받침이 땅 밑 −0.11에서 0.89까지 서고(1.25배) 모퉁이를 **엉뚱한 높이의 반폭**으로 잡아 본체
  낯과 안 맞물렸다 — 2D 는 화가 차례가 덮어 안 보였고 GL 에서 '받침이 흘러나오고 속이 보이는' 꼴로 드러났다(뒤 모퉁이만
  본체 뒤에 들어가 멀쩡했다). ★ 교훈: **z 를 쓰는 자리에 화면 자 상수가 섞이면 2D 에서는 안 보인다** — GL 이 그 감사자다.
- **정답 만들 때**: withModelScale 은 바깥 배수를 덮어쓰므로(곱하지 않음) 제 배수를 거는 빌더(refinery·academy·plane·tank·turret·
  trapezoid·tombFlat·dship)는 `--zk 0.8` 정답이 틀린다. 평면은 원본 트리(카메라 누름 0.8)를 `--modes top` 으로 뜬 **진실**과, 입체는
  withModelScale 을 곱셈으로 잠시 고친 원본 트리의 `--zk 0.8` 과 대조한다(model-faces-snap.mjs 머리 주석). `--diff` 는 독점 없는
  최근접 짝짓기 + `--min 0.2`(잔 조각 제외)로 센다 — 탐욕 짝짓기는 궤도 패드·구 껍질에서 이웃을 가로채 과장했다.
- 정규화(MODEL_NORM 26종)·잉크 중심표는 코드모드 때 **틀어진 기하로** 재측정한 값이었다 — 기하를 되찾으며 정규화는 코드모드 전 값으로
  되돌리고 잉크 중심표는 다시 뽑았다.

## 모서리를 깎은 네모 기둥(팔각)
`boxOctFaces3(cx, cy, w, d, h, z0, cut, omit?, noTop?)` — `boxFaces3` 와 같은 자리·같은 규약이고 세로 모서리
넷만 `cut` 만큼 사선으로 잘라 **벽이 여덟**이 된다(2026-09, 요청: "배럭 … 위에서 보면 8각형").
· `cut` 은 **모서리에서 각 변을 따라 물러나는 길이**다 — 사선 자체는 그 √2 배. w/2·d/2 의 절반에서 잘라 둔다.
· ⚠ **띠·창은 저절로 안 따라온다** — 앞 낯의 좌우폭은 `cut` 만큼 줄고, 옆 낯의 앞뒤도 그만큼 준다.
  그리고 **사선 낯에도 제 토막을 둘러야** 띠가 모서리에서 안 끊긴다(안 두르면 두른 것이 아니라 낯마다 그은
  줄로 읽혀, 깎은 뜻이 없어진다). 배럭은 그 셋을 `BLK9` 표 한 줄(x·반폭·반깊이·깎임·y중심)로 쥔다.
· 눈으로 고른 값: 배럭 `CUT9 = 0.35`(바깥 판 두께 2.7 의 앞 낯이 2.0 로 남는다). 0.5 는 1.7 까지 깎여
  "살짝만"이 아니었다.
· **깎임은 모퉁이마다 줄 수 있다**(2026-09, 요청: "앞쪽 건물 뒤쪽 두 모서리 사선 깎은 거 없애고
  대신 뒷쪽 기둥과 그 위의 판들의 네 모서리 사선을 깎아 줘") — `cut` 에 숫자 대신 네 값을 주면
  (+x,+y) · (+x,−y) · (−x,−y) · (−x,+y) 차례로 걸리고, 0 인 모퉁이는 직각으로 남아 그만큼 벽이 준다.
  ★ **덩이가 맞물리는 낯의 모퉁이는 깎지 마라** — 배럭 좌우 판의 뒤 모퉁이는 기둥과 맞대는
  자리라, 거기를 깎으면 판과 기둥 사이에 쐐기꼴 틈이 난다. 곧 깎는 자리는 **실루엣에 드는
  모퉁이**뿐이다.
· 법선은 이제 **제 변에서 뽑는다**((−dy, dx)/|·| — 도는 방향의 바깥쪽) — 모퉁이를 골라 깎으면
  벽이 다섯~여덟으로 오락가락하므로 법선 표를 못 박을 수가 없다.
· 띠 표(`BLK9`)도 앞·뒤 깎임을 따로 든다 — 한 값으로 쥐면 안 깎은 뒤 낯의 띠가 깎인 몫만큼
  들어가 짧아지고, 있지도 않은 사선 낯에 토막을 두른다.

## 속에 켠 불이 위에서 안 보일 때
★★ **처마가 덮은 속은 부감에서 한 각도 안 보인다 — 빛은 바닥으로 새어 나오는 몫이 먼저다**(2026-09,
요청: "커맨드 팩토리 활성 시 격납구 안쪽에서 노란 불빛(블루밍) 보이게 — 다른 불빛처럼 깜빡임") —
팩토리는 격납구가 옆을 보고 있어 뒷벽에 판 하나를 켜면 그대로 보였다. 커맨드는 **돔이 처마처럼
덮어** 여덟 각 어디에서도 뒷벽이 안 보였다(실측 `model-gl --lit`). 그래서 커맨드는 문간 **앞
경사로에 깔리는 빛의 자국**을 함께 얹는다 — 속에 불이 들면 실제로도 그것이 먼저 보이는 몫이다.
· 켜고 끄는 자는 `glowLit(켠색, 꺼진색)`(몸 면) · 켜졌을 때만 얹는 덧칠은 `if (bldLitNow)` + `winLit`.
  ⚠ **꺼진 판에 투명 면을 두지 마라** — 그만큼 삼각형만 는다.
· 깜빡임·번짐은 **따로 지어내지 않는다**: 박자는 엔진의 `op.lit`(그 종류가 LIT_KINDS 에 있어야 한다),
  번짐은 `winLit`·`glowLit` 이 그 색을 EMIT_FILL9 에 적어 저절로 먹는다.
· ⚠ 벽에 얹는 판은 **한 뼘 띄운다**(0.03) — 같은 평면이면 z 싸움으로 깜빡인다.
· ⚠ 빛 자국의 채도가 높아야(0.71) mesh9 의 덧칠 접기(무채색만)에 안 걸려 제 부품으로 남고 번짐을 탄다.
· 🔎 **켠 채로 보는 자**: `node scripts/model-gl.mjs --kinds <k> --lit` (2026-09 에 더했다 — 여태 이 자는
  늘 꺼진 판만 냈고, 켜진 자리는 브라우저 한 판이 드는 `doc-sheet --anim` 으로만 볼 수 있었다).

## 원둘레에 늘어놓은 네모는 다 같은 쪽을 본다
★★ **자리만 흩지 말고 프레임째 돌려라**(2026-09, 요청: "트리뷰널 기둥들이 모두 중심을 바라보게
수정(지금은 같은 방향을 봄)" · "아모리 본체 주변 톱니들도") — `boxFaces3` 는 **축 정렬** 상자다.
중심에서 `(sin a·R, cos a·R)` 로 자리만 옮기면 열넷이 다 같은 방향을 보고, 그러면 드럼을 **두른
이빨**이 아니라 원 위에 늘어놓은 네모가 된다(둥글게 둘러선 기둥도 마찬가지다).
· 고침은 상자를 손으로 다시 짜는 것이 **아니다**: 제자리 `(0, R)` 에 세워 두고
  **`withModelSpin(−각)`** 으로 감싸면 자리와 방향이 **한 번에** 맞는다
  (`spun` 이 반시계라 −각이 `(0, R)` 을 `(sin a·R, cos a·R)` 로 보낸다).
· ★ `withModelSpin` 은 **더한다** — 바깥 감싸개(트리뷰널의 −45) 안에 겹쳐도 그대로 합쳐진다.
· ★ `depthNow` · `faceLight` · `facingRatio` 도 같은 문을 지나므로 **안에서 `(0, R)` · `(0, 1)` 로
  재면 된다** — 밖에서 px·py 로 재던 값과 같은 수이고, 어느 낯이 보이는지도 저절로 맞는다.
· ⚠ 돌린 뒤 `bld-norm`·`ink-center` 를 다시 재라(트리뷰널은 잉크폭이 안 변했고 잉크 중심만 0.01
  움직였다 — 안 변하는 것이 정상인 경우도 있으니 **재서 확인**하는 것이 규약이다).

## 나란히 선 덩이 사이의 틈
★ **덩이를 잇는 상자는 이웃 벽에 딱 닿는 것이 아니라 파고들어야 한다**(2026-09, 요청: "배럭
건물 사이사이 틈 없게") — 배럭의 사이 상자는 두께 1.15 로 가운데 판(±1.2)과 0.175 · 바깥
판(2.6)과 0.075 를 남기고 있었다. 눈으로는 '그늘'로 보이지만 **자홍 바탕(`model-shot --bg
"#ff00ff"`)에 뜨면 실루엣 안으로 배경이 새는** 진짜 틈이다. 보이는 몫은 두 벽 사이 그대로
두고 상자만 **양쪽으로 0.12 씩 더** 파고들게 한다 — 딱 맞추면 두 벽이 같은 평면에 겹쳐 z
싸움을 하고, 파고들면 이웃 판이 제 벽으로 덮으므로 2D·GL 어느 붓에서도 깔끔하다.
· **띠 표는 보이는 자(GVW9)를 따로 든다** — 파고든 폭으로 띠를 그으면 이웃 판 속으로 넘어간다.
· **밑면도 맞춘다** — 이음 상자가 판보다 높이 떠 있으면 그 밑으로 배경이 보인다(부감이라
  낮은 틈도 뚫린다). 윗면은 그대로 두고 키만 그만큼 늘린다.
· 검산은 눈이다: `model-shot --kinds cube --rots 0,90,135,180,225 --bg "#ff00ff"` 로 실루엣
  안에 자홍이 한 점도 없어야 한다. `model-gl` 은 '그려지나'만 보는 자라 **틈은 못 잡는다**.

## 통풍구로 읽히게 하는 자
★ **살 간격이 살 굵기의 두 배 안쪽이라야 격자로 읽힌다**(2026-09, 요청: "가운데 건물 위쪽
벤트가 벤트처럼 안 보이는 듯 색은 더 검게 하고 가로 살도 늘리기") — 배럭 가운데 뚜껑은 길이
6.8 에 살이 셋이라 간격이 1.7 이었다. 살(0.18) + 골(0.22) = 0.4 가 그 1/4 밖에 못 메우니
'줄 세 개 그은 판'이다. 일곱으로 늘려 간격을 0.85 로 좁히면 절반이 차 통풍구가 된다.
바탕도 한 단 더 죽인다(옆 벤트 #4c4c4c → #2e3033) — 어두운 속이 있어야 살이 도드라진다.
⚠ 어두운 바탕은 **결(결 재질 문)에서 빠진다**(휘도 0.19) — 통풍구는 맨 강철이 아니므로 그게 맞다.

## 연기가 무엇을 가리고 무엇을 안 가리나(다크 스웜)
★ **효과는 화가 차례로만 서므로 '어떤 몸만 안 가리기'는 마스크로 판다**(2026-09, 요청: "디파일러
다크 스웜 연기에 공중 유닛이나 건물은 가려지면 안 돼") — 효과(fx)는 몸을 다 그린 **위에** 얹히는
2D 켜라, 그 안에서 몸과 앞뒤를 가릴 길이 없다. 몸을 두 번에 나눠 그리는 길(지상 → 연기 → 건물·공중)은
못 쓴다: 그러면 **건물이 제 앞의 지상 유닛을 덮는다**(칸은 화가 차례로 정해지는데 그 차례가 갈린다).
그래서 **가리면 안 되는 것만 한 판 더 그려 알파를 마스크로 쓴다**:
  ① `gl9.maskOver` — 방금 flush 한 큐에서 `over`(건물·공중 유닛)만 캔버스에 다시 그린다.
     ⚠ **그림자는 뺀다** — 땅에 눕는 그림자는 연기 아래다(안 빼면 그림자 모양으로 연기가 뚫린다).
  ② 연기는 **딴 판**(swarmPlate9)에 그린다 — 화면 캔버스에서는 파낼 수가 없다(파낼 자리에 이미 몸이 있다).
  ③ `destination-out` 으로 ①을 파내고 한 번에 얹는다.
· 얹는 자리는 **효과 고리보다 앞**이다(GL 합성 바로 뒤) — 트레이서·피격은 연기 위에 남는다.
· 지상 유닛은 ①에 없으므로 종전대로 연기 밑이다 — 연기의 뜻이 거기 있다.
· **연기가 화면에 없는 프레임은 그 가지에 아예 안 든다** — GL 한 판이 공짜가 아니다.
· `#gl=0` 폴백에는 마스크가 없다(몸을 GL 이 안 그리므로) — 거기서는 종전처럼 통째로 덮는다.

## 오목한 속(패인 그릇)을 낼 때 — 그리고 그것을 덮는 것들
★ **명암으로 흉내 낸 패임은 GL 에서 안 통한다** — 어두운 원반 두세 장을 계단으로 쌓거나
(서포트 베이) 은 원반 + 초승달을 겹쳐 칠한 것(옛 커맨드 꼭대기)은 진짜 깊이에서 그냥
얼룩이다. 꼭짓점이 축 방향으로 물러나야 한다: **`dishFaces9`**(bake9)가 그 자다 —
아가리 테의 중심·반지름·깊이·축(z 는 접힌 모델 자)을 주면 켜와 그늘을 낸다.
· **옆선은 셋 중 하나다**: `pow`(2 = 포물면 · 1.4 쯤이 위성 접시 · 1 은 원뿔) ·
  `dome: true`(구 단면 = **역돔**, 테에서 벽이 서고 바닥이 둥글게 여민다) ·
  맨 안쪽 켜를 **0** 으로 두면 꼭짓점 하나로 모은다(바닥의 평면이 아예 없어진다).
  지적 두 번이 이 손잡이를 낳았다: "바닥이 납작한 접시가 아니라" → "역돔이여야지".
  ⚠ 포물면은 기울기가 **가운데에서 0** 이라 안쪽 절반이 사실상 평면이다 — 넓고 얕을수록
  '납작한 바닥'으로 읽힌다. 깊이는 아가리 반지름의 0.35~0.5 가 접시로 읽히는 자리다.
· ⚠ **아가리를 덮는 판을 얹지 마라** — 컴샛 접시의 '바깥 테'라 적힌 한 장이 꼭짓점 열일곱을
  한 평면에 두어(같은 ρ = 같은 깊이) 패임을 통째로 지우고 있었다.
· ⚠ **등 껍질은 깊이 눈금이 갈릴 만큼 물려야 한다** — 축이 카메라와 14도인 컴샛 접시에서
  0.12 물린 뒷면은 화면 깊이로 0.116 뒤일 뿐이라 안 갈려, 등(볼록)이 속(오목)을 덮었다.
· ⚠ 켜는 **각을 고르게** 나눈다(역돔에서 반지름을 고르게 나누면 테 쪽 한 켜가 깊이의 절반을
  먹어 계단이 진다).

★★ **덩이의 윗면을 파 놓았으면 그 낯을 남기지 마라 — 그 낯이 곧 뚜껑이다**(2026-09, 지적:
"커맨드랑 베이 접시 바닥 평평하면 안 된다니깐" · "베이 역돔 위에 평평한 판 제거해야 할 듯").
2D 는 화가 차례로 그릇을 나중에 얹어 보이지만, GL 은 진짜 깊이라 **위에 있는 판이 이긴다**.
· `frustumFaces3`·`boxFaces3` 는 `noTop` 으로 끄고, **뚜껑을 못 끄는 프리미티브**
  (`cylinderFaces3`)는 뚜껑을 고를 수 있는 기둥(`spirePillar` · `caps: "bottom"`)으로 바꿔 짓는다.
· ⚠ `cylinderFaces3` 의 뚜껑은 `topFace(discPath3(...))` 라 2D 에서는 덧칠이지만, **접을 몸이
  없는 덧칠은 메시에서 제 부품으로 남는다**(지붕 살창·둔덕 얼룩이 그렇게 산다) — 곧 GL 에서는
  꽉 찬 판이다. '덧칠이니 괜찮겠지'가 안 통하는 자리다.
· 윗면을 끄면 그 자리를 **손수 깐다**: 커맨드는 네모 테 ↔ 둥근 아가리의 고리를 마디마다
  네모 한 장으로(감기는 프리미티브 윗면과 같게 — 시계 방향 = 법선이 아래).
· 🔎 **찾는 법**: 그릇을 `fill: "#ff0000"` 으로 칠하고 굽는다. **테두리만 붉고 속이 제 색이면
  뚜껑이 있는 것이다.** 커맨드·베이·로보틱스를 이 한 수로 잡았다.

★★ **두 다각형을 이어 붙인 고리는 GL 에서 꽉 찬 판이다**(2026-09, 로보틱스 테두리 링) —
`polyPath3(바깥) + " " + polyPath3(안쪽)` 은 2D 캔버스에서 **감기 규칙(nonzero)** 으로 안쪽이
파여 고리가 되지만, GL 은 폴리곤을 **낱개로** 삼각화하므로 안쪽 다각형이 **판**이 된다.
로보틱스에서는 그 판(반지름 3.5·0.08 위)이 지붕 한가운데를 통째로 덮어, 지붕을 아무리 파도
그림이 안 바뀌었다. 고리는 **마디마다 네모 한 장인 띠**로 짜거나 `annulusPath3`(제 로프트를
적는다)를 쓴다. 전수 검색으로 이 꼴은 그 한 자리뿐이었다.

## 다각 기둥에 무엇을 붙일 때
★★ **외접 반지름에 얹으면 뜬다**(2026-09, 지적: "커맨드 옆면의 데칼들과 황동띠가 본체에
딱 안 붙음 — 떠 있거나 위가 삐져나옴") — `spirePillar` 로 지은 기둥의 반지름 함수 `rOf(z)` 는
**꼭짓점까지**의 자다. 낯 한가운데는 그보다 `r·(1 − cos(π/sides))` 만큼 안으로 들어가 있다
(14각·r 3.55 에서 **0.089 모델칸**). 그 자에 띠·데칼의 안쪽 면을 맞추면 꼭짓점 자리에서만
닿고 낯 한가운데에서는 공중에 뜬다 — 요잉을 돌리면 떴다 붙었다 한다.
· 고침은 '나란히 선 덩이 사이의 틈'과 같다: **닿게 두지 말고 파고들게**. 안쪽 면을
  그 몫 + 한 뼘(0.03)만큼 벽 속으로 민다(커맨드 `ribFaces9` 의 `sink9`).
· ⚠ **곧은 현은 굽은 벽을 못 따라간다** — 커맨드 데칼은 아래 반지름에서 위 반지름까지
  **납작한 네모 한 장**이었다. 기둥의 옆선은 taper 0.42(아래는 완만하고 위에서 급히 좁아진다)
  라 그 현이 위쪽에서 살 밖으로 삐져나왔다. 띠와 **같은 자**(호를 마디로 나눈 리브)로 지으면
  굽이를 타고, 얇은 두께(0.05)를 주면 어느 각에서도 살에 박힌 채 보인다.
· ★ 붙이는 것의 각 개수를 몸의 낯 수와 맞출 필요는 없다(커맨드는 데칼 12 · 낯 14) —
  **파고들게 하면 어긋나도 된다**. 맞추려 들면 개수를 요청과 다르게 바꿔야 한다.
· ★★ **두께 없는 것(페인트칠)은 '내접'이 아니라 '그 방향의 낯까지'로 재라**(2026-09, 지적:
  "1층 돔의 옆 데칼들이 깊이가 안 맞아서 안 보여") — 내접 반지름은 **낯 한가운데**의 거리다.
  데칼이 낯 한가운데에 있으란 법이 없으므로(데칼 12 · 낯 14), 낯 모퉁이 쪽으로 밀린 데칼은
  **0.21 모델칸까지** 벽 속에 잠긴다. 데칼 깊이 편향이 0.25 라 아슬아슬하게 살거나 그냥 사라졌다.
  · 정n각형이면 그 거리는 **정확히** 셀 수 있다: 낯 한가운데에서 벗어난 각 d 에 대해
    `R·cos(π/n) / cos(d)`. 거기에 0.02 만 띄우면 어느 자리에서도 벽에 딱 붙는다.
  · ⚠ 그러려면 **단면의 위상**을 알아야 한다 — `spirePillar` 의 기본 위상은 `π/sides` 인데,
    그 (u, v) 축이 세계 축과 어떻게 맞물리는지는 주석만 봐서는 못 푼다. **재라**: 메시를 켜고
    기둥 하나를 구워 꼭짓점의 `atan2(x, y)` 를 찍으면 된다(실측: 꼭짓점이 k·360/n · **위상 0**).
  · 검산: `#glbias=3` 으로 깊이 편향을 세 배로 키워도 **그림이 안 바뀌면** 깊이 싸움이 아니다.

## 세계 광원을 쓰는 것은 메시에 구우면 몸과 함께 돈다
★★ **`faceLight` 는 요잉을 먹은 법선으로 재는데 메시는 요잉 0 에서 한 번만 굽는다**(2026-09, 지적:
"빛의 각도상 지금 포탑 옆면이 어두우면 안 되는 각도인데 어둡단 말이지") — 그 자는 `currentYaw()` 를
지난 법선을 세계 광원(`LIGHT_PLAN`)과 내적해 흰·검 덧칠을 내는데, `collectMesh9` 는 **요잉 0 한 벌**만
굽는다. 곧 거기서 난 명암이 `aOv` 로 접혀 **몸에 문신처럼 굳고**, 요잉 0 에서 그늘이던 낯은 어느 각으로
돌려도 그늘이다. 판을 요잉 칸마다 굽던 2D 시절에는 칸마다 다시 돌아 옳았다 — **붓이 바뀌며 무효가 된
값**이고, 87 자리가 그 자를 부른다.
· 실측(`tanksiegegun` 여덟 각 · 화면 왼쪽−오른쪽 평균 휘도. 빛은 화면 왼쪽이니 죽 양수라야 한다):
  +24.7 · +10.5 · **−1.0** · **−10.1** · +12.0 · +25.5 · +23.0 · +28.7 — 90·135 에서 **부호가 뒤집혔다**.
  고친 뒤: +22.8 · +1.3 · +18.8 · +11.8 · +37.4 · +36.9 · +23.1 · +22.3(뒤집힘 없음).
· 고침은 **굽는 쪽을 닫고 셰이더가 내는 것**이다: `MESH9.on` 이면 `faceLight` 의 `face()` 가 빈 목록을
  내고(2D 폴백 `#gl=0`·도록 SVG 는 종전 그대로 — **면 수가 한 톨도 안 바뀐다**), gl9 정점 셰이더가
  **같은 곡선**을 돌아간 법선으로 낸다(0.3 위 흰 · −0.1 아래 검 · `shadeBoost` ×1.25 상한 0.7).
  자는 **수평 성분** `dot(n.xy, uLight.xy) × |LIGHT|`(= faceLight 가 재던 그 2D 내적)이다.
· ⚠ **미봉을 함께 걷어라** — 이 병을 덮으려고 방향광 폭을 0.82~1.08 → 0.68~1.22 로 넓혀 두었는데,
  명암이 제 각으로 돌아온 지금 그대로 두면 빛 쪽이 **두 번** 밝아진다. 옛 값으로 되물렸다.
· ★ 이 꼴을 찾는 자는 **'굽는 시각에 세계를 묻는 것'** 이다 — 요잉·카메라·빛처럼 **그릴 때 정해지는 것**을
  빌더가 물으면, 메시는 그 답 하나를 굳혀 버린다. 그런 몫은 다 셰이더(유니폼)의 일이다.
  (같은 자리를 이미 둘 지났다: 마주 봄 판정 `facingRatio` 는 GL 에 아예 없고, `visible` 은 늘 참으로 연다.)
· 🔎 **재는 자는 좌우 휘도차다** — 여덟 각을 돌려 화면 반쪽씩 평균을 내면, 빛이 세계에 못 박혀 있는지
  몸에 붙어 도는지가 **부호 하나로** 갈린다. 눈으로는 "좀 이상한데"에서 멈추는 자리다.
★★ **`facingRatio` 를 '깊이 키의 크기'에 쓰면 메시에서는 늘 '마주 봄'이다**(2026-09, 지적: "오버로드
주황색면이 계속 비치는데 고정도 아니고 계속 움직이면서 비쳐 오버로드는 가만히 정지상태") — 같은 병의
둘째 꼴이다. 오버로드는 배(임자색 돔)가 매달린 다리·집게를 언제 가리나를 **화가 키**로 흉내 낸다
(`hangK` × 2.6 × `hangFace9`), 그 방향 자가 `facingRatio` 다. 그런데 메시를 굽는 동안 그 문은 **늘 1** 을
낸다(마주 볼 때만 그리는 장식을 3D 에 다 싣기 위한 규약이다) — 곧 GL 에서는 방향 문이 통째로 열려
**몸 뒤로 돌아간 다리까지** 배 위로 올라온다. 요잉이 조금만 돌면 어느 다리가 뚫고 나오는지가 바뀌니
'가만히 있는데 계속 움직이며 비친다'가 된다.
· GL 은 **진짜 깊이**가 있어 이 흉내가 애초에 필요 없다 — `MESH9.on` 이면 그 몫을 **0** 으로 둔다
  (2D 폴백 `#gl=0`·도록 SVG 는 종전 그대로라 스냅샷이 안 바뀐다).
· ⚠ 값은 **키의 크기가 아니라 차례**로 든다 — gl9 의 `aOrd` 는 tagKey 값이 아니라 **정렬된 부품 번호**를
  0~0.3(+0.4)으로 편 것이다. 곧 +2.6 이 2.6 타일 앞으로 나오는 것이 아니라 **그 부품을 목록 맨 끝으로
  보내** 최대 0.7 타일의 편향을 받게 한다. 그래도 배(반지름 2.3)를 이기기에는 넉넉하다.
· ★ 가려 쓸 자리: `facingRatio` 로 **안 그리기**(visible)는 GL 에서 저절로 '다 그린다'가 되어 옳은 쪽으로
  깨지지만, **키에 얹기**는 '다 앞으로'가 되어 틀린 쪽으로 깨진다. 후자는 늘 `MESH9.on` 으로 닫아라.

## 같은 부품을 딴 헬퍼로 지으면 음영의 자가 갈린다
★★ **기둥의 어두운 쪽은 0.38 · 관의 배 그늘은 0.26 이다**(2026-09, 지적: "시즈모드는 포탑 옆면과
포신이 너무 어둡고 … 통일할 수 없나 부품을") — 같은 굵기의 같은 포신인데 탱크는 `tubeFaces`,
시즈는 `spirePillar` 로 지어 놓았다. 관은 배에 **부드러운 그늘 한 겹**(OP.sideSoft 0.26 → 메시에서
×1.25 = 0.33)이고, 기둥은 **낯마다 `faceLight`**(어두운 쪽 상한 0.38 → 0.47)다. 그래서 시즈 포신만
숯빛이 됐다 — 색을 손댄 적이 없는데 색이 다르다.
· 고침은 **자를 맞추는 것**이다: `spirePillar` 에 `litK`(음영 세기 배수)를 두고 시즈 포신에
  **0.68**(= 0.26/0.38)을 준다. 면은 그대로 두고 음영 알파만 곱하므로 꼴이 안 바뀐다.
· ★ 낯을 많이 가진 기둥으로 **관을 흉내 낼 때**는 늘 이 배수를 물어라 — 헬퍼가 다르면 음영의 자도
  다르다는 것이 이 자리의 규약이다.
· ⚠ 함께 고친 것: 탱크 모드에 빠끔 나온 **접힌 시즈 포신 끝**의 깊이 키가 `+0.15`(포탑 몸보다 앞)라
  몸 위에 겹쳐 떠 보였다(지적: "탱크모드의 시즈포신은 키가 안 맞고") — 차체 뒤로 나온 토막이므로
  `−0.25`(몸보다 뒤)라야 포탑이 그 뿌리를 덮는다.

## 눌린 단면은 `trueNormal` 이 있어야 한다
★★ **`oval` 로 누르면 법선은 안 눌린다**(2026-09, 지적: "이제 다 동일한데 포탑만 좀 달라 키값이나
색이나 그런 거") — `spirePillar` 의 `oval: 2` 는 단면을 2:1 로 누르는데, 법선은 **누르기 전 원의
반지름 방향**으로 나간다. 곧 낯의 실제 기울기와 어긋난 법선으로 음영을 매겨 한쪽은 지나치게
어둡고 반대쪽은 거의 안 밝아진다.
· 실측(`model-mesh --kinds tanksiegegun --dump`): 시즈 포신이 **검0.47** 한쪽으로 몰려 바탕
  #6a7286 이 숯빛이 됐다 — 탱크 포신(`tubeFaces`)은 검0.33 · 흰0.30 짝이다.
  `trueNormal: true` 를 주면 꼭짓점에서 법선을 뽑아 눌린 살의 기울기를 그대로 탄다.
· 값: 두 포탑의 중앙 휘도가 105.6 vs 123.9 → **128.8 vs 129.0** 으로 맞는다.
· 🔎 **재는 자는 `--dump` 다** — 접힌 덧칠 세기(흰N·검N)가 낯마다 찍히므로, 한쪽으로 몰린 것이
  눈에 바로 걸린다. '색이 다르다'는 말은 대개 이 수에서 먼저 보인다.
· ★ 이 자를 안 준 `oval` 호출이 또 있으면 같은 병이다 — 눌린 단면에는 **늘** 붙여라.

## 눌린(타원) 살에 띠를 두를 때
★★ **늘이는 축은 눈으로 판정하라**(2026-09, 지적: "해저드 띠가 왜 90도 롤링돼 있지") — 시즈 포신은
`spirePillar` 의 `oval: 2` 로 단면이 2:1 타원이다. 그 위에 두르는 띠(hazSleeve9)를 손으로 짜며 축을
**외적으로 얻은 쪽**(축 × x)에 2 를 걸었더니, 그것이 포신이 눌린 쪽과 **직각**이라 띠만 세로로 선
타원이 되어 통째로 90도 굴러 보였다.
· `spirePillar` 의 u/v 축이 세계 축과 어떻게 맞물리는지는 주석만으로는 못 푼다(위 '다각 기둥' 의 ⚠와
  같은 자리다) — **굽어서 보라**. 띠가 굴러 보이면 늘인 축이 반대다. 여기서는 x(가로)가 긴 쪽이었다.
· ⚠ 이런 어긋남은 **한 각에서는 안 보인다** — 정면에서는 타원이 원으로 보이므로, 45·135 둘을 함께 뜬다.

## 궤도가 도는 것처럼 보이게(자세 컷 셋)
★ **두 컷은 떨림이고 세 컷이라야 방향이 읽힌다**(2026-09, 요청: "이동 시 탱크 캐터필러 실제로
돌아가게 가능?") — 걸음 컷(1 ↔ 3)은 서로의 **거울**이라 앞뒤가 없다. 궤도는 어느 쪽으로 도는지가
보여야 하므로 **0 → 1 → 3** 세 컷을 돌린다: 레일판을 고리 위 자리 `s9` 하나에 판 간격의 0·1/3·2/3
을 더한 그림이다(`POSE_KINDS.roll` · bake9 `tankTrack` 의 `roll9`).
· **자리 하나만 민다** — 윗면·앞뒤 반원·바닥이 한 매개변수 `s9` 로 이어져 있으므로 부품을 따로 돌릴
  일이 없다. 앞(+y)으로 갈 때 윗면 판은 앞으로 흐르니 **더하는** 쪽이다.
· 컷 값은 걸음의 자리(0·1·3)를 빌린다 — 2·4·5 는 공격 컷이라 `atk` 없는 종류에서 `poseTag` 가
  통째로 걸러 버린다.
· ⚠ **실제 굴림 속도는 못 낸다** — 탱크는 초당 40토막쯤 지나가는데 그리는 박자가 30Hz 다. 어느 값을
  줘도 앨리어싱이니, '도는 것으로 읽히는' 박자(4~12Hz)를 눈으로 고른 값이다.
· ⚠ **저배율에서는 안 돈다**(liteView 에서 0) — 몸이 대여섯 화소면 판 사이가 안 읽히는데 컷마다
  메시 한 벌이 더 산다. 그 자리의 kindMain 은 합본 `tank` 다.
· 기동륜(축)은 안 돌린다 — 매끈한 원판이라 어느 각에서도 같은 그림이고, 돌리면 열쇠만 는다.

## 축을 옮겼으면 그 축을 되돌리던 보정을 전부 훑어라
★★ **감아 둔 180 은 어딘가 셋쯤 더 있다**(2026-09, 지적: "시즈모드 후 갑자기 포탑과 포신이 180도 돌아가
있네 — 뒤쪽이 앞을 향한 것") — 시즈 포신을 뒤(−y)로 돌린 뒤(c8e100e), **그 포신이 +y 로 나가던 시절에
감아 둔 `withModelSpin(180)` 셋**이 그대로 남아 겹셈이 됐다: 합본 `tanksiege` 안쪽 · 포탑 판 `tanksiegegun` ·
전환 홑판 `siegebarrel`(그 홑판 셋은 뒤에 한 판 `tankturretxf` 로 모았다 — 위 '딸림 부품은 제 개체다').
· 규약: **포탑 판의 +y 는 겨누는 쪽**이다(엔진이 `rotDeg` 로 표적 각을 준다) — 그래서 `tanksiegegun` 만은
  180 을 **남기고**(모형의 −y 를 그 규약으로 맞춤), 합본·전환 홑판에서는 **걷는다**(거기서는 −y 가 곧
  '차체 뒤'다). 쉬는 각(engine9 `gunRest9` = +90)은 그 위에서 차체 뒤를 가리킨다.
· ★ **감싸개를 걷으면 그 안의 평행이동도 부호가 돌아온다** — 합본의 `TURRET_BACK9` 도 함께 뒤집었다.
· 🔎 **찾는 자는 총구표다**: `tanksiege` 의 앵커가 `[0, −3.75, …]` → `[0, +3.75, …]` 로 뒤집히는 것이 곧
  고쳐졌다는 증거다. 포신 방향은 눈으로 헷갈리지만 이 수는 안 헷갈린다.

## 딸림 부품은 '제 개체'다 — 한 몸으로 보여야 하면 한 판이어야 한다
★★ **attach 로 겹친 판은 GL 에서 서로를 못 가린다**(2026-09, 지적: "아직도 탱크 → 시즈 변경 애니 중에
시즈 포신이 포탑에 안 가려지고 색도 훨씬 밝게 나와") — 전환 창의 포탑은 홑판 셋이었다: 포신 없는 몸
`tankturret0` + 앞 포신 `tankbarrel`(attach2) + 뒤 포신 `siegebarrel`(attach). 붓은 딸림 부품을 **저마다
`gl9.push`** 하므로 셋이 **딴 개체**이고, 깊이 칸은 개체 사이를 화가 차례로 가른다("개체 사이는 칸이,
개체 안은 칸 안이") — 곧 나중에 민 포신이 **늘 포탑 앞**에 선다. 빌더 안의 `tagKey` 는 한 판 안에서만
쓰는 자라 이 자리를 못 가른다.
· **색도 같은 뿌리다**: mesh9 의 덧칠 접기는 '그 pid 의 마지막 몸'과 견주는데(`areaK9`), 포신만 든 판은
  견줄 몸이 달라 그늘이 옅게 접힌다. 실측(자세 3): 홑판 `siegebarrel` 의 가장 짙은 접힘이 **검0.32** 인데
  정착 판 `tanksiegegun` 은 **검0.47** 이다 — 32% 덜 어두우니 "훨씬 밝게"가 된다.
· 고침은 **한 판으로 모으는 것**이다(`tankturretxf` · `tankTurretV2` 의 새 인자 `xf`): 나온 몫 하나로
  탱크 쌍포신을 `1 − xf`, 시즈 포신을 `xf` 로 **함께** 그린다. 홑판 셋은 걷었다. 깊이도 음영도 정착 판과
  같은 자가 되고, 메시도 세 벌에서 한 벌로 준다.
· ★ 가를지 모을지의 자: **딸림 부품은 '제 각을 따로 갖는 것'을 위한 자다**(터렛 머리·성큰 혓바닥처럼
  메시 열쇠를 줄이려고 요잉을 뗀 자리). **몸과 한 각으로 붙어 서로를 가려야 하는 것은 한 판이다.**
· ⚠ 갈라 둔 별본은 **`SHAPE_GALLERY` 에도 적어라**(`hidden: true`) — 광택 표(`glossOf9`)가 그 표로 종족을
  찾으므로 빠지면 그 판만 '그 밖' 값으로 그려진다. 홑판 셋과 `tanksiegelegsF` 가 다 빠져 있었다(전환 판이
  정착 판과 달라 보인 까닭 하나가 이것이다).
· 🔎 **보는 자**: `node scripts/model-gl.mjs --kinds <k> --pose N` (2026-09 에 더했다 — 자세로 움직이는
  판은 자세 0 한 컷만 봐서는 아무것도 안 보인다).

## 시즈 전환의 세 마디
★ **돌기 → 포신 갈아 끼우기 → 쳐올리기**(2026-09, 요청: "변신하기 전에 시즈 정위치로 자연스럽게 회전
이동(정면에서 135도 요잉한 방향) → 포탑의 뒤쪽으로 포신이 나오고(앞 탱크 포신은 들어가고) → 포탑+포신의
각도가 뒤를 쳐올리는 거야"). 여태 ①은 **툭 앉았고**(창이 열리는 순간 고정 방향) ③은 **툭 튀었다**(창이
끝나 시즈 판이 되는 순간 −16도).
· ① 창의 앞 35%(`SIEGE_TURN_U9`)는 **돌기만** 한다 — 참값 각에서 `SIEGE_FACE_DEG9` 까지 가장 짧은 호로
  smoothstep. 원작(order_Sieging)도 몸을 고정 방향으로 돌린 뒤에야 변태한다.
· ②③ 나머지 65%(`xfMech9`)가 다리·포신의 몫이다. 그 안에서 자세 여섯 칸을 둘로 나눈다(bake9):
  **나오고 들어가는 것 `xfOut9` = p/3.5** · **쳐올리는 것 `xfTilt9` = (p−3)/2**.
· ⚠ **포탑 몸도 함께 기울여야** 한다(전환 판 `tankturretxf` 를 `siege: true`·`tiltK` 로 짓고 POSE_KINDS 에
  등록) — 포신만 기울면 창이 끝나는 순간 포탑이 툭 튄다.
· 언시즈는 거꾸로다: 먼저 접고(`xfMech9` 가 앞 65%) 나중에 돈다(뒤 35%).

## spirePillar 로 뚜껑을 덮을 때
⚠⚠ **`tipW` 를 안 주면 끝 뚜껑이 안 난다**(2026-09, 지적: "프로브 프러스텀과 위 뚜껑면 사이 틈이 크게
있는데?") — 위 뚜껑의 문이 `tipW > 0.01` 이고 기본값은 0 이라, **`widthOf` 로 굵기를 직접 그려도 이 문은
여전히 `tipW` 를 본다**. 안 주면 그 끝이 통째로 열린 관이 되어 속(다른 부품의 안쪽)이 들여다보인다.
· 같은 자리에서 둘을 물렸다: 프로브 몸통의 어깨(속의 금색 치마가 뚜껑처럼 보였다)와 스러스터 드럼의
  아가리(뒤 낯이 아예 없었다).
· 🔎 **찾는 법**: `model-mesh --kinds <k> --dump` 에서 **점이 sides 개인 낯**(12각이면 12점)을 세라 —
  뚜껑은 그 한 장이다. 없으면 안 난 것이다.

## 쌓아 둔 원반은 옆벽이 없다
★★ **납작한 원반을 겹쳐 쌓은 '계단 돔'은 하나라도 세우는 순간 틈이 된다**(2026-09, 같은 지적) — 프로브
몸통은 z 4.96·5.2·5.44 에 놓인 원반 셋이었다. 화가 차례로만 서는 2D 에서는 돔으로 읽혔지만, 배를 절두체로
세우자 그 위 원반이 **0.24 만큼 허공에 뜬 것**이 드러났다. 한 자리를 세우면 **몸통 전체를 이어진 회전체**로
지어야 한다(치마 + 어깨 + 뚜껑, 이음매의 두 테를 같은 자리에 두면 닫힌 입체라 뒷면 걷기도 먹는다).
· 얹어 둔 **덧칠(topFace)도 함께 내려라** — 안 내리면 그것 역시 허공에 뜬 데칼이다.

## 누름(Z8)은 회전보다 먼저다
★★ **설계 자에서 돌린 뒤 z 만 0.8배 하면 직각이 안 남는다**(2026-09, 지적: "터렛 포드가 z 좌표 0.8배
누름하면서 옆면이 직사각형이 아니라 평행사변형으로 틀어졌어") — 회전 다음의 **비균일 배수**는 전단이다.
· 고침은 차례를 바꾸는 것이다: **높이를 먼저 누르고**(t·Z8 · 축 높이도 Z8) 그 눌린 자에서 회전을 **강체로**
  건다. 회전은 각을 지키므로 모든 낯이 직사각형으로 돌아온다.
· ⚠ 그만큼 **키가 0.8배 준다** — 정규화(BLD_NORM·MODEL_NORM)를 다시 재라(터렛 1.899 → 2.210).
· ★ 부르는 자리는 **설계 자를 그대로** 쓰게 두고, 누르는 일은 그 함수 **안에서만** 한다.

## 모드가 바뀌어도 같은 부품은 같은 크기여야 한다
★★ **잉크 상자에 '그 모드에만 있는 것'이 들면 본체가 쪼그라든다**(2026-09, 지적: "탱크
시즈모드시 크기가 작아지고 색이 어두워지는데 왜 그러지 모델이 다른가") — 시즈의 잉크
상자에는 네 귀의 **버팀다리**와 뒤로 뻗은 **포신**이 든다. 그 상자를 목표에 맞추면 두 모드가
나눠 쓰는 **궤도·차체가 8.6% 작아진다**(MODEL_NORM tankbody 0.687 vs tanksiegebody 0.628).
· engine9 에 "시즈탱크 넷은 차체 하나의 값으로 못 박는다"는 ★ 가 이미 적혀 있었는데,
  포신을 뒤로 돌린 뒤 `model-norm` 값을 그대로 실어 **그 못이 뽑혔다**. 넷을 0.687 로 되돌린다.
· ⚠ **'어두워졌다'는 색이 아니라 크기다** — 여덟 각 평균 휘도를 재면 138.9(탱크) vs
  140.6(시즈)로 시즈가 오히려 조금 밝다. 작아진 만큼 결·광택의 배율 문턱(k = 상자px/16 ×
  정규화)이 함께 내려가 덜 반짝인 것이다. **어둡다는 말을 색에서만 찾지 마라.**
· 이것은 '짐을 든 별본의 정규화'와 같은 자리다 — **자세·부속으로 달라진 잉크는 재측정으로
  덮지 않는다**. 상한(축)이 1.14 라 0.687 로도 안 잘린다(model-norm 이 그 칸을 찍는다).

## 건설 단계는 '낯 수'로 단조를 못 박아라
★★ **특례를 나중에 얹으면 그 칸만 앞 칸을 앞지른다**(2026-09, 지적: "벙커만 그런 건진 몰라도
공사중 단계가 처음에 더 많이 지어졌다가 오히려 거꾸로 줄어드는 현상 있음") — `stageFaces` 는
단조를 **넓이로 고른 stop 번호**(si9)에만 걸고, 1단의 바닥 특례("맨 아래 층은 통째로")를 그
뒤에 따로 얹고 있었다. 벙커(tombFlat)는 바닥 평면 부품이 여럿이라 그 층이 두꺼운데 2단의
넓이 몫은 0.07 뿐이라, **1단이 2단보다 더 지어진 그림**이 나왔다.
· 고침은 아홉 칸을 한 번에 풀고 **최종 낯 수를 앞 칸 값으로 죄는** 것이다 — 특례가 무엇이든
  거꾸로 갈 수가 없다.
· 🔎 **보는 자**: `node scripts/model-gl.mjs --kinds <k> --stages 1,2,3,4,5,6,7,8,9,0 --rots 45`
  — 한 줄에 열 칸이 서므로 거꾸로 가는 자리가 눈에 바로 걸린다.

## 표창(tri)은 줄기가 아니다
★★ **제 몸을 그린 갈래는 그 자리에서 `continue` 해야 한다**(2026-09, 지적: "도록에서 벌처랑 뮤탈
글레이브가 안 나오고 이상하게 나와") — `st.tri`(글레이브·파편) 갈래가 별을 그려 놓고 빠져나가지
않아, 아래 **일반 줄기**(글로우 + 몸 획)가 그 위에 덧그려지고 있었다. 획 굵기가 `w × 배율 × 3.4`
라 glave(w 2.85)는 도록 배율에서 별을 통째로 덮는 **갈색 얼룩**이 되고 frag(0.95)는 은색 알약이
된다. 지도에서는 배율이 작아 획이 가늘어 '별에 붙은 잔상'으로 읽혔을 뿐 같은 병이다.
원작의 글레이브 웜·파편 수류탄은 **날아가는 표창 하나**이지 선이 아니다 — 미사일 갈래가 제 몸을
그리고 `continue` 하는 것과 같은 자리다.
· ★ **속색이 몸과 같으면 안 보인다** — glave 의 #6b4732 은 **뮤탈의 몸과 같은 갈색**이라, 몸에서
  막 떠난 별이 통째로 묻힌다. 갈래표가 이미 든 번짐색(`st.glow`)을 1.4배 크기로 한 겹 뒤에 깔면
  그 자리가 갈린다(새 표 값을 지어내지 않는다 — 줄기 갈래가 글로우 획을 먼저 긋는 그 짜임이다).
· ★ **도록에서는 별을 키운다**(`FxPaintEnv9.triK9` · 도록 2.2) — 지도의 자 그대로면 글레이브가
  몸의 15% · 파편이 6px 라 칸에서 안 읽힌다. 스플래시를 도록에서만 0.78배로 줄이는 것과 **같은
  자리**다: 무기의 자(FX_BEAM.w)는 안 건드리고 **보여 주는 자리에서만** 키운다.
· ⚠ 도록의 공격 칸은 **총구가 칸 가장자리**라 날 자리가 13px 남짓뿐이다(실측: 뮤탈 140×104 칸에서
  총구 (46, 91) · 사격 방향이 아래-왼쪽) — 그래서 '날아가는 것'은 사실상 총구에 머문다. 칸을 넓힐
  수는 없으니(창이 곧 모델의 잉크 상자다) **크기로 읽히게** 하는 것이 이 자리의 답이다.

## 같은 몸을 두 이름으로 굽지 마라
★★ **부품 등급(autoTier)은 요잉 0 의 화면 넓이로 매기므로, 돌려 구운 쌍둥이는 더 세게 깎인다**
(2026-09, 지적: "탱크/시즈가 같은 각도에서 색이 다른 게 문제" · "색뿐만 아니라 디테일도 달라
보여 애니메이션 시 다른 모델이나 2D 그림 쓰는 거 아닌지 확인") — 2D 는 아니고 기하도 같았다.
갈린 것은 **메시를 어떻게 쪼개고 어떤 이름으로 굽느냐**다.
· 실측: 정착 시즈 `tanksiegebody` 1004부품/1387폴리 = 몸 539/655 + 다리 465/732 ·
  전환 중 `tankbody` 539/655 + 다리 홑판 465/732(310/488 + 155/244) — **합이 정확히 같다**.
· 그런데 등급 예산은 **종류마다**라, 한 덩이로 묶인 쪽이 훨씬 세게 깎인다:
  lod0 에서 tankbody 655 → 597폴리(91% 남음) · tanksiegebody 1387 → 730(53%).
· 게다가 `tanksiegebody` 는 **spin 270** 으로 굽고 몸 각에서 **−270** 을 빼고 있었다 — 둘은 서로
  지우는 짝이라 **같은 세계 각의 같은 기하**인데, 등급을 매기는 요잉 0 에서는 궤도가 끝으로 서서
  훨씬 작게 잡혀 또 한 번 깎였다(다리를 뗀 뒤에도 lod0 328 vs 597).
· 고침은 **한 판으로 모으는 것**이다: 정착 시즈의 차체도 `tankbody`(spin 0 · rotDeg = 앞)를 쓰고,
  버팀다리는 전환 때와 **같은 홑판**(attach·attach2, 자세 5 = 다 뻗음)으로 붙인다. 함께 따라오는
  세 상수: 몸 각의 −270 을 걷고 · 포탑 뒤 치우침의 시즈 갈래를 걷고(두 모드가 같은 자) ·
  쉬는 포신 각 90 → **180**(차체 rotDeg 가 hdg 로 바뀐 몫).
· ★ 유닛의 딸림 부품도 이제 **제 요잉**을 가질 수 있다(`op.attachRot` — 건물 쪽과 같은 자).
· 🔎 **재는 자**: 두 종류를 lod 0~3 으로 굽어 부품·폴리 수를 견주라(`collectMesh9` + `autoTier` +
  `lodFilter`). 수가 다르면 그림도 다르다 — 눈으로 갸웃할 일이 아니라 세면 나온다.

## 더하는 빛은 바탕 밝기를 타야 한다
★★ **절대값으로 더하면 어두운 재질이 통째로 뜬다**(2026-09, 지적: "같은 모델인데 저렇게 다를
이유가 없어 보이고 특히 탱크 모드 캐터필러 색이 검지 않아서 수상해") — GL 셰이더의 더하는 몫
셋이 다 **절대값**이었다. 궤도 레일판(#4b5158 · 휘도 0.30)에서 재 보면:
· 테두리 빛 `+0.10` → **+33%** · 윤기 바닥 `0.30 + 0.70·휘도` → 검은 낯에도 3할이 깔린다 ·
  봉우리(테란 0.24) → **+80%**.
합치면 어두운 낯이 밝은 낯보다 **더 많이** 밝아지고, 셋 다 요잉(법선 × 시선·빛)을 타므로 그
몫이 각마다 들쭉날쭉하다 — 곧 "검어야 할 궤도가 회색이고, 돌 때마다 색이 바뀐다"가 된다.
· 고침은 셋 다 **바탕 밝기를 태우는 것**이다: 테두리 `×(0.30 + 0.70·휘도)` · 윤기 바닥
  `0.30 → 0.10` · 봉우리 `×(0.45 + 0.55·휘도)`. **흰 낯(휘도 1)은 한 톨도 안 바뀐다** —
  밝은 것은 그대로 두고 어두운 것만 내리는 꼴이라 전수 표가 안 흔들린다.
· ⚠ 봉우리를 **0 으로 죄지는 않는다** — "젖은 검은 키틴에도 반사광 한 점은 맺힌다"는 규약은
  그대로다(그래서 0.45 가 바닥이다).
· 실측(tankbody 여덟 각 · 중앙 휘도): 127.7~158.2(폭 **30.5**) → 115.3~140.9(폭 **25.6**).
  몸이 고르게 어두워지고 각에 따른 흔들림도 함께 준다.
· ★ 이 규약은 '**알베도가 낮은 낯은 덜 반사한다**'는 물리 그대로다 — 새 더하기 항을 넣을 때는
  늘 휘도를 곱할지 먼저 물어라.

## 요청이 이름 붙여 뺀 것만 빼라
★★ **"총구와 탄착지 폭발 효과는 제거"는 '셋 다 제거'가 아니다**(2026-09, 지적: "트레이서가 안
나가") — 일반 탱크 모드(`cannon`)를 `MUZZLE_BURST_FX9` 에서 빼면서 그 갈래를 **아무것도 안 그리는
자리**로 보냈다(NO_BEAM_FX 라 선도 없다). 그런데 그 요청이 이름 붙여 뺀 것은 **총구 불꽃**과
**탄착지 폭발** 둘이고, 가운데의 **날아가는 포탄**은 뺀 적이 없다.
· 그래서 갈래를 하나 더 둔다: `SHELL_ONLY_FX9`(지금은 cannon 하나) — 셋 중 ②만 낸다.
· ★ 교훈: 요청이 **목록으로 말할 때는 그 목록만** 건드려라. '그 무리 전체'로 넓히면 말하지 않은
  것까지 사라지고, 그 손실은 한참 뒤에 "왜 안 나오지"로 돌아온다.
· ⚠ **폭발의 자는 두 번 곱해진다** — 엔진이 `fxPx × 1.5` 를 주고 갈래표(`FX_IMPACT.tankboom`)가
  다시 `r 1.55` 를 곱해 실제로는 **쏘는 몸 상자의 2.3배**가 깔렸다(지적: "탄착지 폭발 너무 큰 듯").
  1.0 으로 내려도 몸의 1.55배 · 포구 불꽃(0.38)의 2.6배라 '넓게'라는 뜻은 그대로다.
  **새 크기를 고를 때는 갈래표의 r 까지 곱해 보라.**
· 도록은 거기에 **0.6 을 더 건다** — 시즈는 몸이 4타일이라 그 폭발이 칸을 통째로 주황 안개로
  덮는다(무기의 자는 안 건드리고 보여 주는 자리에서만 줄이는 그 규약이다).

## 앵커는 '그것을 든 판' 위에 찍어 봐야 한다
★★ **합본 모델 위에 찍으면 멀쩡한 표도 틀려 보인다**(2026-09, 지적: "시즈모드 트레이서가 포신
입에서 나오는지 다시 확인") — 탱크 둘은 총구 앵커를 **포탑 판**(tankgun·tanksiegegun)에서 뽑아
합본 이름(tank·tanksiege)으로 싣는다(`muzzle-table` 의 `KEY_OF`). 그 이름으로 `muzzle-sheet` 가
**합본 모델**을 굽고 그 위에 십자를 찍으면, 합본의 포탑은 **쉬는 각**으로 서 있으므로 십자가 90도
어긋난 자리에 앉는다 — 표가 틀린 것이 아니다.
· 판정표가 `MUZZLE_PLATE` 를 뒤집어 **판 이름으로도** 찾게 고쳤다:
  `node scripts/muzzle-sheet.mjs --kinds tanksiegegun,tankgun --rots 0,90,180,270`
  → 십자가 해저드 띠(포구)에 정확히 앉는다(0°는 포신이 화면 안쪽을 봐 가려지는 것이 맞다).
· ★ 지도에서 쓰는 자도 그 판의 자다: 원점은 **포탑 op 의 자리**(`turretAt9.fx/fy`)이고 각은 그
  포탑의 겨눈 각(`mzHdg9`)이다 — 곧 앵커는 '포탑 판의 +y = 겨누는 쪽' 규약 위에 있다.

## 모드가 바뀌는 유닛은 '그리는 판'과 무기를 한 벌로 움직여라
★★ **`siegeOn` 이 아니라 `siegeShow9` 다**(2026-09, 지적: "탱크모드의 트레이서가 왜 자꾸 시즈모드의
트레이서를 쓰는 거야 … 탱크모드에서 시즈 포신이 나오는 애니메이션 동안이 문제라고") — 전환 창이
열리는 순간 `siegeOn` 은 이미 1 이지만 몸은 아직 **탱크 판**이다. 그 동안 시즈 무기를 쓰면 탱크가
시즈의 큰 포탄을 쏜다. `siegeShow9`(= `siegeXf9 ? 0 : siegeOn`)는 **그리는 판이 무엇인가**를 그대로
말하므로, 무기·총구 앵커·갈래가 몸과 한 벌로 움직인다.
· ⚠ 자도 따로다 — 90mm 포와 자주포는 아예 다른 무기다: 포탄 자 **시즈 0.68 · 탱크 0.26**.

## '싸우는가'의 어림이 모드를 모르면 먼 표적이 통째로 빠진다
★★ **탱크는 모드에 따라 사거리가 7.8 ↔ 12.8 로 갈린다**(2026-09, 지적: "시즈 트레이서는 가까우면
나오고 타겟이 멀면 안 나옴") — 참값 이름은 시즈를 걸어도 "Siege Tank (Tank Mode)" 그대로이고,
`turretAim9`·`chaseAim9`(참값 상태가 ST_FIGHT 가 아닐 때 '싸우는 것으로 보는' 유일한 문)는
`siegeOn` 을 셈하기 **전**이라 모드를 모른다. 그래서 정착 시즈가 **8~12타일**을 쏘면 그 문이
7.79 에서 막혀 트레이서가 통째로 안 났다 — 가까우면 나오고 멀면 안 나오는 그 꼴이다.
· 그 자리는 **어림**이므로 두 모드 중 **큰 쪽**으로 연다(`reachMax9`) — 진짜 겨눔 문(`aimTiles9`)은
  뒤에서 `drawUnit2` 로 제 모드의 사거리를 따로 보므로 느슨하게 열어도 새지 않는다.
· ★ 교훈: **모드로 표가 갈리는 유닛은 '표를 읽는 자리'가 모드를 아는지 늘 물어라** — 이름 하나로
  두 무기를 가리키는 자리가 이 코드에 셋 있었다(갈래 · 겨눔 · 싸움 판정).

## 시즈 중이면 무기가 다른 유닛이다
★★ **참값의 이름은 시즈를 걸어도 "Siege Tank (Tank Mode)" 그대로다**(2026-09, 지적: "시즈탱크
재생기에서 트레이서 안 나옴") — 그래서 `fxUnit` 을 갈아 끼우지 않으면 `attackFxOf9` 가
**cannon**(탱크포)을 내고 시즈의 `siege` 갈래를 못 만난다. cannon 은 `NO_BEAM_FX` 라 선을 안 긋고
`MUZZLE_BURST_FX9` 에도 없으므로(요청: "일반 시즈모드에서는 총구와 탄착지 폭발 효과는 제거")
**한 톨도 안 그려진다**. 총구 앵커(fxKind)만 이 갈아 끼우기를 하고 있었다.
· 이름을 한 자리에서 갈면 **갈래·쿨다운·사거리가 한 벌로** 시즈의 것이 된다 — 그 셋이 다 이
  이름에서 나온다(시즈는 사거리 12 · 쿨다운도 다르다).
· ★ 교훈: **'무엇으로 그리나'를 갈아 끼우는 자리가 여럿이면 갈래도 함께 갈아야 한다** — 하나만
  고치면 그림은 시즈인데 무기는 탱크인 어긋남이 조용히 생긴다.

## 날아가는 것의 각은 접선이 아니라 곧은 선이다
★ **포물선의 접선을 각으로 주면 쏘는 순간이 거의 수직이다**(2026-09, 지적: "시즈탱크 포탄
세로로 긴 게 아니라 가로로 긴 타원형이어야 하고") — 전차포의 들림은 사거리의 0.12 라
발사 접선의 세로 몫이 **0.48·사거리**다. 나는 몫이 0.16초뿐이라 눈에 드는 것은 그 앞부분이고,
그래서 에너지포가 늘 세로로 선 타원이었다. 덩이의 긴 축은 **날아가는 길**을 가리켜야 하고
그 길은 총구와 표적을 잇는 선이다 — 포물선은 그 위에 얹힌 들림일 뿐이다.

## 짐을 든 별본의 정규화는 다시 재지 마라
★ **몸이 같은데 자세만 다른 별본은 본체의 배수를 쓴다**(2026-09, 요청: "자원 들었을 때 팔은 좀 모으기") —
팔을 모으면 잉크 상자가 그만큼 **작아지고**, 재측정 값을 그대로 넣으면 배수가 커져 **짐을 들 때 일꾼이
커진다**(실측: scvMin 0.793 → 0.816 · 3%). '비가 안 맞을 때 늘이는 자리'의 ⚠와 같은 자리다 — 자세로
달라진 잉크는 재측정으로 덮지 않는다. 잉크 **중심**은 앵커라 따로다(그 쪽은 재서 올린다).

## 도는 부품이 건물의 자리를 흔들면 안 된다
★★ **애니메이션 부품은 잉크 상자에서 빼고 재라**(2026-09, 지적: "포지 톱니바퀴 회전할 때 위로
들썩거림" → "포지는 톱니바퀴만이 아니라 **전체가** 들썩였는데") — 두 번째 말이 진짜 병을 짚었다.
붓은 건물을 **잉크 상자의 바닥을 바닥선에 앉혀** 놓는데(`footOf(glB9).bot`), 그 상자를 **그 칸의
메시**로 재고 있었다. 포지 톱니는 칸마다 이가 실루엣 밖으로 들고 나므로 상자 바닥이 칸마다
움직인다 — 실측(요잉 여덟 칸 × 회전 여덟 칸): 요잉 45·315 에서 **0.33 모델칸**, 90 에서 0.17.
그 몫이 그대로 건물의 y 로 실려 **건물 전체가** 오르내렸다.
· 고침은 한 줄이다: 상자는 **칸 0 의 메시**로 잰다(`bldMesh({ ...op, spin: 0 })`). 그리는 것은
  종전대로 그 칸의 메시다 — 톱니는 돌고 건물은 안 흔들린다.
· ⚠ 값은 안 든다 — 안 도는 종류는 메시 열쇠가 같아(`spinTag` "0") 같은 벌이 그대로 오고,
  도는 종류만 칸 0 한 벌을 더 쥔다(그 벌은 안 도는 프레임에서 어차피 쓰인다).
· ★ 이것은 터렛에서 이미 적어 둔 규약(**"각은 몸의 요잉으로 못 박아 재라"**)의 **회전 칸 판**이다.
  머리 각 쪽은 막아 두고 spin 쪽은 안 막혀 있었다 — 그 규약은 '도는 값 전부'에 걸어야 한다.
★★ **공사 발판도 같은 자리다**(2026-09, 지적: "테란 건설 완료 시 스캐폴드 없어지면서 건물이 살짝
내려오거든? 건설 중 조금 위로 올라가는 원인 수정") — 회전 칸은 막아 두었는데 **건설 단계
(buildStage)** 만 안 막혀 있었다. 공사 중 메시에는 `stageFaces` 가 세우는 **발판 두 채**가 함께 드는데,
그것들은 건물 **앞 모퉁이 땅에** 서므로 화면 아래로 더 내려온다. 붓은 상자 바닥을 바닥선에 앉히므로
그만큼 **건물이 통째로 떠오르고**, 완성되어 발판이 사라지는 순간 도로 내려앉는다.
· 실측(지도의 건물 각 = footOf 자로 320 · **단계 1~9 가 모두 같은 값** — 곧 범인은 자라는 몸이 아니라
  늘 서 있는 발판이다): 스타포트 **2.55** · 커맨드 1.06 · 아카데미 1.00 · 배럭 0.81 · 리파이너리 0.79 ·
  팩토리 0.22 · 서플라이 0.11 모델칸.
  ⚠ **각을 잘못 잡으면 0 이 나온다** — 요잉 40 에서는 대부분 0.00 이다. `footOf` 에 넘기는 각은
  `−op.rotDeg` 라 지도의 40 은 여기서 **320** 이다. '안 움직인다'가 나오면 부호부터 의심하라.
· 고침은 한 줄이다: 상자를 **다 지은 모델**로 잰다(`bldMesh({ ...op, spin: 0, buildStage: 0 })`).
  딸림 부품 쪽도 같은 자로 잰다(그리는 것은 종전대로 그 단계의 메시다).
· ★ 덤으로 **총구 앵커표(`BLD_INK_BOX`)가 안 오염된다** — 그 표는 종류마다 한 번만 적히는데, 그 종류를
  처음 본 프레임이 공사 중이면 **발판이 든 상자**가 그대로 굳었다.
· ⚠ 값은 거의 안 든다 — 더 굽는 그 한 벌은 완성되는 순간 어차피 쓰는 벌이라 미리 데워 두는 셈이다.
· 재는 자: 단계별 상자 바닥(`scratchpad/stagebox.mjs` 꼴 — `collectMesh9(빌더, f => stageFaces(f, stg, kind))`)
  · 눈으로는 `node scripts/scene-sheet.mjs --race 테란`(공사 중 건물이 함께 선다)을 고치기 전후로 뜬다.
· 재는 자: **`node scripts/spin-box.mjs <종류…>`** — 칸마다 메시의 화면 상자를 요잉 여덟 각으로
  재서 바닥이 얼마나 흔들리는지 찍는다. **그 값이 0 이 아닌 종류가 이 병에 걸리는 종류다**
  (상자를 칸마다 재면 그만큼 건물이 오르내린다 — 붓이 칸 0 으로 고정해 재므로 지금은 안 흔들리지만,
  그 고정을 풀거나 새 애니메이션 부품을 넣을 때 이 자로 먼저 확인하라).
  실측: 포지만 흔들린다(요잉 45·315 에서 0.327 · 90 에서 0.165) — cyber·mshop·trapezoid·sunken 은 0.
  ⚠ **정지 그림 한 장으로는 안 보인다** — 칸마다 따로 구우므로 각 장은 멀쩡하다.

## 도는 부품을 지면에서 자를 때
★★ **중심각이 아니라 '걸치는 구간'으로 보고, 꼭짓점을 죄라**(2026-09, 지적: "포지 톱니바퀴
회전할 때 위로 들썩거림") — 반쯤 파묻힌 톱니바퀴는 판을 현(지면)에서 깔끔히 자르는데,
**이빨은 중심각만** 땅 위인지 보고 있었다(`sin(a) > 0`). 이의 반각이 7.6도라 중심이 2도쯤인
이는 아래 절반이 현 밑으로 삐져나왔고, 그것이 칸마다 났다 사라지니 **도는 것이 아니라
위아래로 들썩이는 것**으로 읽혔다.
· 네 꼭짓점의 각을 [A0, A1] 로 죄면 땅에 닿은 이는 현 위에 납작하게 눕고(넓이 0에서 시작해
  돌면서 솟는다) 바퀴의 아랫변은 어느 칸에서나 정확히 지면선이다.
· 보임 판정도 **이가 걸치는 구간**(a−TW < A1 && a+TW > A0)으로 본다 — 중심만 보면 현에
  반쯤 걸친 이가 통째로 사라져 그 자체가 또 하나의 깜빡임이다.
· ⚠ 이런 떨림은 **정지 그림 한 장으로는 안 보인다**(칸마다 따로 구우므로 각 장은 멀쩡하다) —
  칸 0~7 을 한 줄로 늘어놓고 봐야 잡힌다(`model-shot --spin N`).

## 몸에 딸린 것은 몸이 받는 평행이동을 같이 타야 한다
★ **부양 높이 통일표(MODEL_Z_OFF9)는 표에 든 빌더만 옮긴다**(2026-09, 지적: "일꾼들 미네랄
가스 든 거 위치가 이상함" → "테란·프로토스만 이상하네" → "저그는 괜찮음") — 그 표를 먹이는
고리가 `Object.keys(MODEL_Z_OFF9)` 를 도는 자라, **딴 빌더로 갈라 둔 짐**(load…)은 제자리에
남았다. scv +2.072 · probe −2.168 · drone +0.192 이므로 SCV 는 짐이 2.07 아래로, 프로브는
2.17 위로 어긋나고 드론은 0.19 뿐이라 안 보였다 — **"저그는 괜찮다"가 곧 범인을 가리키는
말**이었다(세 종족의 차이가 그 표의 세 숫자와 그대로 맞아떨어진다).
· 고친 자리는 표가 아니라 **짐 쪽**이다: `loadZ9(몸 종류, …)` 로 몸과 같은 값을 표에서 읽어
  건다. 표를 고치면 짐도 함께 따라간다.
· ★ 부품을 **딴 빌더로 가를 때**(op.attach·load…)는 그 몸이 받는 감싸개를 전부 세어 보라 —
  z 평행이동·배수·요잉이 몸에만 걸려 있으면 딸림 부품이 조용히 어긋난다.

## 비가 안 맞을 때 늘이는 자리
★ **한 종류만 손보기 전에 '그 무리 전체가 작은가'를 먼저 물어라**(2026-09, 요청 세 번:
"마린/파뱃 비율이 이상함 높이를 1.2배해야함" → "마린 파뱃 1.2배 메딕 고스트 1.1배 다 원복
해 줘 그리고 마린 파뱃만 다리길이만 1.2배" → "마린 파뱃 다리길이 원복하고 테란 보병 전체
높이를 1.1배로") — 세 번째가 답이었다. 보병끼리의 비는 이미 서 있었고, 모자란 것은 **다른
유닛에 견준 키**였다. 그래서 넷(마린·파뱃·메딕·고스트)에 **같은 몫**을 준다.
· 자는 모형 공간의 z 배수다(`withModelScale(1, 1, k, …)`) — 꼭짓점에 굳으므로 그리기
  배수(MODEL_NORM)가 아니라 모델 제 좌표가 바뀐다. 보병은 `SUIT_TALL9` 한 자리에 모았다.
· ⚠ **MODEL_NORM 은 다시 재지 않는다** — 재면 잉크 상자가 커진 만큼 배수가 줄어 폭까지
  같이 작아진다(비만 서고 키는 제자리다). '높이를 N배'는 재측정 값으로 덮지 않는 값이다.
· 되돌린 두 길도 적어 둔다(다시 그 길로 가지 말라고):
  ㉠ **키를 통째로 1.2배** — 어깨·헬멧·배낭까지 커져 덩치만 커지고 비가 안 선다.
  ㉡ **다리만 1.2배**(suitLegs 의 zk 가 골반 1.888·무릎 1.072 에만 곱한다) — 다리는 늘지만
     그 위를 `suitBodyRatio` 로 같이 올려야 하고(안 그러면 몸통이 골반에 안 앉는다),
     정작 눈에 띄던 것은 다리가 아니었다.
· 검산: 되돌린 종류의 `model-norm` 값이 **한 자리도 안 틀리고 옛 값**이면 원복이 깨끗하다.

## 모델을 통째로 돌릴 때
`SHAPE_ROT` 은 **2D 길(resolveShapeFaces)만 탄다** — GL(bldMesh)은 `op.rotDeg` 만 보므로 그 표에 적으면 붓이 둘로 갈린다.
모델의 기본 방향을 바꿀 때는 빌더를 **`withModelSpin(도, …)` 로 감싼다**(2026-09, 요청: "파일런 −45도 요잉") — 모형 공간이
셈에 드는 모든 입구(project·depthNow·faceLight·facingRatio)가 그 회전을 지나므로 2D·GL·도록이 한 자를 쓴다.
⚠ 돌린 뒤 `bld-norm`·`ink-center` 를 다시 재라 — 파일런은 둘 다 안 변했지만(잉크폭 10.32 → 10.34) 실루엣이 길쭉한 모델은 변한다.
⚠⚠ **띠·데칼의 키 기준점은 늘 '그 몸통이 tagKey 에 준 그 자리'다**(2026-09, 지적: "앞면 양옆
건물에 창이랑 띠 어디 갔지") — 배럭 좌우 판을 줄이며 그 y 중심이 0 에서 PCY9(1.19)로 옮겨
갔는데 띠만 `depthNow(bx9, 0)` 으로 재고 있었다. 그 판의 몸통 키는 1.9·depthNow(0,1) 만큼
커졌으므로 띠·창이 제 판 **뒤로** 깔려 통째로 사라졌다(그림에서 앞면 띠가 가운데에만 남는다).
덩이의 자리를 옮길 때는 **그 덩이에 얹힌 것들의 키 기준점도 함께** 옮겨라.
이 한 자리에서 **세 번** 물렸다: 앞면 띠·창 → 옥상 갈라짐 선("양옆 건물 위의 갈라짐 선
안 보임") → 옆면 배관. 다 `depthNow(±PX, 0)` 로 굳어 있던 값이다. 덩이를 옮기면
**그 덩이의 x 를 쓰는 키를 전부 훑어라**(배럭은 `depthNow(sx9 * PX, …)` 가 다섯 자리).
얹힌 것이 뒤 기둥 위면 그 기둥의 자리(VC9)로 — 벤트 한 벌이 그렇다.
⚠ `BLD_NORM` 에 손으로 눌러 둔 값(주석에 '상자 상한에 걸림' 따위)이 있으면 **재측정 값으로 덮지 마라** — 파일런의 1.905 가
그것이고, bld-norm 은 돌리기 전에도 2.255 를 냈다(곧 그 차는 회전 탓이 아니다).

## 메시 층·WebGL 시제(2026-09)
- **메시 층**: 도형 헬퍼(polyPath3·관·뿔·돔·구·원통·원반·고리·곡면판, bake9.rodFaces)가 `MESH9.on` 일 때 면의 경로 문자열을 열쇠로
  **판 모형 공간 3D 폴리곤**(`Poly3`, 모델 배율·이동·회전은 먹고 요잉·카메라는 안 먹음)을 곁표 `MESH9.byD` 에 적는다. 2D 그림은
  한 톨도 안 바뀐다(`model-faces-snap --diff` 허용 0.001 로 지킨다). `mesh9.collectMesh9(builder)` 가 빌더 하나를 요잉 0 으로 굽고
  음영 덧칠 면(얕은 알파의 흰·검·종족 광택색, `isOverlay9`)을 뺀 부품 메시를 낸다. 검사·미리보기: `node scripts/model-mesh.mjs
  [--kinds a,b] [--svg out.html --rots 0,45,90,180]`(덮임 154종 100%; 미리보기는 html 캔버스 — svg 는 수천 면에서 스크린샷이 멎는다).
  새 도형 헬퍼를 만들면 `meshPut9(d, polys)` 를 함께 적는다 — 안 적으면 그 부품이 GPU 그림에서 빠진다(덮임 표가 잡는다).
  화면 자로 얹던 **광·무늬 타원**은 `shinePath3(cx, cy, cz, r, ox, oy, rr, ry?)` 로 놓는다 — 2D 경로(screenCircle·
  groundEllipse)는 종전 그대로 내면서 3D 로는 **구 표면에 붙는 고리**를 적는다(구·오브의 흰 광 · 아가리 · 눈).
- **숫자 경로**: polyPath3 가 화면 2D 좌표를 `POLY2` 에 적고 `bake9.pathOf` 가 그 숫자로 Path2D 를 짓는다(문자열 파싱 생략, 판당 약 20%).
- **WebGL 붓(어느 기기에서나 기본 켬 · `#gl=0` 이면 면을 곧장 그리는 느린 폴백)**(`gl9.ts`): 유닛·건물 몸을 **판 없이** 메시로 GPU 가 그린다.
  메시는 열쇠별 한 벌(유닛 `u:종류:자세:lod`, 건물 `b:종류:건설단계:포탑각:불빛:회전:lod`), 상한은 기기 표(`DEV9.glMeshMax`: PC 600 · 폰 240). 정점 셰이더가 `project` 와
  같은 카메라(평면 sin40/cos40 · 입체 pitchFlatNow·0.7/0.9/앞숙임 0.34/시각 밀림 tan(vq), `camOf9`)를 걸고, 자리·배수는 판 블릿과 같은 자다
  (유닛 앵커 (sx, sy−px·0.24−lift)·px/16·MODEL_NORM·원점 줄 12/12.6 · 건물 x 상자 가운데, 잉크 바닥을 바닥선−띄움에). 판이 주던 잉크 상자
  (그림자·링·체력바·BLD_INK_BOX)는 `footOf`(메시 꼭짓점을 카메라·요잉으로 돌려 잰 상자)가 같은 자로 준다.
  · **메시 기록 중(MESH9.on)엔 빌더의 시점 가지가 다르다**: `faceLight().visible` 은 늘 참(뒷면도 낸다), `facingRatio()` 는 1(마주 볼 때만 그리는
    장식도 다 든다) — 2D 굽기에는 안 걸린다(스냅샷 동일).
  · 색: 고정색은 tone9 를 지나고, 같은 경로 위의 흰·검 덧칠(topFace·sideFace·faceLight)은 정점의 aOv 로 접는다(mesh9.collectMesh9).
    접을 때 **2D 와 같은 알파 보정**(shadeBoost, 반투명 색면 ×1.25·상한 0.7)을 태우고 셰이더도 그 세기를 그대로 쓴다 — 0.7 배로 눌렀던 것을
    걷었다(그만큼 모델이 납작했다). 제 꼴을 가진 그늘을 몸에 접을 때는(같은 부품의 마지막 몸) **상자 넓이 비만큼만** 먹인다 — 통째로
    먹이면 작은 부품이 반토막으로 어두워졌다(실측: 일꾼이 든 미네랄 밝기비 0.53 → 0.86).
  · 음영은 셋이다: ① 면 덧칠(aOv, 위) ② 실루엣 빛(silhouetteLight 식, 개체 상자 안의 화면 기울기) ③ **방향광**(2026-09, 2D 에 없던 몫) —
    반쪽 램버트(**0.82 + 0.26**·(0.5+0.5·n·L))에 카메라를 스치는 낯의 **테두리 빛**(+0.10·(1−|n·cam|)⁴)을 얹는다. 효과(uFlat)는 셋 다 안 탄다.
    ★ **폭을 한 번 좁혔다**(2026-09, 지적: "정면으로 빛을 받을수록 환해지는 게 정도가 너무 심한 거 같기도 — 정면 아닌
      부분은 좀더 밝히고 정면은 좀 낮춰 보자") — 방향광과 윤기(광택)가 **둘 다 n·L 로 같은 방향**을 가리켜, 빛을 마주 본
      낯에서 두 몫이 겹쳐 쌓였다. 0.78~1.10 → **0.82~1.08**(등진 낯은 올라가고 마주 본 낯은 내려간다) + 윤기에 바닥 0.10.
      ⚠⚠ (**폐기된 자**) 여기 적혀 있던 '0.5 선'은 옛 gl-check 의 2D 견줌 값이었다 — 0.86~1.06 까지 좁혔더니
        diamond 가 0.501 로 그 선을 넘었다는 기록이다. **그 선은 이제 안 지킨다**(아래 '전수조사'의 ★★):
        2D 는 대비가 더 세므로 GL 이 좋아질수록 그 수는 오르고, 그 선을 지키려는 것이 곧 **없어진 붓에 맞춰
        새 붓을 깎는 일**이었다. 빛을 좁힐지 넓힐지는 **눈으로** 고른다(`model-gl`).
        다만 값 자체(0.82~1.08 · 윤기 바닥 0.10)는 눈으로도 골랐던 값이라 그대로 둔다.
  · 앞뒤: 깊이 칸은 **화면 겹침**으로 나눈다(개체마다 나누면 400기에서 칸이 너무 얇다) + 칸 안은 카메라 가까움 + 부품 차례(aOrd) 편향.
    ★ **개체 사이는 칸이, 개체 안은 칸 안이 가른다**(2026-09, 지적: "모델끼리 겹쳐질 때 각 부품이 별개로 앞뒤가 판단되어서
      정신없이 바뀐다 — 모델 안에서는 각 부품의 값을 비교하지만 모델 간에는 하나로 정해서 비교해야") — 그 지적이 이 자의 규약
      그대로다. `near` 는 **모델 제 좌표**라 어느 개체가 앞인지를 모른다. 곧 겹친 두 개체가 **같은 칸**에 들면 두 모델의 부품이
      서로 끼어들고, 그 짝이 프레임마다 바뀌니 흔들려 보인다.
      옛 셈은 x 로 정렬해 한 번 훑고 **완화를 두 번** 더 도는 꼴이었는데, 이것은 겹침 그래프의 **가장 긴 사슬**을 푸는 일이라
      난전처럼 사슬이 길면 수렴하지 않았다(실측 886기 한 장: 칸 75 → 제대로 풀면 **146** — 절반이 덜 갈려 있었다).
      이제 **화가 차례(i 오름차순)** 로 한 번만 돈다: 나보다 먼저 칠하는(j < i) 겹친 개체의 칸 + 1. j < i 는 이미 확정이라
      한 번에 정확하다(DAG 의 가장 긴 경로). 훑기도 셋에서 하나로 줄었다.
      ⚠ 칸이 늘면 칸 폭(2/(M+1))이 좁아지므로 상한을 둔다(`SLOT_MAX9` — 24비트 깊이 4096 · 16비트 512).
    ★ **데칼은 작아야 데칼이다**(2026-09, 지적: "배럭 큰 세 개 건물 사이 두 개 건물이 반투명하게 보이고 가려져야 할
      부분도 안 가려지는 현상") — `isDecal` 이 `if (p.alpha < 0.98) return true;` 로 **반투명이면 크기를 안 묻고** 있었다.
      그런데 `frustumFaces3` 는 벽마다 제 음영 덮개를 **한 장짜리 반투명 면**으로 낸다 — 배럭 판의 벽 덮개(대각선
      5.5칸)가 통째로 데칼로 잡혀 **1.3 모델칸** 앞으로 끌려 나왔고, 그러면 뒤 판의 덮개가 앞 판을 이기고 그 위에
      그려져 앞 판이 비쳐 보이고 가려야 할 것을 못 가린다. **몸은 멀쩡히 불투명으로 있는데도** 그렇다(실측: 배럭의
      불투명 몸 #373 폴리 5 · 합친 경로 그대로 있다). 크기 문(대각선 < 2.8)을 반투명에도 똑같이 건다 — 돔 뒤의
      초승달 그늘 같은 진짜 데칼은 작아서 그대로 남는다(실측: 18종이 좋아지고 3종이 0.005 안쪽으로 나빠졌다).
    **데칼**(한 장짜리 작은 부품: 줄무늬·창·환풍구)의 깊이 편향(**유닛 0.18 · 건물 0.25 모델칸**)을 **정점(aOrd)에** 얹는다.
    편향을 그리기 단위로 주면 같은 구간의 다른 면까지 끌려 나온다. 구간은 둘뿐이다: 불투명 | 반투명(깊이 안 씀).
    ★ **편향은 자리를 옮기는 자가 아니라 무승부를 가르는 자다**(2026-09, 지적: "배럭 건물 옆면의 띠가 가려져야 하는데
      안 가려지네") — 옛 값 1.3(유닛 0.8)은 **벽 한 장 두께보다 크다**. 배럭 옆면 띠(`bandY9`)는 벽 **표면에** 놓인 한
      장인데 그만큼 앞으로 끌려 나와 **이웃 판을 넘어** 그려졌다. 큰 값의 근거였던 '2D 는 데칼을 벽 안쪽 0.5칸쯤에
      그려 둔다'는 **메시에는 해당이 없다** — 메시가 적는 3D 자리는 진짜 자리(벽 표면)다.
      훑어 재 보면 값도 못 벌고 있었다: gl-check 154종 평균이 편향 **1.3 → 0.6 → 0.3 → 0.12 → 0** 에서
      **0.190 → 0.189 → 0.188 → 0.188 → 0.189**. 0.12~0.3 이 바닥이고 1.3 은 그냥 과했다(0 은 같은 평면의 데칼이
      z 싸움을 해 깜빡인다 — 그 몫만 남긴다). 내린 뒤 41종이 좋아지고 20종이 나빠졌다(평균 0.190 → 0.188).
      **새 편향 값을 고를 때는 `#glbias=N` 으로 훑어 재라** — 짐작으로 키우면 데칼이 이웃 부품을 넘는다.
  · ★★ **제 경로를 가진 작은 그늘은 접지 말고 남긴다**(2026-09, 지적: "배럭 벤트의 가로 줄이 없어짐") —
    접기의 문이 `k9 < 0.6`(상자 넓이 비)이었는데 `areaK9` 는 **그 pid 의 마지막 몸**과 견주는 자라, 몸이
    작으면 1 이 나온다. 그래서 벤트의 가로 살처럼 **제 경로로 또렷이 그린 줄**이 판에 통째로 접혀
    '줄'이 아니라 '조금 어두운 판'이 됐다(실측: 배럭에서 경로가 다른데 k9 ≥ 0.6 으로 접힌 그늘 37장).
    가름의 자는 **경로 + 절대 크기**다: 몸과 같은 경로의 그늘(faceLight 낯 음영)은 낯 전체의 밝기이니
    접고, **제 경로를 가진 한 장짜리 작은 것**(3D 상자 지름 < 2.8 — gl9 `isDecal` 과 같은 문)은 남긴다.
    ⚠ 크기 문을 안 걸고 다 남기면 **판이 허옇게 뜬다**(실측 배럭 0.21 → 0.26) — 벽 한 장을 덮는 큰
      그늘은 데칼 자에 안 걸려 깊이 편향을 못 받고, 몸과 같은 평면이라 깊이 싸움에서 져 아예 안 그려진다.
      곧 **접어야만 보이는** 몫이다. ⚠ '몸의 절반보다 작을 것'을 더해 봤더니 줄이 도로 죽었다 —
      `byPid` 가 가리키는 것이 그 뭉치의 **마지막** 몸이라 견줌의 기준이 엉뚱하다(그 자는 따로 고칠 일).
    값: gl-check 평균 0.188 → 0.191(배럭 0.142 → 0.170 · 시즈 다리 0.471 → 0.360) — **표가 조금
    나빠지고 빠졌던 살창이 돌아온다**. 2D 에 있는 것이 GL 에 없던 자리라 그 쪽이 옳다.
  · ★★ **화면 자로 놓은 장식은 요잉을 안 탄다 — 모형 자로 옮겨라**(2026-09, 지적: "파일런 고리 위
    동그라미 위치 흐트러짐") — 파일런의 청록 띠 여섯은 `groundEllipse(cx + cos·R, cy + sin·R·0.45, …)`
    로 **화면 좌표에** 찍혀 있었다. 화면 자로 그린 원은 3D 기록이 없어 메시가 **가장 가까운 기록점의
    높이를 빌려** 되짚는데, 그 이웃이 갈고리나 수정이면 띠가 엉뚱한 높이에 흩어진다. −45도로 돌려
    세운 뒤 더 크게 드러났다(화면 자 장식은 요잉을 안 타는데 메시는 돈다).
    링과 같은 **모형 자리**(갈고리와 같은 (sinθ, cosθ) · 높이 PY_M)에 `discPath3` 로 놓고 앞뒤도 제
    자리 깊이로 준다 — 되찾을 일이 없고 돌려도 띠가 링에 붙어 함께 돈다.
    실측: **diamond 0.490 → 0.409**(0.5 선의 여유가 처음으로 넉넉해졌다 · 최악이 tankbarrel 로 바뀌었다).
    ★ 같은 꼴이 남아 있으면(화면 자로 더한 중심) 다 이 손이다 — **자리를 옮겨라**(아래 ★★ 뒤로는
      메워 주는 자가 아예 없으므로, 안 옮기면 그 면이 GL 에서 빠진다).
  · **덧칠 면은 몸에 접거나 · 제 부품으로 남기거나 · 버린다**(mesh9). 접을 몸을 두 자로 찾는다: ① 같은 경로 ② 같은 부품 번호(pid)의
    마지막 몸 면. **통째로 얹히는 덧칠은 반드시 접어야** 한다 — 제 부품으로 남기면 모델이 통째로 비쳐 보인다(실측: 보급고 103부품 중
    54개가 반투명이었다). 남기는 것은 둘뿐이다:
      · 몸보다 **작고**(상자 넓이 비 < 0.6) 제 기하가 있는 그늘 — 접으면 부품 전체가 고르게 어두워져 결이 뭉개진다.
      · 접을 몸이 **아예 없는** 데칼(제 뭉치에 몸 면이 없다 — 지붕 살창·둔덕 얼룩) — 단 불투명 몸의 **상자 안**에 들 때만
        (여유 = 상자 지름 12% + 0.4 · 상자는 앞잡이로 한 번 다 돌아 잰다). 안 보면 자리가 틀린 데칼이 몸 밖 허공에 뜬다
        (실측: 라바의 마디 구슬이 사슬로 떴다 · 셔틀 돔의 흰 무늬). 옛날엔 '높이를 빌려 지은 자리가 아닐 것'(GUESSED9)이
        한 문 더 있었는데, 빌리는 길 자체를 걷으며 함께 없앴다(아래 ★★).
  · **개구부(격납구)는 붓마다 다르게 낸다**(2026-09): 오목한 속은 화가 차례로만 서는 2D 가 낼 수 없는 꼴이다 — 속은 앞벽보다
    뒤·뒷벽보다 앞인데 두 벽이 한 프리미티브라 차례로 가를 수가 없다. 그래서 2D 는 속을 벽 **위**에 얹고 새는 몫을 덧댐판으로
    덮는다. GL 은 진짜 깊이가 있어 그 손이 필요 없고, 오히려 덧댐판이 속보다 앞에 서서 **속을 통째로 먹는다**. 그래서 빌더가
    `MESH9.on` 으로 갈라, GL 에서는 벽에 **진짜 구멍**을 뚫고 개구부 둘레만 벽으로 메우고 덧댐판을 하나도 안 댄다(2D 는 종전 그대로 —
    스냅샷이 안 바뀐다). 프리미티브의 손: `frustumFaces3`/`boxFaces3` 의 `omit`(평면 법선으로 벽 하나 빼기)·`noTop`(위에 같은 바닥의
    덩이가 앉는 3단 몸통의 **속살** 윗면 빼기) · `spirePillar` 의 `skipFace`(낯 한가운데를 받는 술어). 쓴 자리는 팩토리·커맨드 격납구다.
    ⚠ 구멍을 뚫으면 **덩이 속의 수평면**도 함께 베어야 한다 — 수평 원반은 같은 화면 자리에서 속벽보다 카메라에 가까워 구멍을
    통째로 메운다(팩토리는 절두체 윗면을 빼고, 커맨드는 받침 테 윗면을 개구부만 베어 낸 채 손수 깐다).
    ⚠⚠ **아래를 보는 뚜껑도 마찬가지다 — 눈에 안 보이면서 깊이만 쓴다**(2026-09, 지적: "격납구 경사로가
      테두리에 가려짐 — 테두리는 저 부분은 없어야 해"). 커맨드 받침 첫 단의 **밑 뚜껑**(반지름 5.15 원반)이
      선체 밑을 통째로 덮는데, 격납구 경사로는 그 밑을 지나 앞으로 나온다. 카메라는 늘 40도 위에서
      내려다보므로 그 원반의 **그림은 한 번도 안 보이지만**, GL 은 깊이를 쓰므로 경사로가 통째로 가렸다
      (2D 는 화가 차례라 안 보이던 자리다). 밑에서 올려다보는 시점이 없으니 GL 에서는 안 깐다.
      ★ **`skipFace` 는 옆 낯에만 걸린다** — 뚜껑(caps)은 그 문을 안 지나므로 늘 따로 막아야 한다.
      ★ 이런 '안 보이는데 가리는 것'은 **떼어 보는 실험**으로 가장 빨리 잡는다(한 줄 고쳐 굽고 되돌리기).
  · GL 뒷캔버스(`.scr-motion-gl9`, display:none)는 붓이 몸을 다 큐에 넣은 뒤 **효과 전에** 유닛 캔버스에 합성한다. 캔버스가 맡는 것은
    크립 얼룩(creepPlate9)·맨 네모·그림자 타원·링·체력바·효과뿐이다. `#gl=0` 이나 WebGL 이 안 설 때만 붓이 면을 곧장 그린다(`SPRITE_PERF.direct`).
  · ★★ **면을 내는 자가 3D 를 함께 적는다 — '승격'이라는 단계는 없앴다**(2026-09, 요청: "우회로 없애고
    설계도도 짓는 것도 3D 로 통일해서 승격하는 과정 없앨 수 있어?" → "게이트로 검사하는 것과 승격 과정
    필요없게 설계부터 바로 100% 3D 로 해서 짓는 건 안 되는 이유가 있나? 2D 로 시작할 이유가 없어 보이는데").
    걷어낸 것: `mesh9.meshFromPath9`(경로 문자열을 글자로 도로 읽어 3D 를 지어 내던 자) ·
    `shapeOblique.unproject9` · 화면점→모형점 표 `PROJ9`(project 가 점마다 적던 몫) ·
    되찾기 면 수 기준선(`scripts/model-mesh-baseline.json`).
    · **세 걸음이 두 걸음이 되었다**: 옛 길은 '화면 자로 짓기 → 3D 로 승격 → 다시 2D 로 사영'이었고,
      지금은 '**3D 로 짓기 → 2D 로 사영**'이다. 사영은 우회로가 아니라 **출력**이다(캔버스 폴백 `#gl=0`·
      도록 SVG·model-shot·`model-gl --vs2d` 가 Path2D 를 먹는다) — 없앨 수 있는 것은 '2D 가 원본인 자리'뿐이었다.
    · **왜 2D 로 시작했었나**: 설계가 아니라 내력이다. 캔버스가 유일한 붓이던 때 짠 코드고 GL 은 나중에 왔다.
    · **왜 위험했나**: 되찾기는 **경로만** 본다 — 반지름 둘이 2% 넘게 다르면 공을 땅에 누운 판때기로 읽었고
      (사이언스 베슬의 몸통이 그랬다), 관이 적어 둔 빈 표를 만나면 엉뚱한 낯을 구로 되살렸고, 한때는
      이웃의 높이를 빌려 장식을 딴 부품 위에 띄웠다(파일런 청록 띠 · 트리뷰널 돔의 광 · 탱크 옆 흰 구슬 ·
      셔틀 돔의 흰 무늬 — 네 번 물렸다).
    · **어떻게 0 으로 갔나**(786 → 0면). 순서가 값이다:
      ① **프리미티브부터** — `cylinderFaces3` 가 몸통만 적고 **윗면 타원은 화면 자**로 내고 있었다.
        그 한 줄이 299면 중 **195면**을 혼자 냈다. ★ 종류별로 뒤지지 말고 공용 헬퍼를 먼저 보라
        (`miss-sites.mjs` 가 짚어 준 줄이 죄다 그 헬퍼를 부르는 줄이면, 범인은 빌더가 아니라 안쪽이다).
      ② **새 3D 헬퍼로 갈아탄다** — 다 **경로 문자열이 글자까지 같고** 3D 만 더 적는다(2D 스냅샷 불변):
        `discPath3(x, y, z, r[, ryS][, seg])` 땅 원반(ryS 는 빌더가 눈으로 누른 **화면** 세로 반지름) ·
        `annulusPath3(x, y, z, ro, ri)` 땅 고리 · `orbPath3(x, y, z, r[, ryS])` 공 ·
        `billPath3(x, y, z, r)` **빌보드 원반**(늘 카메라를 본다 — 꼴이 없는 빛) ·
        `shinePath3` 구 표면의 광 · `shellMesh9`/`sphereSurf9` 구 껍질 위의 무늬.
      ③ **남은 손 면은 빌더가 직접 적는다** — 파일런 링(모형 자리는 PY_M 높이의 둥근 반고리) ·
        포지 돔의 띠 · 크립 얼룩의 테두리 · 스포어 아가리(기둥 축에 수직인 고리) · 패인 그릇의 초승달.
    · **관문은 이제 덮임 100% 하나다**(`node scripts/model-mesh.mjs --check`). 기준선 파일은 **없다** —
      빠진 면은 늘 0 이어야 하는 값이라 눌러 둘 자리가 아니다. 어느 줄이 3D 를 안 적었는지는
      **`node scripts/miss-sites.mjs`** 가 짚는다(화면 자 헬퍼가 SITE9 에 제 호출 줄을 적게 켜고
      빠진 면의 경로를 그 표에 묻는다 — 경로 숫자를 눈으로 뒤질 일이 없다).
    · ⚠ **새 도형 헬퍼를 만들면 `meshPut9(d, polys)` 를 함께 적어라** — 안 적으면 그 면은 되살아나지
      못하고 **그냥 빠진다**(옛날엔 되찾기가 어떻게든 메워 주었다). 그게 이 규약의 값이다: 틀린 기하로
      조용히 사는 대신, 빠져서 관문에 걸린다.
    · ⚠ **조각 수는 짝이 정한다** — `discPath3` 의 기본 12 는 원통 몸통 링(12)과 맞춘 값이다. 뚜껑만 잘게
      쪼개면 모서리를 두 낯이 안 나눠 써 '닫힌 입체' 판정(solidSigns9)이 깨진다. 짝이 없는 큰 땅 원반
      (핵 충격파·구름)에서만 `seg` 를 32 로 올린다 — 16 이면 둘레가 다각형으로 읽힌다.
    · 값: 덮임 156종 **74960/74960(100%)** · `model-faces-snap --diff` 어긋난 종류 **0/156**(2D 그림이
      한 톨도 안 바뀐다) · gl-check 평균 **0.190** 그대로(0.5 넘는 종류 0).
    ★ **빈 표는 '못 적었다'가 아니라 '적을 것이 없다'는 말이다**(2026-09, 지적: "탱크 오른쪽 좀 떨어진
      곳에 구슬이 있어 뭐지") — 관·막대 헬퍼(tubeFaces·rodFaces …)는 제 메시를 **첫 낯 하나에 몰아**
      적고 나머지 낯에는 빈 표(`meshPut9(d, [])`)를 적어 '이건 딴 낯이 낸다'고 표시한다. 셋을 가른다:
      **없음**(빠진 면 — 관문이 잡는다) · **빈 표**(그냥 둔다) · **있음**.
      ⚠ **그 빈 표가 남의 기하를 지울 수도 있다** — 곁표의 열쇠는 경로 문자열이라, 관이 제 둘째 낯에
        적는 빈 표가 **글자까지 같은 딴 부품의 경로**에 떨어지면 그 부품이 통째로 사라진다. 게다가 빈 표는
        덮임 셈의 분모에서 빠지므로 **표에도 안 걸린다**(실측: 핵 사일로의 지름 0.6 짜리 구 하나. 156종을 다
        훑어 이런 충돌은 그 하나뿐이었다 — 곧 경로 열쇠가 실제로 겹치는 일은 7만 면에 한 번꼴이다).
        meshPut9 가 **빈 표로는 있는 기하를 못 덮게** 막는다.
  · **덧칠 판정은 회색 계열만**(mesh9.isOverlay9): 얕은 알파(<0.4) + 휘도 끝(<0.2 · >0.8) + **채도 ≤ 0.25**. 채도 문턱이 없으면
    네온 몸(간헐천 가스 #80ff96, 휘도 0.86)이 덧칠로 접혀 사라졌다(실측: 간헐천 IoU 0.68 → 0.97 · 어사이밀레이터 0.73 → 0.95).
  · 빠진 부품 찾기: `node scripts/model-mesh.mjs --kinds <k> --dump` — 부품마다 색·알파·폴리·z 범위·접힌 덧칠 세기를 찍고,
    3D 가 없는 면의 경로를 `⚠ 빠진 면` 으로 낸다(줄 번호까지 짚으려면 `node scripts/miss-sites.mjs`).
    덮임 셈의 **분모는 3D 를 적어야 하는 낯만**이다: 접힌 덧칠(skipped)과 헬퍼가 **일부러 빈 표를 적어 둔 낯**
    (blank — rodFaces 는 관 하나를 첫 끝 낯에 몰아 적고 나머지에 빈 표를 적는다)은 뺀다.
    그것을 빠진 것으로 세면 표가 거짓으로 낮아지고 ⚠ 목록이 쓸모를 잃는다(실측: 배럭의 '빠진 면' 16 중 12 가 이것이었다).
  · **화면 효과 여섯(warpin·storm·nukeblast·nukecloud·archon·darchon)도 GL 이 맡는다**(`GL_CANVAS_KINDS9` 는 비었다). 발광 종류
    (`GL_GLOW_KINDS9`)의 규약: ① mesh9 glow — 반투명 흰·검 면은 덧칠이 아니라 **몸**(같은 경로에 몸이 있을 때만 접는다) ② 화면 원은
    구가 아니라 **카메라를 보는 원반**(빌보드, 정점 aBb — 요잉을 안 돌리고 가운데만 돌린다). ⚠ 그 판정을 mesh9 가
    경로를 보고 내리던 것은 걷었다 — **빌더가 `billPath3` 로 직접 놓는다**(소환구·아콘·다크아콘·게이트 보석) ③ 깊이 없이 **화가 차례**(불투명·반투명을
    안 가른다)·음영 없음(`flat`, uFlat) ④ 폭풍·핵은 **더하기 합성**(`add`, 2D 의 lighter). 폭풍·핵 폭발·핵 구름은 효과 op 이라 붓이
    GL 큐의 맨 뒤에 `glFxPush9`(판 fxModelCv9 의 fit 자와 같은 자: 모델 상자를 w×h 에 맞춰 가운데에)로 넣고 판은 안 굽는다(`glFx9`).
    효과 메시 열쇠 `f:종류:칸`. 탄두·섬광·연기(모델 아님)는 캔버스에 남는다.
  · **그림자**(2026-09): 몸은 꼭짓점을 **빛 방향으로 밀어 z 를 0 으로 눌러** 바닥에 눕힌다(정점 uShadow · 기울기 SHADOW_K9
    [0.5, −0.25] = −평면빛/높이빛). 실루엣이 그대로 바닥에 깔리므로 높은 부품이 멀리 눕는다. 알파는 `SHADOW_ALPHA9` 0.26 **한 겹**이다.
    ⚠ 무른 가장자리를 낸다고 같은 실루엣을 1.1 배로 한 번 더 깔던 것(SHADOW_BLUR9)은 걷었다(2026-09, 지적: "그림자가 연한 거·진한 거
    한 장씩 두 장") — 배수는 **앵커를 중심으로** 먹으므로 1.1 배 실루엣은 후광이 아니라 **비스듬히 밀린 복사판**이 된다.
    흐림은 그리기를 한 번 더 하는 것으로는 안 난다 — 필요하면 판(FBO)에서 흐려야 한다.
    · **캔버스 타원은 이제 대타뿐이다**(2026-09, 물음: "타원 캔버스 그림자는 이제 걷어낸 거지?") — 공중 유닛에서는
      걷었지만 **부양 지상 유닛**(일꾼·벌처·아콘류)은 문이 `op.air && …` 라, 배율이 문턱을 넘으면 메시 그림자와
      타원을 **둘 다** 받고 있었다(공중에서 한 번 고쳤던 '그림자가 둘'과 같은 자리다). 이제 그 문은 GL 바닥
      그림자의 조건과 **같다** — 타원은 그것이 안 깔리는 프레임에만 선다(덜어내기 1단↑ · 손짓 중 접힘 ·
      `shadowGroundMinZoom` 아래 배율). 그 아래에서도 몸의 자리는 읽혀야 하므로 아주 걷지는 않는다.
      ⚠⚠ **그 문을 갈래 **둘 다**에 달아야 한다**(2026-09, 지적: "프로브 같은 유닛 그림자가 사영 그림자와
        타원 그림자 두 개가 나와") — 부양 갈래에만 `!glShadow9` 를 달았는데, 막힌 몸이 떨어져 나가는 것이
        아니라 **다음 `else if`(땅에 선 몸의 접지 타원)로 굴러들었다**. 부양 지상 유닛은 `op.air` 가 거짓이라
        그 문을 다 지나므로, GL 이 깐 사영 그림자 위에 접지 타원이 한 겹 더 깔렸다.
        ★ 갈래를 `else if` 로 이을 때는 **앞 갈래를 막은 조건이 뒷 갈래의 입구가 되지 않는지** 보라.
    · **나는 몸도 같은 손이다**(공중 유닛): 꼭짓점 z 에 **나는 높이**(uShadow.w, 모형 칸)를 더해 눕히고(그만큼 빛 방향으로
      멀리 눕는다) 앵커가 들린 몫은 화면에서 도로 내린다(dy = lift). 예전에는 몸을 검게 아래로 밀어 그리고 그 아래 캔버스
      타원을 또 깔아, 밀어 놓은 **몸의 복사판** 때문에 그림자가 둘로 보였다(지적). 캔버스 타원은 공중 유닛에서 걷었다 —
      부양 지상 유닛·낮은 배율(GL 바닥 그림자가 안 깔리는 자리)에만 남는다. 이사 중인 건물은 아직 dy 다.
    · **한 화소에 한 겹만** — 그림자는 몸의 삼각형을 그대로 검게 깔므로 겹친 부품(날개 위 포탑)마다 알파가 쌓여 실루엣
      안에 얼룩이 났다. 깊이를 못 박고(uShZ 0.996) 깊이 쓰기를 켠 채 LESS 로 그리면
      같은 깊이의 두 번째 화소가 걸러져 고르게 깔린다(몸 ≤0.99 보다 뒤라 몸을 안 가린다).
  · **닫힌 입체의 뒷면은 버린다**(2026-09, mesh9.solidSigns9 · 정점 aBb 128): 뭉치(pid)의 폴리를 모아 ① 모든 모서리를 두 낯이
    나눠 쓰는 **다양체**인가 ② 이웃을 타고 번져 가며 감기 차례를 맞추고(BFS — 헬퍼마다 감기가 뒤섞여 있다) ③ 부호 있는
    부피로 안·바깥을 정한다. 반투명 뭉치는 속이 보여야 하므로 안 건다. 감기는 **폴리마다** 싣는다(flips) — 한 부품 안에서도
    감기가 섞인다. 버는 것은 **화소 채우기**(닫힌 덩이 넓이의 절반)라 폰의 GPU 몫이 그만큼 준다(부품 66755 중 12894 = 19%).
    ⚠ 이 자는 **2026-09 에 셰이더까지 이어졌다** — 처음 넣을 때(506caef) mesh9 만 고치고 gl9 를 안 건드려, 표식도 셰이더도 없이
    셈만 하고 버렸다(그래서 "그림이 안 바뀐다"는 검산도 저절로 참이었다). 이은 뒤 gl-check 평균 0.181 → 0.180(19종 좋아지고
    8종이 0.002 안쪽으로 나빠진다 — 실루엣 가장자리에서 뒷면이 덮던 한 줄 화소다).
  · **광택(스페큘러)**(2026-09, 요청: "셰이더에 스페큘러 항 넣어서 광택 되살려줘 … 금속뿐 아니라 저그 살점 윤택도 있고") —
    ⚠ 먼저 **없던 까닭**을 적어 둔다: 2D 의 광택 겹(`glowBake9`)은 **부르는 자리가 사라졌다**. 유일한 호출자가 `rasterBld9`
    인데 판 굽기 길을 걷은 뒤 그것을 부르는 것은 `creepPlate9` 하나뿐이고, 그 크립 종류는 바로 그 줄의 `DECAL_KINDS` 로
    걸러진다 — 곧 앱에서 한 번도 참이 안 되는 조건이었다(실측: `[스프라이트] 직접 0`). 그래서 `DEV9.glow`·`glowSet9`·
    폰 1단의 `glow: true` 는 **죽은 스위치**이고, `brushOn9`(결)도 두 쓰임이 모두 glowBake9 안이라 같이 죽어 있다.
    그 몫을 캔버스가 아니라 **정점 셰이더**에서 낸다:
    · **정점에서 재도 정확하다** — 이 메시의 법선은 낯마다 하나(평면 음영)라 한 삼각형 안에서 안 변한다. 화소마다
      다시 잴 몫이 한 톨도 없다.
    · **겹은 둘이다**(옛 GLOW9 와 같은 짜임): ⓐ 빛을 마주 본 낯에 고르게 깔리는 **윤기**(옛 GLOW9.flat ≒ 0.10)와
      ⓑ 한 자리에서 타는 **봉우리**(옛 GLOW9.hot ≒ 0.20). 하나만 두면 어느 쪽도 안 산다 — 봉우리만 두면 다면체라
      몇 낯만 번쩍이고(실측 밝기비 +4%: "티가 안 난다"), 윤기만 두면 그냥 밝아진다.
    · **봉우리는 pow 가 아니라 로렌츠 봉우리**다: `t = (1−n·H)·날카로움` 의 `1/(1+t²)`. pow 는 SwiftShader 에서
      exp2·log2 둘이라 정점마다 지면 비쌌다(실측 헤드리스 폰 4배 조임 p50 267 → 375ms). 곱셈 셋으로 꼴이 거의 같다
      (반값 자리를 맞추면 **날카로움 ≒ 1.44 × 옛 지수**). 반각 H 는 빛·시선이 둘 다 유니폼이라 **CPU 에서 한 번** 낸다.
    · **자는 종족이 든다**(`glossOf9`) — 낯마다 재질을 적으면 빌더 154종을 다 만져야 하고 색으로 짐작하면 틀린다.
      도록 표(`SHAPE_GALLERY`)가 154종을 이미 종족으로 갈라 두었다: 테란 [14, 0.50, 0.30, 0.16, 0.85] · 프로토스
      [18, 0.44, 0.70, 0.14, 0.35] · 저그 [12, 0.22, 0.15, 0.08, 0] · 그 밖 [14, 0.12, 0.30, 0.06, 0.15]
      (날카로움 · 봉우리 · **제 색에 물드는 몫**(금속은 제 색으로 빛나고 살점은 흰 광택이다) · 윤기 · **결**).
    · ⚠ **저그를 넓게 주면 알·고치가 허옇게 뜬다** — 처음에 날카로움 7 로 뒀더니 그것은 봉우리가 아니라 또 하나의
      환경광이었다(lurkeregg 밝기비 1.17 → 1.40, 눈으로도 하얬다). 젖은 살은 '넓게 밝은' 것이 아니라 '한 자리가
      번들거리는' 것이다 — 14 로 좁히고 세기를 내렸다(그래도 테란·토스보다 두 배 넓다).
    · ⚠ 윤기의 각 곡선은 **바닥 0.10 + smoothstep(0.15, 1.00)** 이다(위 방향광의 ★ — 등진 낯도 제 몫의 1할을 받는다).
    · ⚠ **윤기는 바탕 밝기를 태운다**(`0.30 + 0.70·휘도`) — 더하는 값이라 검은 낯에서는 그 몫이 곧 배수다
      (실측: 저그 굴 burrowhole 밝기비 1.48 → 1.79. 하늘을 보는 납작하고 어두운 낯이다). 봉우리는 안 태운다 —
      젖은 검은 키틴에도 반사광 한 점은 맺힌다.
    · **끄면 정말로 안 돈다**: `uGloss` 는 유니폼이라 `if (uGloss.y > 0.0 …)` 가지는 한 그리기 안에서 한쪽으로만 간다.
      손잡이 `#glspec=0|1|N`(표 값에 곱한다) · 기기 표 `DEV9.glSpec`(PC 켬 · **폰은 0단에서 끄고 1단부터 표가 켠다**).
      `#glshade=0` 도 CPU 에서 접어 끈다 — 정점마다 물을 일이 아니다.
    · 값(2026-09 실측): 한 장 **1.21M 삼각 = 3.6M 정점**에 곱셈 여남은 번이다. 헤드리스(소프트웨어 GL·CPU 4배 조임,
      곧 정점 셰이더가 통째로 CPU)에서 중앙값 308 → 358ms 였지만 낱 표본이 217~483ms 로 겹쳐 이 자로는 못 가른다 —
      실기 GPU 에서 정점 몫은 병목이 아니다. 새 패스·새 그리기·새 정점 바이트는 **하나도 없다**(유니폼 둘뿐).
    · ⚠ **테란은 바탕이 이미 회색**이라 봉우리를 제 색에 물들이면(물듦 0.5) 회색 위에 회색을 얹는 꼴이라 대비가
      안 산다(지적: "테란 광택 봉우리 너무 약한 듯"). 프로토스 금이 세 보이는 까닭은 세기가 아니라 **금빛이 제
      바탕과 밝기가 다르기** 때문이다 — 그래서 테란만 세기를 올리고(0.46 → 0.70) **물듦을 내려**(0.50 → 0.30)
      흰빛에 가깝게, 날카로움도 한 칸 좁혔다(23 → 28 · 넓게 퍼지면 그만큼 옅어진다).
      ★ **금속의 광택 세기는 제 바탕색과 얼마나 다른가로 정한다** — 종족마다 같은 숫자를 주면 안 된다.
    · ★★ **봉우리는 둥근 것을, 윤기는 납작한 것을 맡는다**(2026-09, 지적: "동그란 부분은 괜찮은데 테란 건물처럼
      수직 딱딱인 면들은 봉우리가 잘 안 지네") — 셈이 분명하다. 반각 H = 정규화(빛 + 시선) 은 z 성분이 **0.73**
      이라 거의 위를 본다. **세운 낯(nz = 0)이 낼 수 있는 n·H 는 최선이 0.682** 고, 날카로움 28 이면 봉우리가
      0.0087 이다(지붕도 0.0122). 곧 납작한 낯은 봉우리에 **닿을 수가 없다** — 둥근 것은 조각 중 하나가 H 와
      맞아떨어져 제값(0.70)을 받으므로 '둥근 건 괜찮고 납작한 건 안 된다'가 된다.
      그래서 납작한 낯의 광택은 **윤기**(n·L)가 내야 하고, 이것은 한 종족의 일이 아니다(요청: "테란만 올리지
      말고 공통으로") — 넷 다 **날카로움을 넓히고**(28·32·14·17 → 14·18·12·14) **윤기를 올리고**
      (0.04·0.05·0.05·0.02 → 0.16·0.14·0.08·0.06) **봉우리는 도로 내렸다**(한 번 1.45배 올렸던 몫).
      결(vSpec 에 곱한다)도 그만큼 벽에서 보이기 시작한다 — 긁힌 강철은 그 둘이 함께 있어야 읽힌다.
      ⚠ 그전에 테란만 손보며 알아낸 것도 함께 적어 둔다: **금속의 광택 세기는 제 바탕색과 얼마나 다른가로
        정한다**. 테란은 바탕이 이미 회색이라 봉우리를 제 색에 물들이면(물듦 0.5) 회색 위에 회색이라 대비가 안
        산다 — 물듦을 0.30 으로 내려 흰빛에 가깝게 한 것이 세기를 올린 것보다 크게 먹혔다.
    · gl-check 154종: 평균 나쁨 0.174 → **0.190** · 밝기비 1.044 → **1.163** · 0.5 넘는 종류 0(최악 0.498 diamond)
      (가장 밝아진 것은 tanksiegelegs 1.56 → 1.84 · burrowhole 1.48 → 1.65 — 원래도 밝던 작고 어두운 부품들이다). 2D 에 없는 빛이라
      오르는 것이 정상이다(방향광·테두리 빛과 같은 자리).
    · ★★ **봉우리 폭은 낯이 실제로 차지하는 n·H 폭에 맞춘다**(2026-09, 지적: "빛을 90도로 받는 면과 135도로
      받는 면이 있으면 90도로 받는 면은 디테일이 안 보일 정도로 너무 밝고 135도 면에는 이렇다할 봉우리가
      없어" → "아 모든 봉우리가 한 면에 집중이야?") — 그렇다. 바탕 0.45 짜리 테란 낯의 **더한 빛**을 재면:
        경사45°(빛 쪽) n·H 0.933 → 봉우리 **0.267** · 지붕 0.732 → 0.033 · 벽135° 0.659 → 0.021 ·
        벽90° 0.588 → 0.015 · 등진 벽 → 0.003.   **12.7배가 낯 하나에 몰린다.**
      셈이 분명하다: 로렌츠 봉우리의 반폭은 `1/날카로움` 이고 14 면 **0.071** 인데, 이 고정 카메라·고정 빛에서
      **실제 모델 낯이 차지하는 n·H 폭은 0.17~0.93** 이다 — 바늘 하나가 그 폭의 **8%** 만 덮으니 H 와
      맞아떨어진 낯 하나만 타고 나머지는 한 톨도 못 받는다. 다면체에서 그것은 광택이 아니라 '흰 조각 하나'다.
      ★ 그래서 **재질의 교과서 광택도가 아니라 그 폭으로** 날카로움을 정한다: ×0.29(반폭 0.25 — 벽·지붕·경사를
        한 줄로 잇는다) · 세기 ×0.26 으로 **에너지 중립 재분배**(13개 표본 평균 더한 빛 0.0746 → 0.075 로 같다):
        경사45° 0.343 → **0.196**(대비 1.81× → 1.48×) · 벽135° 0.090 → **0.113**(봉우리 몫만 0.021 → 0.046,
        **2.2배**) · 지붕 0.107 → 0.133 · 벽90° 0.033 → 0.049 · 등진 벽 0.012 → 0.013.
        표: 테란 [4.0, 0.13] · 프로토스 [5.0, 0.11] · 저그 [3.5, 0.06] · 그 밖 [4.0, 0.03] · 윤기 바닥 0.10 → 0.05.
      ⚠ **세기를 그대로 두고 넓히기만 하면 그것은 환경광이다**(옛 저그 날카로움 7 의 실패와 같은 자리) —
        넓힌 배수만큼 세기를 내려야 총량이 안 는다. 둘은 **늘 짝으로** 움직인다.
      실측: gl-check 평균 0.187 → **0.184** · 밝기비 1.166 → 1.157 · 47종이 좋아지고 25종이 0.02 안쪽으로
        나빠진다 · **파일런 0.497 → 0.494**(0.5 선의 여유가 오히려 늘었다).
  · **결(긁힌 광택)은 셰이더가 절차적으로 낸다**(2026-09, 요청: "긁힘과 함께 광도 생겨야 하는데" → 광은 위 스페큘러가
    이미 내고 **결은 그 광을 타고 올라탄다**. 옛 2D `BRUSH9` 은 캔버스에 줄을 수십 개 긋는 길이라 GL 로 못 옮긴다).
    · 곱하는 자리가 규약이다: 몸에는 아주 옅게(×0.035) · **광(vSpec)에는 그대로**(×1). 곧 옛 규약 그대로 —
      "결은 낯 전체에 있고, 광택 띠는 그중 어디가 환한가만 정한다". 긁힌 자국은 **빛이 튀는 자리가 갈리는 것**이지
      물감이 바뀌는 게 아니다.
    · **줄 눈금은 모형 좌표로 잰다**(`aNrm`·`aPos`) — 요잉이 돌아도 빛이 바뀌어도 자국이 안 흐른다(결은 낯의 성질이다).
      줄이 서는 쪽은 세계 수직이라, 가로지르는 자는 낯의 **수평 접선** h = (−ny, nx, 0) 이고 눈금은 s = P·h 다.
      낯이 누우면(지붕) h 가 사라지므로 모형 x 로 물러난다. s 는 자리의 **1차 함수**라 정점에 실어 보간해도 정확하다.
    · 무늬는 사인 둘(27·63 · 모형 칸당)에 아주 느린 사인(3.7)으로 **세기를 묶어** 굵은 무리·가는 무리를 낸다.
      ⚠ 처음 9·21·47 로 뒀더니 배율 8에서 8화소 띠가 되어 **골판지**로 읽혔다(실측) — 모형 한 칸이 그 배율에서
        10~20화소(k = 상자px/16 × 정규화)임을 먼저 셈하고 주파수를 고르라.
    · ⚠ **가까이서만 켠다**: k 6 아래 0 · 14 위 full. WebGL1 은 화소 미분(dFdx)이 없어 셰이더가 스스로 못 줄이는데,
      줄 간격이 화소보다 촘촘해지면 결이 아니라 **물결무늬**다. 페이드는 CPU 가 `uGrain` 에 접어 보내므로 낮은
      배율에서는 그 식이 **돌지도 않는다**(유니폼 가지).
    · 이 자는 광택과 달리 **화소마다**다(정점이 아니다) — 그래서 '가까이서만'이 값의 문지기이기도 하다.
      자: 테란 0.85 · 프로토스 0.35(닦은 금이라 옅게) · 저그 0 · 그 밖 0.15. 손잡이 `#glgrain=0|1|N`.
    · ★★ **긁힘은 되풀이가 아니라 드문 사건이다**(2026-09, 지적: "테란 스크래치 … 메탈 헤어라인 느낌이
      아니고 요철 느낌이야") — 처음 짜임은 **진폭이 꽉 찬 사인**(27·63)이었다. 사인은 밝음↔어둠이
      부드럽게 오가므로 그것은 긁힘이 아니라 **둥근 이랑의 음영**이고, 광택에 ×(1 ± 1) 로 걸려 밝은
      벽의 휘도가 **±24%** 흔들렸다 — 골함석이 될 만한 대비다(그림으로 확인: 배럭이 골함석, 커맨드
      돔이 골판 껍질). 헤어라인은 그 반대다: **대개 매끈하고 가는 줄만 드문드문**, 간격도 고르지 않다.
      ① **간격을 되풀이 없이** — 주파수 셋(34 · 43.1 · 26.3)을 무리수에 가까운 비로. 고른 박자가 곧
         '기계로 낸 홈'이다(비가 정수면 몇 화소마다 같은 마루가 서서 이랑이 된다).
      ② **가운데를 눌러 끝만 남긴다**(n·|n| — 부호 있는 제곱). 셋의 합은 어쩌다 한 번만 큰 값에 닿으므로,
         제곱하면 그 드문 마루만 줄로 남고 나머지 낯은 매끈해진다. 이것이 '드문 사건'의 꼴이다.
      ③ **흔들 폭을 줄인다** — 광택 ×1.0 → **×0.55** · 몸 ×0.035 → **×0.012**. 몸(확산)이 띠를 지면
         그건 표면이 울퉁불퉁한 것이다 — 브러시드 메탈의 확산은 고르고, 결은 **반사에만** 있다.
      ⚠ 63 은 k 14 에서 1.4화소로 이미 나이퀴스트 아래여서 지글거렸다 — 걷었다. 맨 위는 43.1(k 18 에서
        2.5화소)이고, 그래서 페이드를 k 6~14 → **9~18** 로 올렸다. 멀리서 헤어라인이 보이는 것은 사실도
        아니다. 실측: gl-check 156종 평균 0.184 → 0.183(움직인 종류 하나) — 결은 그 자가 재는 배율에서
        거의 안 켜지므로 이 손질은 전수 표를 안 흔든다. **눈으로 봐야 하는 값이다**
        (`model-gl --kinds cube,factory,tomb --rots 45 --cell 460`).
    · ★★ **결은 재질이지 낯이 아니다**(2026-09, 지적: "원작을 보면 건물 전체에 헤어라인이 있진 않고
      **보이는 부분이 있는** 거 같아") — 원작 판을 보면 긁힌 마감은 **큰 맨 강철 장갑판**에만 있고
      관·어두운 속·붉은 띠·발판은 맨질하다. 여태는 종족 하나로 켜고 낯을 안 가려 건물이 통째로 같은
      천을 두른 꼴이었다. 낯마다 재질을 적을 수는 없으므로(빌더 154종) **이미 있는 두 자로** 가른다:
      ① **바탕색**(정점의 base) — 채도가 있으면 페인트지 맨 강철이 아니고(임자색·붉은 띠·플라즈마·창),
         너무 어두우면 속 그늘, 너무 밝으면 빛나는 낯이다. 가운데 밝기의 무채색 회색만 남긴다.
         실측 문 값: 강철 회색(#8d94a0·#666e77·#767e87·#8b939d·#7d848d) **1.00** · 어두운 속
         (#3c3c3c·#373d47) 0.12 · 임자색 #2b62e8 · 켠 창 #ffe790 · 프로토스 금 · 붉은 띠 **0.00**.
      ⚠ **부품 차례(aOrd)로 흩는 길은 넣었다가 걷었다** — 판마다 마감을 다르게 주려던 손인데, 표면이
        **여러 부품으로 쪼개진 모델**에서 통째로 무너진다: 스타포트 착륙판은 쐐기 여럿이라 해시가
        쐐기마다 갈려 **부채살**로 떴다(`#glgrain=0` 으로 끄면 사라지는 것으로 범인을 확인했다).
        실물 사진(브러시드 알루미늄)을 보아도 **맨 강철은 판 전체가 고르게 솔질돼 있다** — '보이는
        부분'을 가르는 것은 **재질(색)이지 판 번호가 아니다**.
      결과는 varying 에 실어 넘긴다 — 낯마다 한 값이라 보간해도 안 흐른다.
    · ★★ **결은 겹이 둘이다**(2026-09, 보기로 받은 브러시드 알루미늄 판 사진) — 실물 결은 **빽빽한 잔
      줄이 낯 전체에 깔리고**(길이로 이어진다) 그 위에 **드문 기스 몇 가닥**이 얹힌 것이다. 한 겹만 두면
      둘 다 틀린다: 잔 줄만 두면 긁힌 자국이 없고, 기스만 두면 판이 맨질해서 금속으로 안 읽힌다
      (한 번 기스만 남겼다가 '빽빽한 결'이 빠져 되돌린 자리다). 둘은 **같은 줄자리**(n)를 나눠 쓴다 —
      기스도 솔질 방향을 따라 나기 때문이다.
      ⓐ 바탕 결: `n·|n| × (0.60 + 0.40·q)` — 가운데를 한 번만 눌러 골판지를 막고, 길이 자로 세기를
        느리게 흔들어 줄이 흐려졌다 진해졌다 한다(사진의 그 얼룩덜룩함).
      ⓑ 기스: `n⁵ × 2.0 × smoothstep(0.30, 0.95, q)` — 마루만 남겨 가늘게, 봉투로 양끝을 여위게.
      섞는 자: `g = 바탕·0.8 + 기스` · 몸 ×0.018 · 광택 ×0.45 · 흰 섬광은 기스의 도드라진 쪽만.
      ⓒ **긴 기스도 섞는다**(요청: "판의 크기에 따라 길이도 좀 길기도 해야 할 듯") — 파장을 3.5 배로
        늘린 둘째 봉투를 **더 높은 문턱**(0.55)으로 얹어 드물게만 켠다. 파장이 11 모형칸이라 **작은 부품은
        그 토막을 담을 수가 없고 큰 판에서만 긴 줄이 나온다** — 곧 판이 클수록 기스도 길어진다(면 크기를
        셰이더가 몰라도 되는 길이다).
      ⚠ **줄 수를 늘리면 페이드도 같이 올려야 한다**(요청: "솔질 있는 부분에 좀 더 숫자 늘리기") —
        주파수 ×1.3(34·43.1·26.3 → 44.2·56·34.2)과 함께 페이드를 k 9~18 → **11~21** 로 올렸다.
        촘촘해진 만큼 한 줄이 차지하는 화소가 줄기 때문이다(k 21 에서 맨 위 56 이 2.4화소).
        ★ 이 '한 벌의 고정 주파수 + 통짜 페이드'는 아래 ★★ 에서 **옥타브**로 바뀌었다.
    · ★★ **결은 배율을 타야 한다 — 주파수를 못 박으면 가까이서 굵어지기만 한다**(2026-09, 지적:
      "테란의 메탈 결 — 스케일을 생각하면 결이 훨씬 더 얇아야 하고 밀도도 높아야 해"). 셈이 분명하다:
      주파수가 고정이면 화소 주기가 **배율에 비례**해 커진다(맨 위 56 이 k 21 에서 2.4화소 · k 31 에서
      3.5화소 · k 47 에서 5.3화소). 실물 금속은 반대로, 다가갈수록 **더 가는 줄**이 드러난다.
      · 옥타브를 **다섯**(30·42·58·80·110 rad/모형칸)으로 늘리고, 몫을 **CPU 가 배율로 깎아** 보낸다
        (`uGrain.y` + `uGrainA` · `grainOct9`): 화소 주기가 2.1 아래면 0, 3.1 위면 제 몫.
        곧 **멀리서는 굵은 줄 몇, 가까이서는 가는 줄 여럿**이고, 화소보다 촘촘한 줄은 애초에 안 켜지므로
        물결무늬가 안 진다(WebGL1 은 dFdx 가 없어 셰이더가 스스로 못 줄인다).
      · ⚠ **몫의 합을 1 로 맞추지 마라** — 맞추면 저배율에서 가는 옥타브가 꺼진 몫까지 굵은 옥타브가
        떠안아 나이퀴스트 아래에서 **혼자 커진다**. 안 맞추면 합이 곧 '지금 보이는 결의 총량'이라
        멀어질수록 저절로 여윈다(0.34 → 1.00).
      · ⚠⚠ **거듭제곱으로 줄을 얇게 만들려 하지 마라** — n·|n| → n³ 로 날을 세워 봤더니, 줄이 얇아진 것이
        아니라 **느린 세기 봉투의 마루만 남아 굵은 띠**로 뭉쳤다(실측 렌더: 착륙판에 폭 100화소짜리 쓸린
        자국 몇 줄). **얇고 촘촘한 것은 주파수가 내고, 안 거슬리는 것은 낮은 대비가 낸다** — 그래서
        바탕 결은 사인 합 그대로 두고 광택에 **±13%** 로만 얹는다(옛 ±45%). 기스만 ±55% 로 도톰하게
        따로 얹는다(한 값으로 묶으면 잔 줄을 보이게 올릴 때 기스가 골함석이 된다).
      · ⚠ 세기 봉투도 함께 잘게 쪼갠다(3.1 → 9.0 · 흔들 폭 0.62±0.38 → 0.80±0.20) — 파장이 모형 두
        칸이면 그 마루가 가까이서 **넓은 얼룩**으로 읽힌다.
      · 켜는 문(k ≥ 11)은 이제 **값의 문지기**일 뿐이다 — 나이퀴스트는 옥타브마다 따로 보므로, 그 아래
        에서는 어느 옥타브도 몫이 0 이라 튐 없이 꺼진다.
      · 값: 화소당 사인 셋 → 다섯(결이 켜진 화소에서만 · 테란·프로토스만 · 가까운 배율만).
        눈으로 봐야 하는 값이다: `model-gl --kinds plane,cube --rots 45 --cell 760`.
    · ★★ **줄에는 자가 둘 필요하다**(2026-09, 요청: "헤어라인은 얇고 **양끝은 더욱 얇아야** 해 — 기스
      느낌" · "빛을 받는 면의 헤어라인은 유난히 밝게 빛나기도 해(거의 흰색)") — 가로지르는 자(vGr.x)
      하나만으로는 줄이 **끝이 없다**(그 자의 함수이므로 길이 방향으로 값이 안 변한다 = 낯을 가로지르는
      무한한 줄). 기스는 시작과 끝이 있는 **토막**이다. 그래서 낯 위의 **둘째 축**을 함께 싣는다:
      줄이 서는 쪽이 세계 수직이므로 세운 낯은 **높이 z**, 누운 낯(지붕)은 모형 y 다(vGr.y).
      · **길이 봉투** `smoothstep(0.30, 0.95, ½+½·sin(1.9·y + 5.3·x))` 로 토막을 내면 양끝이 여윈다.
        ⚠ 위상에 **가로 자를 섞어야**(×5.3) 한다 — 안 섞으면 이웃 줄의 토막 끝이 한 줄로 서서
        **가로 띠**가 된다(줄이 아니라 무늬가 된다).
      · **n⁵ 로 더 얇게** — n·|n| 는 아직 굵었다. 셋의 합은 어쩌다 한 번만 큰 값에 닿으므로 제곱을
        거듭할수록 그 드문 마루만 가는 줄로 남는다(이득 2.0 으로 여윈 마루를 도로 세운다).
      · **흰 섬광은 빛 받는 낯에서만** — '얼마나 빛을 받나'는 이미 광택(vSpec)이 들고 있다. 그 휘도를
        곱하면 등진 낯에서 그 항이 **저절로 0** 이 되므로 문을 따로 둘 일이 없다. 도드라진 쪽(g>0)만
        태운다 — 기스는 빛을 튕기는 자국이다(`+ w·2.4·max(g,0)·휘도(vSpec)`).
      실측: gl-check 156종 평균 0.185 · 밝기비 1.161 — **움직인 종류 0**. 이 자가 재는 배율에서는
      기스가 몇 화소라 표를 안 흔든다. **눈으로 봐야 한다**(`--kinds cube,tomb,plane --rots 45 --cell 500`).
    · ★ **광택을 내는 빛에 색을 줄 수 있다**(2026-09, 요청: "테란 건물에 빛나는 효과 … 불빛을 좀
      청색톤을 넣을 수 있나") — `uSpecTint` 를 새로 두고 광택 표의 여섯째 칸이 그 색을 든다.
      테란만 **찬 강철빛** [0.82, 1.02, 1.36] 이고 나머지는 흰빛이다. 함께 테란의 **물듦을 0.30 → 0.18**
      로 내렸다 — 회색 바탕이 섞이면 파랑이 죽는다.
      ⚠ **휘도를 1 로 맞춰 둔다**(0.299·0.82 + 0.587·1.02 + 0.114·1.36 = 1.00) — 색만 바꾸고 밝기는
        안 건드리므로 0.5 선이 안 흔들린다. 색을 더 세게 하려면 이 비를 지키며 기울여라.
      ⚠ 이것은 **일부러 2D 와 달라지는 값**이다 — gl-check 평균 0.183 → 0.185 · 테란 35종이 0.01 안팎
        나빠진다(색차를 재는 자이므로 당연하다). 0.5 넘는 종류 0 · 최악 0.494(diamond)는 그대로다.
    · ★★ **넓은 낯의 '전구 반사'는 거울 반사로는 못 낸다 — 얼룩으로 그려야 한다**(2026-09, 요청: "넓은
      면에도 봉우리가 있어서 전구나 태양빛이 반사되듯한 강한 부분이 있으면 좋겠는데 되나?") — 셈이
      분명하다. 이 카메라는 40도로 **내려다본다**(시선 c = (0, cos40, sin40)). 세운 낯(nz = 0)이 어떤
      빛을 거울로 비치려면 그 빛이 반사 방향 `r = 2(n·c)n − c` 에 있어야 하는데, 그 **z 성분은 어떤
      벽에서도 −0.643** 이다 — 곧 **지평선 아래**다. 빛을 어디에 놓아도(해든 전구든, 평행광이든
      점광원이든) 세운 벽에는 거울 반사가 안 맺힌다. **벽이 비추는 것은 하늘이 아니라 땅**이기 때문이다.
      (반각을 정점마다 재는 점광원 길도 따져 봤지만 최고값 0.68 은 그대로다 — 자리마다 조금 달라질 뿐이다.)
      그래서 이것은 **그리는 몫**이다: 빛 쪽 한 점(LIGHT×7)을 잡고 거기서 멀어질수록 여위는 밝은 얼룩을
      모형 자리에 얹는다 — 옛 2D 광택 겹(glowBake9)이 캔버스 그라디언트로 하던 바로 그 몫이고, 판 길을
      걷으며 사라졌던 것이다. 이제 모형 공간이라 **요잉을 돌려도 얼룩이 같이 돈다**.
      ⚠ 자리는 **요잉을 먹인** 좌표(rx, ry, z)라야 한다 — aPos 를 그대로 쓰면 개체가 돌 때 얼룩이 몸에
        붙어 같이 돌아 빛이 아니라 무늬가 된다. ⚠ 등진 낯에는 안 얹는다(×ndl).
      ⚠ **세기는 파일런이 정한다** — 종족 구분 없이 주면 diamond 가 **0.500** 으로 선에 닿는다(실측).
        표에 일곱째 칸을 두어 테란 1.0 · 프로토스 0.40 · 저그 0.60 · 그 밖 0.50 으로 나눴다(diamond 0.498).
    · ★ **푸른빛은 센 자리에만**(2026-09, 요청: "강하게 빛나는 곳은 푸른색을 입히고 아닌 곳은 원래색이
      나오게") — 광택 전체에 한 색을 먹이면 판이 통째로 차가워진다. **세기로 가른다**:
      `hot = smoothstep(0.08, 0.30, 윤기+봉우리+얼룩)` 으로 약한 쪽은 **제 바탕색**(물듦), 센 쪽은
      찬 강철빛. 곧 **물듦은 약한 쪽의 색이고 uSpecTint 는 센 쪽의 색**이다 — 그래서 테란 물듦을
      0.18 → **0.55** 로 도로 올렸다(약한 자리가 제 색으로 보이라고).
    · ★ **봉우리 높이는 파일런이 정한다**(2026-09, 지적: "빛나는 봉우리가 너무 약한 듯 티가 잘 안 남") —
      폭을 넓히며 에너지 중립으로 내렸던 세기를 도로 올렸다: 테란 0.13 → **0.24**(거의 두 배) ·
      저그 0.06 → 0.08 · 그 밖 0.03 → 0.04. ⚠ 프로토스는 **0.18 까지 올렸다가 0.13 으로 되물렸다** —
      그 값에서 diamond 가 **정확히 0.500** 으로 선에 닿았기 때문이다(0.13 에서 0.496).
      **봉우리를 올릴 때는 늘 diamond 를 먼저 보라** — 가장 먼저 선에 닿는 종류다.
      값(전체): 평균 0.185 → **0.194** · 밝기비 1.161 → 1.193 · 0.5 넘는 종류 0.
      ⚠⚠ **GLSL 주석에 역따옴표를 쓰지 마라** — 셰이더는 JS 템플릿 문자열 안에 있어서 역따옴표 하나가
        문자열을 끊고 `TS1005` 로 터진다. 이 자리에서 두 번 겪었다(`#glspec=0` · n·|n|).
  · **번짐(블룸)**(2026-09) — 빛나는 낯만 **1/4 크기 판**에 한 번 더 그리고(정점 셰이더가 나머지를 클립 밖으로: uEmit),
    가로·세로로 흐린 뒤(다섯 번 읽는 가우시안 · 핑퐁 판 둘) 화면에 **더한다**. 2D 판에는 없던 몫이다.
    · **밝기 문턱으로 안 고른다** — 색만 보고는 '빛'과 '진한 물감'을 못 가른다(프로토스 금 #e6d063 은 휘도 0.85·채도
      0.51 로 켠 창 #ffe790 과 거의 같다). 빛나는 자리를 **표식으로** 고른다: 발광 종류(폭풍·핵·아콘·워프인)는 밝은
      색 면만(어두운 속 몸·임자색 면은 몸이다), 그 밖은 `shapeOblique.EMIT_FILL9` 에 든 색만 — 그 표는 **켜지는 자리가
      스스로 적는다**(bake9 `winLit`·`glowLit` 이 불이 켜질 때 제 색을 넣는다. 늘 빛인 플라즈마는 미리 적혀 있다).
      ⚠ 그래서 표는 **굽는 차례를 탄다**(켜진 건물을 아직 안 구운 판에서는 그 색이 표에 없다) — 메시 열쇠가 불빛(litTag)을
      가르므로 꺼진 판이 빛나는 일은 없지만, 새 빛 색을 넣을 때는 winLit·glowLit 을 지나게 하라.
    · 정점 표식은 aBb 한 칸의 **비트**다: 1 빛 · 2 닫힌 입체 · 4 빌보드.
    · 값은 거의 다 '화면 한 겹 더하기'라 **가위(scissor)로 빛이 있는 자리만** 훑고, 화면에서 14px 보다 작은 개체는
      건너뛴다(낮은 배율에서는 그 자로 거의 다 걸러져 번짐이 저절로 꺼진다). 실측(헤드리스 폰 프로필·CPU 4배 조임):
      전체 화면 8배 → 가위·작은 것 걸러내기 뒤 1.4배. PC 프로필은 두 쪽 다 p50 67ms(헤드리스 프레임 박자)로 차이가 안 보인다.
    · 기본은 **기기 표 + 벤치 단**(`DEV9.glBloom`): PC 켬 · 폰은 **1단(보통)부터 켠다**(PHONE_TIERS9 — 요청:
      "번짐도 보통 단에 넣어줘". 가위로 빛 있는 자리만 훑으므로 폰 프로필 p50 이 같았다: 66.6 → 66.6). `#glbloom=0|1` 로 끄거나 못 박는다.
      벌의 `meshMax`·`bloomOn` 은 읽기 전용이 아니다 — 단이 오르면 `glUnits9` 가 다음 칠하기에서 그 벌에 새 값을 일러 준다.
      `perf-check --shot x.png --probe-gl` 첫 줄이 그 둘을 찍는다(`상한 360벌 · 번짐 켬`).
    · ⚠ **`model-gl` 은 기본으로 번짐을 끈다**(`--hash` 기본값 `glbloom=0`) — 옛 2D 견줌에서 온 값이다
      (2D 에 없는 빛을 얹으면 발광 종류의 색차가 통째로 뛰었다: 폭풍 0.04 → 0.59). **번짐까지 눈으로 보려면
      `--hash ""`** 로 켜라 — 이제 2D 와 안 견주므로 끌 까닭이 그림 취향 말고는 없다.
  · **폴리곤은 귀 잘라내기로 삼각화한다**(2026-09, gl9 `triIdx9` · 지적: "넥서스 … 피라미드에서 나오는 삼각발판") —
    여태 **0번 꼭짓점 부채꼴**이었다. 그것은 볼록하거나 0번에서 별꼴인 다각형에서만 옳다: 넥서스의 표창 발판 윗면은
    **네 날 별**(날 끝 ↔ 오목점이 번갈아 여덟 꼭짓점)이고 0번이 날 끝이라, 부채질이 **날 사이 오목한 자리까지 메워**
    날이 넓은 판때기로 퍼졌다(2D 는 Path2D 가 제 꼴로 채우므로 안 났다 — 붓이 다르면 드러나는 자리다).
    이제 낯의 법선에서 지배 축을 빼 2D 로 눕히고 **볼록하면 종전 부채꼴**(빠른 길)·**오목하면 귀를 하나씩** 자른다.
    삼각형 수는 늘 n−2 라 미리 잡은 버퍼가 그대로 맞고, 꼭짓점이 서른 남짓이라 O(n²)도 값이 없다(메시는 한 번 짓는다).
    실측: gl-check 평균 0.176 → 0.175 · 11종이 좋아졌다(넥서스 0.203 → 0.146 · 뮤탈 −0.035 · 벌처 −0.021 · 크립얼룩 −0.015).
  · **몸 밖을 감는 장식은 2D 에서 '걷지' 말고 키를 낮춰 뒤로 보낸다**(2026-09, 고치 밑동의 장기 파이프) — 껍질 **위**를
    타는 핏줄은 등진 것이 실제로 안 보이므로 `facingRatio` 로 걷어도 되지만, 껍질 **밖**을 감는 관은 등진 것도 옆구리로
    삐져나온다. 걷으면 2D 에만 그 몫이 없어 GL 과 어긋난다(실측: gl-check 0.315 → 0.393 · GL 화소가 28% 많았다).
    뒤쪽 것을 **몸보다 낮은 키**로 따로 묶어 화가 차례가 몸 뒤에 깔게 하면 두 붓이 같은 그림이 된다(0.393 → 0.196).
  · **평평한 네모로 흉내 낸 '이음매'는 GL 에서 판때기다** — 고치(cocoon)는 껍질 두 쪽이 맞물린 자리를 그 높이의
    **현(弦)에 깐 네모 + 옅은 덮개**로 냈다(몸 면 없는 덧칠). 2D 는 화가 차례가 돔 위에 얹어 그늘로 읽혔지만, 진짜
    깊이에서는 그 네모가 굽은 낯을 뚫고 지나가 **반투명한 직사각형 띠**로 떴다(지적) — 걷었다. 그런 결은 화면 자의
    네모가 아니라 **회전 단면을 타는 관**(고치 핏줄처럼 spirePillar)으로 지어야 한다.
  · **되찾은 원의 조각 수**(mesh9 RSEG9 32 · meshSphere9 12×20): 16 조각이면 핵 충격파·워프인의 둘레가 다각형으로 읽혔다.
  · GL 캔버스는 **미리곱한 알파**(premultipliedAlpha: true · 셰이더가 rgb·a 를 내고 합성 (ONE, 1−a)) — 예전(SRC_ALPHA 짝)은 알파 채널이
    a² 로 쌓여 반투명 빛무리가 어두웠다(밝기비 0.75 → 1.0). 더하기는 (ONE, ONE).
  · **폰도 기본 켬(2026-09)** · 정점은 **36바이트**(pos·nrm float, rgb·team·alpha·덧칠·빌보드는 정규화 바이트, 부품 차례 float — 예전 float 15개 60B)이고
    footOf 용 사본은 **겹치지 않는 꼭짓점 xyz 만**(정점 사본 통째 270KB/벌 → 수십 KB). 메시 상한은 기기 표 `DEV9.glMeshMax`(PC 600 · 폰 240;
    실측 폰 프로필 157벌 24MB → 240벌 37MB). 데우기는 rAF 당 6ms·8벌로 나뉜다. 처음 보는 종류의 메시를 지은 몫은 `glBakeMsTake9()` 가
    프레임마다 내고, 시계가 그것으로 '굽는 프레임'을 안다(옛 판 굽기의 그 문지기 자리).
  · ★ **메시 보관함은 LRU 여야 한다**(2026-09, 지적: "모바일에서 화면 이동도 안 하고 유닛 변화도 거의 없는데
    모델 굽는 중이 계속 나오는 현상") — `meshFor` 가 **찾았을 때 다시 안 넣어** 버리는 자가 Map 의 **삽입 차례**를
    보고 있었다(FIFO). 삽입 차례는 '얼마나 오래되었나'이지 '얼마나 안 쓰나'가 아니다 — 매 프레임 쓰는 메시일수록
    먼저 늙어, 상한에 닿는 순간 **가장 많이 쓰는 것부터** 버려지고 곧바로 다시 지어져 다음 것을 밀어낸다.
    · 갈리는 자리는 **열쇠 churn** 이다: 포탑 각(headTag)·건설 단계·불빛·회전 칸 때문에 새 열쇠가 끊이지 않는다.
      정책만 떼어내 흉내 낸 값(상한 240 · 늘 쓰는 메시 150벌 · 600프레임): 새 열쇠가 프레임당 둘이면 FIFO 는
      **늘 쓰는 메시를 프레임당 3.25벌** 다시 굽고, 하나뿐이어도 1.5벌이다. LRU 는 **0**이다.
      ⚠ churn 이 없으면 둘이 **똑같다**(한 번 다시 구우면 그것이 곧 맨 뒤가 되므로) — 짧은 계측으로는 못 가른다.
    · 그래서 화면이 한 톨도 안 움직여도 굽기가 영영 이어지고, '모델 굽는 중' 띠가 안 꺼진다. 게다가 시계가
      **굽는 프레임에는 시간을 안 보내므로**(BAKE_HOLD) 유닛도 거의 안 움직인다 — 그 둘이 한 뿌리였다.
    · 진단: `#diag=draw` GL 줄의 `버림 N` (`stat.evict`). **0 이 아니면 보관함이 좁다**. 상한을 손으로 바꿔 재현하려면
      `#glmesh=N`.
  · ★★ **도는 부품은 열쇠가 아니라 유니폼으로 돌린다**(2026-09, 지적: "아까 모델 굽는 중 뜨는 거 다른 원인도
    있나 봐 — **서플라이랑 터렛 많은 화면**에서 그래") — LRU 로 고친 뒤에도 남은 둘째 뿌리다.
    · 미사일 터렛은 **쉴 때도 96°/s 로 돈다**(idle 탐지 회전). 그 각을 `headTag` 가 **메시 열쇠에 박는다**.
      칸 수(7.5도)는 **판 굽기 시절**에 고른 값이고 근거는 바이트였다 — "터렛 판은 2×2 발자국의 작은 판이라
      48장이라도 몇 MB". 그런데 **메시 보관함의 상한은 바이트가 아니라 벌 수**(`DEV9.glMeshMax` · 폰 240)다.
      곧 그 근거는 붓이 바뀌며 **통째로 무효**가 되었는데 값만 남아 있었다.
      실측(`perf-check --wide --probe-gl`, 테란 킷 = 터렛 4채·서플라이 12채): 터렛 열쇠 **93벌**(각이 접힌 뒤에도
      요잉과 엇갈려 48보다 많다) · 메시 총 **157벌** · VBO **26.4MB** · 누적 굽기 **2250ms**.
    · ★ 고칠 자리는 칸 수가 아니라 **각이 열쇠에 드는 것** 자체다 — 요잉·시각 밀림이 이미 그 길을 갔다
      (카메라 유니폼이라 열쇠에 안 든다). 터렛 머리는 `withModelSpin(headYawNow)` 한 겹이 통째로 감싼
      **강체 회전**이므로, 그 겹을 떼어 **제 딸림 부품**으로 만들면 그 부품의 '모델 각'이 곧 **개체 요잉**이다.
      붓이 `op.attachRot`(딸림 부품만의 절대 요잉)로 그 각을 주면 메시는 **한 벌**로 끝난다 —
      **93 → 2벌 · 157 → 67벌 · 26.4 → 9.9MB · 2250 → 736ms**. 성큰이 혓바닥을 갈라낸 그 규약(op.attach)이다.
    · 가를 때 함께 봐야 하는 것 넷(하나라도 빠지면 조용히 어긋난다):
      ⓐ **도록 표(SHAPE_GALLERY)에 두 별본을 적는다** — 광택 표(`glossOf9`)가 그 표로 종족을 찾으므로, 빠지면
        터렛이 테란 광택을 잃고 '그 밖' 값으로 그려진다.
      ⓑ **`BLD_NORM_PAIR` 에 접는다** — 배수(`bldNormOf`)·앵커 열쇠(`bldAnchorKey`)·잉크 가운데(`bldMidK9`)가
        한 벌이라야 머리가 밑동 위에 앉는다. ⚠ `ink-center --emit` 은 SHAPE_BUILDERS 를 다 돌아 **별본의 줄까지
        찍어 낸다** — 붙일 때 그 줄은 빼라(제 줄이 있으면 접기보다 그것이 이겨 둘이 따로 논다).
      ⓒ **잉크 상자는 딸림 부품까지 아울러 잰다**(footOf 둘의 합집합) — 터렛은 몸의 절반 넘는 몫이 머리라,
        밑동만 재면 체력바·링·총구 앵커가 통째로 내려온다. 각은 **몸의 요잉으로 못 박아** 재라(도는 각으로 재면
        상자가 프레임마다 바뀐다). 덤으로 종전보다 안정적이다 — 옛 상자는 '그때 그 머리 각'의 것이었다.
      ⓓ **폴백(#gl=0)도 딸림 부품을 그리게 한다** — 여태 `op.kind` 하나만 그려 성큰 혓바닥이 통째로 빠져 있었다
        (터렛은 '머리 없는 터렛'이 될 참이었다).
    · ⚠ 깊이 검사(`model-depth-check`)는 빌더 본문을 **글자로** 훑는 자다 — 한 본문 안에 `partKey` 가 한 번이라도
      있으면 그 빌더는 통과한다. 그래서 **빌더를 가르면 가려져 있던 옛 열쇠가 드러난다**(turret 의 머리 쪽
      `20 + depthNow` 가 밑동의 partKey 에 가려 있었다). 새로 어긴 것이 아니므로 기준선에 더하되, 커밋에 까닭을 적는다.
    · 서플라이(trapezoid)는 회전 8칸 × 불빛 2 = **16벌로 이미 닫혀 있다**(`% SPIN_STEPS`) — 실측 열쇠도 여덟이다.
      곧 '애니메이션 건물'이 다 문제인 것이 아니라, **각을 칸으로 안 접고 열쇠에 싣는 것**이 문제다.
  · **헤드리스는 GL 을 못 잰다**: 크로뮴 헤드리스는 소프트웨어 GL(SwiftShader)이라 WebGL 캔버스 → 2D `drawImage` 합성이 ReadPixels 로 서서
    1454² 한 장에 1~2초(실측, 옵션 무관)고 삼각형 채우기도 CPU 다. perf-check 는 기본으로 `#glblit=0`(합성만 뺌 — GL 은 다 돈다)을 붙여
    GL 의 CPU 몫만 잰다(`--glblit` 로 도로 붙임). PC 프로필 gl=0/gl=1 둘 다 p50 67ms(헤드리스 프레임 박자)라 차이가 안 보인다 — GPU 채우기·
    첫 진입 덜컥임은 실기에서만 안다.
  · **도록 아이콘 `DocIcon9`**(ReplayMotionPlayer): GL 이 켜져 있으면(GL_ON9 · `gl` 프롭) `<img>` 에 메시 그림, 아니면 ShapeIcon(SVG).
    손은 gl9 `glIconRequest9` — 한 프레임의 청을 모아 숨은 GL 캔버스 한 판(4096² 상한, 격자)에 그리고 **한 번 읽어** 칸마다 PNG dataURL 로
    나눠 준다(아이콘마다 문맥을 열면 상한 16에 걸리고, 헤드리스는 읽기 한 번이 1~2초). 자·원점은 gl-check 와 같다(16-상자 x = 8 + rx ·
    y = 12 + Y). 창은 ShapeIcon 의 viewBox 규약 그대로(fit 은 footOf 상자 + 짧은 변 12%·fitBox·wide·16-상자). 평면(flat)만 — 입체 보기는
    SVG 로 떨어진다. 색은 요소의 currentColor(--scr-doc-own).
  · 진단: `#diag=draw` 'GL' 줄(개체·삼각·메시/상한(굽기 ms·VBO MB·**버림**)·깊이칸/bit·번짐 개체 수·판으로 떨어진 종류) · `#glmesh=N` · `#glspec=0|N` · `#glgrain=0|N` · `#glshade=0|1|2` · `#gldepth=0` · `#glbias=N` · `#gllod=N` · `#glbloom=0|1` ·
    `#glwarm=0` · `#glblit=0`. 계측 `perf-check --hash gl=0`(캔버스 비교) `--shot x.png --probe-gl`(메시 표: 삼각·색·KB) `--warm 0`. 메시만 따로 보려면 `scripts/model-mesh.mjs`.
  · **전수조사 `node scripts/model-gl.mjs [--kinds a,b] [--rots 45,225] [--rows 40] [--out <scratch>/gl.png] [--json x.json] [--vs2d]`** —
    156종을 **GL 로만** 굽고 눈으로 보는 시트다(5초). 재는 것은 하나뿐이다: **칸마다 실루엣이 있나**
    (빈 칸 = 빌더가 터졌거나 통째로 안 그려졌다 — 그것은 여전히 자동으로 잡을 값어치가 있다).
    ★★ **2D 와 견주는 자는 걷었다**(2026-09, 물음: "이제 gl-check 필요없지 않아? 이전의 2D 캔버스 붓은
      더 이상 기준이 아니고 새로운 세계가 시작됐어") — 옛 `gl-check` 의 '나쁨'(1−IoU + 색차)은 **틀린 쪽을
      가리키게 되었다**:
        · 사이언스 베슬의 몸통이 GL 에서 **땅에 누운 판때기**였던 진짜 버그를 고치자 점수가 **나빠졌다**
          (0.14 → 0.21) — 2D 가 그리는 것이 납작한 타원이기 때문이다.
        · 방향광·테두리 빛·광택·결·푸른빛·번짐은 **2D 에 없는 몫**이라 GL 이 좋아질수록 평균이 오른다
          (0.174 → 0.190). 그 0.5 선 때문에 광택 세기를 여러 번 되물렸다 — **없어진 붓에 맞춰 새 붓을 깎은 셈**이다.
        · '빠진 부품이 없나'는 이제 `model-mesh --check`(덮임 100%)가 **정확히·1초에** 본다.
      그래서 이 자는 **눈으로 보는 시트 + 빈 그림 관문**만 남는다. 2D 폴백(`#gl=0`·도록 SVG)을 감사할 때만
      `--vs2d` 로 옛 견줌을 켜고, 그때도 그 수는 '**2D 폴백과의 차이**'이지 '나쁨'이 아니다 — 낮추려 들지 마라.
    ⚠ 자세 0 에서 비는 것이 맞는 종류는 명단으로 뺀다(`EMPTY_OK9` — 시즈 전환의 뒤 포신은 나온 몫이
      `poseNow / 5` 라 자세 0 에서 길이가 0 이다).
    · 아래·위의 옛 숫자들(평균 0.17~0.19 · 밝기비 · '0.5 넘는 종류')은 **그 옛 자로 잰 기록**이다 — 자취로 남겨 둔다.
    ★ **마주 봄 판정(facingRatio)은 GL 에 없다** — 2D 는 '뒤로 돌아간 장식'(돔 둘레의 구리띠 따위)을 아예 안 그렸지만,
      메시는 요잉을 안 싣고 셰이더가 돌리므로 기록 때 그 문을 닫을 수가 없다. 되살리는 두 길을 다 재 보고 접었다(2026-09):
      ① 빌더가 물어 온 법선을 tagKey 도장으로 부품에 딸려 보내기 — 프리미티브도 제 안에서 facingRatio 를 부르는 탓에
         도장이 엉뚱한 부품에 붙고, 꼴·크기로 걸러 내면 전체 부품의 2.2%(12종)에만 남는다(정작 커맨드는 0).
      ② 꼴로 판정해 한 장짜리 세운 낯을 등졌으면 걷기 — 저그 막·아비터 날개처럼 **진짜 몸**인 한 장짜리 판이 걷혀
         47종이 나빠졌다(0.179 → 0.188 · 하이브 23292 → 20015 화소).
      부감 카메라에서 돔의 먼 쪽 벽은 실제로 보이므로 그 위의 장식도 보이는 것이 옳다 — 2D 의 '안 그리기'가 속임수였다.
      붐비는 모델이 있으면 그 빌더에서 줄인다.
