# scplay 작업 규약

## 도록(카탈로그) 뽑기
"도록 뽑아줘"는 아래 한 줄이다. 파일명·조건은 스크립트가 정한다 — 손으로 바꾸지 않는다.

    node scripts/doc-catalog.mjs --out <scratch>/dorok

결과: `1. terran_units_blue.png` `2. protoss_units_blue.png` `3. zerg_units_blue.png`
`4. terran_bldgs_blue.png` `5. protoss_bldgs_blue.png` `6. zerg_bldgs_blue.png`
`7. extra.png` `list.txt`, 그리고 상위 디렉터리의 `도록.zip`. zip을 사용자에게 보낸다.
조건(4방위 45·135·225·315 · narrow · **한 줄에 3모델**(카드 안 4방위 2×2 — 2026-09, 요청: "모델 변천사 도록 — 한 줄에 3모델로 변경") ·
흰 배경 · 임자색 #2b62e8 · 폭 660 · dpr 3)도 스크립트 안에 있다. 카드 판형은 doc-sheet 의 `--per-row N`(0 = 옛 한 종 한 줄)이다.
그림은 **GL 붓**(`DocIcon9` → gl9 메시, 지도가 그리는 그 그림)이 기본이다(2026-09) — 키값·마주 봄 판정 같은 2D 전용 어긋남이 도록에
안 실린다. 옛 2D 면 그림(ShapeIcon SVG)은 `--2d`(폰·`#gl=0` 이 아직 그 길이라 검토용). 앱 도록(scplayer GalleryScreen)도 DocIcon9 다.

## 모델 변천사(시점 비교) 뽑기
"변천사 도록"(2026-09, 요청: "7·8·9월 비교 — 한 줄에 3모델" → 보기 그림 → "9/10 은 없어도 될 듯 · 배경도 7월과 같은 색으로 검게 ·
각도도 7월에 맞춰" → "한 모델 안에서 세로 구분선 제거 · 제목은 각 모델 위에 줄 위에")는 **종족마다 한 장**(유닛 절 · 건물 절)이고,
모델 하나가 **이름 한 줄 + 그 아래 시점 셋이 왼쪽에서 오른쪽으로**(7월 그림 · 8/29 2D · 9/19 GL — 재요청: "모델별로 세로 말고
가로로 배치")이며 **한 줄에 모델 하나**다(재재요청: "한줄에 한모델만" — 한때 기둥 셋 → 가로 둘이었다). 카드 상자·칸 테두리는 없다 — 칸 사이에 세로 줄이 하나도 없어야 한다.
배경은 7월 그림과 같은 검정 #0a0a0a.
· 저장소는 8월 28일에 시작해 7월 기록이 없다 — **7월 열은 사용자가 준 옛 도록 그림 셋**(테란·프로토스·저그 · 초록 단색 · 1400px 폭)에서
  칸을 잘라 쓴다: 첫 칸이 x 264 · 폭 200 · 높이 197, 행 y 는 `scripts/history/july-rows.mjs`(칸 판 왼 가장자리 x 265~271 의 어두운 띠로 잰다 —
  모델이 안 닿는 자리라 안정적이다. 넥서스 행만 띠가 123 으로 짧게 잡히지만 시작 y 는 맞다). 행 → 종류는 hist-compose 의 `JULY_KINDS`
  (테란 33 · 프로토스 32 · 저그 31 — 그림 차례 그대로 · 마인·인터셉터·미네랄·간헐천은 지금 목록에 없어 안 쓴다).
· ⚠ **7월 그림의 각은 유닛/건물로 안 갈린다** — 눈금 글자를 읽어 보면(`scripts/history/july-angles.mjs` — 글자가 둘뿐이라 기준 조각과
  화소로 견줘 july_rows.json 둘째 칸에 적는다) **SCV 만 45° 이고 나머지 96 종은 죄다 23°** 다. 그래서 옛·지금 시점을 **두 각으로 다 굽고**
  (유닛 45·23 · 건물 23·45) 종류마다 7월 각으로 고른다. 7월에 없는 종류는 23°.
· ⚠ **7월 그림은 −요잉이다**(지적: "7월 그림은 −요잉인데 나머지는 +요잉") — 옛·지금 여덟 판은 `--rots 315`(−45)·`337`(−23)로 굽는다.
  거울로 뒤집어 맞추면 안 된다(빛이 왼쪽에서 오므로 뒤집으면 명암이 반대로 선다).
· 옛 시점(8/29 `1e20b08` · 2D 붓)은 그 커밋의 붓으로 굽는다: `git worktree add $S/wtA <해시>` 뒤 `node_modules` 를 심볼릭 링크로 걸고 그 트리의
  `model-shot.mjs --kinds <전부> --rots 315|337 --mode top --cell 400 --bg "#0a0a0a" --fit`(사본에 `--fit`·`--norm` 을 손으로 넣었다). ⚠ 8/29 도구는 새 헤드리스 되물림이 없어 작업 트리 사본의
  launch 에 `headless: false` + `--headless=new` 를 손으로 넣는다. 지금 시점은 `model-gl --kinds <전부> --rots 315|337 --cell 400 --rows 300
  --bg "#0a0a0a" --fit --json`(2026-09 에 `--bg` 를 두었다 · 시트 행 차례는 화소 적은 순이라 json 의 배열 차례가 곧 행이다 — json 의 `row` 는
  묶음(chunk) 안의 행이라 다른 값이다).
· ★ **칸은 400px 이고 옛·지금 판은 `--fit` 으로 굽는다**(2026-09, 지적: "변천사 화질이 너무 안좋아 그리고 아비터 아직도 작게나와
  왜그래?") — ⓐ 화질: 200px 칸을 400 으로(`--cell 400`). 7월 그림만 소스가 200px 칸이라 hist-compose 가 두 배로 늘린다(`JC`).
  ⓑ 아비터: 두 도구(model-shot·model-gl)는 **빌더의 원 좌표를 16-상자 배율(k = cell/16)로 그대로** 띄우고 지도의 `MODEL_NORM`
  (아비터 2.307 — 원 좌표가 작아 지도가 키우는 종류)을 안 곱한다. 곧 아비터는 칸에서 캐리어의 1/13 넓이였다(도록 팝업이 잉크 상자를
  창에 맞추는 것과 달리 시트는 상자를 안 맞춘다). 정규화를 곱하는 `--norm [Z]` 도 두었지만 그것은 지도의 상대 크기라 건물은 16-상자를
  넘쳐 잘리고(그레이터 스파이어) 나는 몸은 원점 위로 솟아 잘렸다. 그래서 **`--fit [몫]`**(기본 0.84 — 칸마다 실루엣 상자를 재어 칸의
  그 몫에 맞춘다 · 변천사는 0.7 로 굽는다(요청: "각 셀의 패딩 좀 늘리기" · 칸 사이 틈 GAPC 12) · model-gl 은 반 배율로 한 번 굽어 재고 다시 굽는다 · 옛 model-shot 사본은 딴 판에 그려 잰다)로 굽는다 — 7월 도록
  그림도 칸마다 제 크기로 채워져 있으니 그 자와 같다. 변천사는 크기 비교가 아니라 꼴 비교다.(스크래치 사본으로 돌린다 — kinds_u.txt·kinds_b.txt·histA_*·histC_*·july_*.png·july_rows.json 을
  그 자리에서 읽는다) — 칸 자리는 model-shot `y = r·CELL + 26` · model-gl `y = r·(CELL + 22) + 22`. 셋 다 **2px 안쪽**을 잘라 소스 격자선
  (model-shot 은 위·아래 두 줄)을 버리고, 소스 칸에 박힌 글자(7월 돋보기·눈금 · 8/29 종류 이름)는 검정으로 덮은 뒤 꼬리표를 얹는다.
  ⚠ 소스마다 '검정'이 조금씩 달라(4 · 10 · 11) 칸의 네모가 도로 드러난다 — 거의 검정(≤13)은 다 바탕 10 으로 편다. 결과 `변천사/1~3. *_history.png`
  + `변천사.zip`.
· ★ **파일로 보낼 때는 절 조각 여섯이다**(2026-09, 요청: "변천사 다시 주는데 파일로 압축하지 말고 줘") — 한 장 통째(1252×15000 · 3~4MB)는
  파일 전송이 400 으로 막힌다(실측 · 8000px·2MB 조각은 간다). hist-compose 가 절(유닛·건물)마다 머리띠를 얹은 조각
  `*_history_1_units.png`·`*_history_2_bldgs.png` 도 함께 낸다 — 그 여섯을 ASCII 이름으로 베껴(`$S/hist_out/`) 종족마다 둘씩 보낸다.
  지금 시점의 꼬리표는 `NOW`(굽는 날) 한 상수다 — 다시 뽑는 날 그 값만 고친다. 옛 재료(7월 그림·8/29 판 histA_*·kinds_*.txt·july_rows.json)는
  스크래치에 그대로 있으니 **지금 판(histC_* 넷)만** 다시 굽는다 — 한 줄로는 `sh scripts/history/hist-run.sh $S`(굽기 → 합치기 →
  `$S/hist_out/` 여섯 조각까지 · `--old` 를 주면 8/29 판도 wtA 에서 다시 굽는다 · 종류 목록 `kinds_u/b.txt` 도 그 폴더에 있다).
  ⚠ 판을 dataURL 로 넘기는 첫 decode 가 가끔 `EncodingError` 로 튄다 — hist-run 이 세 번까지 되돌린다.
· ★ **시대마다 제 카메라·제 그림자다**(2026-09, 요청: "8월은 +45도 요잉 9월은 +40도 요잉(각 시대 표준값) — 시대별 카메라 시점
  파라미터 갖기 · 임자색은 7월에 적용한 색(연녹색) 모든 시대에 · 배경 흰색으로(7월도) 그림자 표현 변천사도 보여주고 싶어서") —
  '7월 눈금에 다 맞추기'(위 두 ⚠ · 판 여덟)는 되물렸다. hist-compose 의 `ERAS` 표가 자다: 8/29 `+45`(model-shot --rots 45) ·
  지금 `+40`(model-gl --rots 40 · BUILDING_BASE_YAW 와 같은 자) · 7월 그림만 제 눈금(−요잉 · 다시 못 굽는다). 판은 넷(histA_u·b ·
  histC_u·b). 임자색 `OWN9` #7ed491(7월 그림에서 잰 값 · 두 도구 다 `--color`) · 바탕 `BG9` 흰색.
  · **7월 칸은 알파로 되짚어 흰 바탕에 얹는다**(`julyWhite`) — 칸 안 바탕은 (10,10,10)이고 칸 **사이** 여백은 (20,27,43)이다(실측 ·
    처음 여백 색을 잡아 칸이 회색 판으로 섰다). 위·아래 3px 는 칸 판의 테두리 띠라 잘라 낸다(흰 바탕에서 회색 줄로 남는다).
  · **그림자는 유닛에만**: 8/29 는 그 시대 재생기의 접지 타원(wtA 의 model-shot 사본 `--shadow` — 잉크 상자 바닥에 폭의 반 · 납작 0.32 ·
    α0.3) · 지금은 gl9 의 사영 그림자(`model-gl --shadow` — push.shadow ground · α0.26 · `--fit` 의 첫 굽기(상자 재기)에는 안 얹는다).
    ⚠ 지도의 기본은 접지 타원이고 사영은 `#glshadow=1` 의 그림이다. 건물도 준다(재요청: "건물 그림자도 있어야지" — 지도에는
    건물 그림자가 없지만 변천사는 그 시대 붓이 낼 수 있는 그림자를 보이는 자리다).
  · **7월 누끼의 검은 테두리**(재지적: "거의 누끼 다 땄는데 아직 테두리가 남네") — 알파를 `(dm − 8)/36` 으로 잡으면 반쯤 덮인
    가장자리 화소(dm 101)가 a 1 로 그 어두운 색 그대로 남는다. 꽉 찬 초록의 거리(≈140~200)로 나눠(`(dm − 4)/140`) 그 화소를
    제 색으로 풀어 흰 바탕에 섞는다.

## 도록의 칸(2026-09)
★ **칸을 세우는 일은 scplay 가 한다**(`docCellsOf9(kind, t, yaw)`) — 앱(GalleryScreen)은 배치만 한다.
칸은 넷이고 **있는 것만** 선다: 대기 · **이동**(유닛)/**활성**(건물) · 공격 · 액션.
· **공격 칸에 트레이서를 겹친다**(`DocTracer9 overlay`) — 따로 세우면 총알만 덩그러니 남아 무엇이 쏘는지 안 보인다.
· **액션 칸이 변신을 맡는다** — 시즈 모드는 탱크의, 럴커 버로우는 럴커의 액션 칸이다(요청: 별도 목록 없앰).
· ⚠ **값은 지도의 값을 그대로 쓴다** — 앱이 제 숫자를 들면 조용히 어긋난다(실측: 도는 걸음을 초당 4칸으로
  베껴 두어 지도(17.6칸)의 5분의 1로 끊겼고, 코어 디스크가 **쉬는 중에도** 돌아 '업그레이드 중'이라는 신호가
  못 됐다). 늘 도는 것 2.2바퀴/초 · 일할 때만 도는 것(포지·코어·머신샵) 1.6 · 불빛 깜빡임 0.9초의 3분의 2 ·
  성큰 사격 1.34초. **활성은 켜 둔 한 컷이 아니라 깜빡임이다.**
· ⚠ **창(배율)은 칸마다 따로 잰다** — 한 창으로 묶으면 가장 큰 몸에 맞춰져 알·고치가 점이 된다. 칸 **안에서는**
  못 박는다(그 칸이 그릴 수 있는 종류 × 여덟 각의 합집합) — 돌려도 컷이 바뀌어도 배율이 안 흔들린다.
· ⚠⚠ **도록 표(SHAPE_GALLERY)에서 항목을 지우지 마라 — 숨겨라**(`hidden: true`). 광택 표(`glossOf9`)가 그
  표로 종족을 찾으므로, 지우면 그 모델이 제 종족 광택을 잃고 '그 밖' 값으로 그려진다(터렛을 가를 때 적어 둔
  그 함정이다). 지금 숨긴 둘: `tanksiege` · `sunkenfire`.
· ⚠ **갈라 놓은 별본은 `docBldKind9` 에 적어라**(sunkenrear·sunkentongue·turretbase·turrethead) — 표 밖이라
  이 문이 거짓을 내면 아이콘이 **유닛 길**로 굽고, 유닛 길에는 딸림 부품이 없어 혓바닥·포탑이 통째로 빠진다
  (지적: "성큰 … 공격 애니메이션도 안 나오고").
· ★★ **트레이서는 칸의 자가 아니라 모델의 자로 그린다**(2026-09, 지적: "모델의 좌표랑 안 맞네 —
  실제 재생기에서처럼 보이길 원했던 건데, 회전하면 트레이서도 그에 맞게 회전되고") — 지도가 쓰는
  자 셋을 그대로 가져온다: **자리**는 총구 앵커표(`muzzlePoint` · 건물은 `BLD_MUZZLE`, 머리가 도는
  종류는 `spinMuzzle9`)가 낸 16-상자 점을 칸에 모델을 앉히는 식(원점 8·12 · 창을 칸에 맞추는 배수 k)에
  태운 것, **방향**은 몸의 요잉(겨누는 포탑이 있으면 그 각), **배율**은 `k·16 ÷ 그 종류의 지도 타일 수`.
  · ⚠ 여기서는 **MODEL_NORM 을 안 곱한다** — 도록은 창을 잉크 상자에 맞추므로 그리기 배수가 k 에 이미
    들어 있다(지도는 판 배수를 따로 곱한다 — 자가 다르다).
  · ⚠⚠ **`tilePx` 에 타일 px 를 넣지 마라** — 붓은 `tz9 = zoom · tilePx/8` 로 갈래표의 굵기를 키우므로
    둘 다 넣으면 **제곱**이 된다(실측: 마린 줄기가 칸을 통째로 덮었다). `zoom = 타일px/8 · tilePx = 8`.
  · 배율을 8 로 못 박아 두면 모델이 칸을 꽉 채운 도록에서 줄기만 지도 크기로 남아 **안 보인다**
    (지적: "파뱃 … 안 나오는 게 아니라 작아서 안 보인 거였어").
· ★ **별본은 제 본체 바로 뒤에 선다**(2026-09, 요청) — 애드온은 제 본 건물 뒤, 시즈 모드는 탱크 뒤,
  짐 든 일꾼은 그 일꾼 뒤다. 한때 "혼자 못 서는 것들이 목록을 토막 낸다"며 끝으로 몰았던 손질을
  되물렸다 — 붙는 자리가 곧 그 모델의 뜻이라 옆에 있어야 읽힌다.
· ★ **전차포는 선이 아니라 셋이다**(`NO_BEAM_FX` + `MUZZLE_BURST_FX9`) — ① 총구에서 터지는 타원 불꽃
  ② **포물선을 나는 에너지포**(`FX_IMPACT.tankshell` · 가로로 납작 · 나는 몫 `u^1.45` 가속)
  ③ 착탄 자리의 같은 그림, 더 넓게(스플래시). 타원은 `FX_IMPACT.flat` 이다.
  · 나는 시간은 **쿨다운의 12%·상한 0.16초** — 원작은 쏘는 것과 맞는 것이 거의 같은 순간이다.
  · ⚠ 박자는 **진짜 쿨다운(fxCdRaw)** 이다 — NO_BEAM 자리의 `fxCd` 는 '번쩍임 주기'(≤0.32초)라
    그것으로 재면 전차포가 기관총처럼 연사한다(커세어 flare 는 그 번쩍임이 맞다).
  ⚠ 도록의 hit op 은 `splash: true` 와 `dist: 0` 을 꼭 준다 — splash 가 없으면 붓이 **파편**으로 그리고,
    dist 를 비우면 기본값(상자의 0.71배)만큼 **옆으로 밀려** 난다.
· ★ **추진체 불은 이동 칸의 것**(`POSE_KINDS.thrust`) — 다리로 걷지 않는 몸(나는 것·호버·SCV·프로브)은
  걸음 컷을 지어내지 말고 이 칸을 켠다: 움직이면 자세 1, 빌더는 **그 자세에서만** 불꽃을 낸다.
  ⚠ 짐 든 별본(scvMin·probeGas…)도 표에 적어야 한다 — 그리는 종류가 갈리므로 본체만 적으면
    자원을 나르는 동안 불이 꺼진다(가장 많이 움직이는 때다).
· ★ **준비 동작은 공격 칸이 아니라 액션 칸이다**(2026-09, 지적: "고스트 공격 모션은 총을 들고 퉁퉁 앞뒤
  반동 주는 거여야 하고 총을 꺼내는 건 기타 액션으로 가야 해") — 고스트의 옛 공격 한 바퀴는 '반쯤 →
  겨눔 → 반쯤 → 등에 멤'이라 **쏠 때마다 총을 넣었다 꺼냈다** 했다. 원작도 그렇지 않고, 그것은 사격이
  아니라 준비 동작이다. 공격 칸은 겨눈 채 반동만 치고(쿨다운 앞 18% 만 뒤로 밀림), 꺼내는 길은
  `DOC_ACT_POSE9`(자세 컷 몇 장을 차례로 도는 액션)로 옮겼다.
  · ⚠ **반동을 '든 몫'에 묶지 마라** — 고스트는 `kick = 0.18 · at` 이라 겨누기만 하면 총이 **늘 밀린
    자리에 서 있었다**. 곧 반동이 아니라 자세의 일부였다. 퉁퉁 치려면 같은 겨눔 안에 밀린 컷과
    안 밀린 컷이 **둘 다** 있어야 한다(겨눔 2 = 0 · 쏜 순간 POSE_ATK_R = 0.34 · 평균이 옛 값이다).
  · ⚠ 그 대신 지도에서 '등에 멤 → 겨눔'이 교전에 들어설 때 **한 번** 튄다 — 쏠 때마다 튀던 것을 교전마다
    한 번으로 줄인 것이고, 그 한 번까지 매끄럽게 하려면 '방금 교전에 들어섰나'를 엔진이 알아야 한다.
· ★ **'두 줄기'와 '두 칸'은 딴 이야기다**(2026-09, 지적: "줄기가 두 개인 거 말한 건 아니고 — 그건
  또 그거대로 표현되어야 하지만") — 칸을 가르는 자는 **지상·대공 무기가 다른가**이고, 줄기가 둘인
  것은 **그 무기의 발사관이 좌우 한 쌍인가**(미사일: 골리앗·레이스·발키리·스카우트 대공)다. 지도는
  이미 둘로 그리는데(engine9 의 `twin9`) 도록만 한 줄이라 무기의 꼴이 달랐다.
  · 자도 지도의 것을 그대로 쓴다: **두 발 전체의 폭이 몸 폭 하나**(바깥 가장자리끼리 = 잉크 폭)이고,
    잔상이 몸보다 굵으면 0 으로 죄어 한 줄로 겹친다(작게 그릴 때는 어차피 한 획으로 읽힌다).
  · ⚠ 빼는 값은 **실제로 그려지는 굵기**(w × 배율 × 3.4)여야 한다 — 갈래표의 w 는 상대 값이라 그대로
    빼면 단위가 섞인다(지도에서 한 번 물렸던 그 자리다).
· ★ **지상·대공이 다른 무기면 공격 칸이 둘이다**(2026-09, 요청: "공격 트레이서가 두 종류 이상
  (골리앗 스카우트 배틀 등)인 경우 나눠서 두 셀로 표시") — 골리앗·스카우트는 총구 스파크 ↔ 미사일,
  레이스는 레이저 ↔ 미사일로 **아예 다른 무기**인데, 도록은 표(ATTACK_FX) 한 줄만 보고 늘 지상
  것만 그렸다. 그 갈래가 **그리는 자리에 인라인으로 박혀** 있어서다 — `attackFxOf9(이름, air)`
  공용 문으로 빼서 지도와 도록이 같은 수를 본다(`docAtkFx9` 가 칸 목록을 낸다).
  · ⚠ **다를 때만 가른다** — 배틀크루저는 두 무기가 같은 그림(레이저)이라 두 칸이 똑같아 값이 없다.
    가르고 싶으면 무기 갈래를 먼저 둘로 만들어야 한다(도록이 아니라 표의 일이다).
  · ⚠ 못 때리는 표적은 칸이 안 선다 — `weaponVs(프로필, air)` 가 없으면 그 쪽 무기가 없는 것이다
    (발키리·커세어는 대공 한 칸 · 파이어뱃·질럿은 지상 한 칸).
  · 칸마다 제 갈래를 `DocCell9.fx` 로 싣고 `DocTracer9 fx` 가 그것을 받는다(안 주면 종류의 기본).
· ★ **혼자 안 서는 몸은 제 주인의 칸에 든다**(2026-09, 요청: "인터셉터는 캐리어 공격에 포함시키고
  목록에선 제거 · 스캐럽 마인도 목록에선 제거 · 벌처는 마인 심기를 기타 액션으로") — 캐리어·리버는
  **제 몸으로 안 쏜다**(내보낸 것이 쏜다), 벌처의 마인은 무기가 아니라 심는 것이다. 목록에 따로
  서면 '이게 뭐지'가 되고, 주인의 칸에 들면 곧 그 뜻이다.
  · 공격 칸이 대신 그린다: `DOC_ATK_KIND9`(캐리어 → 인터셉터 · 리버 → 스캐럽) — 트레이서도
    **그 몸의 것**이라야 한다(캐리어의 burst 가 아니라 인터셉터의 gun).
  · 액션 칸: `DOC_ACT_KIND9`(벌처 → 마인) · 몸은 그대로 두고 **한 발만** 겹치는 것은 `DOC_ACT_FX9`
    (배틀크루저 → 야마토포. 요청: "배틀은 야마토를 말한 거였는데 그건 기타 액션에 넣어 줘").
    ⚠ 야마토는 지도에서 **캐스트**(표적 자리의 빛덩이)라 갈래표에 없었다 — 도록이 한 발을 그리려면
      제 갈래가 있어야 해서 같은 색 결로 `FX_BEAM.yamato` 를 새로 적었다.
  · ⚠ 목록에서 빼는 것은 **`hidden: true`** 다(지우지 마라 — 광택 표가 이 표로 종족을 찾는다).
    `doc-catalog` 는 정규식이 `race: "…" }` 로 끝나는 줄만 잡아 **저절로** 빠지고, `doc-sheet` 는
    `!g.hidden` 을 손으로 걸어야 한다.
· 성큰의 사격은 **선이 아니라 `erupt` op** 한 장이다(표적 발밑에서 솟는 주황 가시) — FX_BEAM 의 "spike"(럴커
  가시)를 빌려 쓰면 **작은 삼각형**이 된다(지적).

## 도록의 공격 칸에는 표적이 선다
★★ **무기는 '무엇을 향해 쓰는가'로만 읽힌다**(2026-09, 요청: "도록 공격신에 가상의 타겟을 세워 줘
(지상 타겟은 곰돌이 인형 공중 타겟은 참새 인형) 그래서 실제 타깃을 공격하는 모션을 보여 주는 거야 ·
타겟의 피격 효과는 평소 웃던 얼굴이 슬픈 눈이 되고 눈에서 만화같이 눈물줄기가 흐르는 것 · 그리고
재생기의 로직을 그대로 가져와서 보이게 해 줘 그래야 앞으로 두 쪽 수정을 안 할 수 있어") ─────────
· **표적은 인형 둘이다**(`DOC_TGT_KIND9`): 지상 `dollbear` · 공중 `dollbird`. 자세 1 이 **피격 컷**
  (슬픈 눈 + 눈물줄기)이고 그 박자는 트레이서와 **같은 시계**(`DOC_SHOT_CYC9` 1.1초 · 날아가는 무기는
  뒤 20%, 즉발은 앞 20% 뒤)를 쓴다 — 도록이 제 숫자를 들면 줄기가 닿기도 전에 인형이 운다.
· **거리는 지도의 사거리에서 난다**(`docReachTiles9` → `reachTiles`) — 자는 **최대 사거리의 반**이다
  (요청: "타겟 인형과 거리는 최대 사거리의 반이면 되지 않을까"). 참값 그대로면 마린이 몸의 6.6배 ·
  스캐럽이 22배 떨어져 쏘는 몸이 칸의 10%가 된다. 무기의 자(사거리표)는 한 톨도 안 건드린다 —
  스플래시 0.78배 · 표창 2.2배와 같은 규약이다.
  · ⚠⚠ **'반'은 가운데 대에서만 성립한다** — 반으로 줄인 값(16-상자 자)이 울트라 2.65 · 다크템플러
    3.45 · 저글링 4.20 인데 **몸의 잉크 폭이 5.2** 이므로 그 셋은 인형이 **쏘는 몸 속에** 앉고,
    스캐럽은 54.8 이라 쏘는 몸이 칸의 **4.5%** 가 된다. 그래서 바닥(잉크의 1.05배 = 안 겹치는 자리)과
    상한(잉크의 2.0배 = 읽히는 자리)으로 죈다 — 그 사이(파뱃 6.56)는 반값이 그대로 선다.
  · 값: 근접 5.46 ↔ 원거리 10.4(1.9배 차) · 마린 공격 칸의 창 24.5×25.9 에 몸 4.75 — 104px 칸에서
    몸이 14% · 팝업(240px)에서 18%다. **몸을 더 키우려면 상한(2.0)을 내리는 것이 그 손잡이다.**
· **자리·크기·창을 셋이 나눠 쓴다**: 겹판의 치우침 `DocPart9.dx/dy`(16-상자 · 화면 방향) · 트레이서의
  길이 `DocCell9.tgt` → `DocTracer9 tgt`(줄기가 **그 거리까지만** 간다) · 칸의 창 `docCellBox9`.
· ★ **창을 내는 일이 앱에서 scplay 로 옮겨 왔다**(요청의 "두 쪽 수정을 안 할 수 있어") —
  `docCellBox9(kind, group)` 한 문이 칸(라벨)마다의 창을 내고 `GalleryScreen` 과 `doc-sheet` 가 그 표를
  받아 쓴다. 칸에 무엇이 하나 늘 때마다 세 곳을 맞추던 자리다.
· ⚠⚠ **치우침은 점이 아니라 원으로 재라** — 표적의 자리는 `(−sin d, cos d)·거리` 라 **요잉과 함께 돈다**.
  창은 종류·무리로만 기억되므로(앱 useMemo 가 kind·group 뿐) 한 각의 자리로 재면 **손가락으로 돌리는
  순간 표적이 창 밖으로 나간다**. 그 거리를 반지름으로 삼아 원을 아우르면 어느 각에서도 안 잘리고
  배율도 안 흔들린다('칸 안에서는 배율을 못 박는다'는 그 규약이다).
  · 그만큼 창이 커진다(마린 공격 칸 13.7 → 16.8×18.1) — 거리 상한을 2.3 → 1.25 로 내린 까닭이 이것이다.
    **창을 원으로 재기와 거리를 죄기는 한 커밋이다.**
· ⚠⚠ **인형은 '참으로 마주 보게' 돌리면 등만 보인다** — 카메라는 늘 40도 위에서 내려다보고 표적은
  총구 앞(화면 아래)에 서므로, 쏘는 쪽을 마주 보는 각(`deg + 180`)이 곧 **등을 보이는 각**이다(실측:
  요잉 45 에서 표적 225 = 등). 그런데 이 칸의 뜻은 **피격 표정**이다. 그래서 인형은 **카메라를 보고 서서
  쏘는 쪽으로만 몸을 튼다**(`DOC_TGT_FACE9` 32도 · 쏘는 쪽이 화면 오른쪽이면 −32). 무대 소품의 자다.
· ★ **표적의 자는 '그 칸이 그리는 몸'의 것이다**(요청: "캐리어, 리버도 인터셉터 스캐럽만 띄우는 게
  아니라 실제 공격하는 장면을 띄워야 해") — 캐리어·리버의 공격 칸은 몸이 **인터셉터·스캐럽**이라,
  거리를 주인(캐리어)의 자로 재면 작은 몸 옆에 엉뚱하게 먼 표적이 선다. `withTgt9` 가 `c9.kind ?? kind`
  로 사거리·잉크·지도 타일을 다 그 몸에서 읽는다.
· ⚠ 표적이 서는 칸은 `docAtkFx9` 가 낸 **지상·공중 갈래**를 그대로 따른다 — 골리앗은 지상 칸에 곰,
  공중 칸에 참새가 선다(사거리도 6 vs 5 로 다르다). 못 때리는 쪽은 애초에 칸이 안 선다(`weaponVs`).
· ⚠ **2D 폴백(#gl=0 · 도록 SVG)에는 치우침이 없다** — `ShapeIcon.parts` 는 몸과 같은 자리에만 얹는다
  (시즈 전환의 다리·포탑이 그 자리다). 곧 폴백에서는 표적이 몸에 겹친다 — GL 이 기본이라 받아들였다.
· ⚠ **도록 시트(`doc-sheet --anim`)는 앱과 같은 클래스여야 트레이서가 선다** — 줄기 캔버스의 자리·
  크기를 주는 규칙이 `scr-doc > scr-doc-popart > scr-doc-shot` 하나인데 이 자가 제 클래스를 쓰고 있어
  **캔버스가 1×1** 로 섰다(그림에 줄기가 아예 안 보인 까닭이다 · 실측: 고친 뒤 칸마다 줄기 화소 80~101).
  ⚠ 그 파일의 브라우저 몫은 **템플릿 문자열 안**이다 — 주석에 역따옴표를 쓰면 문자열이 끊긴다(GLSL 에서
  두 번 물린 그 함정과 같은 자리다).
· **인형의 꼴**: 팔·다리는 **고른 굵기의 관 + 끝의 둥근 공**이다(지적: "곰 팔 아몬드도 이상 인형 팔
  형태여야 해 보통 원통형") — 방추(sin 굵기)는 양끝이 뾰족해 아몬드로 읽힌다. 봉제 인형은 통을 꿰매고
  끝만 오므린 꼴이다. 참새는 **나는 중**이라(지적) 날개를 펴고 다리를 뒤로 접는다 — 날개는 캐리어 잎과
  같은 처방(등뼈 = 날개 폭 · `widthOf` = 코드 · `ovalOf` = 두께 · `trueNormal`)이다.
· ⚠ 인형의 **눈동자는 흰자보다 앞으로** 나와야 한다(0.62r 중심·0.45r 반지름) — 2D 는 화가 차례로
  보였지만 GL 은 진짜 깊이라 흰자가 눈동자를 통째로 덮었다(실측: 눈이 흰 알 둘).
· ⚠ **입은 주둥이 구면 위에 앉힌다**(`dollMouth9` 가 마디마다 그 자리의 구면 y 를 푼다) — 곧은 y 에
  두면 호의 양끝이 얼굴 앞 0.3 에 떠 있다(지적: "곰 입도"). 새는 **부리가 곧 입**이라 호를 안 그린다.

## 인형도 맞으면 피가 튄다
★ **인형의 피격은 지도의 그 op 다**(2026-09, 요청: "곰돌이랑 새에 테란 생체유닛 피격효과 넣기") — 지도가 체력이 깎인
몸에 얹는 `kind: "hit"`(splash 없음 → 붓의 결 파편 · `mat: "bio"` = 붉은 살점 + 피떡)을 `DocTracer9` 가 인형의 몸 가운데
(분수 1 = 줄기가 닿는 점)에 얹는다. 박자는 우는 컷과 **같은 시계**(`docHitPh9` — 맞은 몫이 시작되는 순간부터 지도의 0.14초
창) · 자는 지도의 그 식(보이는 몸 폭 × 0.69 · 튀는 자리 0.1)에 **인형의 폭**(k9 × docDollK9 × 잉크) · 파편은 사수 쪽에서
밀려나는 쪽으로 난다(dx·dy = 사수 쪽). 무기 갈래(style)는 파편 세기(FX_IMPACT.r)만 정한다.
· 🔎 좁은 창이라 기본 시각 여덟으로는 안 잡힌다 — `doc-sheet --anim --times 0.24,0.29,0.92,0.97`(2026-09 에 `--times` 를
  두었다 · 즉발은 0.22~0.36 · 날아가는 무기는 0.88~1.02 사이).

## 도록 공격 칸의 열한 가지(2026-09)
★ **딸림 판의 기본각을 합본의 것과 섞지 마라**(지적: "성큰 공격시 처음 공격방향으로 고정이고 가시는 혓바닥 뻗어간
방향에서 나와야 함" — **재생기도 같은 병이었다**) — 혀 빌더의 '표적 없을 때 오른쪽(−90)'은 합본 `sunkenfire` 를 위한
것인데, 붓은 딸림 판 `sunkentongue` 도 **headDeg 없이**(headAimNow 거짓) 한 벌 굽고 `attachRot` 으로 돌리므로 그 −90 이
그 한 벌에 함께 구워져 혀가 표적에서 90도 비껴 뻗었다. 기본각은 `sunkenFire && !headAimNow` 일 때만 건다.
  · 🔎 찾는 자: 도록 칸 한 장을 요잉 0·90 으로 구워 인형과 견준다(`$S/icon1.mjs` 꼴 — glIconRequest9 를 곧장 부른다).
    인형이 아래면 혀도 아래여야 한다. 90도 비끼면 기본각, 180이면 축, 제멋대로면 겹셈(headDeg + attachRot)이다.
★ **자세 컷이 없는 유닛은 유닛 길의 문을 못 지난다** — `docCellsOf9` 의 `if (a9.pose)` 가 유닛·건물을 가르는 문이라
`tanksiege`(POSE_KINDS 밖)는 **건물 길**로 흘러 공격 칸이 합본(포탑이 쉬는 각 = 차체 뒤)으로 섰다(지적: "시즈모드
탱크가 옆을 보고 공격"). 문을 `a9.pose || kind === "tanksiege"` 로 연다 — 걸음 칸은 pk9 가 없어 저절로 안 선다.
  · 🔎 `node $S/cells.mjs <kind>`(esbuild 로 docCellsOf9 를 노드에서 부른다) — 칸에 `kind`·`parts` 가 없으면 건물 길이다.
★ **도록의 배율은 지도의 트레이서 문턱 아래다** — `FxPaintEnv9.docView9` 가 그 문(FX_MIN_ZOOM · detailAt)을 건너뛴다.
안 열면 럴커 가시(2)·성큰 분출(2)·버로우 먼지가 도록에서 통째로 빠진다(ZOOM9 ≈ 1.6~4).
★ **겹판이 큰 몸 뒤에 앉으면 안 보인다** — 인터셉터 고리를 캐리어·인형 한가운데에 두었더니 셋 중 둘이 캐리어 몸에
묻혔다. 고리는 **인형 쪽**(0.72·거리 · 반경 0.36)에 둔다. 원작도 인터셉터는 표적 둘레를 맴돈다.
★ **겨누는 각은 인형이 선 쪽으로 못 박는다**(`aim9 = yaw`) — ±40 흔들기는 인형이 없던 시절의 '겨누는 중' 표시였다.
★ **망토 띠의 겉면 띄움 0.035 는 눈금 아래였다** — 개체 안의 깊이 칸이 좁아 그 차가 사라져 왼 띠만 자락 뒤로 졌다.
0.08(띠 반폭 아래)이면 넷 방위에서 다 선다 · 0.16 은 떠 보인다. **띄움은 '보이나'가 아니라 '떠 보이나'까지 두 끝을 재라.**
★ 도록에만 있는 숫자: 캐리어·리버 사거리 `DOC_REACH_TILES9`(무기표에 없다) · 표창 배수 `DOC_TRI_K9` 1.3 · 일반
탱크 광전포는 도록에서 0.6초를 난다(지도 0.16초면 한 바퀴의 15%만 보여 빈 칸으로 읽힌다).
★ **'시즈 포구 불꽃이 포탑 아래쪽에 선다'는 오진이었다**(2026-09) — 요잉 90·180 으로 구워 보니 불꽃이 포신 끝에
정확히 앉는다(`doc-sheet --anim --kinds tanksiege --yaw 90`). 45 에서 '아래쪽'으로 보인 것은 포신이 **카메라 쪽**(아래)을
향해 짧게 사영된 끝이었고, 그 위에 '포신이 위-왼쪽을 본다'고 읽은 그림은 고치기 전 합본(쉬는 각)의 것이었다.
표식은 `gunAt9(...)(1)` 이 기울임(T9)까지 지난 점이라 표의 z 4.18 이 맞다. **한 각에서 어긋나 보이면 다른 각으로
한 번 더 구워라** — 그래서 doc-sheet 에 `--yaw N` 을 두었다(움직임 판의 요잉을 못 박는다).

## 쌍 발사관의 두 발은 나란히 나가 표적 한 점에서 모인다 — 화염만 빼고
★ **쌍은 끝이 하나다**(2026-09, 요청: "한 쌍 트레이서의 경우 타겟 쪽은 타겟 중심 한 점을 향해 가야 함 · 미사일이
타겟에 가까워질수록 좀 휘어야겠지 · 단 파뱃은 제외") — 도록의 두 발은 제 총구에서 **같은 각**으로 나란히 뻗어
끝이 둘이었다(지도는 `tx·ty` 로 발마다 표적을 다시 겨눠 이미 한 점이었다). 도록도 표적 점(`tx: 1, ty: 1` —
DocTracer9 의 zx/zy 는 분수 0 = 총구 · 1 = 표적)을 싣는다.
· **미사일의 굽이는 조종점의 자리다** — 붓의 나는 길은 총구 → 표적의 2차 베지에고 조종점이 발사 방향(`d0`)으로
  나간 자리에 선다. 쌍의 한 발(`FxOp.twin`)은 그 자리를 0.5 → **0.72** 로 밀어, 발사관에서 오래 나란히 가다
  표적 가까이에서 튼다. 홑발은 종전 그대로(표적이 옮겨 갔을 때만 굽는다). 도록도 `d0`(몸의 겨눔)를 싣는다.
· **화염방사기는 길이가 고정이다**(`FX_BEAM.fixed` — flame): 붓이 표적 거리(reach)로 안 죄고 늘 `l` 만큼 뻗고,
  방향은 **몸의 자리**에서 표적을 겨눠 두 줄기가 나란히 간다(제 총구에서 한 점으로 모으면 뿜는 불이 아니라 쏘는
  총이 된다). 도록도 같은 붓이라 저절로 같다.
★ **터렛 총구는 포드 앞 낯의 한가운데다**(요청: "터렛 트레이서는 포드 앞면 중앙에서 시작") — 손 값(2.2, 0.6, 8.64)은
옛 포드 꼭대기였다. 포드 블록이 `P3(PL9, PH9/2, 0)`(기울임·팔자 롤을 다 먹인 점)으로 `markMuzzle9` 를 찍는다 →
총구표 [2.1, 3.16, 7.96]. 🔎 `node scripts/muzzle-sheet.mjs --kinds turrethead --rots 0,90,270`.

## 도록의 원점은 땅이 아니라 '그 사수가 있는 층'이다
★ **공중 사수 곁의 참새는 뜨지 않는다**(2026-09, 지적: "도록에서 공중타겟과 공중유닛이 왜 높이가 달라") — 지도는
모든 공중 몸을 **같은 높이**(airLiftPxOf · 기준 몸 하나의 값)로 띄우므로 레이스와 그 표적은 같은 하늘에 있다. 그런데
도록은 쏘는 몸을 늘 원점(12)에 그리면서 참새만 `docTgtLift9` 로 그 높이만큼 올려, 레이스·스카우트·인터셉터 곁에서
참새가 사수보다 한 층 위에 떴다. 사수가 나는 몸이면(`docShooterAir9` = 지도의 `isAirUnit` 을 종류 이름으로 되짚음)
인형의 높이는 **사수에 견준 상대값**이다(`docTgtLift9` = 표적의 뜬 몫 − 사수의 뜬 몫): 공중 ↔ 공중은 0(같은 하늘) ·
공중 사수 ↔ 곰돌이는 **음수**(곰돌이가 사수 아래의 땅에 선다 — 되물음: "지상 타겟과 공중 유닛은 높이가 왜 같구") ·
지상 사수(골리앗·터렛)는 종전 그대로 참새만 뜬다. 원점을 땅으로 읽으면 틀리고 '사수의 층'으로 읽으면 맞다.
· ⚠ 창(`docCellBox9`)은 위로 든 몫(up)과 **아래로 내린 몫(dn)** 을 따로 아울러야 한다 — `max(up)` 하나로 재면 음수가
  0 에 먹혀 곰돌이가 창 밖으로 잘린다. 그만큼 공중 사수의 지상 칸은 창이 커진다(레이스 자로 9.6) — 지도의 자를
  그대로 옮긴 값이라 받아들였다(한때 곰돌이를 사수의 층에 두어 창을 지켰다가, 지도와 다르다는 되물음에 되돌렸다).

## 인형은 모델이 선 땅 위에 선다 — 그 땅은 부감에 눌려 있다
★★ **화면의 둥근 원에 인형을 앉히면 겨눔선 아래에 선다**(2026-09, 지적: "공격 타겟 인형 위치가 … 좀 낮게" ·
"지상 곰돌이 위치도 이상했어" · "파뱃 트레이서가 휘었어") — 모델은 40도 부감이라 모형의 앞 한 칸이 화면에서
sin40 = 0.643 칸인데, 인형은 `(−sin d, cos d)·거리` 의 **원** 위에 앉았다. 요잉 45 에서 총구가 가리키는 선은
화면 33도 아래인데 인형은 45도 아래 — 곧 곰돌이가 겨눔선 밑에 서고, 줄기는 총구에서 인형까지 잇느라 노즐
방향에서 **꺾여** 나갔다(파뱃 화염의 그 휨). 앞뒤 몫에 `DOC_GROUND_K9`(sin40)을 곱하면 인형이 모델과 같은
땅(타원)에 서고 줄기가 노즐과 한 선이 된다. 창(docCellBox9)은 눌린 몫을 도로 펴서 원으로 재므로 안 잘린다.
· **줄기 끝은 발밑이 아니라 몸 가운데다**(`docTgtUp9` → `DocCell9.tgtUp` → `DocTracer9 tgtUp`) — 지도가
  `foeBody9`(잉크 가운데) + `foeLift9`(뜬 높이)로 겨누는 그 점이다. 앱(GalleryScreen)·doc-sheet 둘 다 넘긴다.
· **공중 인형은 뜬다**(`docTgtLift9` — 지도의 airLiftPxOf 를 칸의 자로 옮긴 것 · `DocPart9.up`) — ⚠ 거리와 **같은
  상한**(잉크 폭 × DOC_TGT_MAX9)으로 죈다: 레이스 2.3타일은 터렛(1.24타일)의 자로 **30** 이라 창이 통째로 커져
  둘 다 점이 된다(골리앗 10.1 · 레이스 9.6 은 참값 그대로). 창은 `up` 을 따로 받아 땅의 원을 그만큼 올려 아우른다.
★ **포드가 둘인 터렛은 한 발씩 번갈아 쏜다**(요청: "터렛 트레이서 위치는 포드 한쪽씩 번갈아서") — `TWIN_POD_BLD9`
(engine9). 표의 앵커는 오른쪽 포드이고 홀수 발은 모형 x 를 뒤집는다. 발 번호는 지도 `fireIdx9`(사격 박자의
몇 번째 발) · 도록 `floor(t / DOC_SHOT_CYC9)`. 🔎 `doc-sheet --anim --kinds turret --yaw 0` — 포드가 좌우로 서는 각.
★ **가스 연기는 곧게 오른다**(지적: "휘어서 가는 거 같아 — 수직이 나을 거 같고 색과 크기 좀 키워야") — `gasPuffs9`
의 흔들림(wob)·흘러감(drift)을 걷었다(덩이 셋이 딴 위상의 사인을 타 뱀처럼 휘어 보였다). 크기 0.5~1.5 → 0.7~1.9 ·
짙기 0.45 → 0.62. bld-norm 은 넷 다 표 그대로(덩이가 잉크 폭을 안 넓힌다 — 세로로만 크다).

## 가스 덩이는 화면 자로 견줘라 · 알은 ×1.2
· **익스트랙터 연기**(2026-09, 지적: "익스트랙터만 가스연기 크기가 좀 작네") — `gasPuffs9` 의 r·h 는 모형 자인데 익스트랙터는
  BLD_NORM 0.853 으로 가스 건물 넷 중 가장 작아(정제소 1.492 · 어시밀 1.749 · 간헐천 1.706) 같은 r 0.55 가 화면에서 0.47(정제소
  1.19 · 어시밀 1.0~1.27)이었다. r 1.3 · h 3.4 로 화면 1.1 · 2.9 에 맞췄다. GL 은 덩이를 메시에 안 구우므로 bld-norm 은 안 변한다.
  🔎 `doc-sheet --anim --kinds extract,refinery,assim` — 셋의 덩이가 같은 크기로 서야 한다.
· **변태알** MODEL_NORM `0.915 * 1.2`(요청: "저그 변태알 크기도 작아졌어 1.2배" — 히드라처럼 곱한 값 · 재측정 아님). 잉크 중심은 그대로.

## 간헐천 분화구 윗쪽은 밝은 회색 얼룩이다 — 낯마다 색을 정하는 `fillAt`
★ (2026-09, 요청: "간헐천 각 분화구 윗쪽을 밝은 회색으로 자연스럽게 그라데이션 처리(너무 직선으로 층을 만들게 아니라 자연스러운
회색얼룩처럼) · 경계선을 좀 랜덤한 곡선으로") — `spirePillar` 의 `fillTop·topFrom` 은 t 하나로 자르는 **곧은 층**이라 얼룩을 못 낸다.
`fillAt(t, u)`(마디 t · 둘레 u → 색 | undefined) 훅을 새로 두었다 — top·front/back 보다 먼저 든다.
· 분화구: 경계 tb(u) = 0.5 + 0.2·(사인 셋 2·3·5 바퀴 · 분화구마다 딴 위상) — 닫힌 랜덤 곡선 · 그 위로 0.3 폭 smoothstep 으로
  ROCK → `LROCK9` #d0d1cd(바위의 노란 기를 뺀 차가운 회색이라 갈린다) · 매끈한 2차원 잡음 `blot9`(±0.07 경계 · ±14% 밝기).
  마디 16 · 낯 24(옛 4 × 12).
· ⚠ **낯마다 튀는 해시 잡음은 계단이다** — 처음 ±0.14 해시로 두니 경계가 마디 크기의 네모로 들쭉날쭉했다(실측). 얼룩은
  매끈한 잡음(사인 곱)이라야 곡선으로 읽힌다.
· bld-norm 1.706 → 1.696(24각 내접 차 0.6%)은 안 갈았다.
· 🔎 `model-gl --kinds geyser --rots 0,45,90,135 --cell 460` 을 crop 으로 확대.

## 오버로드 몸통은 임자색 한 벌이다 — accent 낯을 밑칠 목록에 겹쳐 넣지 마라
★★ (2026-09, 지적: "오버로드 몸통 — 저그색 몸통 위에 임자색을 깔았어? 그래서 자꾸 아래 주황색이 부분부분 보이는거 같은데?" —
그 짐작이 맞았다) — `raceBase(faces, tone, accent)` 는 `faces` 를 종족 바탕색으로 밑칠하고 `accent` 를 **뒤에 덧붙인다**. 오버로드는
배·갑각·집게 갈래(belly9·shell9·clawGrip)를 `faces` 에도 넣고 `accent` 에도 넣어, 같은 기하가 **주황(#c0632f) 한 벌 + 임자색 한 벌**로
두 번 섰다(실측 `model-mesh --kinds ovie --dump`: #408 주황 · #410 임자 — z 2.53~4.55 같은 배). 2D 는 화가 차례로 임자색이 위라 안
보였고, GL 은 같은 깊이의 두 낯이 z 싸움을 해 주황이 얼룩덜룩 비쳤다(요잉마다 자리가 달랐다).
· 고침은 `raceBase` 안이다 — accent 와 **같은 참조**의 낯은 밑칠 목록에서 걷는다. 다른 호출자(아머리·게이트·트리뷰널)는 accent 를
  따로 지어 넣으므로 안 바뀐다.
· 🔎 **같은 기하가 두 색으로 두 번 찍히면 이 병이다** — `--dump` 에서 폴리 수·z 범위가 같은 줄이 색만 다르게 이웃해 선다.

## 포신 반동은 포탄과 같은 시계를 탄다
★★ (2026-09, 지적: "시즈탱크 공격하는 순간 포신이 움직여야 트레이서랑 맞는데 지금 서로 타이밍이 안맞음") — 포탑 op 의 반동
(`fireK` — 자세 2 + 0.09타일 밀림)이 `(t + ei·0.7) % 1.5 < 0.18` 이라는 **제 시계**였다. 포탄(전차포 셋)은 `firePhase('u'+holdKey,
fxCdRaw)` 로 나므로 둘이 무관하게 돌았다 — 탱크(쿨다운 1.5초)는 어쩌다 맞고 시즈(3.1초)는 한 바퀴에 반동이 두 번 치며 늘 어긋났다.
이제 같은 열쇠·같은 쿨다운(그리는 판 drawUnit2 의 무기 = fxCdRaw 와 같은 수)으로 위상을 읽어 **쏜 직후 `GUN_KICK_SEC9`(0.18초)**만
밀린다 — 포탄이 나는 0.16초를 덮는다.
· ⚠ 같은 열쇠를 fx 블록보다 **먼저** 부르지만 같은 t 라 시작점이 안 흔들린다(`at === t` 면 잇는다).
· ★ 교훈: **같은 사건을 두 자리에서 그리면 시계도 하나여야 한다** — 반동·불꽃·포탄·착탄은 한 발이다.

· **변태알** MODEL_NORM `0.915 * 1.4`(요청: "저그 변태알 1.4배로 그리기" · 앞서 ×1.2 — 곱한 값 · 잉크 중심 그대로).

## 프로브 다리는 닫힌 상자다 · 눈은 치마 벽 · 뒷윗날개는 위로 · 스러스터는 아래로
★★ (2026-09, 지적 넷: "프로브 다리들 면이 안보여 다 뚫려있어 · 눈 위치가 너무 높음 · 뒷윗날개 좀더 길게 하고 각도를 더 위를
향하게 · 스러스터 좀더 아래로") — 다리(`wing`)는 **윗판 + 옆 두께면 둘**뿐인 열린 홈통이었다. 2D 는 화가 차례로 윗판이 덮어
안 보였지만 GL 은 진짜 깊이라, 가파르게 내려간 뒤 아래 날개(5 → 4)를 비스듬히 보면 열린 밑으로 **반대쪽 옆면의 뒷면**이 드러나
속 빈 판때기로 읽혔다(실측: 135·180 에서 다리마다 흰 안쪽 띠). 밑판 + 뿌리·끝 뚜껑을 더해 여섯 낯의 닫힌 육면체로 — 2D 는
그 낯을 `faceLight` 로 보이는 각에서만 싣고(메시 기록 중엔 늘 참이라 여섯이 다 든다), 겹친 경로는 종전처럼 한 bodyFace 에 잇는다.
· 눈: z 5.04(어깨 = 위 테)는 정수리 언저리였다 → 치마 한가운데 z 4.70 · 벽 앞 0.11(옛 띄움 그대로 · y 1.37·BD). 총구(입)도 4.52 로.
· 뒤 위 날개: 길이 ×1.4 · 끝 z 4.8 → 5.6(아래로 처지던 것이 19도 위를 본다). 스러스터 zC 4.60 → 4.42(치마 밑동 4.34 · 벽 반지름 0.71 이라 r0 0.68 이 아직 파고든다).
· MODEL_NORM 그대로(요청으로 달라진 잉크) · 잉크 중심 옛/새 차 −0.06 · 총구표 다시 뽑음.
· 🔎 `model-gl --kinds probe --rots 0,45,90,135,180 --cell 520 --pose 1` 을 crop 으로 확대 — 다리 안쪽에 흰 띠가 없어야 한다.

## 울트라는 켄타우로스다 — 곧추선 둘째 몸통 위에 머리·팔, 이마엔 코뿔소 뿔
★★ (2026-09, 요청: "상체에서 이어지는 상체가 하나 더 있음 · 켄타우로스 같은 형태 · 거기에 현재의 머리와 팔 두 개가 붙은 형태 ·
머리 이마에 코뿔소 같은 뿔 하나 추가") — 앞몸 가슴(y 1.9 · 등뼈 z 4.8)에서 **곧추선 둘째 몸통**(`spirePillar` · 축 z · 뿌리 가슴
살 속 z 5.4 → 어깨 z 8.4 · 반폭 1.9 → 가운데 2.3 → 1.9 · 앞뒤 0.65 납작 · caps top)이 솟는다(`UT_ROOT9`·`UT_TOP9`). 얼굴은 그
꼭대기(y +0.55 · z −0.25)에 앉고, 볏 뿌리는 옛 자리에서 **옮긴 몫**(`UT_DY9`·`UT_DZ9`)을 그대로 타며 옆뿔·등마루 뿔은 CR0 를
읽어 저절로 따라온다. 팔 뿌리는 상체 어깨(±1.65 · y 2.5 · z 7.8) — 어깨가 높아진 만큼 낫을 더 깊이 내렸다(조종점 −0.1 → −1.0 ·
끝 −1.5 → −4.0). 상체는 휘두르기 묶음(upper9) 안이라 공격 컷에 함께 돈다. 어깨 패드(임자색)는 네발 몸통 옆에 남는다(안장).
· **코뿔소 뿔**: 이마(zergFace 의 입선 + 수직 낯 위 4분의 1 구 앞 30도 — y + 0.8 · z + 1.3 · s 1.5)에서 앞·위로 나가며 끝이 살짝
  뒤로 휘는 2차 베지에 뿔(뿌리 0.42 → 점 · 상아 — 낫·발톱과 한 벌).
· **발 앞 뭉툭한 발톱 셋**(같은 날, 요청: "울트라 발 앞쪽 뭉툭한 발톱(코끼리같은) 세개씩") — 발(반지름 1.1 절두체) 앞 테두리
  ±28도 부채꼴 · 뿌리는 발 속 0.8 · 앞으로 0.6 · 끝은 뚜껑(tipW 0.65배 · caps both) · 상아.
· ★ **상체는 곧은 기둥이 아니라 몸통에서 굽어 오르는 굽이다**(재요청: "몸크기 축소 및 앞부분이 상반신과 하반신이 곡선으로 자연스럽게
  이어져서 구부러지게") — 네발 몸통 앞 토막을 t 0.79(y 1.5)에서 **캡 없이** 끊고, 그 단면(반폭 tW9 · 납작함 tOv9 · 낯 14 · ref x)을
  그대로 이어받아 앞으로 나가다 위로 굽는 2차 베지에(뿌리 접선 +y · 조종점 (0, 3.7, 등뼈 z) · 꼭대기 (0, 3.2, 8.4))로 짓는다.
  반폭은 몸통 값 → 어깨 1.95(가운데 살짝 불룩) · 납작함 0.64 → 0.65. 옛 앞끝(y 3.3)의 캡은 걷었다.
· MODEL_NORM 0.369 → **0.329**(같은 재요청의 "몸크기 축소" — model-norm 재측정 값 · 켄타우로스로 잉크가 커진 몫을 표준 상자에 도로
  맞춘다) · 잉크 중심 옛/새 차 −1.00/−1.13.
· 🔎 `model-gl --kinds ultra --rots 0,45,90,135,180 --cell 600`.

## 프로브 눈은 치마 벽에 붙은 콘택트렌즈 · SCV 와셔는 팔 끝에서 연장 뿌리까지
· **프로브 눈**(2026-09, 요청: "눈을 크기를 줄이고 콘택트렌즈 부품으로 바꾸고 몸에 딱붙이기") — 벽 원반(wallDiscPath)은 벽 앞
  한 뼘의 납작한 데칼이라 각에 따라 떠 보였다. `contactLens9` 는 타원면 위에 붙는 사발인데 치마는 **원뿔**이라, 눈 자리(y 0.875 ·
  z 4.70)에서 원뿔 벽에 **접하는 가상의 구**(반지름 1.2 · 중심을 벽 법선 반대쪽으로 1.2 물림 · 벽은 위로 넓어져 법선이 앞·아래
  −31도)를 자로 준다 — 렌즈 반지름 0.15 안에서 구와 원뿔의 차 0.01. 좌우는 몸 축 둘레 방위각(±23.6도)이라 `withModelSpin` 으로
  돌린다(렌즈의 방향은 y-z 평면뿐이다). 크기 0.28 → 0.15 · 두께 0.05 · 초록 셋(#4e7f18 · #8fe63a · #d9ff8c).
· **SCV 손목 와셔**(지적: "scv 손부품들이 팔에서 떨어져있어서 붙여야해") — 옛 와셔는 원판 구간을 **한가운데 기준 0.34배**로 죄어
  팔 끝과 와셔 사이 0.27 · 와셔와 집게 첫 고리 사이 0.2 가 비었다. 시작을 팔 자락 안쪽(t 0.94)으로, 끝을 연장 뿌리(드릴 + 0.07 ·
  집게 고리 + 0.02)로 — 틈이 없다. 🔎 `model-gl --kinds scv --rots 45 --cell 520` 을 crop 으로.

## 볏 띠는 기울 수 있다(V) · 디바우러 장식은 얼굴 위 양옆에서 난다
· **울트라 볏의 임자색 V**(2026-09, 요청: "울트라 머리장식에 임자색 데칼 V자로 붙이기") — `crestPlate9` 의 `decals` 항목에 셋째 값
  `cu1` 을 두었다(`[cu0, wf, cu1?]`): 둘레 자리가 뿌리 cu0 에서 끝 cu1 로 곧게 옮겨 가는 띠. 울트라는 `[[0, 0.16, −0.62], [0, 0.16,
  0.62]]` — 뿌리 한가운데에서 만나 끝으로 벌어지는 V. 안 주면 종전대로 곧은 띠(히드라 한 줄 그대로). 띠의 말림(skewV)도 그 자리
  (cuAt)로 잰다.
· **디바우러 더듬이(푸른 장식 두 장)**(요청: "더 앞아래로 이동(얼굴 위 양옆에 붙이기)") — 뿌리 (±0.75, 1.55, Z0+0.55 · 투구 앞 위) →
  얼굴 머리뼈 위쪽 옆 (±0.5, 2.2, Z0−0.02). 뻗는 벡터(뒤·바깥·위)는 그대로라 끝은 여전히 껍질 위로 솟는다. 잉크 중심 +0.07/+0.09.

## 울트라 상체는 역삼각형 · 말 몸통은 허리 약하고 20% 작다 · 디바우러 껍질은 투구 겉면의 조각
· **울트라**(2026-09, 요청: "허리 잘록하게 상체 토르소가 역삼각형 · 말몸통부분엔 허리 약하게 하고 몸통 크기 20프로 더 줄이기") —
  상체 `widthOf`: 몸통 단면 → t 0.45 허리 1.25 → 어깨 2.5(둘 다 smoothstep) · 팔 뿌리 x ±1.65 → ±2.15(어깨 반폭 안쪽 0.35).
  말 몸통: TORSO9 허리 반폭 2.0 → 2.4(잘록함 완화) · 반폭 전체 ×`HB_K9` 0.8(높이도 함께 줄고 등뼈 높이는 그대로 · 다리·엉덩이 구는
  안 옮김 — 구 반지름 1.08 이 줄어든 옆구리에 아직 든다). MODEL_NORM 0.329 그대로(재측정 0.325 · 1% 안) · 잉크 중심 −0.04/−0.03.
· **디바우러 등껍질 날개**(요청: "등껍질날개 형태를 몸통표면과 동일하게") — 옛 판은 x-z 자오선 하나를 따라 굽고 **앞뒤(y)로는 곧은
  띠**(반폭 R·cosθ·0.82 + 0.15)라 정수리 곁에서 앞뒤 가장자리가 돔 위 0.7 까지 떠 있었다(돔은 y 로도 굽는데 띠는 안 굽는다). 이제
  회전타원면(R9 · H9 · 중심 (0, CY9, ZB9) — 투구 겉면 3% 띄움) 위의 점을 **극각 θ(0.12~1.25) × 방위각 φ(+x 에서 ±70도)** 8×8 격자로
  찍어 네모 낯으로 잇는다. 어디서나 겉면에 붙고, 들기(LIFT9 · y 축 경첩)는 종전대로 x-z 회전이다. 잉크 중심 +0.06/+0.06.
  🔎 `model-gl --kinds devourer --rots 0,45,90 --pose 1` — 들린 컷에서 딱정벌레 겉날개처럼 돔 조각 둘이 선다.
  · ★ **경첩은 앞이다**(재지적: "껍질은 양옆이 들리는게 아니라 뒤가 들리는거야 앞이 붙어있고") — 등마루(y 축) 둘레로 옆이 들리던 것을
    앞쪽 x 축(y CY9 + 0.8 · z ZT9 − 0.3) 둘레로 돌려 뒤가 오르게 했다(경첩 앞은 살 속으로 살짝 들어 안 보인다).
  · **더듬이 잎**(요청: "위끝에 일자로 튀어나온거 제거하고 잎모양 배흘림줄이기") — 옛 폭 곡선 `sin(π·min(1, t/0.84))^0.7` 은 t 0.84
    에서 이미 0 이라 남은 16% 가 반폭 0.03 짜리 **곧은 바늘**이었다. 사인을 끝까지 펴고 지수 1.15 · 최대 반폭 0.62. ⚠ `max(0.03, …)`
    바닥이 곧 바늘이다 — 끝을 점으로 모으려면 곡선이 끝에서 0 이어야지 자르면 안 된다.
  · ★ **잎은 돔 위 반폭만큼 띄워라**(재지적: "더듬이가 등에 파묻히지 않게") — 조종점·끝이 돔 겉면에서 0.15~0.95 위였는데 잎은 45도로
    선 반폭 0.62 라 아래 가장자리가 잠겼다. 조종점을 앞·위(±1.0, 1.9, Z0+1.5)로 세워 뿌리에서 곧장 솟게, 끝 Z0+2.1 — 잎 한가운데가
    돔 위 0.6. 잉크 중심 −0.04/−0.05.
  · ★ **껍질 조각은 극점까지**(재지적: "날개 맨 위가 왜 둥글게 패여있어?") — 극각을 0.12 에서 시작하니 정수리 둘레 반지름 0.29 짜리
    둥근 구멍으로 밑의 돔(3% 낮다)이 패인 것처럼 비쳤다. θ 0 부터 굽고 첫 고리는 극점 한 점으로 모은 삼각형이다.

## 뮤탈 날갯짓 상한
· (2026-09, 요청: "뮤탈 날개짓 위쪽 상한각을 좀 낮추기 아래는 그대로") — 마디 각표 `SEG9` 의 올리는 컷 20·45·100 → **14·32·75**(수평에서
  잰 각 · 끝 마디만 여전히 안쪽으로 감긴다). 내리는 컷 −38·−90·−128 은 그대로. 🔎 `model-gl --kinds muta --rots 0,90 --pose 1|3`.

## 마린의 총은 오른팔이 정한다 — 상완 거의 수직, 하완 수평, 총은 가운데 앞
★ (2026-09, 요청: "총을 좀더 가운데 앞으로 옮기고 뒤 받치는 팔이 뒤로 구부러지지 않게 — 상완은 거의 수직이지만 앞을 향해 내려가고
하완은 지면과 수평") — 여태 총 축(GB9 → GM9)을 먼저 두고 두 손을 `jointBetween` 으로 풀었는데, 굽힘 힌트(뒤·아래)가 오른 팔꿈치를
어깨 **뒤**로 보냈다. 이제 오른팔 마디를 설계 자로 못 박는다: 상완 (0.12, 0.30, −0.95) · 하완 수평 1.2(안쪽 35도) · 손등 0.38 —
그 손이 쥐는 자리에 총의 방아쇠(`GRIP_R9` 0.22)가 오도록 개머리판(GB9)을 **거꾸로** 푼다(GB9 = 손 − 0.275·GD9). 왼손은 종전대로
앞손잡이(`GRIP_L9` 0.40)를 잡고 풀린다. 걸음 흔들림·반동·낮춤은 GP9 의 몫이라 오른팔 마디 둘에 같은 치우침을 얹는다.
· ⚠ **왼팔의 닿는 거리(2.2 + 손등 0.38 = 2.58)가 총의 자리를 정한다** — 오른손이 어깨 앞 1.5 에 서므로 총이 오른쪽에 남으면 왼손이
  앞손잡이에 못 닿는다(x −0.4 이면 2.8). 하완을 안쪽으로 35도 틀어 총을 몸 가운데(x −0.18 → 0.07)로 보내야 닿는다 — 그것이
  '가운데'다. 곧 개머리판은 가슴 앞(y 0.97)에 뜬다(어깨에 안 댄다 — 하완이 수평이면 그 자리다).
· 총구표 [0.15, 2.7, 2.68] → [0.08, 3.22, 2.88] · 잉크 중심 −0.02/−0.01(→ 다시 −0.03/−0.04) · MODEL_NORM 그대로(자세로 달라진 잉크).
· **왼손은 총 밑바닥을 받친다**(재요청: "앞쪽 팔 손이 정확히 총 밑바닥을 받치게") — 총 단면은 반폭 0.17 을 위아래로 2.7 배(0.46)
  늘인 타원이라 축 위의 점을 쥐면 손이 총몸 속에 든다. 앞손잡이 점을 (0.46 + 손등 반두께 0.09)·Z8 내려 손등 윗면이 총 밑에 닿는다.
· **마린·파뱃 다리통 지름 −10%**(요청) — `suitLegs` 의 `thin` 배수(마린 1 → 0.9 · 파뱃 0.82 → 0.738). 잉크 폭은 안 변한다.
· ★ **총은 왼쪽으로 틀어 들고, 쏠 때는 상체를 오른쪽으로 비틀어 총구를 앞으로 낸다**(재요청: "마린 총을 약간 왼쪽으로 틀어서
  들고 있을거야 · 공격시엔 상체를 오른쪽으로 틀어서 총구를 앞으로 향하게 하고 총은 지면에서 수평으로 똑바로 들기") — 총의
  방향은 **몸통 자**에서 늘 `GUN_YAW9`(18도) 왼쪽(+x)이고, 쏠 때(자세 2)는 어깨 위(몸통·허리띠·목·헬멧·통풍구·어깨판·팔·총)를
  통째로 `withModelSpin(GUN_YAW9)` 로 돌려 그 좌향 총이 세계 +y 를 본다. 다리는 안 돈다(허리에서 비튼다). 들림은 쏠 때 0(수평).
  · `spun` 은 (x, y) → (x·cos d − y·sin d, …) 라 왼쪽(+x)으로 a 튼 방향을 +y 로 보내는 각은 **+a** 다(오른쪽으로 비틀림).
  · ⚠ 총구 표식은 **쏘는 자세**로 손수 짓는다 — 표는 자세 0 에서 굽히므로, `withModelSpin(GUN_YAW9)` 안에서 수평 총의 끝을
    찍는다(총구표 [0.08, 3.22, 2.88] → [−0.59, 3.11, 2.6] — 비튼 만큼 오른쪽으로 옮겨 앞을 본다). `muzzle-sheet` 는 자세 0
    (좌향 총)을 그리므로 십자가 총 끝에서 비껴 보이는 것이 맞다.
  · **더 옆으로 · 몸 가까이**(재요청: "대기 상태에서 더 총구를 옆을 향해 몸 가까이 들기") — 좌향 18 → **35도**(비틀림도 35) ·
    대기의 하완은 안쪽 **55도**(쏠 때 35)로 접어 손이 가슴 앞 y 1.2 에 온다(총구표 [−0.46, 2.78, 2.6]).
  · **대기는 55도**(재재요청: "대기중 총을 좀더 옆을 향해 홱 더 돌리기") — `GUN_YAW_REST9` 55 · 쏠 때는 몸통 자에서 35 로
    돌아오고 상체를 35 비튼다(비틀림을 55 까지 주면 허리가 과하다 — 곧 쏠 때 총도 몸통 자에서 20도 돈다).
  · **헬멧은 정면**(재요청: "공격시 몸 돌릴 때 얼굴·헬멧은 정면 향해야 함") — 헬멧·귀 원판을 비틀림 블록 안에서
    `withModelSpin(−tw9)` 로 되돌린다(목은 몸통과 함께 돈다).
  · 잉크 중심(자세 0) 그대로 · MODEL_NORM 그대로(자세로 달라진 잉크). 🔎 `model-gl --kinds gunner --rots 0,45,90 --cell 520 [--pose 2]`.

## 시즈 전환의 두 마디는 겹치지 않는다
★ (2026-09, 요청: "일반 → 시즈: 우선 포신 전환이 다 이루어지고 나서 각도가 기우는 거야 · 반대 모드 전환 시에는 각도가 수평으로
돌아가고 나서 포신 전환") — `xfOut9`(포신 나오는 몫)이 `p/3.5` 라 자세 3~4 에서 `xfTilt9`((p−3)/2)와 **겹쳐** 포신이 다 나오기
전에 기울기 시작했다. `p/3` 으로 — 자세 0~3 이 포신 갈아 끼우기(3 에서 다 나옴 · 수평), 3~5 가 쳐올리기. 언시즈는 같은
사다리를 거꾸로 밟으므로(xfMech9 1 → 0 · gunU9 1 − e9) 저절로 '수평으로 돌아온 뒤 포신이 들어간다'.
· 🔎 `model-gl --kinds tankturretxf --rots 90 --pose 2|3|4` — 3 에서 포신이 다 나온 채 수평, 4 부터 기운다.

· 🔎 `model-gl --kinds gunner --rots 45,90 --cell 520 [--pose 1|2]` — 90도에서 오른 팔꿈치가 어깨 바로 아래·살짝 앞에 서고 하완이
  수평이어야 한다. 걸음 컷은 총구가 대각선 위로 선다(carry9).

## 막의 힘줄은 화면 띠가 아니라 관이다 — 화면 좌표로 그은 덧칠은 GL 에서 접혀 사라진다
★ (2026-09, 지적: "드론 날개의 줄기가 안보이네") — 공용 막 도형 `membraneFaces`(드론 갈퀴치마 · 퀸 날개막 · 히드라덴 막)의
힘줄이 **`project` 로 화면에 사영한 두 점을 잇는 띠 한 장**(α0.22 검은 sideFace)이었다. 3D 기록이 없는 덧칠이라 mesh9 가
막 한 장에 통째로 접었고(`--dump` 에 막이 검0.34 로만 남는다), GL 에서는 막이 조금 어두워질 뿐 줄기가 없었다. 2D 폴백에서만
보이던 그림이다. 이제 뿌리 → 끝점(0.97)을 잇는 **관**(`spirePillar` · sides 5 · 반지름 `rib`×1.25 → 0.65 · 막 위로 `rib`×0.6
띄움 · 막 색의 0.58 배)이다 — 2D·GL 어느 붓에서나 제 기하로 선다. 잉크 중심 변화 ≤0.02 라 표는 그대로.
· ★ 이 꼴을 찾는 자는 **`project(...)` 로 얻은 화면 점을 문자열 경로에 직접 박는 자리**다 — 그 낯은 `polyPath3` 를 안 지나
  3D 가 없고, 덧칠 색이면 덮임 표(분모에서 빠진다)에도 안 걸린다. 몸 색이면 관문이 잡지만 덧칠 색이면 **조용히 접힌다**.
· ★ **곧은 관은 가운데가 막에 묻힌다**(재지적: "줄기가 보이긴 하는데 중간이 좀 잘려 보이네") — 바깥 변의 골 점은 끝점 z 합을
  **1.6** 으로 나눠(2 가 아니다) 끝점보다 0.5 위에 서므로, 막 낯이 뿌리→끝 직선보다 가운데서 솟는다. 관을 이웃 골 둘의 높이가
  직선 가운데보다 높은 몫만큼 활(sin πt)로 올린다(`bow9` · segs 4).
· 🔎 `model-gl --kinds drone,queen --rots 0,45,90 --cell 520` 을 crop 으로 확대 — 갈퀴마다 어두운 줄기가 뿌리에서 끝까지 끊김 없이 서야 한다.

## 퀸 등 방패판은 얇고 좁다
· (2026-09, 요청: "퀸 등 쉴드 두께를 좀더 얇게 하고 폭도 줄이기") — 꽁무니 팔 끝의 잎 두 장 `w 0.62 → 0.48 · tipW 0.16 → 0.13 ·
  oval 2.2 → 3.4`(oval 이 클수록 더 눌린다) · 눌린 단면이라 `trueNormal`. MODEL_NORM 0.585 그대로(재측정 0.599 · 요청으로 달라진 잉크) ·
  잉크 중심 변화 ≤0.01. 🔎 `model-gl --kinds queen --rots 0,90 --cell 520`.
· **좌우 폭 −25%**(재요청: "퀸 임자색 방패 두 개 좌우 폭 25프로 줄이기") — w 0.48 → 0.36 · 두께(= w/oval 0.141)를 지키려 oval 3.4 → 2.55 ·
  tipW 0.10. 잉크 중심 +0.07/+0.08(표 7.92/8.72) · MODEL_NORM 그대로(재측정 0.605).
· **앞뒤 +20% · 더 높이**(재요청: "앞뒤 길이는 20프로 늘리고 더 등에서 높이 띄우기") — 판 y −1.5 → +1.5(2.5 → 3.0 · 앞으로) ·
  띄움 `QS_UP9` 1.16 → 1.56(팔 끝의 z 도 같은 값 · 팔 조종점 1.04 → 1.3). 잉크 중심 −0.08/−0.09(표 7.84/8.63) · MODEL_NORM 그대로.

## 퀸즈 네스트는 2층 살 단이고 옥상 옆면을 검은 갈고리가 두른다
★ (2026-09, 요청: "퀸네스트 2층으로 구성하고 위쪽 옥상 옆면을 검은 뿔을 둘러서 박기 — 약간 안쪽으로 휘면서 위로 향하는 형태") —
큰 돔 하나(반지름 4.6 · 높이 4.48)였던 봉분을 **살 단 둘**(`spirePillar` · taper 0.7 · caps top)로 쌓는다: 아래 4.6 → 3.4 ·
높이 `QN_H1` 2.3, 위 3.1 → 1.7 · 높이 `QN_H2` 2.3. 옆선 자 `tierR9(z)` 가 그 높이의 반지름을 내어 아가리(아래 단 앞 · z 0.3~2.05)가
살에 파인 홈으로 앉는다. 살 혹 세 줄은 아래 단 옆구리 · 위 단 밑동 · 위 단 옆구리. 상아 엄니 넷은 그대로(아래 단 어깨).
· **갈고리 열**: 옛 왕관 다섯(위로 돋아 바깥 1.8배로 벌어짐)을 걷고 **열둘**을 지붕 가장자리(반지름 1.7) 옆면에 박는다 — 뿌리는 옆면
  속(반지름 1.6 · 지붕 밑 0.3) · 끝은 지붕 위 1.25 · 반지름 1.35(안쪽) · 배는 바깥으로(bowX·Y = 바깥 0.45) 부풀어 끝이 안으로 굽는다.
· 개인색 뚜껑은 지붕 위(z QN_TOP) 반지름 1.45. BLD_NORM 그대로(재측정 1.146 · 표 1.145) · 잉크 중심 +0.11/+0.15(표 4.63/3.96).
· ★ **뿔은 45도로 나와서 굽는다**(재요청: "뿔 살짝 위 45도로 나와서 구부러지기") — `spikeHorn` 의 사인 배불림으로는 뿌리의 나가는
  각을 못 정하므로 2차 베지에 뿔(`spirePillar` · 뿌리 r 1.6 · 지붕 밑 0.3 → 조종점 r 2.3 · 지붕 위 0.4 → 끝 r 1.75 · 지붕 위 1.4)로
  짓는다 — 뿌리 접선이 바깥·위 45도, 끝 접선이 위·안쪽. 잉크 중심 +0.05/+0.04(표 4.68/4.00).
· 🔎 `model-gl --kinds queensnest --rots 0,45,90 --cell 520`.

## 나이더스 입구는 사다리꼴 문틀이다 · 디파일러 마운드 +90
· **나이더스**(2026-09, 요청: "커널 입구 좀더 높히고 사다리꼴형으로 변경") — 반원 아치(호 2.55×2.2)를 걷고 문틀을 **세 토막**
  (왼 다리 → 윗보 → 오른 다리 · `NY_FR9` 바깥 밑 x ±2.8 · 꼭대기 x ±2.15 · z 2.8)의 한 관(굵기 0.85 · segs 15 라 모퉁이가
  마디에 앉는다)으로 세운다. GL 구멍(`cutFace`)은 그 관의 안쪽 가장자리인 사다리꼴 — `max(|x| − hw(z), z − NY_ZT9)`
  (밑 반폭 1.96 · 위 1.45 · 높이 1.95 — 옛 타원 꼭대기 1.51). 굴 속(caveInside9) 높이 2.4 → 2.7. 2D 폴백의 아치 넷도 사다리꼴(`trap9`).
  잉크 중심 +0.13/+0.14(표 4.45/3.72) · BLD_NORM 그대로(재측정 1.181 · 표 1.184).
· **디파일러 마운드** `withModelSpin(375 → 465)`(요청: "값 자체 +90도 요잉"). 잉크 중심·BLD_NORM 그대로.
· 🔎 `model-gl --kinds nydus,dmound --rots 0,45,90 --cell 520 --lit`.

## 울트라덴은 주황 핏줄 그물을 두르고 발치에 마디 진 촉수를 깐다
★ (2026-09, 사진 요청: "울트라덴 몸체에 저그 기본색 핏줄들 · 바닥에 촉수들") — 옛 판은 어두운 초록 기둥 여섯(몸과 색이 붙어
안 읽혔다)과 발치 좌우의 곧은 뿔 다섯이었다. 핏줄은 공사 고치의 수법 그대로: 껍질 위의 점을 (방위각, 높이) 자로 되짚어
(`onSkin9`) 줄기 열하나가 흔들리며 오르다 45% 에서 둘로 갈라지고 갈래가 잔가지를 낸다(굵기 등급 여섯 돌림). 색은
`RACE_BASE_TONE.zerg`. 아가리(|각| < 50도)와 겹치는 줄기는 이빨 능선 위(t 0.58)에서 시작한다.
· ⚠⚠ **기운 등뼈의 단면은 수평 원이 아니다** — 둔덕의 축이 (0, −1.1, 5.12) 로 뒤로 기우므로 spirePillar 의 단면은 그 접선에
  **수직**이다. 수평 원(z = 5.12t · 반지름 r(t))으로 껍질을 되짚으니 앞 살은 0.21·r 만큼 실제보다 낮게 잡혀 핏줄이 살 속에
  묻히고 뒤는 그만큼 떠서 허공에 걸렸다(실측: 0° 에서 앞 핏줄이 토막만 비치고 뒤 줄기가 떴다). 단면 법선
  n = (0, 0.978, 0.210) 을 타고 되짚어야 어느 각에서도 붙는다 — 옛 굵은 기둥은 반지름 0.42 라 그 어긋남을 삼켰을 뿐이다.
  ★ **path 로 기운 기둥 위에 무엇을 얹을 때는 그 기둥의 단면 틀로 재라.**
· 촉수 열 가닥: 둔덕 밑동 살 속(r 3.9)에서 2차 베지에로 밖(r 6.9 · 옆으로 비낌)으로 기어 나가고 `widthOf` 가 0.42 → 0.06 로
  줄며 사인 마디(열넷)를 얹는다 · z = 그 자리 반지름(땅에 눕는다) + 끝 들림 1.0·t³ · 정면 |각| < 30도는 비운다.
· BLD_NORM 그대로(bld-norm 1.032 vs 표 1.2 — 촉수로 넓어진 잉크라 넥서스 불가사리의 그 규약) · 잉크 중심 −0.02/−0.06(표 4.96/4.14).
· 🔎 `model-gl --kinds cavern --rots 0,45,90,180 --cell 520`.

## 공사 고치는 단계마다 딴 모델이다 — 납작한 원반에서 반구로
★ **배수로만 키우던 것을 셋으로 갈랐다**(2026-09, 요청: "공사고치 3단계를 별도 모델로 변경 — 관다발은 굵기 변하지 않게 하고 길이만
점점 늘게 · 핏줄도 굵기는 그대로 개수만 늘리기 · 처음이 크기만 작은 게 아니라 높이도 더 낮게 납작한 모양에서 점점 반구로 가는 형태로")
— `cocoonBuild9(stage)`(bake9) 한 함수가 껍질 높이 hk(0.45 · 0.72 · 1) · 핏줄 개수(4 · 8 · 12) · 밑동 관 길이 lk(0.45 · 0.72 · 1)를
단마다 갈라 낸다. **핏줄·관의 굵기는 셋이 같다**. 본판 `cocoon` 이 3단이고 `cocoon1`·`cocoon2` 는 별본이다.
· 엔진은 진행률 셋째 칸마다 종류를 고른다(`prog < 1/3 → cocoon1 · < 2/3 → cocoon2 · 그 밖 cocoon`) — 선 건물이 변태하는 자리
  (BLD_FROM_BLD)는 다 자란 몸이라 3단 그대로다. 옛 배수 성장(0.55 → 1.2)은 그대로 얹힌다(작고 납작한 것에서 크고 둥근 것으로).
· ⚠ 별본은 **`BLD_NORM_PAIR` 로 본판 배수에 접는다** — 납작한 잉크를 제 배수로 도로 키우면 단의 뜻(작고 납작함)이 사라진다.
  도록 표에는 `hidden: true` 로 적는다(광택 표가 종족을 여기서 찾는다). 등급표(tier-table)는 두 종을 더해 다시 뽑았다(160 → 162).
· 핏줄은 열두 칸의 각을 그대로 두고 `(i·5) % 12 < n` 인 것만 세운다 — 번호 해시가 같아 단이 올라도 있던 핏줄은 제자리에 남고 새 것만 는다.
· **관다발의 수도 단마다 는다**(요청: "고치 관다발 개수도 처음에 적다가 늘어나게") — 173 중 `(i·7) % 3 < 단` 인 관만(1/3 · 2/3 · 전체). 같은 해시 규약이라 있던 관은 제자리에 남는다.
· 🔎 `node scripts/model-gl.mjs --kinds cocoon1,cocoon2,cocoon --rots 45,135` — 한 줄에 셋이 서서 납작 → 반구, 관이 길어지고 핏줄이 느는 것이 보인다.

## 건물 체력바는 GL 건물 블록이 그린다 — 판 길과 함께 지워졌던 그림
★★ **판(스프라이트) 길에만 있던 그림은 그 길을 걷을 때 함께 사라진다**(2026-09, 지적: "건물 체력바가 안 나옴") — 건물
체력바는 옛 판 블릿 블록(`bspr`) 안에만 있었고 GL 건물 블록은 처음부터 바를 안 그렸다. 3bd2930 이 판 길 1900줄을 걷으며
그 바도 같이 갔다(유닛 바는 GL 이든 아니든 한 자리라 살았다). `bldHpBar9`(UnitLayer 건물 고리)가 GL 블록·폴백 둘에서
그린다 — 자는 유닛 바와 같다(원작 폭 `hpBarFrac` · `hpBarRects9` 3색 · 몸 아래). 자리는 **메시 잉크 상자의 바닥선**
(`gay9` = 지면선 − 뜬 높이 · 붓이 바닥을 거기 앉힌다) + 2 + 발자국폭·0.03 — 옛 판 길의 `bspr.bot + 2 + wPx·0.03` 그 자리다.
· ⚠ **바는 배율 4(DEEP_MIN_ZOOM)부터**라 scene-sheet 기본 배율 2.6 에서는 안 보인다 — `--zoom 5` 로 봐야 한다.
· 🔎 `node scripts/scene-sheet.mjs --race 테란 --hurt --zoom 5`(2026-09 에 `--hurt` 를 두었다 — 건물마다 체력 줄(절대값 ·
  0.5초에 300 · 그 뒤 2.5초마다 1 씩)을 픽스처에 실어 '맞은 지 3초 안'을 늘 참으로 둔다. 성한 건물은 바를 안 띄우므로
  이 손 없이는 검증이 안 된다).
· ★ 교훈: **같은 뜻의 그림이 붓마다 따로 있으면 한 붓을 걷을 때 다른 붓에 그 그림이 있는지 세어 보라** — 유닛 바처럼
  붓 밖(공용 자리)에 두는 것이 안전하다.

## 프로토스 보병의 다리 뭉치는 몸통 밑동 밑에 앉는다
★★ **허리를 세웠으면 다리가 앉는 자리도 함께 옮겨야 한다**(2026-09, 지적: "프로토스 보병들 상하체 허리 만나는 선이 안
맞아 — 하체가 더 앞으로 가 있어") — 고관절(y −0.3)·발목(−0.75)·발끝(0.5)·질럿 허리 보호구(−0.1)는 다 **36도 숙인 옛
몸통**의 자였다: 그때는 밑동(−0.75)에서 앞으로 기울어 고관절 높이에서 축이 −0.1 언저리였다. 허리를 15도로 세우자(위 절)
밑동은 −0.75 에 그대로 남고 고관절만 0.45 앞에 돋아, 옆에서 보면 배가 다리 뒤로 들어간 꼴이었다.
· `P_LEG_BACK9`(0.45) 한 값이 `protossLegs` 의 고관절·발목·발끝·발가락 뿌리와 질럿 허리 보호구 중심을 **통째로** 뒤로
  물린다 — 마디 사이 각(대퇴 거의 수직 · 정강이·발 > 꼴)은 한 톨도 안 바뀐다. 셋(질럿·하템·다크)이 같은 함수라 같이 간다.
· 하템 앞가리개는 몸통 앞면에 매인 천이라 그대로다(다리가 그 뒤로 들어간다). MODEL_NORM 그대로 · 잉크 중심표는 옛/새
  측정 차만큼(zealot 0 · htemp +0.01 · dtemp −0.03).
· 🔎 `model-gl --kinds zealot,dtemp,htemp --rots 90 --cell 520` — 옆에서 고관절이 몸통 밑동 한가운데 밑에 있어야 한다.

## 저그 공용 얼굴은 '세로로 자른 돔' — 둥근 앞, 평평한 뒤, 그 사이 평행 몫
★★ **상자 셋을 걷었다**(2026-09, 요청 여섯: "얼굴 윗부분 돔 형태 반절(이마와 정수리까지) · 아래턱도 돔 밑면 반절" → "공용 얼굴
자체를 바꿔" · "지금 사각은 부자연스럽" → "정확히 반구는 아니고 반의 반구 · 이마부터가 반구 시작이고 그 아래 **수직으로 내려가는
부분**이 있어야 해 거기에 눈이 위치하고 코도 들어가" → "어느 정도 **뒤로 길이**(옆면이 평행한 부분이 반반구 뒤로 연장)" →
"반의 반구 = 돔 가운데를 **수직으로 칼로 자른** 느낌" → 후보 넷 뒤 "2번의 윗부분을 180도 요잉 — 굽은 부분이 앞, 평평이 뒤 ·
턱도 뒤는 평평하게 · 위아래 다 뒷부분만 20% 길게") — `zergFace`(열 유닛이 나눠 쓴다)의 지금 꼴(s = 배수):
· **머리뼈** = 앞: 입선(z + 0.3s) 위 **수직 반원통 벽**(R 0.72s · 키 FH 0.34s — 눈이 그 앞 낯 0.6 높이에 앉고 코가 들 자리) + 그 위
  **4분의 1 구**(반구의 앞 반쪽) · 뒤: 옆면이 평행한 몫(L 0.68s = 옛 0.45s 의 1.5배 — 전체 길이의 20%를 뒤에만 더함 · 같은 키의
  상자 + 반원통 지붕의 위 반쪽) · 뒤끝: **평평한 단면**(수직 벽 네모 + 반원 아치 한 폴리곤).
· **아래턱** = 앞: 뒤집힌 얕은 돔(0.6s · 깊이 0.5R)의 앞 반쪽 · 뒤: 반타원 단면 홈통의 아래 반쪽 · 뒤끝 평평(반타원 폴리곤) ·
  윗면(아가리 밑)은 반원 + 네모 뚜껑 한 장. 뒤끝은 머리뼈와 같은 y.
· ★ **뒤끝도 반의 반구다**(2026-09, 요청: "머리 뒤에도 반반구 붙여서 닫기") — 평평한 단면(반원 아치·반타원 폴리곤)을 걷고 앞과 거울인
  몫(머리뼈: YB9 의 수직 반원통 벽 + 4분의 1 구의 뒤 반쪽 · 턱: 뒤집힌 돔의 뒤 반쪽 · `skipFace my > YB9 + 0.01s`)을 세운다. 턱 윗면
  뚜껑은 앞·뒤 반원을 다 덮는 한 폴리곤. 뒤로 R 만큼 길어지지만 열 종의 잉크 중심은 안 움직였다(≤0.01 · 실측).
· 반쪽을 내는 자는 `spirePillar` 의 **`skipFace`**(낯 한가운데의 y 또는 z 로 절반을 뺀다). ⚠⚠ **경계에 여유를 둬라**(`my < YC9 −
  0.01s`) — 낯 중심이 정확히 경계 위에 놓이는 위상(16 각 · 축 위 꼭짓점)에서 부동소수 오차로 그 낯이 빠져 **세로 틈**이 났다
  (실측: 앞 반원통과 상자 사이 검은 슬릿 · 두 실험(상자 벽 빼기 끔 / 낯 빼기 끔)으로 잡았다).
· 송곳니 넷·아가리 총구(markMuzzleSoft9)는 옛 자를 잇는다 — 호출자 열 곳의 자리표가 그대로다(잉크 중심 변화 ≤0.01 · 히드라만
  목·머리장식 손질과 겹쳐 움직였다).
· ⚠ 차례는 여전히 **턱 → 아가리 → 머리뼈**다(호출자가 한 키로 감싸므로 배열 차례가 곧 앞뒤).
· **히드라 머리장식 뿌리는 앞 4분의 1 구의 정수리**(같은 요청: "이마 정수리 머리장식이 부드럽게 이어지기") — 입선 + 0.34s + R ·
  앞 반원 중심 살짝 앞(0.1) · 0.06 묻힘. 옛 뿌리(+0.5 · +0.8)는 상자 정수리 위 허공이었다.
· 🔎 얼굴만 크게 보려면 임시 빌더 `SHAPE_BUILDERS.zfacetest = () => zergFace(0, 2.0, 3.2)` 를 dollbear 앞에 넣고
  `model-gl --kinds zfacetest --rots 90,45,0,135` — **커밋 전에 지운다**(등급표 종수가 는다). 후보 여럿은 `$S/compose4.mjs`
  (시트 여럿을 세로로 쌓아 꼬리표)로 묶어 보낸다.

## 히드라 옆모습(사진) — 목은 앞·아래로, 크레스트는 짧게, 어깨에서 솟는 덮개, 상아는 낫뿐
★ (2026-09, 사진 재요청: "목은 아까처럼 앞으로 굽어서 쭉 앞아래쪽으로 내려가는 게 맞나 봐 · 팔 두 마디는 저그색이고 마지막 마디만
상아색 낫 · 크레스트 길이를 줄이고 대신 목 뿌리 부분을 덮는 저런 덮개 — 크레스트보다는 아래 깔리고 목 위로 덮이고 높이는 크레스트보다
높이까지") — 앞서 '반쯤 세운 뒤 절반으로 줄인' 목(앞 0.72 · 위 1.1)을 되물려 **어깨에서 살짝 솟았다가 앞으로 1.7 나가며 0.55 내려오는**
목으로(사인 0.75) — 머리가 어깨 앞·아래에 매달린다. 크레스트 len 5.0 → 3.6. **덮개(HOOD)** 는 같은 `crestPlate9` 를 어깨 뒤(y −1.0 ·
z 4.4)에서 15도 뒤로 기울여 세운 판(len 4.4 · wk 1.15 · rootK 1.3 · oval 0.12 · 데칼 없음 · 키는 크레스트보다 한 단 뒤) — 꼭대기
z 8.65 로 크레스트(≈7.8)보다 높고 뿌리 반폭 1.5 가 목(0.85)을 덮는다. 팔 셋째 마디는 DARK(낫만 ivory).
· 재요청 둘(같은 날): "팔관절 좀 펴서 낮추고 목 더 길게 앞으로 빼고 목 덮개가 등 뒤에 붙는데 그게 아니라 목을 덮어야지" · "크레스트 임자색은
  한 줄만 깔끔하게 끝에서 끝까지" — 목 앞 1.7 → 2.4(내림 0.7) · 팔 둘째 마디 끝 z 4.32 → 3.9 · 셋째 6.16 → 5.0(V 로 치솟던 관절을 편다) ·
  덮개 뿌리를 목 밑동(y −0.45 · z 4.6)으로 당기고 **15도 앞으로** 기울여 목 위를 지나게(꼭대기 y 0.66 · z 8.85) · 그래서 크레스트 축은
  50 → 75도로 세웠다(누이면 둘이 가운데서 교차한다) · 띠는 가운데 한 줄(wf 0.16)을 본판 길이 0.01~0.99 로 — 켜 앞 낯 높이(본판 반폭·oval
  + 0.15)에 두면 켜 밖 구간에서도 같은 층으로 이어진다.
· 재요청 다섯(같은 날, 차례로): "목 덮개는 뒤로 많이 눕혀야지 45도 정도로 · 머리 크레스트도 그 정도 각도" → 둘 다 축 (0, −0.707, 0.707) ·
  "낫 살짝 끝이 뒤로 휘게" → 낫은 곧은 뿔(hornFaces)이 아니라 2차 베지에 날(spirePillar · oval 0.45 · ref z · 뿌리 반지름 0.36 → 점 ·
  조종점 y 3.5 → 끝 y 2.5 로 끝이 되돌아온다. ⚠ 처음 hornFaces 의 반지름(0.62×0.3)을 그대로 썼더니 철사였다) · "목덮개 위쪽 뾰족하게"
  → `tipK` 0.06 · "목 덮개 위치만 다시 잡자 목을 덮어야 해 목보다 아래에 있잖아" → 뿌리를 **목 한가운데 위**(y 0.7 · z 5.35)로(목 밑동에서
  내면 판이 목 뒤·아래에서 솟는다) · "덮개 뿌리쪽 폭 줄이고 좌우를 아래로 향하게 호 모양으로 말기" → `rootK` 0.8 · `curlK` 2.4(가장자리
  말림 배수 — 판이 목을 감싸는 홈통이 된다). 저글링 낫도 같은 날로(요청: "저글링 낫도 히드라처럼") — zling 잉크 중심 +0.10.
· 재요청 다섯(같은 날, 차례로): "목 덮개는 뒤로 많이 눕혀야지 45도 정도로 · 머리 크레스트도 그 정도 각도" → 둘 다 축 (0, −0.707, 0.707) ·
  "낫 살짝 끝이 뒤로 휘게" → 낫은 곧은 뿔(hornFaces)이 아니라 2차 베지에 날(spirePillar · oval 0.45 · ref z · 뿌리 반지름 0.36 → 점 ·
  조종점 y 3.5 → 끝 y 2.5 로 끝이 되돌아온다. ⚠ 처음 hornFaces 의 반지름(0.62×0.3)을 그대로 썼더니 철사였다) · "목덮개 위쪽 뾰족하게"
  → `tipK` 0.06 · "목 덮개 위치만 다시 잡자 목을 덮어야 해 목보다 아래에 있잖아" → 뿌리를 **목 한가운데 위**(y 0.7 · z 5.35)로(목 밑동에서
  내면 판이 목 뒤·아래에서 솟는다) · "덮개 뿌리쪽 폭 줄이고 좌우를 아래로 향하게 호 모양으로 말기" → `rootK` 0.8 · `curlK` 2.4(가장자리
  말림 배수 — 판이 목을 감싸는 홈통이 된다).
· **저글링**(같은 날): "저글링 낫도 히드라처럼" → 같은 베지에 날(뿌리 0.28 · 끝이 뒤로) · "발가락 3개고 발가락 사이에 물갈퀴" → `toes9`
  (발목에서 ±24도 부채꼴로 상아 발톱 셋 + 이웃 사이 살색 삼각 막 앞뒤 두 장 — 뿌리에서 발톱 55% 자리까지). zling 잉크 중심 +0.14.
· 🔎 `model-gl --kinds hydra --rots 90,45 --cell 520` 을 `$S/crop.mjs` 로 확대 — 옆에서 머리·크레스트·덮개가 사진처럼 겹쳐 서야 한다.

## 볏 판은 공용이다 — 히드라 머리장식 = 울트라의 판 · 울트라 판은 두껍고 길어졌다
★ (2026-09, 요청 넷: "히드라 머리장식을 울트라 머리장식의 판만 가져와서 크기와 가로세로 비율만 수정해서 쓰자 데칼도 넣고" ·
"울트라 머리장식은 두께를 늘려서 옆면에 뿔뿌리가 잘 들어가게 … 특히 2번째 뿔이 잘 안 맞아서 팍 꽂아봐" · "울트라 머리장식 위아래
길이 1.2배" · "히드라 크레스트 뿌리폭을 특히 줄이고 전체적으로도 폭은 살짝 줄이고 각도를 살짝 더 눕혀야") — 울트라 안에 박혀
있던 층진 판(본판 + 0.72 배 켜 · 가장자리 cu² 말림)을 `crestPlate9(root·axis·nrm·len·wk·oval·rootK·decals)` 로 뽑아 둘이 나눠 쓴다.
· **히드라**: 갈비 다섯의 부채를 걷었다. len 5.0 · wk 0.9 · rootK 0.55(뿌리만 좁게) · 축 50도(울트라 63) · oval 0.1(얇게) ·
  base ZERG_FLESH · layer DARK. 데칼 = 켜의 앞 낯 위 **임자색 띠 셋**(cu 0 폭 0.18 · ±0.62 폭 0.09) — 옛 '가운데 갈비 통째로
  임자색'의 후신. 띠는 판의 말림·두께를 타고 앉는다(`skew(cu0 + cu·wf) − skew(cu0)` 을 제 skewV 로).
· **울트라**: len 4.02 → 4.83(×1.2) · oval 0.1 → **0.22**(반두께 0.14~0.23 → 0.3~0.5 — 뿔 뿌리 반지름 0.48~0.64 가 판 앞뒤로
  다 삐져나왔다) · 뿔 뿌리는 테두리 위(0.98)가 아니라 **반지름의 0.8 만큼 안쪽**에 박는다(x = 반폭 − 0.8r · 말림도 그 자리 값).
  켜의 띄움은 고정 0.15 가 아니라 `(본판 반폭 − 켜 반폭)·oval + 0.15` — 판이 두꺼우면 고정값은 본판 속에 잠긴다.
· 울트라 발은 발목 **바로 밑**(요청: "하지와 발 수직 위치 맞추기" — 옛 y +0.22 가 발을 정강이 앞으로 냈다).
· ⚠⚠ **"엉덩이 파트 줄이기"를 골반 반폭으로 읽은 것은 틀렸고, 그 손질이 표를 잘랐다**(되물음: "엉덩이쪽 뒤로 뾰족히 튀어나온 걸
  줄여 주면 되는데 왜 허리를 통짜로 만들었어") — 한 줄짜리 표 리터럴의 한 마디 뒤에 `// 주석` 을 달았더니 **같은 줄의 나머지 마디
  셋(허리·가슴·앞끝)이 주석에 먹혀** 회전체가 두 마디로 섰다(tsc 는 못 잡는다 · 그림으로만 보였다). 되돌리고 뜻대로 **꼬리끝만
  0.9 짧게**(TY0 −6.3 → −5.4 · 마디 t 를 골반·허리·가슴 y 가 그대로이게 다시 매김 0.228·0.470·0.757 · 허리 토막 경계 0.56/0.5 →
  0.50/0.44). ★ **한 줄에 늘어놓은 리터럴 사이에 줄 주석을 끼우지 마라** — 그 뒤가 통째로 죽는다.
· 재요청 셋(같은 날): "다시 엉덩이 전체적인 사이즈도 좀 줄여줘"(골반 반폭 2.5 → 2.25 · 허리 2.0 의 잘록함은 남는다) · "하지와 발
  수직으로 일치"(발목 = 무릎의 x·y — 옛 발목은 무릎에서 안쪽 0.77·앞 0.15 로 비껴 정강이가 기울었다) · "머리를 몸의 좀 위쪽에
  달리게 목+머리+머리장식 모두 위치 올리기"(UH_UP9 0.25 → 1.15 — 얼굴·볏 뿌리·등마루·옆 뿔이 그 한 값을 받는다).
· 잉크 중심표: hydra 8.39/9.17 → 8.56/9.29 · ultra 9.21/9.88 → 9.07/9.65(옛/새 측정 차). MODEL_NORM 그대로.
· 🔎 `model-gl --kinds hydra,ultra --rots 0,45,90,180 --cell 460` · 머리는 `$S/crop.mjs` 로 확대해 본다.

## 울트라 옆 뿔은 볏 판을 따라 눕고 뿌리는 판 테두리에 앉는다 · 머리·볏은 살짝 앞·위
★ (2026-09, 요청 셋: "머리장식의 옆쪽 뿔들 머리장식에 맞게 위치와 각도(뒤로 눕히는) 맞추기" · "뿔 뿌리가 머리장식에 딱 붙게" ·
"울트라 머리와 머리장식 살짝 앞위로") — 뿔의 '위'는 세계 z 가 아니라 **볏 판의 축 A**(CR0 의 방향 (0, −0.447, 0.894))이고 '앞'은
판의 법선 N(CRN9)이다(`HP9(옆, 축, 법선)`). 여태 z 로 곧게 서서 63도로 누운 판에서 앞으로 튀어나온 꼴이었다.
· 뿌리는 반폭 0.98 배 + 가장자리 말림 `crSk9(±1, t)` 을 **법선 방향 +**(HSK9 = 1)로 태운 자리 — 실측으로 부호를 골랐다(−1 은 뿌리가
  판 뒤로 떠 90도에서 틈이 보였다).
· 머리(zergFace)·볏 뿌리(CR0)는 `UH_FWD9·UH_UP9`(0.25 · 0.25)로 함께 앞·위로 — 등마루·옆 뿔은 CR0 를 읽어 저절로 따라온다.
· MODEL_NORM 그대로(측정 0.372 · 표 0.369 · 잉크 중심 변화 ≤0.01).
· 🔎 `model-gl --kinds ultra --rots 0,45,90 --cell 420` — 90도에서 뿔이 볏과 같은 기울기로 서고 뿌리가 판 테두리에 닿아야 한다.

## 히드라 목은 반쯤 섰다 · 그려지는 크기 ×1.2
★ (2026-09, 요청: "히드라 목 앞으로 휜 거 반 정도 곧게 세우고 얼굴이 그에 맞게 이동(위쪽 뒤로)" · "히드라 그려지는 크기
1.2배") — 목 마디(body0 의 t 0.74~1)는 앞으로 2.9 나가며 거의 안 올랐다(사인 0.84 로 올랐다 0.28 내림). 앞 몫을 **1.45**
로 절반 내고 그만큼의 길이를 **위로**(2.2) 돌려 목 길이(≈3.0)를 지켰다 — 손잡이 굽이는 얕게(사인 0.35 · 끝 0.1 처짐).
· ★ **목 길이는 도로 절반**(재요청: "히드라 목 길이 반으로 줄여줘") — 앞 1.45 → 0.72 · 위 2.2 → 1.1 · 굽이 0.18 · 처짐 0.05.
  머리가 어깨 바로 위에 앉는다(잉크 중심 8.25 → 8.47).
얼굴·머리장식·아가리는 `headAt = body(1)` 에서 나므로 저절로 위·뒤로 따라온다(총구표 [0, 3.1, 4.73] → [0, 1.65, 6.76]).
· 크기는 **MODEL_NORM 을 곱한 값**이다(0.646 × 1.2 — 재측정이 아니다. 목을 세워 잉크가 커진 몫까지 재측정으로 덮으면
  요청과 반대로 작아진다). 잉크 중심 8.86 → 8.24(옛/새 측정 차).
· **팔은 어깨 높이 · 상체 갑옷은 한 마디 위**(재요청: "히드라 팔 위치 높이기(어깨에 맞춰서)" → "상체 갑옷도 높이고 팔도
  그만큼 더 높이기") — 팔 뿌리(z 4.24)가 어깨(body(0.74) z 4.88)보다 0.64 아래·0.9 앞이라 가슴에서 돋은 팔이었다. 갈비 테
  (t 0.5~0.71 → 0.54~0.75)·가슴 갑옷판(0.46~0.74 → 0.50~0.78)을 몸 축을 따라 Δt 0.04(≈ z 0.3) 올리고, 팔 전체(뿌리·관절·
  하완·낫)를 `HY_ARM_UP9`(0.64 + 0.3) 위로 · `HY_ARM_BK9`(0.4) 뒤로 통째로 옮긴다(꼴 그대로). 잉크 중심 8.24 → 8.13.
  ⚠ 갑옷을 z 로 띄우지 말고 **t 로 올려라** — 몸이 목으로 굽는 자리라 z 만 올리면 고리 중심이 축에서 벗어난다. 위 켜(t 0.78)는
  목 밑동 굵기를 저절로 받는다.
· 🔎 `model-gl --kinds hydra --rots 90,45,0 --cell 460`.

## 프로토스 보병의 허리는 섰다 — 몸통 위의 부품은 전단으로, 팔은 강체로 따라간다
★ (2026-09, 요청: "질럿 보병들 허리를 좀 세우자 — 질럿·하템은 수직으로, 다크는 살짝만 숙이게") — 공통 몸통(`protossTorso`)의 축은 골반
(y −0.75)에서 어깨로 가며 앞으로 1.27·위로 1.76 나가는 **36도 숙임**이었다. `lean`(ZEALOT_LEAN9 0 · HT_LEAN9 0 · DT_LEAN9 0.25 ≒ 10도)이
그 앞 몫을 줄이고 **축 길이는 지킨다**(`pTorsoKz9` — 수직이면 z 1.76 → 2.17. 숙인 몸을 세우면 키가 크는 것이 맞다 · MODEL_NORM 은 안 갈았다 —
자세로 달라진 잉크는 재측정으로 안 덮는다는 그 규약).
· **몸통 위의 부품은 좌표를 안 고쳤다** — 목·머리·땋은 머리·가슴 관·어깨판·보석·망토·앞가리개는 다 옛 숙임 자로 박혀 있어(세 빌더에 수백 자리)
  `pUpright9(lean, lift, fn)` 이 **같은 셈을 모형 변환으로** 태운다: 새 감싸개 `withModelWarp`(shapeOblique — 배율·이동·회전보다 앞에서
  (x, y, z) 를 비트는 손. project·modelPoint9 가 태워 2D 경로·3D 메시·총구 표식이 한 자다). 골반 아래는 그대로, 몸통 구간(z 3.08~4.84 + lift)은
  높이 몫만큼 뒤로 물리며 위로 늘리고, 어깨 위는 통째로 옮긴다. 다리·질럿 허리 보호구는 그 밖이다.
  · ⚠ `depthNow` 는 z 를 몰라 **안 태운다** — 키는 비틀기 전 자리로 매기므로 부품끼리의 앞뒤 차례는 종전 그대로다(의도한 값).
· ⚠⚠ **팔은 전단이 아니라 강체다**(`pRigid9`) — 어깨(z 4.5)는 몸통 구간이라 1.0 물러나는데 손(z 3)은 골반께라 안 물러나므로, 전단을 그대로
  태우면 어깨만 옮겨 가 **팔이 늘어난다**(잽 컷 1.8 → 2.5). 팔·손·검 무리는 어깨 자리의 변위 하나로 통째로 옮긴다(pUpright9 안에서 그 몫만
  갈아 끼운다 — withModelWarp 는 겹치지 않고 바꿔 낀다). 질럿 검 끝 총구표가 그만큼 움직였다([2.51, 2.71, 1.74] → [2.51, 1.7, 2.06]).
· 잉크 중심표(UNIT_INK_CY9)는 **옛/새 측정의 차**만큼 옮겼다(zealot −0.20 · htemp −0.18 · dtemp −0.10) — `ink-center --emit` 의 둘째 칸이 표와
  계통이 다르다는 그 ⚠ 때문에 측정값을 그대로 안 넣는다(`git stash` 로 옛 값을 같은 자로 재서 뺐다).
· 머리는 손대지 않았다 — 얼굴은 45도로 누운 회전체라 세운 몸통 위에서 그만큼 더 숙여 보인다(원하면 머리 각이 다음 손이다).
· ★ **수직은 되물렸다 — 15도다**(재요청: "허리는 15도 정도는 구부려야 자연스러울 것") — 질럿·하템 0.44(15도) · 다크 0.64(22도).
  lean 은 y 몫이라 각으로 옮기면 `2.17·sin(각)/1.27` 이다.
· ★ **대퇴는 거의 수직, 정강이·발은 뒤에서 > 꼴**(같은 요청: "허리를 폈으니 대퇴도 거의 수직으로 세우고 하지와 발뼈만 뒤에서
  >모양이 되어야") — `protossLegs` 의 무릎은 골반~발목 선에서 앞으로 0.3 까지 bend 배로 나가(질럿 1.4 → y 0.65) 허벅지가 34도
  앞으로 누워 있었다. 이제 고관절 바로 아래에서 `KNEE_FWD9(0.12)·bend` 만 나간다(질럿 0.17 · 하템 0.08). 발목(−0.75)·발끝(0.5)은
  그대로라 정강이는 뒤로 눕고 발은 앞으로 나간다. bend 의 뜻이 '무릎이 얼마나 앞인가'로 좁아졌다(걸음 풀이의 마디 길이는 그대로).
· 🔎 `model-gl --kinds zealot,dtemp,htemp --rots 90,45 --cell 520` · 잽 컷은 `--pose 2`(팔 길이가 늘지 않았나 본다).

## 프로토스 보병의 꽁지는 걷고 투구를 얹었다 · 망토 띠는 마디마다 네모다
★ **투구**(2026-09, 요청: "머리 뒤의 뾰족 꽁지 제거 — 원작에 자세히 보니 그런 부분은 없더라. 대신 질럿·하템·다템 머리에 앞뒤로 길고
납작하며 좌우가 아래로 호로 살짝 휜 투구 — 앞은 양변이 평행하다가 뒤로 갈수록 좁아져 뾰족한 느낌") — `protossFace` 의 두개골 꼬리
(spikeHorn)를 걷고 `pHelmet9(lift, s)` 를 얹는다(공통 얼굴이라 셋이 같이 받는다). 재요청(사진: "크기 20% 축소 · 폭 줄어드는 부분 짧게 ·
앞부분은 부드럽게 구(이마) 형태가 되어 머리 앞부분이 덮이게") 뒤의 값: **이마 돔**(y 0.30 → 0.74 · 위에서 본 윤곽 `√(1 − 0.55q²)` 로 앞 끝에
반폭의 2/3 을 남기고 · 앞으로 갈수록 0.20·q² 내려가 눈썹 위(z 5.40)에서 끝난다) · 평행 토막(반폭 0.34 · y 0.30 → −0.32 — 재요청 "옆변의 평행 부분을 더 길게"로 0.06 에서 뒤로 늘림) · 꼬리(y −0.85)까지 곧게 좁아짐 ·
좌우 처짐은 **절대 폭**으로 0.24·(v/HW)²(좁은 이마·꼬리는 덜 처진다) · 두께 0.06 · 꼬리로 0.05/칸 내려감. 격자 10×6 + 아래 켜 + 옆·앞뒤 띠 · P_GOLD.
· ★ **판은 껍질 위에 앉힌다**(지적: "머리통이 삐져나오는데 — 투구 위치가 안 맞나 봐") — 얼굴 달걀은 축이 45도로 누운 회전체라 정수리
  부근의 껍질 높이가 자리마다 다른데 판을 손 값(5.60)에 두니 정수리 켜가 처진 판을 뚫었다. `headTop9(x, y)` 가 마디 t 를 훑어 그 (x, y)
  기둥의 껍질 꼭대기를 풀고(단면 원 축점 + r(cosθ·x̂ + sinθ·ê₂) · ê₂ = 접선 × x̂ = (0, 0.859, 0.511)), 그것으로 판을 **통째로 올리는
  몫 하나**(lift9 = 껍질 + 0.03 과 판 밑면의 가장 큰 어긋남)만 잰다.
  ⚠⚠ **자리마다 껍질에 붙이지 마라**(재지적: "투구의 등이 구불구불하면 안 돼 — 처음처럼 앞의 돔에서 직선으로 이어져야지") — 한 번
    판 밑면을 `max(손 값, 껍질 + 0.03)` 로 두었더니 등마루가 두개골을 따라 울퉁불퉁해졌다. 투구는 껍질을 베끼는 천이 아니라
    **곧은 판**이다: 꼴은 손 값(앞 돔 + 뒤로 0.10/칸 곧게)이고 껍질은 '얼마나 올릴까'만 정한다.
· 눈의 빛나는 심은 **푸른색**(#5fb4ff · 요청: "플토 보병들 눈 푸른색으로") — 눈두덩은 그대로.
· ⚠ **이마 앞 끝을 점으로 모으면 부리가 된다** — 반원 윤곽(√(1−q²))으로 두었더니 정면에서 눈 사이로 내려오는 V 자 부리로 읽혔다.
  앞 끝에 폭을 남겨야 챙이다(캐리어 잎의 '호를 한 점에 모으면 그 점이 솟는다'와 같은 자리).
· 🔎 머리는 작아서 시트로는 안 보인다 — `$S/crop.mjs <png> "x,y,w,h;…" 4 out.png`(조각을 잘라 확대해 잇는다)로 본다.
· ⚠ **꼬리를 올리면 부감에서 뒤가 솟은 뿔로 읽힌다** — 처음 0.05/칸 오르게 두었더니 정면에서 머리 위로 금빛 가시가 섰다(뒤 = 화면 위).
  내려가게 두면 판으로 읽힌다. 앞 끝은 눈썹 위(0.68)까지만 — 0.82 는 옆에서 얼굴 앞으로 삐져나온 챙이었다.
· MODEL_NORM 은 안 갈았다(측정 zealot 0.946 vs 표 0.968 · dtemp 0.858 vs 0.847 · htemp 1.133) — 꼬리가 빠지고 투구가 든 잉크 차라
  2% 안쪽이고, 요청은 '투구를 얹어라'지 몸을 줄이라는 것이 아니다(캐리어 잎의 그 규약). 잉크 중심표도 그대로.
★★ **망토 띠는 한 다각형이 아니라 마디마다 네모다**(재재지적: "다템 망토 왼쪽 임자색 띠 아직도 잘 안 보임" — 실측 180 에서 왼 띠가
밑단 한 칸만 남고 자락 뒤로 잠겼다) — 띠는 16 꼭짓점짜리 **한 폴리곤**이었다. 자락은 격자(12×7)로 굽은 살을 따라가는데 띠는 GL 이
귀 자르기로 제 현을 긋는다: 어깨에서 앞으로 감기고 밑단에서 말리는 띠를 지배 축 한 평면에 눕히면 자기교차라 귀를 못 찾고 부채꼴로
물러나, 자락을 가로지르는 긴 현이 살 밑으로 파고든다. 왼쪽만 잠긴 것은 거울이라 사영이 갈렸을 뿐이고 **띄움(0.08)을 키워도 현은 못
살린다**. 자락과 같은 v 칸(NV9)으로 네모를 나누니 넷 방위 다 두 띠가 선다.
· ★ 교훈: **굽은 살 위에 얹는 띠·데칼은 그 살과 같은 칸으로 쪼개라** — 한 폴리곤은 삼각화가 제 현을 긋는다(커맨드 데칼의 그 규약).

## 울트라리스크의 볏은 세운 방패 · 뿔은 양옆 셋씩 위로 · 몸통은 회전체 한 마리
★ (2026-09, 사진 요청: "울트라 머리를 저렇게 해 주고 팔도 아래를 살짝 향하게 내리기") — 원작 시네마틱의 머리는 정수리 위로
**넓고 층진 갑판**이 거의 곧게 서고(앞에서 보면 얼굴 위의 방패), 그 위 양 모서리에서 **큰 뿔 한 쌍**이 바깥으로 벌어졌다가
앞·아래로 굽어 얼굴 옆을 감싼다. 옛 판은 히드라처럼 36도로 뒤로 누운 한 장이라 앞에서는 납작한 띠였다.
· 볏 `CR0`: 63도(뒤 1.8 · 위 3.6)로 세우고 반폭 최대 2.85·UCK9. **층**은 같은 판을 앞 법선(0, 0.894, 0.447) 쪽으로 0.15 띄워
  0.72 배로 한 장 더(HIDE) — 테가 계단으로 읽힌다. 가운데는 등마루 뿔(DARK · 0.62)이 밑동에서 끝까지 지난다.
· 뿔: 처음엔 볏의 가장 넓은 자리(t 0.5)에서 앞·아래로 감기는 한 쌍이었다. **되물렸다**(재요청: "볏 폭 줄이고 양옆에 위로 휜 뿔을
  3개씩 · 위쪽으로 갈수록 뿔이 커짐") — 볏 반폭 2.85 → 1.9(밑동 1.15) · 뿔은 볏 옆 테두리 t 0.2 · 0.5 · 0.8 셋에서 바깥으로
  벌어졌다가 **위로** 감기는 2차 베지에 기둥(배수 0.55 · 0.8 · 1.1 — 위로 갈수록 크다). 갑각색(HIDE) · 끝 38% DARK.
· 낫은 조종점·끝의 z 를 내렸다(+0.32 → −0.1 · −0.44 → −1.5) — 끝이 살짝 아래를 본다.
· ★ **몸통은 회전체 한 마리다**(재요청: "몸통이 절두체들로 돼있어서 너무 어색 생물답게 바꿔주고 다리통 바디에서 너무 얇아지지
  않게") — 앞몸 절두체 셋 + 허리 절두체 + 골반 쐐기 둘 + 피라미드를 걷고, 꼬리끝(y −6.3) → 얼굴(y 3.3)을 등뼈로 한 `spirePillar`
  하나(sides 14 · ref x · trueNormal)로 짓는다. 반폭·등뼈 z·납작함(높이/폭)은 마디 표 `TORSO9`(t 0 · 0.3 골반 · 0.52 허리 ·
  0.78 가슴 · 1)를 캣멀-롬으로 읽는다 — 골반 불룩·잘록한 허리·가슴 불룩이 한 껍질 위의 굴곡이다. 등에는 같은 등뼈 위에 납작한
  갑판(PLATE · 폭 0.8 · 납작함 0.34 · 높이의 0.62 띄움)을 한 켜 얹어 층진 등딱지를 낸다.
  · ⚠ 공격 휘두르기(`upper9`)는 앞몸만 도니 회전체를 허리(t 0.5~0.56 겹침)에서 둘로 끊어 앞 토막만 upper9 에 넣는다 — 같은 표를
    읽어 쉴 때는 이음매가 안 보이고, 돌면 잘록한 허리가 경첩이다. `tipW` 는 뚜껑 문이라 앞 끝에만 1 을 준다(얼굴이 덮는다).
  · **다리는 몸에서 굵게 난다** — 엉덩이를 옆구리 밖(lx·1.12 · z 3.3)으로 내고 **살 덩이 구**(반지름 1.08)를 박아 다리가 근육으로
    이어져 나오게, 허벅지 밑동 1.55 → 2.05(taper 1.9 — 밑동 통통) · 무릎 돔 0.82 → 0.95 · 정강이 1.12 → 1.3.
  · **어깨 판은 둥근 패드다**(재요청: "어깨 판도 둥글게 살에 얹어줘") — 각진 절두체 둘을 걷고, 어깨 자리(y 1.3 · 옆에서 30도 위)의
    몸 껍질 점을 TORSO9 에서 풀어 그 **법선을 축**으로 한 반타원체(spirePillar · 길이 방향 y · 반길이 1.7 · 반폭 1.05 · 두께 0.6 ·
    밑동은 살 속 0.2)를 얹는다 — 어느 각에서도 살을 따라 붙은 볼록 갑판이다. 임자 색(칠하지 않음). 등가시·볏·뿔·낫은 그대로.
· MODEL_NORM 은 그대로(측정 0.383 → 0.380 → 0.371 · 표 0.369) · 잉크 중심표는 옛/새 측정 차만큼 옮겼다(볏·뿔 손질에서 top 9.41 → 9.29 ·
  몸통 손질에서 다시 9.29 → 9.22 · pitch 9.96 → 9.88 · 어깨 패드로 top 9.21).
· 🔎 `model-gl --kinds ultra --rots 0,45,90,180 --cell 420` — 0° 에서 얼굴 위에 방패, 90° 에서 골반·허리·가슴의 굴곡이 한 껍질로 이어지고
  다리가 옆구리의 근육 구에서 굵게 나와야 한다.

## 다크템플러는 반대쪽 어깨로 감았다가 제쪽 뒤로 한 번에 벤다
★ (2026-09, 지적: "다템 칼휘두름이 이상해 — 반대쪽 어깨쪽으로 팔을 올렸다가 제쪽 뒤로 크게 한번에 휘둘러야 함, 중간 모션 있으면 더 부드러울 듯")
— 손 자리표 `DT_HAND9`(dtemp 빌더) 넷: 쉼 0(뒤로 늘어뜨림) · 감기 1(반대쪽 어깨 앞 SW9·−0.85 · 5.7 — 하완이 가슴을 가로지르고 칼끝은 그
어깨 너머로) · 지나감 3(가슴 앞 한가운데 · 칼끝이 앞) · 쏵 2(제쪽 바깥·뒤 SW9·2.3 · 어깨 높이 4.15). 팔꿈치 힌트 `DT_BEND9` 가 하완의
방향을 정하고 칼은 하완과 평행이라, **칼끝이 어디를 보는가는 손이 아니라 팔꿈치가 정한다**. 박자는 engine9 `atkCutOf` 그대로(30·7·11%).
· ★ **지나감·쏵은 팔을 곧게 편다**(재지적: "어깨를 축으로 대각선 회전을 하면서 팔은 직선으로 펴져야 해 — 최대한 멀리까지 벤다는
  느낌 · 마지막에 내려와서는 칼끝이 뒤를 향하게") — 손을 어깨에서 두 마디 합(2.65) **밖**(지나감 2.69 · 쏵 3.0)에 두면 `jointBetween`
  이 곧은 팔로 물러난다. 팔이 반지름이 되어 어깨를 축으로 돌고, 칼은 팔의 연장선이라 끝 컷에서 저절로 뒤·아래를 가리킨다.
  · ⚠ **굽힌 팔로 쏵을 뒤·아래에 두면 칼이 망토 뒤에 묻힌다**(앞 판 1.85·−1.15·3.1 — 네 각 다 빈 컷) — 하완이 몸 쪽으로 꺾여
    칼이 등 뒤로 든다. 곧은 팔은 칼이 어깨에서 6칸 밖으로 나가 망토(반폭 3.06)를 훌쩍 넘으므로 낮춰도 보인다.
· 🔎 `model-gl --kinds dtemp --rots 0,90 --pose 4|2|5` — 0도에서 칼끝이 오른쪽(감기) → 앞(지나감) → 왼쪽(쏵)으로 호를 그려야 한다.
· 앱 도록 팝업의 자동회전(GalleryScreen `auto`)은 기본 **켬**이다(같은 요청) — 손이 잡으면 그때 멈춘다.

## 커세어의 스러스터 빛은 여섯 가지 별이다
★ (2026-09, 요청: "커세어만 스러스터 빛이 특이해 — 6개의 가지로 대각으로 방사형의 뻗치는 흰 불빛. 트레이드마크라 표현 필요") —
프로토스 비행체의 공통 원뿔 화염(`thrustFlame`) 대신 노즐 뒤에 **빌보드 별**을 세운다: `billPoly3` 위의 바늘 여섯(30도 + 60도씩 —
위·아래 + 대각 넷 · 뿌리 반폭 0.07 → 끝 0 · 길이 1.1) 흰 심(`P_PLASMA` #e4f6ff — EMIT_FILL9 에 이미 있어 번짐을 탄다) + 그 아래
하늘빛(#8cc8ff) 번짐 한 겹(반폭 0.16 · 길이 1.25 · α0.34) + 가운데 원반 둘(`billPath3` 0.46 번짐 · 0.2 심). 이동 컷(자세 1)에만.
· 빛은 꼴이 없어 **카메라를 보는 판**이라야 어느 각에서도 같은 별이다 — 몸 뒤(−y −1.74)에 서므로 앞을 볼 때는 깊이로 가려진다.
· 🔎 `model-gl --kinds corsair --rots 0,45,90,135,180 --pose 1 --hash ""`(번짐까지 켜서 본다).

## 넥서스 불가사리 다리 1.25배 · 포토 관은 바깥일수록 굵다
· **넥서스**(2026-09, 요청: "넥서스 발판 튀어나온 불가사리 다리 크기 25프로 확대") — 바닥 표창 판(날 끝 8.9 · 오목점 3.2)과 날 위의
  뿔(7.2~7.7 · 키 1.48)을 `SK9 = 1.25` 로 닮은꼴 확대. 몸 밑동(반폭 4.5)은 그대로라 삐져나온 몫만 큰다. ⚠ **BLD_NORM 은 안 갈았다** —
  bld-norm 은 1.107 → 0.959(잉크폭 17.35 → 20.03)를 내지만 그대로 실으면 몸이 13% 줄어 '다리를 키운' 요청이 절반 지워진다
  (캐리어 잎의 그 규약). 판은 z 0.5 의 납작한 바닥이라 발자국 밖으로 나가도 이웃을 안 가린다.
· **포토 캐논**(요청: "포토 포탑 3중이잖아 바깥 껍질일수록 반지름을 좀더 키워서 지금보다 넓어지게") — `TUBES` 반지름 0.66 · 1.06 · 1.5 →
  0.66 · **1.28 · 1.88**(고리 구멍 반지름 2 안쪽). 대기·겨눔 두 표 다.
· 🔎 **`model-gl --aim N`**(2026-09 에 더했다) — 머리를 N도로 겨눈 채 굽는다(op.headDeg). 포토는 겨눌 때만 관 셋이 다 솟으므로
  이것 없이는 대기 그림(안쪽 기둥 반 높이·바깥 관 밑동)만 보인다. `--kinds coil --rots 45,90 --aim 0`.

## 소환구는 에너지 그물이다 — 빌보드 띠는 한 중심을 나눠 써야 한다
★★ (2026-09, 요청: "소환구 애니로 변경 — 웹 같은 모양 차용하되 더 에너지로 꽉 차게 하고 에너지가 움직이는 형태") — 다크 웹의 전기
줄기 그물을 빌리되, 그쪽은 붓이 프레임마다 갈아 끼우는 캔버스 스프라이트(`dwebSpr9`)이고 소환구는 GL 발광 모델이라 **회전 칸**에 굽는다:
`SPIN_KINDS` 에 warpin · `spinRateOf9("warpin") = 0.6바퀴/초`(열여섯 칸이 초당 9.6번 — 다크 웹의 지직거림과 같은 박자) · 엔진의 공사 op
(프로토스 갈래)가 `spin` 을 싣는다(저사양·잔상은 0). 칸마다 씨앗이 갈리고 그물 전체가 22.5도씩 돌아 한 바퀴/1.7초로 흐른다.
· **줄기는 빌보드 평면의 띠다**(`billPoly3` — shapeOblique 에 새로 둔, 늘 카메라를 보는 판 위의 다각형). 36 가닥이 원반 가장자리에서 나서
  반대편으로 건너가며(시작·끝점을 먼저 정하고 옆으로 흔든다 — `bolt9` 의 꼴) 가지 하나씩. 번짐 한 겹(#5aa8ff 0.28 · 3배 굵기) + 흰 심.
  속 원반은 #dfeeff → **#6fb0ff 0.75** 로 눌렀다 — 흰 판 위의 흰 줄기는 안 읽힌다.
· ⚠⚠ **빌보드의 중심은 다각형 제 무게중심이었다** — 붓은 빌보드를 '중심은 요잉을 돌리고 중심에서의 치우침은 안 돌리는' 식(gl9 정점
  셰이더의 pc·pd)으로 그리는데, 띠마다 제 무게중심을 쓰면 한 판을 이루는 띠 수백이 저마다 제자리에서 돌아 **토막으로 흩어진다**(실측 —
  2D 폴백(`--vs2d`)은 멀쩡한데 GL 만 흠집투성이였다. 그것이 곧 진단이다). `BILL_CENTER9`(WeakMap · Poly3 → 중심)에 적힌 다각형은
  그 점을 중심으로 삼는다(billPoly3 가 적는다 · gl9 가 읽는다). billPath3 의 원반은 제 중심이 축 위라 종전대로다.
· ⚠ 처음 두 판은 **짧은 토막 여럿**이었다: ① 원반 안 아무 데서 나 걸어가다 원 밖이면 끊기 — 대부분 두세 마디에서 끊겼다.
  ② 길이만 늘려도 같았다. 다크 웹처럼 **끝점을 먼저 정하고 그 사이를 흔들어야** 줄기가 원반을 가로지른다.
· 🔎 `model-gl --kinds warpin --spins 0,1,2,5 --rots 45 --hash ""`(칸마다 다른 그물 · 번짐 켬) · `--vs2d` 로 2D 와 견주면 빌보드 중심 병이
  바로 드러난다.

## 소환구의 속은 흰색이고 가지는 테에만 선다
★ (2026-09, 지적: "소환구 너무 푸르게 보여 — 실제로는 에너지 부분 거의 흰색이고 구체에서 가장자리만 가지들로 보이는 형태") —
앞 판은 원반을 가로지르는 그물 36 가닥에 속 원반 #6fb0ff 였다. 이제 속은 #eef6ff(불투명) · 파란 테는 얇게(3.05 → 2.85 · #7fb8ff) ·
줄기 30 가닥은 **테에만**: 짝수는 테를 따라 도는 호(0.88~1.0R · 0.5~1.1rad) · 홀수는 테에서 후광 속으로 뻗는 촉수(0.75~0.95R → 1.1~1.35R).
· ⚠⚠ **후광 셋은 속보다 먼저 그려라** — 발광 종류(GL_GLOW_KINDS9)는 깊이 없이 화가 차례라, 반지름이 속보다 큰 반투명 파랑 셋(0.2+0.12+0.06)을
  뒤에 두면 흰 속 위에 통째로 얹혀 도로 하늘색이 된다(실측: #eef6ff 로 적었는데 연파랑 — 색을 아무리 희게 해도 안 희어지는 자리였다).
· 🔎 `model-gl --kinds warpin --spins 0,1,2,5 --rots 45 --hash ""`.

## 스톰 줄기는 여러 각도로 내리친다 · 옵저버토리 기둥 25% 위로
· **스톰**(요청: "메인 줄기 무조건 수직 아니고 다양한 각도로 내려오게") — 줄기 꼭대기가 닿는 자리에서 비껴가는 몫 `(rnd−0.5)·R9·0.5`(±1.7 ·
  높이 15.5 라 6도 안쪽 = 사실상 수직) → **`·2.4`**(±8.2 · 수직에서 0~28도). 🔎 `model-gl --kinds storm --spins 2,5,8,11,18,21 --rots 45 --hash ""`.
· **옵저버토리** 기둥 셋(`PILLARS9` 높이 2.56 · 3.52 · 2.56 → ×1.25). bld-norm 1.997 그대로(폭은 안 변한다).
  ⚠ **되물렸다**(재요청: "옵저버토리 가지 플토 기본 금색으로, 앞쪽과 가운데만 높이만 1.25배") — '가지'는 기둥이 아니라
  **아래 들보**였다(기둥은 원래 금이라 색 요청이 그것을 가른다). 기둥은 옛 높이로 되돌리고, 들보는 색을 안 칠해 `raceBase`
  의 프로토스 기본 금(RACE_BASE_TONE.toss)을 받게 하며(IVORY '시본색' 읽기도 되물림), 앞 두 변(2-3·3-0)과 가운데 대각선(1-3)만
  **단면의 세로 두께 ×1.25**(되물음: "높이만 1.25배" → "세로 두께 키우라는 거야" — 한 번 z 를 1.55 로 올렸다가 되돌렸다).
  · 자는 `spirePillar` 의 `ref: [0, 0, 1]`(u축 = 위) + `widthOf` 를 ×1.25 + `oval: 1/1.25`(v = 가로를 도로 0.30 으로) —
    '오른쪽이 아니라 위로만 두꺼운 관'은 이 셋이 한 벌이다. 눌린 단면이라 `trueNormal` 을 준다(그 ★★ 규약).

## 여덟 손질(2026-09 · 프로토스 넷 · 나이더스 · 다크 · 머리)
· **옵저버토리 바닥 이음관**(요청: "프로토스 시본색으로 하고 굵기 살짝 증가") — 들보 넷을 `IVORY`(#eae3d2 — '시본'을 뼈·상아색으로
  읽었다 ⚠ 다른 뜻이면 되물어 고친다) · 굵기 0.26 → 0.30. bld-norm 1.997(표 1.599 = ×0.8 눌러 둔 값 그대로).
· **어시밀레이터 워프문 = 콘택트렌즈**(요청: "지금 부품들 제거하고 진짜 콘택트렌즈 모양으로 붙이기") — `lensFaces`(테 원판 + 볼록 켜 +
  심 + 광)를 걷고 `contactLens9`(bake9): 돔 타원면(r·hh) 위의 점을 법선 쪽으로 두께(가운데 0.14 · 가장자리 0.02)만큼 띄운 얇은 사발
  격자(고리 6 × 둘레 20) + 바깥 켜 금 테. 안면은 돔이 곧 그것이라 안 그린다.
  ⚠ **유리에 알파를 주지 마라** — 옛 렌즈는 뒤에 불투명 청록 판(back)이 있어 비쳐도 청록이었지만, 껍질에 붙는 렌즈의 뒤는 **금 돔**이라
    반투명 아쿠아가 금과 섞여 허연 회색이 됐다(실측). 불투명 아쿠아 두 단(쉼 #5fe6d4/#b7fff5 · 캘 때 #a3fff3/#e0fffb).
· **넥서스 불가사리** 1.25 → **1.125**(재요청: 10% 축소). BLD_NORM 그대로(위 절).
· **나이더스**(요청: "바닥 앞쪽에 작은 뾰족 수평 가시 4개 · 동굴 안 라임색 리트 평소에도 깜빡거리기") — 둔덕 밑동 앞(y 3.15~3.45 · z 0.3)
  에서 앞으로 1.0 뻗는 `spikeHorn` 넷(IVORY_DEEP · w 0.22). 라임 빛은 **바닥 웅덩이**(z 0.09 · y 0.5~2.9)다 — ⚠ 뒷벽에 세우면 안 보인다:
  굴이 3.6 깊은데 카메라가 40도 위라 아가리 위턱(z 1.5)을 지나는 시선이 바닥 1.8 에서 부딪혀 뒷벽에 닿지 않는다(실측: 검은 굴만 보였다).
  `glowLit("#b6ff3a", "#3d5a14")` · `LIT_KINDS` 에 nydus · 엔진의 lit 문에 `|| shapeKind === "nydus"` — 활성과 무관하게 늘 깜빡인다.
  2D 폴백은 아치 안의 작은 호 한 장. ⚠ 굴 옆의 검은 쐐기(0·45·180도)는 이 손질 전부터 있던 것이다(굴 옆벽 — 따로 볼 자리).
  🔎 `model-gl --kinds nydus --rots 0 --lit --hash ""`. ⚠ 이 하네스의 번짐은 팩토리도 실루엣 전체에 후광을 두른다(MRT 없는 아이콘 길) —
  지도의 모습이 아니다.
  · ★ **빛은 반투명하게 퍼진다**(재요청: "커널 동굴 안 라임색 빛은 반투명하게 퍼지게") — 불투명 판 한 장(초록 카펫)을 걷고 심 0.55 에서
    알파가 반씩 줄어드는 겹 다섯(0.55·0.28·0.18·0.11·0.06 · 넓이 0.62~1.4배)으로 깐다. 한두 겹은 계단으로 읽힌다(실측). 아가리 **앞**(y 3.0 ·
    z 0.8)에 카메라를 보는 빛덩이(billPath3 r 1.05 · 0.2)를 띄워 문틀 밖으로 새는 빛을 낸다.
    ⚠⚠ **굴 속에 빌보드를 띄우면 안 보인다** — 카메라를 보는 판의 깊이는 중심의 시선 거리 하나인데, 아가리로 보이는 화소는 대개
      앞쪽 바닥·문틀 아랫입술이라 그보다 가깝다(실측: 굴 속 y 1.5~2.4 에 알파 0.9 로 두어도 화소 하나 안 바뀌었다 — 메시에는 있었다,
      `model-mesh --dump` 의 '빌보드' 줄). 40도 부감에서 굴 속 공기를 빛나게 하려면 판이 문틀보다 **앞**에 서야 하고, 그러면 반지름은
      땅 밑으로 안 내려가게 죈다(z − r·cos40 ≥ 0).
    🔎 화소를 재는 자: `$S/px.mjs <png> x0,x1,y0,y1,step`(playwright 새 헤드리스 — 이 기계에 pngjs·PIL 이 없다).
· **다크템플러 검은 왼팔**(요청: "칼 팔 있는 쪽 반대로") — `SW9 = −1` 한 부호가 손 자리표(DT_HAND9)·팔꿈치 힌트(DT_BEND9)·어깨·반대쪽
  갈퀴 손의 x 를 다 뒤집는다. ⚠ 거울 변환(withModelScale −1)로 하지 않았다 — 감기가 뒤집혀 열린 관(pLimb · caps none)의 법선이 거꾸로
  서서 팔이 검게 그려진다.
· **프로토스 기본 금** `RACE_BASE_TONE.toss` #e6d063 → **#e8cb5e**(요청: 살짝 붉은 톤). 유닛 금 `P_GOLD`(#d9c344 — 앞서 "붉은기 줄인"
  값)는 안 건드렸다.
· **파일런** 수정 기둥 반폭 2.6 → 2.34 · 고리 `RING_R` 4.6 → 4.14(둘 다 10%) — 갈고리·보석은 RING_R 을 따라 저절로 든다. bld-norm 2.255
  (표 1.905 눌러 둔 값 그대로).
· **질럿·하템 뒤통수**(`protossFace` 의 skull9) 반지름 0.3 → 0.35(요청: 면을 살짝 넓게) — 공통 얼굴이라 다크도 같다. 잉크 중심 변화 ≤0.01.

## 웹은 영역, 마엘스톰·이레디에이트는 유닛별이다
★★ **캐스트 셋을 원작 꼴로**(2026-09, 요청: "원작과 비슷하게 수정하자 — 웹은 영역이고 마엘스톰 이레디에이트는 유닛별 · 마엘스톰은 광역이고
이레디에이트는 개별") — 옛 판은 셋 다 **시전 자리의 판 한 장**(웹 자줏빛 격자 · 마엘 자줏빛 원 · 이레디 표적을 따라가는 AREA_FX)이었다.
· **웹(dweb)은 영역** — 땅 위에 깔린 하늘빛 타원(좌우로 긴 0.97×0.66) 위에 **전기 줄기 34 가닥이 겹친 난수 그물**(`dwebSpr9(v)` · 씨앗만
  다른 판 셋을 초당 10번 갈아 끼워 지직거린다). ⚠ 처음엔 구름 덩이 열여덟으로 그렸다가 "동그라미 모임"이라는 지적을 받았다 — 원작의
  웹은 덩이가 아니라 **줄기**다. 줄기는 타원 가장자리에서 스러진다(`inEll9`) · 가지 하나씩 · 번짐 한 겹 뒤에 흰 심.
· **마엘스톰(maelU)·이레디에이트(irrU)는 유닛별** — 엔진이 걸린 유닛(`e.statuses` 의 mael·irr)마다 op 를 싣는다(cage 와 같은 자리 · 몸
  가운데 `rise9·cpx8 + 0.24·cpx8` · 크기는 잉크 폭). 시전 자리의 `mael` 은 0.5초 섬광만 남기고 AREA_FX 의 Irradiate 줄은 걷었다.
  · 마엘스톰은 **주황 혀 셋이 각자의 입체 축을 도는** 소용돌이(지적: "세 개가 각각의 입체축으로 도는 모양") — 한 타원 위를 셋이 나란히
    돌면 접시 하나다. 궤도 셋(`orb9` — 기울기 0·60·120도 · 납작함 0.42/0.55/0.36 · 속도 1.15/−0.95/1.3바퀴/초)이 자이로처럼 얽히고, 혀는
    머리가 굵고 밝게(#fff0a0) 꼬리로 갈수록 가늘고 붉게(#ff5a1a) 스러진다. 지속 7.4초 · 끝 0.6초에 스러짐.
  · 이레디에이트는 **작은 가스 구름 여러 개가 합쳐진 둥근 덩이**(지적 둘: "좀더 둥글게" → "한 덩이가 아니라 아까처럼 작은 가스 구름이
    여러 개 합쳐져서 둥근 모양" → "개수가 과해 좀 줄여서 한 덩이들이 크게") — 가운데 하나 + 바깥 고리 일곱(0.4R · 반지름 0.36R).
    열아홉(안 6 + 밖 12)은 알갱이 무늬로 읽혀 되물렸다. 몸 폭의 2.4배 · 두 겹이 서로 반대로 돈다(0.3바퀴/초) · 숨(0.8초) · 지속 25.2초.
· 🔎 `$S/casts1.mjs` — 세 줄(dweb·maelU·irrU) × 나이 여섯을 한 장에 굽는다. 스커지 죽음은 `$S/scourge1.mjs`.

## 스커지는 시체가 아니라 폭탄이다
★ (2026-09, 요청: "스커지 자폭 효과(사망) 잘 나오는지 확인") — 스커지의 죽음은 저그 결의 **피떡·살점**뿐이었다(나는 몸이라 그 피떡이
공중에 떠 있었다). 원작은 부딪혀 **주황 불덩이**로 터진다. 엔진이 죽음 자리에 `FX_IMPACT.scourgeboom`(전차포 착탄과 같은 문법의 둥근 불 ·
r 1.25 · 눌림 없음) 스플래시를 한 장 더 싣는다(`kind: "hit"` · splash · dist 0 · 몸 폭 1.1배 · DIE_FX_SEC 0.5초). 저그 파편은 그대로 튄다.

## 광자포·독성포는 지름을 한 단 키웠다
★ (2026-09, 요청: "포톤캐논 드라군 광자포, 가디언 스포어 독성포 지름 살짝 키우기") — 갈래표 plasma(드라군·스카우트·아비터 · 포톤은
`FX_BEAM.photon = FX_BEAM.plasma` 로 같은 줄) w 0.85 → **1.0** · r 0.82 → 0.95 · venom(가디언·스포어) w 1.05 → **1.25** · r 0.85 → 1.0.
길이(l)·박자는 그대로다.

## 컨트롤타워 꼭대기 등도 활성에 깜빡인다
★ 철탑 꼭대기 초록 돔(`domeFaces3(…, "#4cd86a")`)이 늘 켜진 채 번짐도 안 탔다(2026-09, 요청: "컨트롤타워 활성화시 위쪽 등도 깜빡이게
하기") — 몸통 창과 같은 `bldLitNow` 문으로 `winLit("#9dff8a")`, 꺼진 때는 어두운 초록(#2f7a3e). 🔎 `model-gl --kinds ctower --lit --hash ""`.

## 미네랄·가스 색은 밝고 따뜻하게
★ (2026-09, 요청: "미네랄 가스 색 검정기 빼서 밝게 하고 색감 따뜻쪽으로 조금 이동") — 미네랄 결정의 검은 덧칠 0.15 → **0.05**(GL 이
방향광·실루엣 빛으로 제 그늘을 내므로 여기 검은 몫까지 얹으면 밭이 통째로 칙칙하다) · 바탕 #689df3 → **#86b4e4**(일꾼이 든 덩이 `mineralLoad` 도 같은 값) · 간헐천 바위 #7a7264 → #948870 · 어두운 자갈 #4e483f →
#6c6252 · 테 안쪽 그늘 #241f19 → #3d342a · 베스핀 #80ff96 → **#8cff86**(가스 덩이 여덟 자리 다 — 정제소·어시밀·익스트랙터·gaspuff) ·
웅덩이 #46e07a → #52e066 · 고인 가스 알파 0.3/0.26 → 0.42/0.38. 잉크 상자는 안 변한다(색만). 🔎 `model-gl --kinds mineral,mineralb,mineralc,geyser,scvMin,scvGas`.
· ⚠⚠ **파랑의 '따뜻한 쪽'은 보라가 아니라 노랑 쪽이다**(지적 두 번: "좀더 따뜻한 톤쪽으로" → "아까부터 반대로 가고 있어 — 노란색 쪽으로
  이동하라는 뜻인데 보라로 가네") — 색상환에서 붉은 쪽으로 돌리면(217 → 224 → 241도) 라벤더·페리윙클이 되어 오히려 차가워 보인다.
  따뜻하게 하는 손은 **노랑을 섞는 것**이다: 채도를 내리고 색상을 하늘 쪽(211도)으로, 밝기를 올린다(#8aa4f4 · #9694f4 는 되물린 값).

## 효과가 늘면 값도 는다 — 단 표에 손잡이를 달고 문턱을 죈다
★ **헤드리스는 GPU 몫을 못 잰다**(2026-09, 지적: "여러 효과가 들어가면서 사양이 올라간 듯 — 최적화할 거 있나 살펴보고 안 되면 벤치
기준 높이자") — perf-check 폰 프로필(4배 조임 · 배율 3)로 심(glvec=0)·광택/결·음영·gl2·gl=0 여섯 변형을 돌려도 **p50 이 다 417~450ms**
로 노이즈 안이다(소프트웨어 GL 이 "(program)" 78%). 곧 GL 효과의 값은 실기에서만 보인다. 그래서 실기에서 값이 드는 자리를 코드에서
골라 **단 표에 손잡이**를 달았다:
· **MSAA 표본 수 `DEV9.glMsaa`**(gl9 `msaa` · `#glmsaa=N`) — MRT 판은 색·깊이·빛 셋이 표본 수만큼 커진다(폰 DPR 3 이면 4 표본에 한 장
  100MB 남짓을 매 장 지우고 채우고 푼다). 폰 0·1단 **2** · 2단·PC 4. 바뀌면 `mrtEnsure` 가 판을 다시 짓는다(기록에 samples 를 든다).
· **심의 곡선 마디 몫 `DEV9.fxQual`**(glctx9 `qual`) — 호·베지에의 마디 수에 곱한다. 테셀레이션은 JS 몫이라(실측 헤드리스 240 op 에
  심 3.4ms vs 캔버스 1.3ms) 폰에서 그대로 프레임 값이다. 폰 0단 0.6 · 1단 0.8 · 2단·PC 1. 붓이 장마다 `vec9.qual` 에 옮겨 적는다.
· **빛나는 개체가 없는 장은 빛 판을 안 쓴다** — 본 패스 앞에서 `nEm === 0` 이면 `drawBuffers([ATT0])`. 둘째 출력은 낯마다 한 번 더
  쓰는 몫이라 번질 것이 없는 장(대부분)에서 그 몫이 통째로 빠진다. 판은 그대로라 갈아 끼우는 값이 없다.
· **폰 문턱 한 칸 죔**: 26 → 23 · 11 → 10 · 입체 24 → 22. 손잡이의 실기 효과를 못 재므로 지적대로 함께 죈다(`#tier=N` 으로 되돌려 견준다).
· 🔎 `perf-check --shot x.png --glblit --mrt --bloom --hash glmsaa=2 --diag` — 표본 2·0 에서 GL 경고 수가 기본(6줄 · 다 SwiftShader
  폐기 경고)과 같고 그림이 선다.

## 고스트는 교전에 들어설 때 총을 꺼내고, 떠날 때 넣는다
★ **한 프레임에 튀던 '등에 멤 → 겨눔'을 앞뒤 한 마디로 잇는다**(2026-09, 요청: "고스트 총 꺼내기 부드럽게") — 공격 컷
(`atkCutOf`)이 '겨눈 채 반동'뿐이라 교전에 드는 순간 총이 등에서 어깨 앞으로 순간이동했다(그 함수의 ⚠ 가 적어 둔 자리).
그 함수는 쿨다운 위상만 알므로 마디는 **부르는 쪽**이 세운다: `drawAge9`(공격 컷 갈래를 지난 프레임을 기억해 이어진
구간의 시작을 낸다)가 `GHOST_DRAW_SEC9`(0.24초) 안이면 반쯤 든 컷(POSE_ATK_L), 갈래를 떠난 뒤 `holsterAge9` 가 같은
창 안이면 같은 컷으로 넣는다(그 뒤 기억을 지운다).
· **새 교전의 자는 프레임 간격**이다 — 한 프레임 넘게(0.2초 · 워커가 무거우면 초당 8장이라 0.125초까지 벌어진다) 갈래를
  안 지났거나 시각이 뒤로 갔으면(탐색) 다시 꺼낸다. 걸으면 걸음 컷이 먼저라(총은 등에) 멈춰 서서 다시 쏠 때 또 꺼내는
  것이 맞다.
· ⚠ 자세는 메시 열쇠(`u:종류:자세`)라 컷을 더 잘게 못 나눈다 — 고스트는 자세 여섯 칸을 다 쓴다(0 등에 멤 · 1·3 걸음 ·
  2 겨눔 · 4 반쯤 · 5 반동). 더 부드럽게 하려면 자세 칸을 늘려야 한다(메시 벌이 는다).
· 도록의 액션 칸(`DOC_ACT_POSE9` · 컷당 0.45초)은 보여 주는 자리라 그대로 두었다.
· 🔎 `$S/draw1.mjs` — 30fps 로 교전 → 쉼 → 교전을 흉내 내어 draw·aim·holster·idle 차례와 탐색·큰 간격을 찍는다.

## 도록의 액션은 '두 그림 갈아 끼우기'가 아니라 애니메이션이다
★★ **지도가 여러 판으로 그리는 동작은 도록도 여러 판으로 그린다**(2026-09, 요청: "도록 팝업에서
탱크 변신 장면을 재생기와 똑같이 애니메이션으로 보여 줘 — **앞으로 다른 액션도 모두 애니메이션으로
올려야 돼**") — 시즈 모드의 액션 칸은 합본 `tank` 와 `tanksiege` 를 1.1초마다 갈아 끼우고 있었다.
그것은 변신이 아니라 **깜빡임**이고, 지도가 보여 주는 세 마디(돌기 → 포신 갈아 끼우기 → 쳐올리기)가
통째로 빠진다. 한 칸이 한 판만 그릴 수 있던 것이 그 까닭이었다(`attach` 한 자리뿐).
· **`DocCell9.parts`** 가 그 자다 — 몸과 **같은 자리·같은 창**에 판을 차례로 얹고 각자 제 자세·요잉을
  쥔다. 시즈 전환은 지도와 똑같이 **차체 `tankbody` + 버팀다리 홑판 둘 + 전환 포탑 `tankturretxf`** 다.
  길은 셋 다 열어 두었다: `gl9.GlIconReq9.parts`(GL) · `ShapeIcon.parts`(2D 폴백) · `DocIcon9.parts`.
· **시계도 지도의 것을 그대로 쓴다**(`docSiegeCell9`) — 창 `SIEGE_XF_SEC`(1.5초) · 앞 35%
  (`SIEGE_TURN_U9`)는 도는 몫 · 나머지가 기계 마디(engine9 `xfMech9`) · 포탑 자세는 창 전체의
  smoothstep(engine9 `gunU9`). **새 숫자를 지어내지 마라** — 지어내면 도록이 재생기와 딴 속도로 돈다.
· 한 바퀴는 **접기 → 접힌 채 → 펴기 → 편 채**다(머무는 몫 0.8초) — 머무는 몫이 있어야 두 끝 모습이
  눈에 남는다. 그 두 몫에서는 지도와 같은 **정착 판**(`tankgun` · `tanksiegegun`)으로 갈아탄다.
· ⚠ **도는 각만 도록의 것이다** — 지도는 참값 각에서 −45(`SIEGE_FACE_DEG9`)로 돌지만 도록엔 참값이
  없고 요잉은 손가락이 쥐고 있다. 그래서 '제자리에서 90도'로 보인다(그 마디가 **있다**는 것이 뜻이고,
  어느 절대각이냐는 도록에서 뜻이 없다).
· ⚠⚠ **칸의 창은 겹판까지 아울러 재야 한다** — 앱(GalleryScreen `boxOf`)이 `c.kind`·`c.attach` 만
  모으고 있었다. 빼면 창이 차체에만 맞춰져 **뻗은 버팀다리와 뒤로 나온 시즈 포신이 잘린다**.
  `doc-sheet --anim` 도 같은 손이 필요하다(둘 다 고쳤다).
· 🔎 보는 자: `node scripts/doc-sheet.mjs --anim --kinds tank`(GL) · `--2d`(폴백) — 여덟 시각의 칸이
  한 줄로 서므로 마디가 빠진 자리가 눈에 바로 걸린다.
· ★ **다음 액션도 이 길이다**: 지도에서 그 동작이 무슨 판을 어떤 자세로 그리는지 먼저 찾고(engine9),
  그 판·그 자세를 `parts` 로 그대로 옮긴 뒤 시계만 도록의 한 바퀴에 맞춘다.

## 가스 연기는 메시가 아니라 프레임마다 놓는 덩이다
★★ **회전 칸(16)에 구우면 초당 4.8장이라 뚝뚝 끊긴다**(2026-09, 지적: "가스 연기가 너무 뚝뚝 끊겨 프레임 훨씬 늘려서
부드럽게") — 칸을 늘리면 그만큼 메시 벌이 는다(가스 넷 × 칸 · 폰 상한 240벌). 그래서 GL 에서는 연기를 **메시에 안 굽는다**:
`gasPuffs9` 가 `MESH9.on` 이면 면 대신 **굴뚝표**(`GasEmit9` — 자리·오르는 높이·반지름·박자, 감싸개의 배수·이동·회전을
`modelPoint9` 로 다 먹인 모형 자)만 적고, gl9 가 그것을 걷어 그 벌에 붙인다(`GlMesh9.gas`). 붓은 프레임마다 소수 시각으로
덩이를 풀어(`gasPuffFrames9` — 2D 굽기와 **같은 식**) 구 한 벌(`SHAPE_BUILDERS.gaspuff`)을 제 자리·제 반지름·제 알파의
**인스턴스**로 민다(`gl9.gasPush9` · 자리는 몸과 같은 원점·배수·요잉·카메라를 지나는 `modelXY9`). 칸이 없으니 프레임 수에
상한이 없다.
· **회전 칸은 소수다** — 엔진(건물·간헐천 자원 op 둘 다)과 도록(`docCellsOf9` 의 `cell9`)이 가스 종류(`GAS_KINDS9`)에서는
  `t·rate·N` 을 **안 접고** 싣는다. GL 메시 열쇠는 `glSpinTag9` 로 0 에 못 박는다(메시가 칸과 무관하다 — 소수를 열쇠에 실으면
  프레임마다 새 벌을 굽는다). 2D 폴백·도록 SVG 는 종전대로 칸에 굽고, `spinTag` 가 가스 종류를 내림해 캐시 열쇠가 안 샌다.
· ⚠ **간헐천은 도록에서 유닛 길이다**('부가' 무리) — `unitMesh` 도 굴뚝표를 걷고, 도록 아이콘의 두 갈래(건물·유닛)가 다
  `gasDraws9` 로 덩이를 얹는다. 도록의 창(`docCellBox9`)은 `gasTops9`(굴뚝 꼭대기의 16-상자 치우침)로 덩이가 오를 자리까지
  넓힌다 — 2D 창(shapeFitBox)에는 오른 덩이가 없어 안 넓히면 칸 위로 잘린다.
· 덩이는 알파 < 1 이라 차례 몫(③)에서 몸 뒤에 그려지고 **제 깊이 칸**을 받는다(몸 바로 뒤에 밀어 몸보다 앞 · 뒤에 선 개체는
  그 위). 익스트랙터의 덩이는 옥상 가운데의 초록 기둥과 같은 색·같은 자리라 옅게 보인다(메시 시절과 같은 기하).
· `model-gl`(메시를 곧장 미는 도구)에는 덩이가 안 선다 — 보는 자는 `doc-sheet --anim --kinds geyser,refinery,assim,extract`
  (시각 여덟 칸에 덩이가 나고 오르고 스러진다) · 붓 하네스는 `$S/gas1.mjs` 꼴(gasPush9 를 u 넷으로 곧장 부른다).
· 🔎 굴뚝표가 잡혔나: `g.bldMesh(op).gas` 를 찍어 본다(넷 다 있어야 한다 · 간헐천 셋·어시밀 셋·정제소 하나·익스트랙터 하나).

★ **김은 정지 그림이 아니라 오르는 덩이다**(2026-09, 요청: "간헐천 및 각 종족 가스건물의 가스연기를 뭉게뭉게로 바꾸고
애니메이션화(간헐천과 어시밀레이터는 가운데와 좌우 가스가 번갈아 나오게하기)") — 옛 김은 위로 갈수록 넓고 옅어지는 타원
세 켜였다. `gasPuffs9`(bake9)가 **저해상도 구**(4×8 낯) 몇을 아가리에서 내어 커지며 오르고 스러지게 한다(위 ★★ 뒤로 GL 은
인스턴스 · 2D 는 회전 칸 bldSpinNow · SPIN_ANIM9 칸 = 한 바퀴). 간헐천·정제소·어시밀레이터·익스트랙터가 `SPIN_KINDS` 에 들었다.
· **걸음은 한 문이 준다** — `spinRateOf9(kind, working)`(engine9): 일할 때만 도는 것 1.6/0 · 늘 도는 것 2.2 · **가스 0.3**.
  지도(op.spin)와 도록(docCellsOf9)이 같은 문을 보므로 어느 쪽 숫자만 고치면 안 된다. ⚠ 더 빠르게 하면 덩이가 튀고, 칸을
  늘리면 메시 벌이 는다(칸 열여섯 × 불빛 둘) — 값을 고치려면 둘을 함께 보라.
· ★ **나왔다 쉬었다**(2026-09, 요청: "가스 나오는 텀 길게(나왔다가 쉬었다가 나오게) · 번갈아 나오는 거에도 똑같이 적용")
  — 옛 시계(0.45)는 덩이 셋을 한 바퀴에 고르게 흩어 **늘 무엇인가 오르고** 있었다. 이제 `gasPuffs9` 의 `duty`(덩이가 나는
  앞 몫 · 기본 0.25)·`life`(덩이 한살이 · 기본 0.4 바퀴)가 바퀴의 뒤 0.35 를 **빈 채로** 둔다(0.3 에서 한 바퀴 3.3초 =
  2.2초 내고 1.2초 쉼).
· **번갈아 나오기는 위상 + 죔이다** — 가운데(간헐천 큰 분화구·어시밀 뒤 굴뚝) 0, 양옆 **0.5**(덩이 둘). ⚠ 위상만 반 바퀴로
  두면 두 쪽의 활동이 반 바퀴를 꽉 채워 이어져 쉬는 때가 없다 — 번갈아 나오는 쪽은 `duty 0.1 · life 0.28`(활동 0.38 ·
  쉼 0.12)로 죄어 한쪽이 **다 스러진 뒤에** 다른 쪽이 난다. 간헐천의 작은 분화구 둘은 같은 0.5 다(옛 셋째 0.25 는 큰 것의
  꼬리와 겹쳤다).
· ⚠ **어시밀레이터에는 옛 정지 김이 남아 있었다**(2026-09, 지적: "양옆 두 개와 가운데 굴뚝이 번갈아야 하는데 잘 티가
  안 남") — 덩이로 바꾸며 걷은 줄 알았던 '위로 갈수록 넓고 옅어지는 타원 세 켜'가 굴뚝 셋 위에 그대로 서 있어, 쉬는
  굴뚝에도 김이 있는 것으로 읽혔다(`model-gl --spins all` 에서 칸마다 안 바뀌는 초록 원반이 그것이다). 걷고 덩이를
  0.42 → 0.5·k9 로 키웠다. 잉크가 좁아져 BLD_NORM assim 1.664 → 1.749 · 잉크 중심 3.82 → 2.88(정지 김이 위로
  끌어올리고 있었다). 정제소·간헐천·익스트랙터의 잉크 중심도 칸 0 의 덩이가 준 만큼 다시 쟀다.
· **익스트랙터 김은 옥상 가운데**(요청) — 애벌레 아가리(앞 끝)가 아니라 안장 위 검은 덮개 꼭대기(0.05, −0.9, 3.15). 잉크
  폭은 안 변했다(bld-norm 1.066 그대로).
· 🔎 `node scripts/model-gl.mjs --kinds geyser,assim,refinery,extract --spins all --rots 45` — 칸 열여섯이 한 줄로 서서
  내는 칸·쉬는 칸이 눈에 바로 걸린다. ⚠ 간헐천은 '부가' 무리라 이 자가 **유닛 길**로 보내 칸이 열쇠에 안 들고 늘 같은
  그림이었다(2026-09 에 SPIN_KINDS 면 건물 길로 보내게 고쳤다) — 시트가 안 움직이면 먼저 이 문을 의심하라.
· 간헐천은 **자원 op** 라 엔진의 자원 자리에서 spin 을 싣는다(고갈 geyserdry 는 김이 없어 안 싣는다).
· ⚠ 빌보드 원반(billPath3)을 쓰지 마라 — 이 종류에서는 셰이더가 법선 대신 가운데 자리를 받아 명암이 자리를 탄다.
  정밀 구(12×20)도 안 된다 — 덩이 아홉에 4천 삼각이라 칸 열여섯이 폰 메모리를 먹는다.
· ⚠ 잉크 상자가 바뀐다 — 옛 세 켜 타원이 잉크를 넓혀 **분화구가 작게 그려지고 있었다**(geyser 1.587 → 1.706 ·
  assim 1.627 → 1.664 · 잉크 중심 geyser 3.88 → 3.14). 붓은 칸 0 으로 상자를 재므로 칸 0 에도 덩이가 선다.

## 재생바는 손이 임자다 — 재생기는 따라간다
★★ **끌기 중에는 슬라이드가 우선이고 지도는 뒤따른다**(2026-09, 지적: "재생바 이동시 로딩이 안 되면 슬라이드가
안 되는 문제 — 슬라이드가 우선이고 재생기가 따라가야") ─────────────────────────────────────────────
손잡이는 비제어 `<input range>` 라 메인 스레드만 비어 있으면 늘 따라온다. 멎은 까닭은 그림이었다: 끌기 이벤트마다
`setT` → **컴포넌트 전체 리렌더** → 붓이 지도를 한 장 칠하는데, 탐색은 늘 새 장면이라 처음 보는 종류의 메시를
그 프레임에 **동기로** 다 짓는다(종류당 1~7ms · 수십 종이면 수백 ms). 그 동안 포인터 사건이 못 돌아 손잡이가 멎는다.
· 셋을 갈랐다(`onInput`): ① 손잡이·시계 글자는 즉시(시계는 `clockElRef9` 에 DOM 으로 직접) ② 지도는 `tLiveRef9`
  만 옮기고 `requestPaint9`(rAF 로 모인다) ③ React 거울(`setT` — 워커 cmd·미니맵·DOM 효과의 임자)은
  `SCRUB_MIRROR_MS9`(100ms)마다만. 손을 떼면(`scrubEnd9`) 마지막 값을 곧장 놓고 예산도 푼다.
· **붓은 끌기 동안 메시를 예산 안에서만 짓는다**(gl9 `glScrubSet9` · `SCRUB_BAKE_MS9` 6ms/프레임) — 넘치면
  `meshFor` 가 **굳히지 않고** null 을 내고, 붓은 그 몸을 그 장에서 뺀다(`continue`). ⚠ 폴백(면 곧장 그리기)으로
  떨어뜨리면 굽기보다 느려 손이 도로 멎는다 — 그래서 붓의 두 자리(유닛·건물)에서 `glScrubbing9()` 를 보고 건너뛴다.
· ⚠ 렌더의 `tLiveRef9 = t` 거울 맞춤은 끌기 중에는 건다(`!scrubbing.current`) — 거울이 손보다 한 박자 늦어 맞추면
  지도가 손잡이 뒤로 되돌아간다.
· 진단: `gl9.stat.scrubSkip`(끌기 중 건너뛴 메시 수).

## 번짐 판은 통째로 지우고, 흐림은 가위를 넓혀 돈다
★★ **사각 얼룩과 잔영은 같은 자리에서 났다**(2026-09, 지적: "사진처럼 사각 얼룩? 잔영이 보임") — 번짐(블룸)은
빛나는 개체의 상자를 합친 **가위** 안만 비우고·그리고·흐리고·더했다. 두 병이 거기서 난다:
· **얼룩**: 소환구처럼 센 빛의 번짐은 여유(PAD 28px = 1/4 판 7텍셀)보다 넓게(1.8×5 = 9텍셀) 퍼지는데 흐림·더하기가
  가위에서 **칼로 자른 듯** 끊겨 원 위에 밝은 네모가 남았다 → 흐림·더하기의 가위를 흐림 반지름(`BR9`)만큼 넓힌다.
· **잔영**: 가위 밖에는 지난 프레임의 빛이 그대로 남아 있고, 흐림은 이웃 다섯 칸을 읽으므로 그 옛 빛이 이번 가위
  가장자리로 스며 네모난 잔상이 됐다 → 두 판은 가위 없이 **통째로** 지운다(1/4 판 하나라 GPU 에서 사실상 0).

## 단색 판(잔상)은 GL 에도 자가 있어야 한다
★ **`op.solid` 는 판 굽기 시절의 자였다**(2026-09, 지적: "하템 잔영이 파란색이 아님") — 하템 귀신 활강의 잔상은
판을 통째로 사이오닉 푸른색으로 굽는 `solid` 였는데, GL 붓은 그 값을 안 받아 몸 색 그대로 반투명하게 그렸다.
`GlInst9.solid` + 유니폼 `uSolid`(rgb + 켬) — 정점 셰이더가 덧칠·실루엣 빛·방향광을 다 걷고 그 색 하나로 민다.

## 살 속의 굴은 GL 에서 진짜 구멍이다
★★ **검은 아치 한 장은 살 속에 묻힌다**(2026-09, 지적: "울트라덴, 커널 앞 동굴 입구가 안 뚫려 있음") — 캐번·
나이더스의 굴은 2D 화가 차례로 몸 앞에 얹은 검은 아치였는데, 그 자리(y 2.35·1.95)는 둔덕 밑동(반지름 4.4)의
**속**이라 GL 은 깊이로 통째로 가렸다. 격납구와 같은 손이다:
· 둔덕 옆 낯을 아가리 자리만큼 뺀다(`skipFace` — **아치꼴**로: 네모로 빼면 이빨 능선 위 양 귀에 검은 홈이 남는다).
· 속은 `caveInside9`(뒷벽·바닥·옆벽) — **앞끝을 둔덕 옆선에 맞춰 자른다**(`front(x, z)`): 네모 상자로 깔면 위로
  갈수록 좁아지는 둔덕 밖으로 옆벽 모서리가 **검은 쐐기**로 삐져나온다(실측).
· ⚠⚠ **밑 뚜껑을 GL 에서 꺼라** — 바닥(z 0.06)과 뚜껑(z 0)의 차보다 부품 차례 편향(aOrd ≤ 0.7)이 커서 뚜껑이
  바닥을 이겨 굴이 도로 메워졌다(실측: 속을 붉게 칠했더니 옆벽 귀퉁이만 보였다). 속의 키도 둔덕 **다음 차례**(1)
  로 둔다 — 2D 에는 없는 판이라 화가 차례는 무관하다.
· ⚠⚠ **구멍의 자는 문틀(이빨 능선·아치 관)의 안쪽 가장자리이고, 낯은 '한가운데'가 아니라 '상자의 가장 가까운 점'으로
  재라**(지적 둘: "검정이 입구 문틀에 안 가려진 곳이 있어" → "이제 안쪽에 안 패인 부분이 나와 버렸어") — 낯은 통째로만
  빠지므로 한가운데로 재면 어느 쪽으로든 반 낯이 틀린다: 자를 안으로 죄면 문틀 안에 둔덕 띠가 남고, 그대로 두면 검정이
  문틀 밖으로 나간다. 낯 상자에서 타원에 가장 가까운 점(한가운데 ∓ 반 낯)이 안쪽 가장자리 타원(캐번 2.16×1.98 ·
  나이더스 1.7×1.35)에 들면 뺀다 — 구멍은 가장자리까지 꼭 가고, 넘치는 몫(한 낯)은 문틀 띠 뒤에 숨는다. 그래서 둔덕을
  GL 에서만 잘게 쪼갠다(sides 36 · segs 14/12 — 한 낯이 띠 폭보다 작아야 한다 · 2D 는 종전 낯 수).
· ★ **계단이 아니라 사선·곡선으로 잘라라**(지적: "직각으로 말고 사선으로 입구를 자르진 못하나") — `skipFace` 는 낯을
  통째로만 빼므로 가장자리가 낯 크기의 계단이다. `spirePillar` 의 **`cutFace`**(꼭짓점의 부호 있는 거리 · 음수 = 파낼 쪽)는
  부호가 갈리는 낯을 0 선(변 위 이분법)에서 **베어 내고 남는 조각**을 다각형으로 남긴다(`cutPoly9`). 캐번·나이더스는
  능선 안쪽 가장자리 타원을 그 자로 그대로 준다 — 위의 '반 낯' 셈이 통째로 필요 없어진다(경계는 낯이 작을수록 곡선에
  가깝다 · 한 낯을 경계가 셋 넘게 지나면 그 낯은 그대로 둔다).
· 🔎 **속을 붉게 칠해 구워라** — 검은 속은 '안 뚫렸다'와 '뚫렸는데 어둡다'를 눈으로 못 가른다.

## 디바우러의 푸른 두 장은 눈썹이고, 날개는 갑각 껍질이다
★ (2026-09, 요청: "디바우러 날개 두개 길이 20프로 폭 25프로 줄이고 좀더 럭비공모양으로(뒤끝이 뾰족해지게-곤충 날개) · 이거 날개가
아니라 눈썹? 머리장식? 그런쪽이였어 — 뿌리를 더 앞쪽 아래쪽 머리근처로 · 날개짓하는건 그 날개가 아니라 몸통반구 껍질이 들썩이는
거더라구 그쪽이 날개야 이제") — 여태 임자색 잎 두 장이 등에서 나와 날갯짓(fl9)을 했다.
· **장식 두 장**: 뿌리를 투구 앞 아래(머리 위 · ±0.75 · y 1.55 · z Z0+0.55)로 옮기고 뒤·바깥·위로 뻗는다(옛 뿌리→끝 벡터의 0.8배 ·
  폭 0.75배 · 끝은 더 위로). 꼴은 럭비공 — `widthOf` 가 t 0.42 에서 가장 넓고 양끝이 점(`tipW 0`). 잎의 면은 `ref [m9·0.7, 0, 0.7]`
  로 45도 세웠다(수평으로 누이면 껍질 위의 얼룩으로 읽혔다 · 실측). 날갯짓 몫은 걷었다.
· **날개 = 껍질 두 장**: 투구 겉면(r 2.35 · h 1.6)을 3% 띄워 따라가는 얇은 판(`spirePillar` · ref y · oval 0.05 · θ 0.12~1.25rad ·
  앞뒤 폭 = 그 자리 투구 현의 0.82배 + 0.15 · 뒤로 0.25 치우침 · #6c5d3d)이 **등마루 경첩**(x 0 · 투구 꼭대기 · y 축)을 축으로
  바깥·위로 든다: `LIFT9 = (fl9 + 1.45)/2.45 × 28도`(쉼 0 · 아래 5 · 가운데 17 · 위 28도). 옆 능선 둘은 걷었다(껍질이 덮는다).
· MODEL_NORM 은 그대로(측정 0.942 · 표 0.780 — 장식이 준 잉크 차라 '몸을 키우라'는 요청이 아니다 · 그 규약) · 잉크 중심 8.90 → 8.91 ·
  pitch 9.71 → 9.68(옛/새 측정 차).
· 🔎 `model-gl --kinds devourer --rots 0,45,90 --cell 420 [--pose 1]` — 자세 1 에서 껍질 두 장이 들리고 장식은 가만히 있어야 한다.

## 아비터는 원 좌표를 키우고 배율을 1 로 내렸다
★ (2026-09, 요청: "아비터 원 좌표를 키우고 배율을 낮춰") — 아비터만 원 좌표 잉크가 2.25 라 지도의 정규화 2.307 로 키워 그리고
있었고, 정규화를 안 곱하는 자(model-gl·model-shot 시트·변천사)에서는 늘 캐리어의 1/13 넓이였다. 빌더를 `withModelScale(ARB_K9,
ARB_K9, ARB_K9)`(bake9 · 2.307)로 감싸 원 좌표를 그만큼 키우고 MODEL_NORM 을 **1.007**(model-norm 재측정)로 내렸다 — 지도의 크기는
그대로고 시트에서만 남들과 같은 자가 된다.
· ⚠ **부양 높이표(MODEL_Z_OFF9)는 감싸개 밖에서 걸린다**(모든 빌더 뒤에서 withModelZOff · 그때 modelZK 는 1) — 곧 오프셋이 배수를
  안 타므로 표 값에 같은 배수를 곱해 둔다(`-2.672 * ARB_K9`). 화면의 뜬 높이 = 오프셋 × k × MODEL_NORM 이라 그래야 종전과 같다.
· 총구표는 `markMuzzle9` 가 감싸개를 지나 저절로 커졌다([0, 1.3, 2.05] → [0, 3, 4.72]) · 잉크 중심표는 옛/새 측정 차만큼
  (top 10.39 → 8.32 · pitch −2.23). 이 표는 원 좌표 자라 배율을 낮추면 값이 커지는 쪽이 아니라 **원점에서 먼 쪽**으로 움직인다.
· 🔎 `model-gl --kinds arbiter,carrier,wraith,scout --rots 45,135` — --fit 없이도 넷이 같은 자에 선다.

## 도록은 정규화를 안 거친다 — 인형·거리는 그 배수로 나눠라
★★ **아비터만 인형의 절반이었다**(2026-09, 지적: "도록에서 아비터 작게 나옴 계속 말하는데 안 고쳐짐 · 인게임에선
너무 큼") — 둘은 딴 병이다.
· **도록**: 칸은 빌더의 원 좌표를 창에 맞춰 띄우고 지도의 `MODEL_NORM`(아비터 2.307)을 안 곱한다. 인형(k 1)·사거리·
  뜬 높이는 정규화 자(16-상자)라 원 좌표가 작은 아비터 곁에서 인형이 두 배로 컸다(캐리어·배틀은 반대). `docNormK9`
  로 거리·뜬 높이를 나누고 인형에는 `docDollK9`(마린을 자로 한 지도 크기 비)를 `DocPart9.k` 로 싣는다.
· **인게임**: 정규화를 2.307 로 올려 잉크가 목표(5.2)에 닿은 뒤에도 `MODEL_INK` 의 옛 4.213 이 남아 크기표가
  16/4.213 으로 **23% 더** 키웠다. 표에서 뺐다(model-norm --emit 이 이제 이 종류를 안 낸다).
  ★ **정규화를 고쳤으면 MODEL_INK 도 같은 명령으로 갈아라** — 둘은 한 측정에서 나온 짝이다.

## 이번 손질 메모(2026-09 · 셋째)
· ★★ **'넥서스 곁의 푸른 얼룩'은 빛이 아니라 겹친 미네랄이었다**(지적: "성능 하락과 얼룩의 정체 … 주변 라이트가 다른 곳에
  영향" → 스샷) — 빠른무한 맵은 한 자리(반 타일 안)에 미네랄 밭을 수십 장 포개고, 밭마다 op 가 서서 **반투명 파란 수정**
  수십 장이 한 자리에 겹쳐 빛 얼룩처럼 뭉쳤다(양옆의 둘은 채취 중인 프로브). 프레임마다 그 수만큼 메시를 밀기도 했다.
  `resUniq9`(engine9)가 같은 반 타일의 미네랄을 하나만 남긴다(남은 단 resStageAt 도 그 격자로 읽으므로 한 장이 곧 그 밭).
· ★★ **몸 그림자(사영 실루엣)는 껐다**(요청: "소환구 자체 그림자는 제거(공통 그림자만 사용)" → "모든 모델의 자체 그림자
  제거") — 소환구 빛 공이 옆에 검은 원을 드리우는 것이 그 계기였다. gl9 `MESH_SHADOW9`(기본 꺼짐 · `#glshadow=1` 로 견줌)
  가 세 문(건물 gsh9 · 유닛 gsh9 · 타원의 대타 조건 glShadow9)을 한 번에 닫아 패스 ① 이 통째로 빠지고, 유닛은 공통 접지 타원
  (캔버스)만 진다. 건물은 그림자가 없다(뜬 건물의 타원은 그대로).
· **번짐(블룸)의 값**은 '빛 상자 합집합 안의 몸을 깊이로 한 번 더 그리기'다 — 늘 켜진 보석·창이 화면에 흩어져 있으면 매 장
  화면 전체를 두 번 그린다(지적: "라이트 블루밍 추가 후 느려진 거 같고"). 다음 손(MRT)이 그 패스를 없앤다.
· **클릭 마커는 한 타일**(지적: "마우스 마커가 너무 작거든? 인게임 크기로 해줘 반타일은 넘는 거 같은데 폭이") — 폭이 타일의
  0.7배인데 애니메이션(1.15 → 0.75 → 0.6)이 곱해져 보이는 폭이 대개 반 타일 남짓이었다. `ckw` 의 0.7 → 1.0.

## 이번 손질 메모(2026-09 · 둘째)
· **게이트 기둥 윗면**: `widthOf` 로 굵기를 그려도 뚜껑 문은 `tipW > 0.01` 이다 — 안 줘서 끝이 열린 관이었다(프로브의 그 함정).
· **오버로드 눈**: 있었는데 얼굴 구의 **중심 깊이**에 있어 GL 에서 살에 묻혔다. 구 중심에서 눈 방향으로 반지름만큼
  나간 껍질 위에 앉힌다(0.22 → 0.27).
· **사용법 덮개 스크롤**: 문서 touchmove 가 좌표(inMap)로만 '지도 안'을 가려, body 포털 덮개 위의 손가락도 확대 중이면
  삼켰다. `onCtl9` 가 **지도 요소 밖 target** 을 먼저 뺀다.
· **도록 팝업 쓸어내려 닫기**(scplayer): 굴릴 것이 있으면 브라우저가 세로 손짓을 스크롤로 가져가 pointercancel 이 온다.
  맨 위에서 아래로 끄는 첫 touchmove 를 non-passive 로 잡아 기본 동작을 끊는다(위로 끌기·굴러 있는 상태는 그대로).
· **폰 벤치 문턱** 한 칸 죔(지적: "체감상 여유가 없는 느낌 · 폰도 엄청 뜨거워져"): 30 → 26 · 13 → 11 · 입체 28 → 24.

## 손 번짐은 걷고 블룸이 낸다
★ **빛나는 낯 둘레에 옅은 면을 한 겹 더 깔던 것은 다 걷었다**(2026-09, 요청: "종족무관하게 lit 들어간 요소에서 번짐효과를
직접 다른 색 면을 추가로 주변에 넣어주던거 다 제거하고 블루밍으로 대체하기(필요하면 블루밍 강도 강화)") — 손으로
그린 번짐은 GL 의 번짐과 겹쳐 두 번 번지고, 요잉을 돌려도 같은 자리에 붙은 스티커였다.
· 걷은 자리: `winRow` 의 유리보다 24% 큰 테두리(0.34) · 스타포트 창문띠의 주황 겹(순노랑 유리 한 겹으로) · 스타포트
  격납구 창·팩토리 문·커맨드 격납구 창의 주황 0.55 겹(심을 유리 크기로) · 캐리어 창의 두 배 넓은 겹 · 관문 보석 뒤 후광.
· 늘 켜진 빛은 `winLit` 을 안 지나므로 **EMIT_FILL9 에 미리 적는다**(캐리어 #5fe6ff · 보석 #c4f4ff·#7fd6ff).
· 번짐 세기 `BLOOM9` 0.45 → 0.62 · 흐림 걸음 1.6 → 1.8(gl9) — 걷은 몫을 블룸이 진다.
· ⚠ 커맨드 경사로의 빛 자국(0.42)은 남겼다 — 그것은 빛 둘레의 번짐이 아니라 **처마에 가려 안 보이는 속불이 바닥으로
  새는 몫**이다('속에 켠 불이 위에서 안 보일 때').

## 부속 여섯은 제 신호로 켜진다
★ **부속에도 활성이 있다**(2026-09, 요청: "모든 애드온 건물의 활성화 효과있어야 함 없는 건물들은 창문이나 램프 만들고
lit 주기") — `LIT_KINDS` 에 comsat·**comsatbase**(그리는 판 이름 — litTag 은 op.kind 를 본다)·nsilo·mshop·ctower·covert·
physlab 이 들었다. 머신샵(라디에이터 틀 + 초록등)·컨트롤타워(몸통 창 넷)·코버트(초록 등줄)·피직스랩(구체 허리 창 여덟 —
새로 냈다)은 **연구**(researching)가 켜고, **컴샛은 스캔**(그 임자의 Scanner Sweep 이 SCAN_DETECT_SEC 안 · castsSrc),
**핵 사일로는 핵**(nukeArm9 — 장전 전 NUKE_BUILD_SEC9 63초는 깜빡, 장전된 뒤 쏠 때까지 켜 둠)이 켠다.
· 켜지는 색은 다 `winLit` 을 지난다 — 그래야 EMIT_FILL9 에 적혀 블룸을 탄다(옛 컴샛 LED·코버트 등줄은 맨 색이라 안 번졌다).

## 애드온 통로는 두 벽이 정한다
★★ **길이를 손으로 안 맞춘다**(2026-09, 요청: "애드온연결부의 길이를 본건물과 애드온 양옆벽을 잇는 좌표로 설정해주는
로직 개발(수동으로 길이조절 안하게)" · "시네마틱 모드 때는 두 건물 사이가 더 벌어질거야. 그거도 자동 적용되게") — 옛 손 값
셋(LINK_IN 1.3 · 본체 0.55 · 스타포트 1.55 · 길이 2/3 · 상한 · 세로 −0.12)은 걷었다. `addonLinkGeom9`(engine9)가 푼다:
· **벽 표**: `node scripts/addon-wall.mjs` 가 본체(+x 벽)·부속(−x 벽)의 옆선 x 를 모델 y 칸마다 메시에서 재어
  `addonWall.gen.ts` 에 쓴다(총구표처럼 `--check` 가 낡음을 잡는다 — **벽을 옮겼으면 다시 뽑아라**). 벽 띠는 본체
  z 0.15~1.3(통로는 본체 자에서 낮다) · 부속 0.9~2.4(받침 슬래브 위의 몸통 벽).
· **셈은 돌린 자에서** — 셋이 같은 각(BUILDING_BASE_YAW)으로 서므로 요잉을 먹인 모델 축이 곧 타일 축이다(gl9 footOf
  의 그 회전). 원점의 타일 y = 지면선 − u·ry0(바닥 앞끝의 돌린 y — 붓이 메시 바닥을 지면선에 앉히므로) ·
  u = 발자국 폭 × `bldDrawK9` × 정규화 ÷ 16(붓의 k 와 같은 식 — **시네마틱 세기가 여기 든다**: 건물이 줄면 벽이
  안으로 들어가고 통로는 그만큼 길어진다. 실측 축 길이 정상 0.3~0.9타일 → 시네마틱 0.7~1.6타일).
· **lane** 은 두 벽의 y 범위를 e 축(모델 +y)에 얹어 **마주 보는 구간의 한가운데** — 거기서 두 벽 x 를 표에서 읽는다.
· ★ **길이는 칸이, 단면은 부속의 자가 정한다** — 통로를 통째로 늘리면 사이가 벌어질수록 관이 굵어진다. 길이는
  회전 칸(op.spin · SPIN_KINDS 의 addonlink · `linkLenOf9`: 칸 0 = 도록·폴백의 옛 11.5 · 1~15 = 4 → 40 의 ×1.179
  사다리)에 싣고, 남는 몫은 배수로 되맞춘다(단면 = 부속 자 × 0.36 에서 ±8%). 도록의 회전 칸 문(docAnimOf9)은 이것을 뺀다.
· 붓은 `op.mkFrac`(타일/모델칸 · 그리드 폭 몫)이 있으면 **원점을 fx·fy 에 그대로 앉힌다** — 잉크 상자 맞춤도 바닥
  앉힘도 안 한다(통로 모델의 z 0 이 곧 땅).
· 🔎 보는 자: `node scripts/scene-sheet.mjs --race 테란 --addons [--hash cine=1]` — 본체·부속 짝 여섯이 원작 자리에
  서고 통로가 두 벽을 잇는지, 시네마틱에서 벌어진 만큼 따라가는지 한 장에 보인다.

## 저그 활성 건물은 숨을 쉰다
★ **저그에는 창이 없다 — 활성은 크기의 숨이다**(2026-09, 요청: "저그 활성건물은 아주 살짝 커졌다 작아졌다 바운스 주기(근데
라바변태중은 제외해줘 그건 라바알로 이미 표현됨)") — 연구 중(모든 저그 건물)·생산 중(해처리·레어·하이브는 뺀다)에
`pulseK` 로 바닥 가운데를 축으로 ±2.2%(ZERG_PULSE_K9 · 주기 1.3초 · 건물마다 위상 흩음). 찍는 배율이라 메시는 한 벌이다
(공사 고치의 그 손). ⚠ 도록에는 안 실린다(DocIcon9 에 배율 자가 없다).

## 이번 손질 메모(2026-09)
· **마린 총은 손이 쥔다**(요청: "총을 팔목이 아닌 손으로 들게") — armChain 의 손등은 손목에서 하완 축을 따라
  PALM(fore×1.15)만큼 뻗으므로 파지점을 손목에 두면 총이 팔목에 얹힌다. 파지점에서 하완 축을 따라 손등 길이만큼
  뒤로 물러난 자리가 손목(`wristOf9` — 팔꿈치가 축을 정하고 손목이 팔꿈치를 정하니 세 번 돌려 푼다).
· **파뱃 포신은 하완 위에 평행**(`gunAxis9` — 팔꿈치→손목 축을 따라 rodFaces 로 얹는다) · **공격 컷은 직각 팔**
  (`ELB_ATK9`·`WR_ATK9` — 상완은 어깨에서 곧게 내려가고 하완은 앞으로 수평. jointBetween 은 이 자세를 못 고르므로
  팔꿈치를 못 박는다). 총구 표식은 그 컷의 수평 포신 끝(총구표 fbat [0, 2.03, 2.88]). 옛 thr(내지르기)는 걷었다.
· **아콘 공격은 장풍**(요청: "팔을 앞쪽으로 내뻗기") — 앞선 '머리 위로 든다'를 되물렸다. 손끝이 가슴 높이에서 앞으로
  나가 몸 앞 한가운데로 모이고(x 2.15 → 0.7) 갈퀴도 앞을 향한다.
· **디파일러 여섯 다리에 무릎**(요청 셋: 앞·뒷다리 안쪽으로 꺾기 → 옆으로도(앞다리 앞안쪽·뒷다리 뒤안쪽) → 가운데 다리도
  무릎) — 넓적다리는 밖·위로(무릎 z 1.56), 정강이는 fold9(0.42/마디)만큼 안으로 + foldY9(±0.34/마디)만큼 앞·뒤로 내려와
  땅을 짚는다. 잉크 폭이 준 만큼 MODEL_NORM 0.861 → 0.870.
· 컨트롤타워 접시: 원판 셋 → `dishFaces9`(컴샛과 같은 자 · 초점 2.6 · 꼭짓점이 기둥 끝 4.72) · 안 돈다 → ctower 1.555 → 1.641.
· 아머리 임자색 원판: 경사 뚜껑 → 드럼 천장(2.46)에 수평(GL 진짜 깊이에서 떠오른 판으로 읽혔다).
· 파일런 `withModelSpin(−45 → 135)` — bld-norm 2.255(눌러 둔 1.905 그대로) · 잉크 중심 4.78 → 4.83.
· 옵저버토리 랜턴 흰 광점·플릿비컨 구슬의 광점 걷음(`sphereFaces3(…, shine=false)`) — 광택은 셰이더의 몫.
· 공사 고치 핏줄: 줄기 `trim`(2티어) · 갈래 `fine`(3티어) — 저배율 자글거림.
· 리파이너리: 보 6.3 → 5.3(세로 몫 1.9 → 0.9) · 뒤 세 채 5.2/5.8/5.2 로 올리고 오른 덩이를 넓혀 뒤로 물림 · 관마다
  **받는 덩이의 앞벽 y** 를 적어 벽 속 0.25 까지 간다(1.456 → 1.492).

## 손으로 얹은 흰 반사는 이제 셰이더와 싸운다
★ **광택은 셰이더의 일이 되었다 — 덧칠로 그려 둔 반사는 걷어라**(2026-09, 요청: "사베 하얗게 덧댄
반사 느낌 제거") — 사이언스 베슬의 구에는 `shinePath3` 한 장이 흰색 0.28 로 깔려 있었다. 캔버스가
유일한 붓이던 때는 그것이 '둥글다'는 유일한 신호였지만, 지금은 셰이더가 윤기·봉우리·얼룩을 제 각으로
낸다. 곧 **요잉을 돌려도 안 따라 도는 흰 자국**이 그 위에 한 장 더 얹히는 꼴이라 스티커로 읽힌다.
· **어두운 그늘은 남긴다** — 그 쪽은 반사가 아니라 몸의 그늘이라 셰이더가 내는 몫과 겹치지 않는다.
· 같은 꼴(몸 위에 얹은 흰 `topFace`·`shinePath3`)이 남아 있으면 다 이 손이다.

## 한 줄로 꿴 부품은 한 값으로 옮겨라
★★ **실린더·노즐·테·불꽃은 한 축이다**(2026-09, 요청 둘: "드랍십 스러스터 위치 위로 살짝 올리기
(불꽃도 위치 맞추고)" → "드랍십 실린더 조금 올리고 **실린더-스러스터-화염 전부 중심이 맞게** 해 줘
안쪽 스러스터-화염도") — 첫 요청의 괄호가 곧 이 자리의 함정이고, 둘째 요청이 그 뿌리를 짚었다.
· 드랍십의 노즐 원뿔은 `tz` 축에 있는데 **테(`nozzleRim9`)와 불꽃(`thrustFlame`)만 `tz + tr·0.36`**
  으로 그 위에 떠 있었다(포드 0.24 · 안쪽 0.17 모델칸). 그래서 불이 아가리에서 나오는 것이 아니라
  **아가리 윗입술에 얹힌** 꼴이었다 — 노즐을 올리면 그 어긋남이 따라 올라갈 뿐 안 사라진다.
· 고침은 **축을 하나로 두는 것**이다: `TH_UP9` 한 값이 포드 실린더(`POD_Z`)와 안쪽 노즐의 높이를
  함께 올리고, 테·불꽃의 치우침은 걷어 넷 다 제 `tz` 에 앉힌다. 위아래로 옮길 일이 있으면 그 한
  값만 고친다.
· ★ 이 꼴을 찾는 자는 **부품마다 다른 z 식**이다 — 같은 뭉치인데 `tz`·`tz + a`·`tz + b` 로 갈려
  있으면 그 차가 곧 '언젠가 드러날 어긋남'이다.
· ⚠ 정규화는 **움직인 몫과 견주어** 판단한다 — dship 은 top 자로 0.680 → 0.689(+1.3%)인데 표에
  박힌 값이 0.684 라 이미 그만큼 낡아 있었다. 그 폭보다 작은 흔들림으로 표를 갈면 크기만 들썩인다.
★ **한 호 위에 앉은 것 중 일부만 옮길 때는 호를 줄이지 말고 평행이동을 얹어라**(2026-09, 요청:
"캐리어 아래 두 잎 x축 살짝 안쪽으로 이동(딸린 부품들도 같이)") — 캐리어 잎 셋은 반지름 `LEAF_R`
의 한 호 위에 120도씩 앉는다(위 90 · 아래 210·330). 그 반지름을 줄이면 셋이 **다 같이** 모이므로
(그것이 앞선 "잎끼리 더 가운데로" 였다) 아래 둘만 옮기려면 자리에 `x + inX` 한 항을 더해야 한다.
· **꼴이 안 바뀐다** — 등뼈에 더하는 상수라 접선이 그대로고, 그래서 단면 축(u·v)·말림(skewV)·
  등마루가 한 톨도 안 흔들린다. 곧 자리만 옮긴다.
· ⚠ **딸린 것을 전부 태워라**(요청의 괄호가 그 자리다) — 덮개 판 넷·그 위의 임자색 데칼·앞
  테두리의 창이 다 이 호에서 자리를 낸다. 창은 판의 `at` 을 지나 저절로 따라오지만, **데칼은
  제 손으로 점을 찍어** 안 따라온다. 그리고 **깊이 키**(`leafKey`·덮개 키)가 `depthNow(cos·R, y)`
  로 재므로 그 x 도 함께 옮겨야 앞뒤가 옛 자로 남지 않는다.
· ⚠⚠ **`Math.sign(cos θ)` 로 좌우를 가르지 마라** — `cos(π/2)` 는 6.1e−17 이라 부호가 **+1** 이고,
  그러면 가운데 잎(θ 90)이 한쪽으로 밀린다. 작은 문(|cos| < 0.05)으로 '가운데는 0' 을 못 박는다.
· 정규화는 **안 갈았다** — 잉크폭 9.62 → 9.31(−3.2%)이라 재측정 배수가 0.619 → 0.634 로 오르는데,
  표에 박힌 값은 그전부터 0.605(옛 측정 0.619보다 2.3% 아래로 눌러 둔 값)다. 그 값을 올리면
  캐리어가 2.4% 커질 뿐이고, 요청은 '잎을 안으로' 였다. 잉크 중심도 top 9.37 → 9.36 으로 안 움직였다.

## 옆선 둘이 모이는 점은 호 끝이다 — 거기를 밀어 넣어라
★★ **폭 곡선으로 실루엣을 누르려 들지 마라 — 그 자는 몸을 여위게 한다**(2026-09, 지적: "정면에서
보니 잎 옆선의 중간이 얼마나 튀어나온지 잘 보이지 … 평평하게 눌러달라" → **"접근을 잘못하고 있는
듯 옆에 연결선이 모이는 점을 안쪽으로 밀어 넣으면 되는 거 아냐?"**) — 그 되물음이 답이었다.
정면에서 캐리어 잎은 아몬드로 보이고 그 **가장 넓은 점**은 호 끝(u = ±1 · y = 0)이다. 그 자리의
반지름이 `LEAF_R` 그대로여서(등마루 `ridge9` 가 u² 로 0 이 되는 자리) x = sin(반각)·1.9 = **1.537**
까지 나가고, 앞뒤로는 폭이 줄어드니 그 점만 툭 튀어나온다.
· ⚠⚠ **내가 먼저 간 길이 틀렸다** — 폭 곡선의 지수(`LEAF_BELLY9`)를 0.85 → 1.45 로 올렸다.
  옆선의 '현에서 벗어난 몫'은 0.563 → 0.381 로 줄지만 그 지수는 **잎의 앞뒤 길이를 통째로 깎는**
  자라(반폭 u0.5 0.78 → 0.66 · u0.8 0.42 → 0.23) 배가 여위어 함선이 **다트**처럼 됐다. 되물렸다.
  ★ 교훈: **실루엣의 한 점이 튀어나온다는 말은 '그 점의 자리'를 고치라는 말이다** — 몸 전체의
  꼴을 정하는 지수로 누르면, 그 점은 조금 들어가고 나머지가 통째로 깎인다.
· 자는 **호 끝의 반지름**이다: `ridge9` 에 `− LEAF_TUCK9·u²` 를 더한다. 잎맥(u 0)은 한 톨도 안
  움직이고 끝만 축 쪽으로 들어가므로 **폭·길이·말림·두께가 다 그대로**다 — 등마루와 정확히
  대칭인 손이고, 곧 '자리만' 눌린다. 값 0.42: 호끝 x **1.537 → 1.197(−22%)**.
· ⚠ **여기서 '현에서 벗어난 몫'은 자가 아니다** — 끝이 들어오면 현도 함께 짧아지므로 그 수는
  오히려 0.563 → 0.589 로 **오른다**. 이 손질을 재는 자는 **호 끝의 절대 x** 다(눈이 보는 것도 그것).
· ★ 덤으로 **꽃봉오리가 더 닫힌다** — 세 잎의 끝이 같은 몫만큼 안으로 오므라드니 이웃 잎 사이의
  각 틈(18도)은 그대로이고 호 길이 틈은 0.60 → 0.50 으로 준다. 곧 틈이 벌어질 걱정은 없다.
· **'등 높이'는 두께다** — 등마루를 반지름에서 두께로 옮겨 둔 뒤(그 위 ★★) 잎의 등 높이를 쥔 것은
  `LEAF_HALF_T` 다(요청: "잎들의 등 높이 올렸잖아 그거 조금 다시 낮춰주고" → 0.62 → **0.52** ·
  등 반지름 2.623 → 2.519). ⚠ 옆에서는 그 차가 거의 안 보인다 — 등 높이는 **정면·부감**에서 읽는 값이다.
· ⚠ 덮개(coverPlate)는 `ridge9`·`LEAF_HALF_T` 를 그대로 읽으므로 **저절로 따라온다** — 두 값을
  고칠 때 덮개 쪽을 따로 손댈 일이 없다(그것이 한 함수로 모아 둔 값이다).

## 호를 한 점에 모으면 그 점이 솟는다 — 앞뒤 두 점에 모아라
★★ **폭 곡선이 끝에서 0 이면 옆선 둘이 한 점에 모여 그 점만 튀어나온다**(2026-09, 요청:
"잎의 호를 옆선의 한 점에서 모으지 말고 **앞뒤 두 점**에 모을 순 없어? 그럼 중간이 평평해질텐데")
— 그 말이 이 자리의 답이었다. 캐리어 잎은 호를 등뼈로 눕힌 기둥이고 폭이 `(1−u²)^p` 라
호 끝(|u| = 1)에서 앞뒤 길이가 **0** 이다. 곧 잎의 옆선 둘이 y = 0 한 점에서 만나고, 그 점이
그 잎의 가장 바깥이라 정면에서 아몬드의 꼭짓점처럼 솟는다.
· 고침은 **호 끝에 폭을 남기는 것**이다(`LEAF_EDGE9` 0.28 = len 몫 · 끝 반길이 1.37).
  그러면 그 자리가 **앞뒤 두 점을 잇는 곧은 벽**이 되고, 그 구간에서 x 가 안 변하므로
  옆선이 **평평해진다**(평평한 토막 2.74 = 잎 길이 9.75 의 28%).
· ⚠ **끝이 점이 아니게 되면 그 단면은 구멍이다** — `caps: "none"` 이면 속이 들여다보인다.
  `caps: "both"` 로 막되, `spirePillar` 의 **위 뚜껑 문이 `tipW > 0.01`** 이므로(그 ⚠⚠ 규약)
  `tipW` 도 함께 줘야 한다 — `widthOf` 로 굵기를 직접 그려도 그 문은 여전히 tipW 를 본다.
· ⚠ **그 뚜껑이 곧 사용자가 말한 '테두리면'이다**(요청: "잎 테두리면이 두꺼워졌어 두께감을
  얇게 줄여줘") — 두께 = 끝 폭 × `LEAF_HALF_T/len` 이라 폭을 남긴 만큼 두꺼워진다. 그래서
  두께를 함께 내린다. **폭을 남기는 손질과 두께를 내리는 손질은 늘 한 커밋이다.**
· ★★ **'평평 더 길게'와 '테두리 더 얇게'는 한 값으로 못 한다 — 단면 비를 마디마다 줘라**
  (2026-09, 요청: "평평 더 길게 테두리 더 얇게") — 그 둘이 같은 곱에 묶여 있어 서로 싸운다:
  끝 폭을 0.28 → 0.38 로 키우면 평평한 토막은 2.74 → 3.72 로 늘지만 테두리면이 0.234 →
  **0.316** 으로 두꺼워진다. 두께비 하나로 되물리려면 `LEAF_HALF_T` 를 0.235 까지 내려야
  하고 그러면 **잎맥 두께가 0.83 → 0.49** 로 몸이 통째로 반이 된다.
  · 그래서 `spirePillar` 에 **`ovalOf`**(마디마다의 단면 앞뒤 비)를 열었다 — 두께는
    `widthAt(t) × ovalOf(t)` 이므로 **끝에서만 비를 죄면 굵기는 남고 두께만 칼날**이 된다.
    잎은 `LEAF_EDGE_T9`(끝에서 남기는 비 0.35) + 나머지를 `√(1−u²)` 로 준다.
    값: 평평한 토막 **3.72(38%)** · 테두리면 0.234 → **0.105(−55%)** · 잎맥 두께 0.83 → 0.79.
  · ⚠⚠ **그 비를 덮개에도 태워라** — `coverPlate` 의 `rOf`·`rSkin9` 가 잎 겉면을
    `wOf·LEAF_HALF_T/len` 으로 재므로, 죈 몫을 안 태우면 덮개가 제 옆 테두리에서 **0.08
    모형칸 뜬다**(바로 앞 커밋에서 고친 그 병이 같은 꼴로 되살아난다). 한 함수
    (`leafOvalK9`)로 모아 **잎 기둥·덮개 겉면·덮개 밑변 셋이 나눠 쓴다**.
  · ★ 이 손잡이를 안 쓰면 '두께가 굵기에 비례한다'가 못 박힌 규약이 된다 — 끝에 굵기를
    남기는 꼴(칼날·지느러미·잎)에서는 늘 이 비를 물어라.
· ⚠ 말림(`skewV`)의 가로 몫이 `wOf/len` 이라 이제 호 끝에서도 0 이 아니다 — 그 자리 주석이
  경계한 '점이어야 할 곳이 안쪽으로 뻗은 가시가 된다'는 **점이 아니게 된 지금은 해당이 없다**
  (뒤 테두리 벽이 통째로 축 쪽으로 말리는 것이 맞다).
· ★ 이 손질은 **호 끝을 밀어 넣는 것(LEAF_TUCK9)과 짝**이다 — 턱은 그 점을 안으로 당기고,
  끝 폭은 그 점을 선으로 편다. 둘 다 폭 곡선의 지수(BELLY)를 안 건드리므로 **잎의 살이
  안 준다** — 지수로 누르려던 길이 틀렸던 까닭이 그것이다(아래 ★★).
· 🔎 **보는 자는 잎 한 장을 떼어 정면으로 뜨는 것**이다 — 셋이 겹친 그림에서는 어느 곡선이
  솟는지 못 가린다. 빌더에서 `...leaf(90, 4.7, 54)` 한 줄만 남기고 `--rots 0` 으로 구우면
  그 잎의 실루엣이 통째로 보인다(이 자리를 그 한 수로 잡았다).

## 눌린 단면 위에 판을 얹을 때 — 반지름 함수 하나로는 못 앉힌다
★★ **살 두께를 '가장 두꺼운 자'로 재면 판이 앞뒤 테두리에서 뜬다**(2026-09, 요청: "캐리어 세 잎의
덮개들 더 뒤쪽으로 옮기고 위치 수정 잘해서 잎에서 뜨지 않게") — 캐리어 잎은 단면이 **타원**인
기둥이다(`spirePillar` 의 `oval: LEAF_HALF_T/len`). 곧 그 t 에서의 살 두께는 잎맥 위(단면 가운데)
에서만 `wOf·LEAF_HALF_T/len` 이고, 앞뒤로 갈수록 **√(1−k²)** 으로 여윈다(k = y/wOf = 단면의 u 자리).
그런데 덮개(coverPlate)는 테두리 띠의 밑변까지 `rOf(t)` **한 값**으로 앉혀, 판 앞뒤 테두리가 그 차만큼
공중에 떴다.
· 실측(뜬 몫, 모형칸): 윗잎 옆앞 0.152 · **아랫잎 뒤겹 옆앞 0.242**(제 두께 0.16 보다 크다) ·
  앞겹은 잎이 두꺼운 앞쪽에 앉아 0.03 뿐이었다 — 그래서 **뒤 덮개만** 떠 보였다. 두께를 0.25 → 0.62
  로 올린 앞선 손질이 이 어긋남을 2.5배로 키웠다(그 손질이 곧 '옆선의 배흘림' 고침이었다).
· 고침은 **겉면과 밑변을 둘 다 타원 자로** 앉히는 것이다(`rSkin9(t, k)`). 판이 잎의 둥근 살을 그대로
  타므로 두께가 고르고, 테두리 띠의 키는 `(1−curlK)·말림차 + liftAt` 뿐이다.
  ⚠⚠ **겉면만 잎맥 자(rOf)로 두면 그 차가 통째로 테두리 띠가 된다** — 한 번 그렇게 지어 굽어 보니
    뒤 테두리의 띠가 **0.6 모형칸짜리 날개**로 떴다(잎이 여위는 0.24 + 말림을 안 따르는 0.24 + liftAt
    0.11). **판은 제가 앉은 몸보다 두꺼울 수가 없다.**
· ★ 이 꼴을 찾는 자는 **'단면이 눌린 기둥 위에 무엇을 얹는 자리'** 다 — 다각 기둥의 내접/외접에서
  두 번 물렸던 그 자리('다각 기둥에 무엇을 붙일 때')의 타원 판이다. 반지름 함수 하나로는 겉면을 못 낸다.

★★ **여위는 몸 위에서 '뒤로'는 y 를 미는 것이 아니라 그 몸의 뒤 곡선을 더 따라가는 것이다**(같은 요청) —
덮개의 `shift` 는 판을 앞뒤로 통째로 미는 자인데, 뒤로 밀면 **옆 모퉁이가 먼저 잎을 벗어난다**: 판의
옆 테두리는 그 자리 잎 뒤끝(−Ws)에서 BACK_IN(0.2)밖에 안 남겨 두었으므로 `shift` 로 갈 수 있는 몫이
**0.2 뿐**이다(판 길이 1.7 의 12%). 더 밀면 모퉁이가 허공에 뜬다 — 요청의 뒷말이 바로 그 자리다.
· 그래서 뒤로 옮기는 자는 `backK`(뒤 테두리가 몸의 뒤 곡선을 따라가는 몫)다: 옆 모퉁이는 제자리에 두고
  **가운데만** 뒤로 간다(길이 lenK 를 뒤 테두리에서 재므로 앞 테두리도 같은 몫만큼 함께 온다).
  실측: 윗잎 0.42 → 0.62(판 −2.91 → −3.40) · 아랫 뒤겹 0.55 → 0.72 · 앞겹은 기와 방향을 지키려
  `shift` 0.09 → 0.05.
· ⚠ **뒤로 보낼수록 말림이 세진다** — 잎 뒤끝은 축 쪽으로 말려 들므로(skewV), 판을 뒤로 보내면
  `curlK`(겉면이 그 말림을 따라가는 몫)가 낮은 채로는 판이 도로 뜬다. 뒤로 옮기는 커밋에서는
  **curlK 를 같이 올려라**(0.35 → 0.6 · 0.3 → 0.55).
· ⚠ 정규화는 **안 갈았다** — 잎맥 반지름이 준 만큼 top 필요배수가 0.637 → 0.658 로 오르는데, 표에
  박힌 값은 그전부터 0.605 다(눌러 둔 값). 올리면 캐리어가 8.8% 커질 뿐이고 요청은 그게 아니었다.
  잉크 중심도 top 9.37 → 9.36 으로 안 움직였다.
  ⚠ `ink-center --emit` 의 **둘째 칸은 표와 계통이 다르다** — 안 만진 scv 도 표 8.70 vs 측정 8.22 로
    같은 만큼 어긋난다(표가 딴 설정에서 뽑힌 값이다). **한 종류만 골라 넣으면 그 종류만 계통이 갈린다.**

## 부감에서 격납구의 몸통은 바닥이다 — 빛도 거기서 나야 한다
★★ **뒤벽만 태우면 갈색 갑판이 그 빛을 통째로 가린다**(2026-09, 요청: "여기 면에 격납구가 있어야하고
… 활성화시 노란 빛이 강하게 나야함") — 스타포트 격납구를 파고 뒷벽을 `winLit("#ffe790")` 으로 태웠는데,
카메라가 40도 위에서 내려다보므로 아가리로 보이는 몫의 대부분이 **갑판**이고 뒤벽은 위쪽 한 오라기
(아가리 높이의 1/7)만 남았다. 곧 커맨드에서 적어 둔 그 규약("빛은 바닥으로 새어 나오는 몫이 먼저다")의
반대쪽으로 틀린 것이다.
· 갑판을 따뜻한 금속으로 올리고(`glowLit("#8a6a2c", …)`) 그 위에 **앞뒤로 흐르는 빛줄기 셋**을 얹는다
  (`winLit("#ffd86a")` → EMIT_FILL9 → 번짐). 줄기 사이에 갑판이 남아야 '빛나는 판때기'가 아니라
  '불이 든 격납고 바닥'으로 읽힌다 — 뒤벽의 문틀 셋과 같은 손이다.
· ⚠ 꺼진 판에는 줄기를 안 얹는다(`if (bldLitNow)`) — 그만큼 삼각형만 는다.
· ★ 아가리는 **낯 전체가 아니라 그 안에 드는 작은 구멍**이다(요청: "면 전체를 격납구로 하면안되고 …
  세로는 면의 2/3 가로 3/4") — 인방·문턱·문설주로 테를 두른다. ⚠ 문설주는 **벽의 꺾임(t 0.5)에서
  토막을 나눠라** — 0 → 1 로 곧게 그으면 굽은 벽 밖으로 0.13 삐져나온다.
· ⚠ **깊이는 아가리 높이가 정한다** — 부감 40도에서 뒤벽이 보이는 한계가 `아가리 높이 ÷ tan40°` 다
  (아가리 2.02 → 2.4). 처음에 아가리를 위 한 단(높이 1.08)만 뚫고 깊이 2.4 를 주었더니 시선이 바닥에
  1.29 에서 부딪혀 **뒤벽에 닿지도 못했고**, 그래서 '납작한 주황 판때기'로 보였다.
· ⚠ `spirePillar` 의 **위상은 재서 알았다**(`sides: 8` 에서 낯 한가운데가 k·45도) — 주석만으로는 못 푼다.
· ⚠ 2D 폴백의 반지름 함수가 **눌린 z 를 설계 높이로 나누고** 있었다(`/ BODY_H` 2.7 · 옳은 값은
  `/ BODY_Hz9` 2.16) — t 가 0.8배로 작아져 반지름이 낮게 나와 판이 벽 속 0.1~0.13 에 잠겼다.
  '화면 자 상수가 z 자리에 섞이면 2D 에서는 안 보인다'는 그 규약의 또 한 자리다.

## 속을 그린 부품의 키가 껍데기보다 크면 속이 밖으로 올라온다
★★ **어두운 아가리의 깊이 키를 관보다 올리면 그것이 관 위로 올라온다**(2026-09, 지적: "스러스터가
각도에 따라 검정부품이 비쳐보임") — `nozzleRim9` 의 `dep` 은 아가리 **속**(어두운 목구멍)이라 늘 관
뒤라야 한다. 그런데 캐리어만 `thrustKey + 0.25` 로 관보다 앞에 두었다.
· 드랍십·셔틀은 죄다 노즐 **뒤쪽** y 로 재서 `key9(…, −4.2, …) + 0.3` 처럼 준다 — 곧 그 +0.3 은
  '관보다 앞'이 아니라 '뒤 자리에서 조금 앞'이다. 캐리어만 관의 **가운데 y** 를 기준으로 재서 그
  관행이 조용히 뒤집혔다. **같은 헬퍼를 부르는 자리끼리 기준 y 가 같은지 보라.**
· gl9 의 `aOrd` 는 tagKey 값이 아니라 **정렬된 부품 차례**라(그쪽 ★★), 키를 조금만 올려도 그 부품이
  목록 맨 끝으로 가 최대 0.7 타일의 편향을 받는다 — 관의 지름 0.7 보다 크니 목구멍이 관을 통째로 이긴다.
· ★ 이 꼴을 찾는 자는 **'속을 그린 부품의 키가 껍데기보다 큰 자리'** 다.

## 꺾인 줄기의 각은 마디 길이가 정한다
★★ **튀는 폭만 보면 짧은 사거리에서 도로 날카로워진다**(2026-09, 지적: "아콘 트레이서 줄기 너무
심하게 꺾임 · 꺾임각이 90도보다 크게 둔각으로") — 안쪽 각을 정하는 것은 폭 하나가 아니라 **마디
길이에 견준 폭**이다. 좌우로 번갈아 튀므로 이웃 두 마디의 어긋남은 최대 2·amp 이고, 안쪽 각
θ 를 지키려면 `2·amp ≤ 마디 × tan((180−θ)/2)` — θ = 100도면 **amp ≤ 마디 × 0.42** 다.
· 그래서 그 죔은 **붓에 둔다**(갈래표의 `zigAmp`·`zigW` 는 상한일 뿐이고 이 기하가 늘 이긴다).
  표 값만 손보면 len 이 작아질 때 마디도 함께 작아져 같은 각으로 되돌아간다.
· 마디 수를 줄이면(9 → 5) 같은 각에서 **더 크게** 튈 수 있다 — 마디가 길어지기 때문이다.

## 연기는 '흐려지는 띠'가 아니라 덩이다
★ **뭉게뭉게는 줄기 위에 흩어 얹은 반투명 원이다**(2026-09, 요청: "파뱃 화염방사 연기 자연스럽게
(반투명 회색 연기 뭉게뭉게로) + 열기 느껴지는 글로우 추가 앞쪽 자연스럽게 호 형태로 처리" ·
"디바우러 트레이서도 연기 느낌 나게 뭉게뭉게 하게") — 그러데이션 끝을 회색으로 빼는 것만으로는
**매끈한 띠가 흐려질 뿐**이라 연기로 안 읽힌다. 갈래표의 `billow`(덩이 수)·`billowFrom`(줄기의 어느
몫부터)·`billowK`(크기)·`billowA`(짙기)·`billowCol` 이 그 자다.
· **앞으로 갈수록 크게·옆으로 더 흩어** 놓으면 끝이 저절로 호로 퍼진다 — 호를 따로 그릴 일이 없다.
· 씨앗은 **번쩍임 위상**에서 뽑는다(한 발 동안 무늬가 몇 번 갈려 연기가 살아 움직인다).
· 화염은 앞쪽 55%만(`billowFrom` 0.45 — 뿌리는 불이다) · 디바우러의 산은 **통째로** 구름이다(0.04).
· ⚠ **이름이 겹친다** — 미사일 연기의 속색이 이미 `smoke` 라, 이 갈래는 `billow` 로 적었다.
★ **열기 번짐(`heat`)은 불 둘레에 도는 것이지 불 자체가 아니다** — 반지름 2.4배·짙기 0.7 로 뒀더니
도록 칸에서 불줄기가 안 보이고 주황 얼룩만 남았다(실측). 1.9배·0.45 로 낮춰 몸 **아래**에 깐다.

## 모델 손질 루프
1. `src/components/replay/ReplayMotionPlayer.tsx` 수정 → `npx tsc --noEmit -p tsconfig.json`
2. `node scripts/model-shot.mjs --kinds <k> --rots 0,45,90,180 --mode top --cell 300 --lit [--zoom 0.5] --out <scratch>/x.png` 로 눈으로 확인
3. 정규화 재측정 — 유닛 `node scripts/model-norm.mjs --kinds <k>`(맨 위 표의 필요배수) → MODEL_NORM,
   건물 `node scripts/bld-norm.mjs --kinds <k>` → BLD_NORM
4. `npx vite build` → `node scripts/model-depth-check.mjs` · 클래스를 새로 붙였으면 `node scripts/css-guard.mjs`(규칙 없는 scr-* 이름이면 실패) (✔ 새로 어긴 모델 없음 이어야 한다; 새 빌더는 partKey 키)
   · **모델을 고쳤으면 `node scripts/tier-table.mjs`로 부품 등급표를 다시 뽑는다**(`--check`로 어긋남 검사).
   · **`node scripts/model-gl.mjs`** — 156종을 GL 로 굽어 **빈 그림이 없나** 보고(5초) 눈으로 볼 시트를 낸다.
     ★ 눈으로 확인할 때는 이것을 쓴다 — `model-shot` 은 **2D 캔버스 붓**이라 앱이 그리는 그림이 아니다.
   · **`node scripts/model-mesh.mjs --check`** — **덮임 100%** 관문이다(아래 '메시 층'의 ★★). 100% 아래면
     새 헬퍼·새 손 면이 `meshPut9` 로 3D 를 안 적었다는 뜻이고, 그 면은 GL 에서 **그냥 빠진다**.
     어느 줄인지는 `node scripts/miss-sites.mjs`.
   · 총구를 가진 모델을 고쳤으면 **`node scripts/muzzle-table.mjs`로 총구 앵커표도 다시 뽑는다**(`--check`).
     앵커의 임자는 빌더다 — 총열·포신·아가리를 짜는 줄에서 `markMuzzle9(x,y,z)`(공용 저그 얼굴은 무른 표식)로 끝점을
     적으면 표(muzzleTable.gen.ts)가 그 값을 싣고 엔진(MUZZLE_ANCHOR)이 손 표 위에 덮어쓴다. 눈으로 볼 때는
     `node scripts/muzzle-sheet.mjs --kinds <k> --rots 0,90,270 --out <scratch>/x.png`(십자가 총구 위에 앉아야 한다).
     그 표(tierTable.gen.ts)는 모델 기하만의 함수라 빌드 시각에 굽는다 — 안 그러면 폰이 들어올 때마다
     종류마다 여덟 방위를 다시 구워 로딩에서 2~3초를 쓴다(실측). 낡으면 틀린 등급이 실린다.
5. 커밋 → 브랜치 푸시

## 배포
**사용자가 "배포"라고 할 때만** 한다. 그 전엔 feature 브랜치 푸시까지만.
배포 = ① scplay `git push origin HEAD:main` → ② scplayer의 `package-lock.json`에서 `node_modules/scplay`의
`resolved` 해시를 그 main 커밋으로 올리는 커밋("재생기 갱신: …")을 브랜치와 main에 푸시.
scplayer 쪽 소스를 만졌으면 그쪽에서 `npx tsc --noEmit -p tsconfig.json`만이 아니라 **`npx vite build`까지** 돌린다(CSS 문법 오류는 tsc가 못 잡는다). scplayer는 락 고정이다(vercel installCommand `npm ci`) — 락을 안 올리면 scplay main을 밀어도 앱에 안 실린다.
⚠ **해시를 보고할 때는 늘 40자리 전체다**(되풀이된 지적: "scplay 해시는 40자리로 줘야지 또 까묵었냐구") —
`git rev-parse HEAD` 의 값 그대로. 짧은 해시는 락에 박는 값이 아니라 눈으로 못 맞춰 본다. scplay·scplayer 둘 다.
옛 방식(빈 "배포 트리거" 커밋 · installCommand의 HEAD 덮어쓰기)은 걷었다.
락을 통째로 다시 만들 일이 있으면 **node_modules를 치운 채** `npm install --package-lock-only`로 만든다 — 설치된 트리에서
뽑으면 이 기계 플랫폼의 선택 패키지만 실려 Vercel의 `npm ci`가 거부한다(esbuild·rollup 바이너리·fsevents).

## 프레임 엔진·워커 구조(2026-09)
- `deriveWorld9`(파생 자료)·`createEngine9`(시각 t → 프레임: unitOps·fxOps·DOM 기록·안개)는 **`engine9.ts`**의
  순수 함수다(엔진이 이행적으로 참조하는 표·헬퍼 151개를 함께 옮겼다 — React·DOM 없음, 워커는 이 모듈만 든다).
  `ReplayMotionPlayer.tsx`는 붓·UI만 남았고 필요한 이름을 engine9에서 import한다(다른 파일이 쓰던 것은 re-export).
  옮긴 기준은 `scratchpad/split_engine.mjs`(TS API로 이행 참조를 닫음)였다. 컴포넌트는 화면 입력(EngineView9)만 건넨다.
- 두 일꾼: **설계 일꾼**(`frameWorker.ts`, 동적 `import("./frameWorker?worker&inline")`, 라이브러리 빌드는
  `inlineDynamicImports`로 한 파일)이 주인(메인 재생 상태)의 명령(`cmd`: 재생/정지·기준 시각·배속, **바뀔 때만**)과
  시점(`view`: 상자·기울기·색·품질·**시야 사각형**)을 받아 제 벽시계로 앞으로 설계도를 지어 둔다(벽시계 3초·24MB 한도(폰 1.5초·4MB)).
  **그림 일꾼**(메인의 붓)은 받은 설계도 중 t 이하 가장 늦은 장을 골라 그때 푼다. 단방향. 메인 엔진 대비 길은 없다 —
  워커가 못 서면 마지막 프레임을 든 채 `SCR_DIAG.worker`(#diag "워커" 줄: on/준비중/off · got/used/missed ·
  짓기 ms · op 수·KB · 앞 s·장·MB · 시야 · ⚠오류)에 까닭이 적힌다.
- 설계도는 `framePack.ts`로 **float32 배열 + 문자열 표**로 싸서 transfer로 넘긴다(구조화 복제 없음). 안개 판 셋은
  바뀐 장에만 싣고, 메인은 그 시각 이하 가장 늦은 안개 판을 붙인다.
- 컬링: 메인이 보이는 사각형에 앞뒤 한 화면씩 여유(3×3)를 붙여 `view.cull`로 보낸다. 보이는 것이 그 안에 있는 동안은
  다시 안 보낸다(작은 팬은 설계도를 안 버린다). 1.2배 이하는 지도 전체. 밖의 개체는 미니맵 점만 남는다.
- 세계의 주인은 워커다: 참값(truth)은 메인이 `postTruth9`로 **transfer**해 넘기고(메인엔 껍데기), 개체 표(entData)는
  워커가 참값에서 만든다. 화면(UI)이 읽는 파생 자료(건물 행·캐스트·핵·가스·생산·업글)는 워커가 `worldui`로 한 번
  보내고, 걷기(entWalks)는 추적을 켤 때 `want walks`(임자별)로 청한다. 메인은 deriveWorld9를 안 부른다. 넘긴 뒤
  메인의 트랙 배열은 비운다. 걷기는 참값 키를 가리키는 창(`WalkView`, `posAtW`)이라 복사가 없다(폰 메모리).
- 참값 자리 형식(`openbwTracks.ts`): 키는 `kt`(초 Float32)·`kxy`(픽셀 Int16×2)·`kh`(방향 바이트)·`kst`(상태)로 나눠 들고
  접근자 `kT/kX/kY/kH/kS`로만 읽는다. 체력·인터셉터·표적은 평평한 형식 배열 `Ticks`([초,값,…], 표적은 Float64)이고
  `tkN/tkT/tkV/tkAt/tkLast/tkSlice`로 읽는다. 자리를 바꾸면 접근자만 고친다. 검사: `node scripts/openbw-tracks-check.mjs`.
- **판(스프라이트) 굽기 길은 걷었다(2026-09)** — 유닛·건물 몸은 어느 기기에서나 GL 붓이 메시로 그린다(아래 "메시 층·WebGL").
  함께 걷은 것: `unitSprite`·`buildingSprite(Bake)`·판 보관함·바이트 예산·LRU·대타(같은 종류의 다른 크기)·프레임당 굽기 몫·
  굽기 일꾼(`bakeWorker.ts`)·색별 물들이기(`tintedOf9`)·그림자 판(`shadowPlate`)·판 크기 사다리·판 갈림 진단. 약 1900줄이다.
  **캔버스에 남은 판은 크립 얼룩 한 가지**(`creepPlate9`, 무늬 셋 × 보기 둘 = 여섯 장·크기 못 박음·색 하나라 구울 때 물들여 합친다):
  크립은 모델이 아니라 땅이라 지형 마스크로 파내야(destination-out) 하고 그것은 2D 캔버스의 일이다. 효과 래스터(`FX_RASTER_CACHE`)도 남는다.
  모델 빌더·표·헬퍼와 크립 래스터(`rasterBld9`)는 **`bake9.ts`**에 있다(DOM 없음 — 캔버스는 `BAKE_ENV9`의 손으로만). 빌더를 고치면
  bake9.ts를 고친다(model-depth-check·doc-catalog도 그 파일을 읽는다). 모듈 전역 깃발(pose·head·lit·spin·lod·pitchFlat)은
  세터(`poseSet9`·`pitchFlatSet9`…)로만 세운다.
  ⚠ bake9.ts의 **문 차례**는 원본 그대로여야 한다 — 파생 빌더 등록문(`SHAPE_BUILDERS.x = …`)이 `SHAPE_FACES` 표보다 앞에 있어야 한다.
- 기기 프로필은 `DEV9` 한 표(폰/PC: 크립 굽기 상한·굽는 판 한도·불티·효과 래스터·그림자 최소 배율·시야 여유·앞 한도(폰 1.5s/6MB·
  PC 3s/24MB)·요잉 8칸·**GL 메시 상한**(폰 240 · PC 600)). 새 문턱은 표에 더하고 자리에서는 `DEV9.x`만 읽는다. 기기 판정은 `smallDevice9` 하나.
  기기는 진입 벤치로 **단**을 오른다(`DEV9.tiers` — PC 네 줄 PC_TIERS9 · **폰 세 줄 PHONE_TIERS9**). `#tier=N` 강제 · 단은 **오르기만** 한다
  (부풀린 첫 값이 내린 단은 짐이지 기기가 아니다) · 유휴 재기(crowdRecheck9)가 더 작은 값을 내면 그 자리에서 오른다.
  **표가 스스로 문턱과 덮어쓸 값을 든다**: `upMs`(2D 벤치 ms 이하) · `up3Ms`(입체 벤치) · `minMem`(deviceMemory GB) ·
  그리고 그 단이 덮어쓰는 DEV9 칸들(`aheadSec·aheadMB·glMeshMax·glBloom·glow·yaw8Always·shadowGroundMinZoom·decalBakeMax·hitShardK·dieShards`).
  0단은 **프로필 기본값과 같아야** 한다(단이 안 오른 기기는 종전 그대로). 자리에서는 다들 `DEV9.x` 를 프레임마다 읽으므로 갈아 끼우면 곧 먹는다.
  ⚠ **모듈 적재 때 베껴 두면 단이 영영 안 먹는다**(2026-09, 지적: "모델 모양대로 그림자 사영되는 거 좋았는데 왜 지금 보니
    타원형으로 바뀌었지?") — `const SHADOW_GROUND_MIN_ZOOM = DEV9.shadowGroundMinZoom` 처럼 한 번 베낀 사본이 셋 있었다
    (그림자 최소 배율 · 크립 굽기 상한 · 효과 래스터 예산). 그래서 폰 표가 적어 둔 접지 그림자 배율이 **한 번도 먹은 적이 없고**,
    낮은 배율의 지상 유닛은 늘 캔버스 타원을 받았다. 지금은 셋 다 자리에서 읽는 함수다(`shadowGroundMinZoom9()` …).
    새 문턱을 더할 때 **`const X = DEV9.y` 꼴로 베끼지 마라** — 표가 덮어쓰는 칸이면 그 순간 죽은 값이 된다.
  · **폰 세 단**(2026-09, 요청: "요즘 폰도 성능차이가 심해서 모바일용 벤치를 별도로 분리하되 3단계로 나누고 … 최고단계에선 광택 글로우도 넣고"):
    0 낮음(벤치 미달 — 종전 폰 값 그대로 · 글로우 끔 · 낮은 배율 죄기) · 1 보통(2D ≤30ms — 광택 켬 · **번짐(블룸) 켬** · **접지 그림자 2배율부터** · 앞 2.5s/10MB · 메시 300벌 · 파편 0.8/16)
    · 2 높음(2D ≤13ms **이고** 입체 ≤28ms — 요잉 16칸 · 접지 그림자 1배율부터 · 손짓 중 그림자 안 접음 · 앞 4s/16MB · 메시 360벌).
    ⚠ 폰 문턱은 PC 자의 비율로 못 나눈다(그러면 0단만 나온다) — 따로 적은 숫자다. ⚠ `minMem` 은 폰에 안 쓴다: iOS 사파리는
    deviceMemory 를 아예 안 내므로 메모리를 문턱으로 삼으면 정작 가장 빠른 폰이 0단에 남는다. 그래서 메모리에 닿는 값(메시 상한)만 조심히 올린다.
    ⚠ 터치 기기 제외 문(touchPc9)은 **PC 프로필에만** 건다 — 폰은 다 터치라, 그 문을 그대로 두면 표가 세 줄이어도 단이 영영 안 오른다.
    결(grain)은 폰의 어느 단에서도 안 켠다(굽기 +25~30%). 품질 알림은 폰도 단을 읽는다(높음/보통/낮음·미달이면 매우 낮음).
  · **문턱은 GL 몫만큼 늦췄다**(2026-09, 지적: "폰이나 PC 매우낮음인데 여유가 있어보여서 혹시 지금 벤치 기준이
    너무 높진 않아?") — 옛 값(PC 16.8·8.4 · 폰 24·9)은 **한 장이 판을 굽고 찍는 일**이던 때 정한 것이다. 몸을 GPU 가
    그리는 지금 실제 몫은 1.24~1.76배 줄었는데(위 GL 실측) **문턱을 재는 벤치는 여전히 판 찍기**라, 같은 기기가 같은
    ms 를 내며 그만큼 놀고 있었다. 보수적으로 1.45배 늦춘다: PC 24·12 · 폰 30·13(실측 헤드리스 PC 벤치 11ms —
    보통 → **높음**으로 오르고 p50 은 66.7ms 그대로였다).
    ⚠ 벤치 자체를 GL 로 바꾸지는 않는다 — 벤치는 '기기가 얼마나 빠른가'의 눈금이고 헤드리스의 GL 은 소프트웨어라
      진입에서 재면 기기가 아니라 드라이버를 잰다.
    ⚠ **입체 문턱(up3Ms)은 한 번 더 어긋나 있었다**: `benchDevice9(true)` 는 2D 판보다 화소를 **2.87배** 칠하는데
      문턱이 16 으로 2D(9)보다 오히려 낮아, 화소당으로는 4.3배 엄격했다(2단에 오르려면 2D 기준 5.6ms). live3d
      문턱에서 한 번 고쳤던 그 어긋남과 같은 것이다 — 28 로 늦췄다. **깊은 벤치에 문턱을 줄 때는 늘 2.87 을 곱해 견주라.**
  · **죽은 `glow`·`brush`(결) 스위치는 걷었다**(2026-09, 요청: "죽은 글로우 코드 걷어내고") — 그것을 쓰던
    `glowBake9`(2D 광택 겹)는 판 굽기 길을 걷으며 부르는 자리가 사라져 **한 번도 안 돌았는데** 표에는 켜진 것처럼
    적혀 있어 "켜 있는데 안 보인다"는 혼동을 두 번 냈다. 함께 지운 것: `GLOW9`·`BRUSH9` 표 · `GLOW_HI9` ·
    `brushOn9/brushSet9` · `glowOn9/glowSet9` · `brushSeed9` · `grainAxes9` · `faceGrain9`, 그 셋만 읽던
    shapeOblique 의 결 자 표(`FACE_GRAIN9` — 낯마다 적던 몫이라 굽기도 그만큼 준다), model-shot 의 `--noglow`.
    합쳐 **671줄** · 번들 1484 → 1470KB. 모델 광택은 이제 GL 셰이더가 낸다(`DEV9.glSpec`·`#glspec`).
  · **손짓 중 실시간 원근**(live3d)은 **PC 3단(매우 높음)에서 표가 켠다**(2026-09, 요청: "PC 매우높음에서 3D 제스쳐중
    모델도 시점 변화 가능하려나 지금은 지도만 실시간인데"). 모델이 안 따라오던 까닭은 둘이었다:
    ㉠ 판정 문턱(입체 벤치 26ms · 손짓 한 장 중앙값 24ms)이 **판 굽기 시절** 값이라 GL 이 번 몫만큼 저절로 꺼졌다 —
       표가 켜 주는 단은 이제 문턱을 안 묻는다(`DEV9.live3d`).
    ㉡ **밀림 기준(PitchGeom9.sox)을 손짓 시작 값에 얼려** 두었다 — 자리는 따라오고 기울기만 얼어 있어서 "지도만
       실시간"으로 보였다. 얼린 까닭은 밀림 각이 **판 열쇠**(6도 칸)였기 때문인데(끌면 판 폭풍: 한 장 23 → 240ms),
       판 길을 걷은 뒤 그 각은 **카메라 유니폼**이고 메시 열쇠에 안 든다. 그래서 실시간 원근을 켠 손짓에서는 안 얼린다.
    같은 까닭으로 그 단에서는 vq 칸을 **2도**로 쪼갠다(그 밖은 종전 유닛 6도·건물 12도) — 끄는 동안 기울기가 계단으로 안 튄다.
    접기(한 장이 33ms 넘으면 이 손짓 동안 실시간 원근을 끈다)는 그 단에서 **연달아 세 장**일 때만 건다 — 한 장 규칙은
    메시를 처음 짓는 첫 프레임에 걸려 손짓마다 접힌 채 돌았다. 접히면 워커에 새 원점을 안 보내므로 기울기가 그 자리에서 멎는다.
    검산: `node scripts/perf-check.mjs --wide --3d --drag --diag [--hash tier=3]` → `[손짓] 9ms 배킹×1 원근 live 밀림 따라옴`.
    실측(헤드리스·소프트웨어 GL·3D 766기): 손짓 한 장 7 → 9ms, rAF p50 16.7 → 33.3ms — 헤드리스는 GL 채우기까지 CPU 라
    이 값은 상한이다(실기 GPU 는 훨씬 싸다). 무거운 자리는 위 접기가 받는다.
  · **GL 로 바꾼 값**(2026-09 실측, perf-check 폰 프로필 390px·유닛 600·조임 없음): 판 굽기 때(27cc1c7) p50 **83ms**·건물 판 74.6MB
    → GL 메시(HEAD) p50 **67ms**·캔버스 6장 31.9MB(gl9 판 7.3MB 포함). CPU 4배 조임에서는 383ms → 217ms. 헤드리스는 소프트웨어 GL 이라
    삼각형 채우기까지 CPU 가 지는데도 **더 빠르다** — 실기(GPU)에서는 차이가 더 벌어진다. 번짐을 켜도 폰 프로필 p50 은 같았다(66.6 → 66.6).
- `#diag`는 요약 한 줄, `#diag=draw|bake|fog|load|gest|mem|worker|truth|brush|view|all`(쉼표로 여럿)로 용도를 가른다 — draw(화면·덜어내기·프레임) · bake(굽기·굽기일꾼·판갈림·캔버스) · fog(안개) · load(로딩·지도판·입체) · gest(손짓·원점). 한 주제가 한 줄, 줄 머리에 주제 이름. `mem`에 메모리 어림(memEst9) 줄.
- 워커는 짓기 시간(ms)에 맞춰 프레임 간격을 벌린다(초당 30장 기본, 최소 8장). 메인은 2초 안의 프레임이면 낡아도 든다.
- **붓은 React 밖에서**(4번): 시계 틱이 살아 있는 시각(`tLiveRef9`)을 한 걸음(벽시계 ≤80ms) 올리고 `paintFnRef9`로
  설계도를 골라(`frameAt9`: 앞·뒤 장 보간) `unitPaintRef`로 유닛 캔버스를 곧장 칠한다. UnitLayer는 `opsSrc/fxSrc`(ref)를
  props보다 먼저 읽고 `driven`이 참이면 effect에서 안 칠한다. React 상태 t는 `REACT_STEP_MS9`(100ms)마다만 올린다 —
  시간 표시·DOM 효과·미니맵·안개는 그 박자다. 탐색으로 t가 밖에서 바뀌면 렌더가 tLive를 맞춘다.
- 엔진은 늘 **자세히** 낸다(요잉 16칸·모든 자세·탱크 차체+포탑). 낮은 배율 간이화는 붓(UnitLayer)의
  `detailAt`·`yawAt`·`moveAt`가 한다. 배율·팬 자체는 프레임에 안 실린다(시야 사각형만).
- 붓 계측(`SPRITE_PERF`)이 세는 것도 그만큼 줄었다: **찍기**(캔버스에 남은 판 블릿 = 크립·효과) · **직접**(GL 이 못 맡아 면을 곧장 그린 몸 —
  0이라야 한다) · 최악 프레임 · 화면 캔버스 무게. 메시 무게·벌 수는 `gl9.stat`(#diag=draw 의 GL 줄)이 따로 낸다.
- 계측: `node scripts/perf-check.mjs [--msgsize]`(vite 번들이 기본) — `--3d`(조종부의 2D/3D 단추를 눌러 입체로 재기 · `--wide` 와 함께 —
  폰 폭에서는 재생기가 3D 를 막는다) · `--drag`(끄는 부하 — 끄는 자리는 `.scr-motion-map` 가운데를 찾아 쓴다) ·
  `[손짓]` 줄(한 장 ms · 배킹 몫 · 실시간 원근 live/접힘/off · 밀림 기준 따라옴/얼림 · 단, `SCR_DIAG.gest`) ·
  `[워커] on got/used/missed`로 워커가 쓰였는지,
  `--msgsize`로 장당 바이트·op 수를 본다. `--wide --cpu 1`이 PC 판(굽기 일꾼 `[굽기일꾼]` 줄이 on이어야 한다). 워커 번들의 `process.env.NODE_ENV`는 vite.config의 define이 박는다(사파리).
  esbuild 도구 번들(model-shot 등, perf-check `--esbuild`)에는 워커가 없어 유닛 프레임이 안 그려진다.

## 모델 z 좌표 손질(2026-09)
- 평면 카메라의 높이 누름(`TOP_Z_PRESS9` 0.8)은 **모델 z 좌표 자체로 옮겨 굳혔다**(`scripts/model-z-scale.mjs --k 0.8`): 빌더·헬퍼의
  z 자리 숫자를 접어 쓴다(리터럴은 값 자체, z 전용 지역 상수는 선언, x·y와 나눠 쓰는 상수는 `NAMEz9` 쌍둥이, 지역 함수는
  z-return 접기 또는 선형 인자 접기). 못 접는 식은 감싸지 않고 `--report` 에 적는다. 카메라는 이제 정직하다(누름 1.0).
  앞으로 모델 높이를 바꿀 때는 카메라가 아니라 좌표를 고친다. 순수 기하 커널(spirePillar 등, KERNEL_FN)은 본문을 안 건드린다.
- 검산: `node scripts/model-faces-snap.mjs --out ref.json --zk 0.8`(원본 + 꼭짓점 z배수 = 정답) · `--out new.json`(고친 소스) ·
  `--diff ref.json new.json`(종류별 어긋난 면·버그급). Node 헤드리스, 전 종류 15초.
- 잉크 중심표(`UNIT_INK_CY9`·`BLD_INK_MID9`)는 `node scripts/ink-center.mjs --emit` 으로 다시 뽑아 engine9 에 붙인다(캔버스 래스터라 브라우저).
- **코드모드가 틀린 자리는 손으로 고쳤다(2026-09, 118종 버그급 → 잔 조각 몇 개)**. 원리: 코드모드는 z **입력**을 접는데 정답은 **꼭짓점** z를
  접는 것이라, 회전·경로 매개변수·접선에서 z를 만드는 자리는 접을 수 없다. 그런 자리는 설계 좌표 그대로 두고 **꼭짓점으로 나가는 자리에서
  `Z8`(bake9, 0.8)을 곱한다** — 경로 람다의 return z, 관절 풀이(jointBetween)는 s·w 를 `/Z8` 로 되돌려 풀고 결과 z 에 `*Z8`, 부품 회전
  (뮤탈 날개 P·포탑 포드 롤·오버로드 bz)은 설계 자로 돌린 뒤 z 에 `*Z8`. 코드모드가 못 본 z 후보: `[x, z]`/`[y, z]` 쌍 표(가디언 다리·
  벌처 hullPlan·옵저버토리 기둥 키), 기본 매개변수(`tankTrack h = 2.6`), 이름이 z 답지 않은 지역 헬퍼 인자(forgeDome9·rampVent·
  sunkenFoot arc), `MODEL_Z_OFF9` 표(withModelZOff 는 modelZK 를 곱하므로 표 값 자체를 ×0.8), 화면 원으로 설계한 것(포지 바퀴 —
  RIM 은 화면 자라 접지 않는다). 남은 잔차(관 단면·구 껍질의 뒷면 판정, 평균 0.02~0.03 모델칸)는 받아들였다.
- **뒤늦게 드러난 잔차 하나**(2026-09, 지적: "넥서스 발판 이상해짐 뒤쪽만 정상임"): 넥서스 모퉁이 받침(cornerPad9)이
  높이·바닥 기준을 **화면 자 상수**(PLINTH_Z9 0.55)로 쓰고, 본체 반폭을 잴 때 피라미드 높이를 **6.4**(제 절두체는
  5.12)로 나누고 있었다. 받침이 땅 밑 −0.11에서 0.89까지 서고(1.25배) 모퉁이를 **엉뚱한 높이의 반폭**으로 잡아 본체
  낯과 안 맞물렸다 — 2D 는 화가 차례가 덮어 안 보였고 GL 에서 '받침이 흘러나오고 속이 보이는' 꼴로 드러났다(뒤 모퉁이만
  본체 뒤에 들어가 멀쩡했다). ★ 교훈: **z 를 쓰는 자리에 화면 자 상수가 섞이면 2D 에서는 안 보인다** — GL 이 그 감사자다.
- **정답 만들 때**: withModelScale 은 바깥 배수를 덮어쓰므로(곱하지 않음) 제 배수를 거는 빌더(refinery·academy·plane·tank·turret·
  trapezoid·tombFlat·dship)는 `--zk 0.8` 정답이 틀린다. 평면은 원본 트리(카메라 누름 0.8)를 `--modes top` 으로 뜬 **진실**과, 입체는
  withModelScale 을 곱셈으로 잠시 고친 원본 트리의 `--zk 0.8` 과 대조한다(model-faces-snap.mjs 머리 주석). `--diff` 는 독점 없는
  최근접 짝짓기 + `--min 0.2`(잔 조각 제외)로 센다 — 탐욕 짝짓기는 궤도 패드·구 껍질에서 이웃을 가로채 과장했다.
- 정규화(MODEL_NORM 26종)·잉크 중심표는 코드모드 때 **틀어진 기하로** 재측정한 값이었다 — 기하를 되찾으며 정규화는 코드모드 전 값으로
  되돌리고 잉크 중심표는 다시 뽑았다.

## 모서리를 깎은 네모 기둥(팔각)
`boxOctFaces3(cx, cy, w, d, h, z0, cut, omit?, noTop?)` — `boxFaces3` 와 같은 자리·같은 규약이고 세로 모서리
넷만 `cut` 만큼 사선으로 잘라 **벽이 여덟**이 된다(2026-09, 요청: "배럭 … 위에서 보면 8각형").
· `cut` 은 **모서리에서 각 변을 따라 물러나는 길이**다 — 사선 자체는 그 √2 배. w/2·d/2 의 절반에서 잘라 둔다.
· ⚠ **띠·창은 저절로 안 따라온다** — 앞 낯의 좌우폭은 `cut` 만큼 줄고, 옆 낯의 앞뒤도 그만큼 준다.
  그리고 **사선 낯에도 제 토막을 둘러야** 띠가 모서리에서 안 끊긴다(안 두르면 두른 것이 아니라 낯마다 그은
  줄로 읽혀, 깎은 뜻이 없어진다). 배럭은 그 셋을 `BLK9` 표 한 줄(x·반폭·반깊이·깎임·y중심)로 쥔다.
· 눈으로 고른 값: 배럭 `CUT9 = 0.35`(바깥 판 두께 2.7 의 앞 낯이 2.0 로 남는다). 0.5 는 1.7 까지 깎여
  "살짝만"이 아니었다.
· **깎임은 모퉁이마다 줄 수 있다**(2026-09, 요청: "앞쪽 건물 뒤쪽 두 모서리 사선 깎은 거 없애고
  대신 뒷쪽 기둥과 그 위의 판들의 네 모서리 사선을 깎아 줘") — `cut` 에 숫자 대신 네 값을 주면
  (+x,+y) · (+x,−y) · (−x,−y) · (−x,+y) 차례로 걸리고, 0 인 모퉁이는 직각으로 남아 그만큼 벽이 준다.
  ★ **덩이가 맞물리는 낯의 모퉁이는 깎지 마라** — 배럭 좌우 판의 뒤 모퉁이는 기둥과 맞대는
  자리라, 거기를 깎으면 판과 기둥 사이에 쐐기꼴 틈이 난다. 곧 깎는 자리는 **실루엣에 드는
  모퉁이**뿐이다.
· 법선은 이제 **제 변에서 뽑는다**((−dy, dx)/|·| — 도는 방향의 바깥쪽) — 모퉁이를 골라 깎으면
  벽이 다섯~여덟으로 오락가락하므로 법선 표를 못 박을 수가 없다.
· 띠 표(`BLK9`)도 앞·뒤 깎임을 따로 든다 — 한 값으로 쥐면 안 깎은 뒤 낯의 띠가 깎인 몫만큼
  들어가 짧아지고, 있지도 않은 사선 낯에 토막을 두른다.

## 속에 켠 불이 위에서 안 보일 때
★★ **처마가 덮은 속은 부감에서 한 각도 안 보인다 — 빛은 바닥으로 새어 나오는 몫이 먼저다**(2026-09,
요청: "커맨드 팩토리 활성 시 격납구 안쪽에서 노란 불빛(블루밍) 보이게 — 다른 불빛처럼 깜빡임") —
팩토리는 격납구가 옆을 보고 있어 뒷벽에 판 하나를 켜면 그대로 보였다. 커맨드는 **돔이 처마처럼
덮어** 여덟 각 어디에서도 뒷벽이 안 보였다(실측 `model-gl --lit`). 그래서 커맨드는 문간 **앞
경사로에 깔리는 빛의 자국**을 함께 얹는다 — 속에 불이 들면 실제로도 그것이 먼저 보이는 몫이다.
· 켜고 끄는 자는 `glowLit(켠색, 꺼진색)`(몸 면) · 켜졌을 때만 얹는 덧칠은 `if (bldLitNow)` + `winLit`.
  ⚠ **꺼진 판에 투명 면을 두지 마라** — 그만큼 삼각형만 는다.
· 깜빡임·번짐은 **따로 지어내지 않는다**: 박자는 엔진의 `op.lit`(그 종류가 LIT_KINDS 에 있어야 한다),
  번짐은 `winLit`·`glowLit` 이 그 색을 EMIT_FILL9 에 적어 저절로 먹는다.
· ⚠ 벽에 얹는 판은 **한 뼘 띄운다**(0.03) — 같은 평면이면 z 싸움으로 깜빡인다.
· ⚠ 빛 자국의 채도가 높아야(0.71) mesh9 의 덧칠 접기(무채색만)에 안 걸려 제 부품으로 남고 번짐을 탄다.
· 🔎 **켠 채로 보는 자**: `node scripts/model-gl.mjs --kinds <k> --lit` (2026-09 에 더했다 — 여태 이 자는
  늘 꺼진 판만 냈고, 켜진 자리는 브라우저 한 판이 드는 `doc-sheet --anim` 으로만 볼 수 있었다).

## 원둘레에 늘어놓은 네모는 다 같은 쪽을 본다
★★ **자리만 흩지 말고 프레임째 돌려라**(2026-09, 요청: "트리뷰널 기둥들이 모두 중심을 바라보게
수정(지금은 같은 방향을 봄)" · "아모리 본체 주변 톱니들도") — `boxFaces3` 는 **축 정렬** 상자다.
중심에서 `(sin a·R, cos a·R)` 로 자리만 옮기면 열넷이 다 같은 방향을 보고, 그러면 드럼을 **두른
이빨**이 아니라 원 위에 늘어놓은 네모가 된다(둥글게 둘러선 기둥도 마찬가지다).
· 고침은 상자를 손으로 다시 짜는 것이 **아니다**: 제자리 `(0, R)` 에 세워 두고
  **`withModelSpin(−각)`** 으로 감싸면 자리와 방향이 **한 번에** 맞는다
  (`spun` 이 반시계라 −각이 `(0, R)` 을 `(sin a·R, cos a·R)` 로 보낸다).
· ★ `withModelSpin` 은 **더한다** — 바깥 감싸개(트리뷰널의 −45) 안에 겹쳐도 그대로 합쳐진다.
· ★ `depthNow` · `faceLight` · `facingRatio` 도 같은 문을 지나므로 **안에서 `(0, R)` · `(0, 1)` 로
  재면 된다** — 밖에서 px·py 로 재던 값과 같은 수이고, 어느 낯이 보이는지도 저절로 맞는다.
· ⚠ 돌린 뒤 `bld-norm`·`ink-center` 를 다시 재라(트리뷰널은 잉크폭이 안 변했고 잉크 중심만 0.01
  움직였다 — 안 변하는 것이 정상인 경우도 있으니 **재서 확인**하는 것이 규약이다).

## 나란히 선 덩이 사이의 틈
★ **덩이를 잇는 상자는 이웃 벽에 딱 닿는 것이 아니라 파고들어야 한다**(2026-09, 요청: "배럭
건물 사이사이 틈 없게") — 배럭의 사이 상자는 두께 1.15 로 가운데 판(±1.2)과 0.175 · 바깥
판(2.6)과 0.075 를 남기고 있었다. 눈으로는 '그늘'로 보이지만 **자홍 바탕(`model-shot --bg
"#ff00ff"`)에 뜨면 실루엣 안으로 배경이 새는** 진짜 틈이다. 보이는 몫은 두 벽 사이 그대로
두고 상자만 **양쪽으로 0.12 씩 더** 파고들게 한다 — 딱 맞추면 두 벽이 같은 평면에 겹쳐 z
싸움을 하고, 파고들면 이웃 판이 제 벽으로 덮으므로 2D·GL 어느 붓에서도 깔끔하다.
· **띠 표는 보이는 자(GVW9)를 따로 든다** — 파고든 폭으로 띠를 그으면 이웃 판 속으로 넘어간다.
· **밑면도 맞춘다** — 이음 상자가 판보다 높이 떠 있으면 그 밑으로 배경이 보인다(부감이라
  낮은 틈도 뚫린다). 윗면은 그대로 두고 키만 그만큼 늘린다.
· 검산은 눈이다: `model-shot --kinds cube --rots 0,90,135,180,225 --bg "#ff00ff"` 로 실루엣
  안에 자홍이 한 점도 없어야 한다. `model-gl` 은 '그려지나'만 보는 자라 **틈은 못 잡는다**.

## 통풍구로 읽히게 하는 자
★ **살 간격이 살 굵기의 두 배 안쪽이라야 격자로 읽힌다**(2026-09, 요청: "가운데 건물 위쪽
벤트가 벤트처럼 안 보이는 듯 색은 더 검게 하고 가로 살도 늘리기") — 배럭 가운데 뚜껑은 길이
6.8 에 살이 셋이라 간격이 1.7 이었다. 살(0.18) + 골(0.22) = 0.4 가 그 1/4 밖에 못 메우니
'줄 세 개 그은 판'이다. 일곱으로 늘려 간격을 0.85 로 좁히면 절반이 차 통풍구가 된다.
바탕도 한 단 더 죽인다(옆 벤트 #4c4c4c → #2e3033) — 어두운 속이 있어야 살이 도드라진다.
⚠ 어두운 바탕은 **결(결 재질 문)에서 빠진다**(휘도 0.19) — 통풍구는 맨 강철이 아니므로 그게 맞다.

## 연기가 무엇을 가리고 무엇을 안 가리나(다크 스웜)
★ **효과는 화가 차례로만 서므로 '어떤 몸만 안 가리기'는 스텐실로 판다**(2026-09, 요청: "디파일러
다크 스웜 연기에 공중 유닛이나 건물은 가려지면 안 돼") — 효과(fx)는 몸을 다 그린 **위에** 얹히는
2D 켜라, 그 안에서 몸과 앞뒤를 가릴 길이 없다. 몸을 두 번에 나눠 그리는 길(지상 → 연기 → 건물·공중)은
못 쓴다: 그러면 **건물이 제 앞의 지상 유닛을 덮는다**(칸은 화가 차례로 정해지는데 그 차례가 갈린다).
그래서 **가리면 안 되는 것의 실루엣을 스텐실에 찍고 그 자리만 빼고 연기를 얹는다**(gl9 `swarmPass` · 문맥은
`stencil: true` 로 연다):
  ① 붓이 연기를 **딴 판**(swarmPlate9)에 2D 로 그려 `gl9.swarmPlate` 에 건다(화면 캔버스에서는 파낼 수가 없다).
  ② flush 끝에서 `over`(건물·공중 유닛) 무리만 색을 끄고 한 번 더 그려 스텐실에 1 을 찍는다.
     ⚠ **그림자는 뺀다** — 땅에 눕는 그림자는 연기 아래다(안 빼면 그림자 모양으로 연기가 뚫린다).
  ③ 그 판을 텍스처로 올려(뒤집기·미리곱한 알파) 스텐실 0 인 자리에만 전체 화면 사각으로 얹는다.
· 옛 길(숨은 GL 캔버스에서 over 만 한 번 더 그려 알파 마스크로 읽고 2D 에서 `destination-out`)은 GL 캔버스가
  **화면 층**이 된 뒤로는 못 쓴다 — 다시 그리면 화면이 지워진다(아래 '층 셋').
· ⚠ **무리(Grp9)의 열쇠에 `over` 가 든다** — 메시·카메라만으로 묶으면 같은 메시의 지상·공중 몸이 한 무리에 들어
  표식이 무리째 찍힌다(실측: 마린 둘로 시험하니 지상 마린까지 안 가려졌다). 값이 공짜라 못 박았다.
· 지상 유닛은 스텐실에 없으므로 종전대로 연기 밑이다 — 연기의 뜻이 거기 있다. 트레이서·피격(효과 층)은 연기 위다.
· **연기가 화면에 없는 프레임은 그 가지에 아예 안 든다** — 판 한 장 올리기가 공짜가 아니다.
· `#gl=0` 폴백에는 스텐실이 없다(몸을 GL 이 안 그리므로) — 거기서는 종전처럼 통째로 덮는다.
· 🔎 `$S/swarm1.mjs` 꼴 — glUnits9 에 over 몸·지상 몸을 하나씩 밀고 초록 판을 걸어 flush 한 뒤 readPixels 로
  두 몸의 불투명 화소 중 초록이 섞인 수를 센다(over 0 · 지상 100% 라야 한다).

## 오목한 속(패인 그릇)을 낼 때 — 그리고 그것을 덮는 것들
★ **명암으로 흉내 낸 패임은 GL 에서 안 통한다** — 어두운 원반 두세 장을 계단으로 쌓거나
(서포트 베이) 은 원반 + 초승달을 겹쳐 칠한 것(옛 커맨드 꼭대기)은 진짜 깊이에서 그냥
얼룩이다. 꼭짓점이 축 방향으로 물러나야 한다: **`dishFaces9`**(bake9)가 그 자다 —
아가리 테의 중심·반지름·깊이·축(z 는 접힌 모델 자)을 주면 켜와 그늘을 낸다.
· **옆선은 셋 중 하나다**: `pow`(2 = 포물면 · 1.4 쯤이 위성 접시 · 1 은 원뿔) ·
  `dome: true`(구 단면 = **역돔**, 테에서 벽이 서고 바닥이 둥글게 여민다) ·
  맨 안쪽 켜를 **0** 으로 두면 꼭짓점 하나로 모은다(바닥의 평면이 아예 없어진다).
  지적 두 번이 이 손잡이를 낳았다: "바닥이 납작한 접시가 아니라" → "역돔이여야지".
  ⚠ 포물면은 기울기가 **가운데에서 0** 이라 안쪽 절반이 사실상 평면이다 — 넓고 얕을수록
  '납작한 바닥'으로 읽힌다. 깊이는 아가리 반지름의 0.35~0.5 가 접시로 읽히는 자리다.
· ⚠ **아가리를 덮는 판을 얹지 마라** — 컴샛 접시의 '바깥 테'라 적힌 한 장이 꼭짓점 열일곱을
  한 평면에 두어(같은 ρ = 같은 깊이) 패임을 통째로 지우고 있었다.
· ⚠ **등 껍질은 깊이 눈금이 갈릴 만큼 물려야 한다** — 축이 카메라와 14도인 컴샛 접시에서
  0.12 물린 뒷면은 화면 깊이로 0.116 뒤일 뿐이라 안 갈려, 등(볼록)이 속(오목)을 덮었다.
· ⚠ 켜는 **각을 고르게** 나눈다(역돔에서 반지름을 고르게 나누면 테 쪽 한 켜가 깊이의 절반을
  먹어 계단이 진다).

★★ **덩이의 윗면을 파 놓았으면 그 낯을 남기지 마라 — 그 낯이 곧 뚜껑이다**(2026-09, 지적:
"커맨드랑 베이 접시 바닥 평평하면 안 된다니깐" · "베이 역돔 위에 평평한 판 제거해야 할 듯").
2D 는 화가 차례로 그릇을 나중에 얹어 보이지만, GL 은 진짜 깊이라 **위에 있는 판이 이긴다**.
· `frustumFaces3`·`boxFaces3` 는 `noTop` 으로 끄고, **뚜껑을 못 끄는 프리미티브**
  (`cylinderFaces3`)는 뚜껑을 고를 수 있는 기둥(`spirePillar` · `caps: "bottom"`)으로 바꿔 짓는다.
· ⚠ `cylinderFaces3` 의 뚜껑은 `topFace(discPath3(...))` 라 2D 에서는 덧칠이지만, **접을 몸이
  없는 덧칠은 메시에서 제 부품으로 남는다**(지붕 살창·둔덕 얼룩이 그렇게 산다) — 곧 GL 에서는
  꽉 찬 판이다. '덧칠이니 괜찮겠지'가 안 통하는 자리다.
· 윗면을 끄면 그 자리를 **손수 깐다**: 커맨드는 네모 테 ↔ 둥근 아가리의 고리를 마디마다
  네모 한 장으로(감기는 프리미티브 윗면과 같게 — 시계 방향 = 법선이 아래).
· 🔎 **찾는 법**: 그릇을 `fill: "#ff0000"` 으로 칠하고 굽는다. **테두리만 붉고 속이 제 색이면
  뚜껑이 있는 것이다.** 커맨드·베이·로보틱스를 이 한 수로 잡았다.

★★ **두 다각형을 이어 붙인 고리는 GL 에서 꽉 찬 판이다**(2026-09, 로보틱스 테두리 링) —
`polyPath3(바깥) + " " + polyPath3(안쪽)` 은 2D 캔버스에서 **감기 규칙(nonzero)** 으로 안쪽이
파여 고리가 되지만, GL 은 폴리곤을 **낱개로** 삼각화하므로 안쪽 다각형이 **판**이 된다.
로보틱스에서는 그 판(반지름 3.5·0.08 위)이 지붕 한가운데를 통째로 덮어, 지붕을 아무리 파도
그림이 안 바뀌었다. 고리는 **마디마다 네모 한 장인 띠**로 짜거나 `annulusPath3`(제 로프트를
적는다)를 쓴다. 전수 검색으로 이 꼴은 그 한 자리뿐이었다.

## 다각 기둥에 무엇을 붙일 때
★★ **외접 반지름에 얹으면 뜬다**(2026-09, 지적: "커맨드 옆면의 데칼들과 황동띠가 본체에
딱 안 붙음 — 떠 있거나 위가 삐져나옴") — `spirePillar` 로 지은 기둥의 반지름 함수 `rOf(z)` 는
**꼭짓점까지**의 자다. 낯 한가운데는 그보다 `r·(1 − cos(π/sides))` 만큼 안으로 들어가 있다
(14각·r 3.55 에서 **0.089 모델칸**). 그 자에 띠·데칼의 안쪽 면을 맞추면 꼭짓점 자리에서만
닿고 낯 한가운데에서는 공중에 뜬다 — 요잉을 돌리면 떴다 붙었다 한다.
· 고침은 '나란히 선 덩이 사이의 틈'과 같다: **닿게 두지 말고 파고들게**. 안쪽 면을
  그 몫 + 한 뼘(0.03)만큼 벽 속으로 민다(커맨드 `ribFaces9` 의 `sink9`).
· ⚠ **곧은 현은 굽은 벽을 못 따라간다** — 커맨드 데칼은 아래 반지름에서 위 반지름까지
  **납작한 네모 한 장**이었다. 기둥의 옆선은 taper 0.42(아래는 완만하고 위에서 급히 좁아진다)
  라 그 현이 위쪽에서 살 밖으로 삐져나왔다. 띠와 **같은 자**(호를 마디로 나눈 리브)로 지으면
  굽이를 타고, 얇은 두께(0.05)를 주면 어느 각에서도 살에 박힌 채 보인다.
· ★ 붙이는 것의 각 개수를 몸의 낯 수와 맞출 필요는 없다(커맨드는 데칼 12 · 낯 14) —
  **파고들게 하면 어긋나도 된다**. 맞추려 들면 개수를 요청과 다르게 바꿔야 한다.
· ★★ **두께 없는 것(페인트칠)은 '내접'이 아니라 '그 방향의 낯까지'로 재라**(2026-09, 지적:
  "1층 돔의 옆 데칼들이 깊이가 안 맞아서 안 보여") — 내접 반지름은 **낯 한가운데**의 거리다.
  데칼이 낯 한가운데에 있으란 법이 없으므로(데칼 12 · 낯 14), 낯 모퉁이 쪽으로 밀린 데칼은
  **0.21 모델칸까지** 벽 속에 잠긴다. 데칼 깊이 편향이 0.25 라 아슬아슬하게 살거나 그냥 사라졌다.
  · 정n각형이면 그 거리는 **정확히** 셀 수 있다: 낯 한가운데에서 벗어난 각 d 에 대해
    `R·cos(π/n) / cos(d)`. 거기에 0.02 만 띄우면 어느 자리에서도 벽에 딱 붙는다.
  · ⚠ 그러려면 **단면의 위상**을 알아야 한다 — `spirePillar` 의 기본 위상은 `π/sides` 인데,
    그 (u, v) 축이 세계 축과 어떻게 맞물리는지는 주석만 봐서는 못 푼다. **재라**: 메시를 켜고
    기둥 하나를 구워 꼭짓점의 `atan2(x, y)` 를 찍으면 된다(실측: 꼭짓점이 k·360/n · **위상 0**).
  · 검산: `#glbias=3` 으로 깊이 편향을 세 배로 키워도 **그림이 안 바뀌면** 깊이 싸움이 아니다.

## 세계 광원을 쓰는 것은 메시에 구우면 몸과 함께 돈다
★★ **`faceLight` 는 요잉을 먹은 법선으로 재는데 메시는 요잉 0 에서 한 번만 굽는다**(2026-09, 지적:
"빛의 각도상 지금 포탑 옆면이 어두우면 안 되는 각도인데 어둡단 말이지") — 그 자는 `currentYaw()` 를
지난 법선을 세계 광원(`LIGHT_PLAN`)과 내적해 흰·검 덧칠을 내는데, `collectMesh9` 는 **요잉 0 한 벌**만
굽는다. 곧 거기서 난 명암이 `aOv` 로 접혀 **몸에 문신처럼 굳고**, 요잉 0 에서 그늘이던 낯은 어느 각으로
돌려도 그늘이다. 판을 요잉 칸마다 굽던 2D 시절에는 칸마다 다시 돌아 옳았다 — **붓이 바뀌며 무효가 된
값**이고, 87 자리가 그 자를 부른다.
· 실측(`tanksiegegun` 여덟 각 · 화면 왼쪽−오른쪽 평균 휘도. 빛은 화면 왼쪽이니 죽 양수라야 한다):
  +24.7 · +10.5 · **−1.0** · **−10.1** · +12.0 · +25.5 · +23.0 · +28.7 — 90·135 에서 **부호가 뒤집혔다**.
  고친 뒤: +22.8 · +1.3 · +18.8 · +11.8 · +37.4 · +36.9 · +23.1 · +22.3(뒤집힘 없음).
· 고침은 **굽는 쪽을 닫고 셰이더가 내는 것**이다: `MESH9.on` 이면 `faceLight` 의 `face()` 가 빈 목록을
  내고(2D 폴백 `#gl=0`·도록 SVG 는 종전 그대로 — **면 수가 한 톨도 안 바뀐다**), gl9 정점 셰이더가
  **같은 곡선**을 돌아간 법선으로 낸다(0.3 위 흰 · −0.1 아래 검 · `shadeBoost` ×1.25 상한 0.7).
  자는 **수평 성분** `dot(n.xy, uLight.xy) × |LIGHT|`(= faceLight 가 재던 그 2D 내적)이다.
· ⚠ **미봉을 함께 걷어라** — 이 병을 덮으려고 방향광 폭을 0.82~1.08 → 0.68~1.22 로 넓혀 두었는데,
  명암이 제 각으로 돌아온 지금 그대로 두면 빛 쪽이 **두 번** 밝아진다. 옛 값으로 되물렸다.
· ★ 이 꼴을 찾는 자는 **'굽는 시각에 세계를 묻는 것'** 이다 — 요잉·카메라·빛처럼 **그릴 때 정해지는 것**을
  빌더가 물으면, 메시는 그 답 하나를 굳혀 버린다. 그런 몫은 다 셰이더(유니폼)의 일이다.
  (같은 자리를 이미 둘 지났다: 마주 봄 판정 `facingRatio` 는 GL 에 아예 없고, `visible` 은 늘 참으로 연다.)
· 🔎 **재는 자는 좌우 휘도차다** — 여덟 각을 돌려 화면 반쪽씩 평균을 내면, 빛이 세계에 못 박혀 있는지
  몸에 붙어 도는지가 **부호 하나로** 갈린다. 눈으로는 "좀 이상한데"에서 멈추는 자리다.
★★ **`facingRatio` 를 '깊이 키의 크기'에 쓰면 메시에서는 늘 '마주 봄'이다**(2026-09, 지적: "오버로드
주황색면이 계속 비치는데 고정도 아니고 계속 움직이면서 비쳐 오버로드는 가만히 정지상태") — 같은 병의
둘째 꼴이다. 오버로드는 배(임자색 돔)가 매달린 다리·집게를 언제 가리나를 **화가 키**로 흉내 낸다
(`hangK` × 2.6 × `hangFace9`), 그 방향 자가 `facingRatio` 다. 그런데 메시를 굽는 동안 그 문은 **늘 1** 을
낸다(마주 볼 때만 그리는 장식을 3D 에 다 싣기 위한 규약이다) — 곧 GL 에서는 방향 문이 통째로 열려
**몸 뒤로 돌아간 다리까지** 배 위로 올라온다. 요잉이 조금만 돌면 어느 다리가 뚫고 나오는지가 바뀌니
'가만히 있는데 계속 움직이며 비친다'가 된다.
· GL 은 **진짜 깊이**가 있어 이 흉내가 애초에 필요 없다 — `MESH9.on` 이면 그 몫을 **0** 으로 둔다
  (2D 폴백 `#gl=0`·도록 SVG 는 종전 그대로라 스냅샷이 안 바뀐다).
· ⚠ 값은 **키의 크기가 아니라 차례**로 든다 — gl9 의 `aOrd` 는 tagKey 값이 아니라 **정렬된 부품 번호**를
  0~0.3(+0.4)으로 편 것이다. 곧 +2.6 이 2.6 타일 앞으로 나오는 것이 아니라 **그 부품을 목록 맨 끝으로
  보내** 최대 0.7 타일의 편향을 받게 한다. 그래도 배(반지름 2.3)를 이기기에는 넉넉하다.
· ★ 가려 쓸 자리: `facingRatio` 로 **안 그리기**(visible)는 GL 에서 저절로 '다 그린다'가 되어 옳은 쪽으로
  깨지지만, **키에 얹기**는 '다 앞으로'가 되어 틀린 쪽으로 깨진다. 후자는 늘 `MESH9.on` 으로 닫아라.

## 같은 부품을 딴 헬퍼로 지으면 음영의 자가 갈린다
★★ **기둥의 어두운 쪽은 0.38 · 관의 배 그늘은 0.26 이다**(2026-09, 지적: "시즈모드는 포탑 옆면과
포신이 너무 어둡고 … 통일할 수 없나 부품을") — 같은 굵기의 같은 포신인데 탱크는 `tubeFaces`,
시즈는 `spirePillar` 로 지어 놓았다. 관은 배에 **부드러운 그늘 한 겹**(OP.sideSoft 0.26 → 메시에서
×1.25 = 0.33)이고, 기둥은 **낯마다 `faceLight`**(어두운 쪽 상한 0.38 → 0.47)다. 그래서 시즈 포신만
숯빛이 됐다 — 색을 손댄 적이 없는데 색이 다르다.
· 고침은 **자를 맞추는 것**이다: `spirePillar` 에 `litK`(음영 세기 배수)를 두고 시즈 포신에
  **0.68**(= 0.26/0.38)을 준다. 면은 그대로 두고 음영 알파만 곱하므로 꼴이 안 바뀐다.
· ★ 낯을 많이 가진 기둥으로 **관을 흉내 낼 때**는 늘 이 배수를 물어라 — 헬퍼가 다르면 음영의 자도
  다르다는 것이 이 자리의 규약이다.
· ⚠ 함께 고친 것: 탱크 모드에 빠끔 나온 **접힌 시즈 포신 끝**의 깊이 키가 `+0.15`(포탑 몸보다 앞)라
  몸 위에 겹쳐 떠 보였다(지적: "탱크모드의 시즈포신은 키가 안 맞고") — 차체 뒤로 나온 토막이므로
  `−0.25`(몸보다 뒤)라야 포탑이 그 뿌리를 덮는다.

## 눌린 단면은 `trueNormal` 이 있어야 한다
★★ **`oval` 로 누르면 법선은 안 눌린다**(2026-09, 지적: "이제 다 동일한데 포탑만 좀 달라 키값이나
색이나 그런 거") — `spirePillar` 의 `oval: 2` 는 단면을 2:1 로 누르는데, 법선은 **누르기 전 원의
반지름 방향**으로 나간다. 곧 낯의 실제 기울기와 어긋난 법선으로 음영을 매겨 한쪽은 지나치게
어둡고 반대쪽은 거의 안 밝아진다.
· 실측(`model-mesh --kinds tanksiegegun --dump`): 시즈 포신이 **검0.47** 한쪽으로 몰려 바탕
  #6a7286 이 숯빛이 됐다 — 탱크 포신(`tubeFaces`)은 검0.33 · 흰0.30 짝이다.
  `trueNormal: true` 를 주면 꼭짓점에서 법선을 뽑아 눌린 살의 기울기를 그대로 탄다.
· 값: 두 포탑의 중앙 휘도가 105.6 vs 123.9 → **128.8 vs 129.0** 으로 맞는다.
· 🔎 **재는 자는 `--dump` 다** — 접힌 덧칠 세기(흰N·검N)가 낯마다 찍히므로, 한쪽으로 몰린 것이
  눈에 바로 걸린다. '색이 다르다'는 말은 대개 이 수에서 먼저 보인다.
· ★ 이 자를 안 준 `oval` 호출이 또 있으면 같은 병이다 — 눌린 단면에는 **늘** 붙여라.

## 눌린(타원) 살에 띠를 두를 때
★★ **늘이는 축은 눈으로 판정하라**(2026-09, 지적: "해저드 띠가 왜 90도 롤링돼 있지") — 시즈 포신은
`spirePillar` 의 `oval: 2` 로 단면이 2:1 타원이다. 그 위에 두르는 띠(hazSleeve9)를 손으로 짜며 축을
**외적으로 얻은 쪽**(축 × x)에 2 를 걸었더니, 그것이 포신이 눌린 쪽과 **직각**이라 띠만 세로로 선
타원이 되어 통째로 90도 굴러 보였다.
· `spirePillar` 의 u/v 축이 세계 축과 어떻게 맞물리는지는 주석만으로는 못 푼다(위 '다각 기둥' 의 ⚠와
  같은 자리다) — **굽어서 보라**. 띠가 굴러 보이면 늘인 축이 반대다. 여기서는 x(가로)가 긴 쪽이었다.
· ⚠ 이런 어긋남은 **한 각에서는 안 보인다** — 정면에서는 타원이 원으로 보이므로, 45·135 둘을 함께 뜬다.

## 궤도가 도는 것처럼 보이게(자세 컷 셋)
★ **두 컷은 떨림이고 세 컷이라야 방향이 읽힌다**(2026-09, 요청: "이동 시 탱크 캐터필러 실제로
돌아가게 가능?") — 걸음 컷(1 ↔ 3)은 서로의 **거울**이라 앞뒤가 없다. 궤도는 어느 쪽으로 도는지가
보여야 하므로 **0 → 1 → 3** 세 컷을 돌린다: 레일판을 고리 위 자리 `s9` 하나에 판 간격의 0·1/3·2/3
을 더한 그림이다(`POSE_KINDS.roll` · bake9 `tankTrack` 의 `roll9`).
· **자리 하나만 민다** — 윗면·앞뒤 반원·바닥이 한 매개변수 `s9` 로 이어져 있으므로 부품을 따로 돌릴
  일이 없다. 앞(+y)으로 갈 때 윗면 판은 앞으로 흐르니 **더하는** 쪽이다.
· 컷 값은 걸음의 자리(0·1·3)를 빌린다 — 2·4·5 는 공격 컷이라 `atk` 없는 종류에서 `poseTag` 가
  통째로 걸러 버린다.
· ⚠ **실제 굴림 속도는 못 낸다** — 탱크는 초당 40토막쯤 지나가는데 그리는 박자가 30Hz 다. 어느 값을
  줘도 앨리어싱이니, '도는 것으로 읽히는' 박자(4~12Hz)를 눈으로 고른 값이다.
· ⚠ **저배율에서는 안 돈다**(liteView 에서 0) — 몸이 대여섯 화소면 판 사이가 안 읽히는데 컷마다
  메시 한 벌이 더 산다. 그 자리의 kindMain 은 합본 `tank` 다.
· 기동륜(축)은 안 돌린다 — 매끈한 원판이라 어느 각에서도 같은 그림이고, 돌리면 열쇠만 는다.

## 축을 옮겼으면 그 축을 되돌리던 보정을 전부 훑어라
★★ **감아 둔 180 은 어딘가 셋쯤 더 있다**(2026-09, 지적: "시즈모드 후 갑자기 포탑과 포신이 180도 돌아가
있네 — 뒤쪽이 앞을 향한 것") — 시즈 포신을 뒤(−y)로 돌린 뒤(c8e100e), **그 포신이 +y 로 나가던 시절에
감아 둔 `withModelSpin(180)` 셋**이 그대로 남아 겹셈이 됐다: 합본 `tanksiege` 안쪽 · 포탑 판 `tanksiegegun` ·
전환 홑판 `siegebarrel`(그 홑판 셋은 뒤에 한 판 `tankturretxf` 로 모았다 — 위 '딸림 부품은 제 개체다').
· 규약: **포탑 판의 +y 는 겨누는 쪽**이다(엔진이 `rotDeg` 로 표적 각을 준다) — 그래서 `tanksiegegun` 만은
  180 을 **남기고**(모형의 −y 를 그 규약으로 맞춤), 합본·전환 홑판에서는 **걷는다**(거기서는 −y 가 곧
  '차체 뒤'다). 쉬는 각(engine9 `gunRest9` = +90)은 그 위에서 차체 뒤를 가리킨다.
· ★ **감싸개를 걷으면 그 안의 평행이동도 부호가 돌아온다** — 합본의 `TURRET_BACK9` 도 함께 뒤집었다.
· 🔎 **찾는 자는 총구표다**: `tanksiege` 의 앵커가 `[0, −3.75, …]` → `[0, +3.75, …]` 로 뒤집히는 것이 곧
  고쳐졌다는 증거다. 포신 방향은 눈으로 헷갈리지만 이 수는 안 헷갈린다.

## 딸림 부품은 '제 개체'다 — 한 몸으로 보여야 하면 한 판이어야 한다
★★ **attach 로 겹친 판은 GL 에서 서로를 못 가린다**(2026-09, 지적: "아직도 탱크 → 시즈 변경 애니 중에
시즈 포신이 포탑에 안 가려지고 색도 훨씬 밝게 나와") — 전환 창의 포탑은 홑판 셋이었다: 포신 없는 몸
`tankturret0` + 앞 포신 `tankbarrel`(attach2) + 뒤 포신 `siegebarrel`(attach). 붓은 딸림 부품을 **저마다
`gl9.push`** 하므로 셋이 **딴 개체**이고, 깊이 칸은 개체 사이를 화가 차례로 가른다("개체 사이는 칸이,
개체 안은 칸 안이") — 곧 나중에 민 포신이 **늘 포탑 앞**에 선다. 빌더 안의 `tagKey` 는 한 판 안에서만
쓰는 자라 이 자리를 못 가른다.
· **색도 같은 뿌리다**: mesh9 의 덧칠 접기는 '그 pid 의 마지막 몸'과 견주는데(`areaK9`), 포신만 든 판은
  견줄 몸이 달라 그늘이 옅게 접힌다. 실측(자세 3): 홑판 `siegebarrel` 의 가장 짙은 접힘이 **검0.32** 인데
  정착 판 `tanksiegegun` 은 **검0.47** 이다 — 32% 덜 어두우니 "훨씬 밝게"가 된다.
· 고침은 **한 판으로 모으는 것**이다(`tankturretxf` · `tankTurretV2` 의 새 인자 `xf`): 나온 몫 하나로
  탱크 쌍포신을 `1 − xf`, 시즈 포신을 `xf` 로 **함께** 그린다. 홑판 셋은 걷었다. 깊이도 음영도 정착 판과
  같은 자가 되고, 메시도 세 벌에서 한 벌로 준다.
· ★ 가를지 모을지의 자: **딸림 부품은 '제 각을 따로 갖는 것'을 위한 자다**(터렛 머리·성큰 혓바닥처럼
  메시 열쇠를 줄이려고 요잉을 뗀 자리). **몸과 한 각으로 붙어 서로를 가려야 하는 것은 한 판이다.**
· ⚠ 갈라 둔 별본은 **`SHAPE_GALLERY` 에도 적어라**(`hidden: true`) — 광택 표(`glossOf9`)가 그 표로 종족을
  찾으므로 빠지면 그 판만 '그 밖' 값으로 그려진다. 홑판 셋과 `tanksiegelegsF` 가 다 빠져 있었다(전환 판이
  정착 판과 달라 보인 까닭 하나가 이것이다).
· 🔎 **보는 자**: `node scripts/model-gl.mjs --kinds <k> --pose N` (2026-09 에 더했다 — 자세로 움직이는
  판은 자세 0 한 컷만 봐서는 아무것도 안 보인다).

## 시즈 전환의 세 마디
★ **돌기 → 포신 갈아 끼우기 → 쳐올리기**(2026-09, 요청: "변신하기 전에 시즈 정위치로 자연스럽게 회전
이동(정면에서 135도 요잉한 방향) → 포탑의 뒤쪽으로 포신이 나오고(앞 탱크 포신은 들어가고) → 포탑+포신의
각도가 뒤를 쳐올리는 거야"). 여태 ①은 **툭 앉았고**(창이 열리는 순간 고정 방향) ③은 **툭 튀었다**(창이
끝나 시즈 판이 되는 순간 −16도).
· ① 창의 앞 35%(`SIEGE_TURN_U9`)는 **돌기만** 한다 — 참값 각에서 `SIEGE_FACE_DEG9` 까지 가장 짧은 호로
  smoothstep. 원작(order_Sieging)도 몸을 고정 방향으로 돌린 뒤에야 변태한다.
· ②③ 나머지 65%(`xfMech9`)가 다리·포신의 몫이다. 그 안에서 자세 여섯 칸을 둘로 나눈다(bake9):
  **나오고 들어가는 것 `xfOut9` = p/3.5** · **쳐올리는 것 `xfTilt9` = (p−3)/2**.
· ⚠ **포탑 몸도 함께 기울여야** 한다(전환 판 `tankturretxf` 를 `siege: true`·`tiltK` 로 짓고 POSE_KINDS 에
  등록) — 포신만 기울면 창이 끝나는 순간 포탑이 툭 튄다.
· 언시즈는 거꾸로다: 먼저 접고(`xfMech9` 가 앞 65%) 나중에 돈다(뒤 35%).

## spirePillar 로 뚜껑을 덮을 때
⚠⚠ **`tipW` 를 안 주면 끝 뚜껑이 안 난다**(2026-09, 지적: "프로브 프러스텀과 위 뚜껑면 사이 틈이 크게
있는데?") — 위 뚜껑의 문이 `tipW > 0.01` 이고 기본값은 0 이라, **`widthOf` 로 굵기를 직접 그려도 이 문은
여전히 `tipW` 를 본다**. 안 주면 그 끝이 통째로 열린 관이 되어 속(다른 부품의 안쪽)이 들여다보인다.
· 같은 자리에서 둘을 물렸다: 프로브 몸통의 어깨(속의 금색 치마가 뚜껑처럼 보였다)와 스러스터 드럼의
  아가리(뒤 낯이 아예 없었다).
· 🔎 **찾는 법**: `model-mesh --kinds <k> --dump` 에서 **점이 sides 개인 낯**(12각이면 12점)을 세라 —
  뚜껑은 그 한 장이다. 없으면 안 난 것이다.

## 쌓아 둔 원반은 옆벽이 없다
★★ **납작한 원반을 겹쳐 쌓은 '계단 돔'은 하나라도 세우는 순간 틈이 된다**(2026-09, 같은 지적) — 프로브
몸통은 z 4.96·5.2·5.44 에 놓인 원반 셋이었다. 화가 차례로만 서는 2D 에서는 돔으로 읽혔지만, 배를 절두체로
세우자 그 위 원반이 **0.24 만큼 허공에 뜬 것**이 드러났다. 한 자리를 세우면 **몸통 전체를 이어진 회전체**로
지어야 한다(치마 + 어깨 + 뚜껑, 이음매의 두 테를 같은 자리에 두면 닫힌 입체라 뒷면 걷기도 먹는다).
· 얹어 둔 **덧칠(topFace)도 함께 내려라** — 안 내리면 그것 역시 허공에 뜬 데칼이다.

## 누름(Z8)은 회전보다 먼저다
★★ **설계 자에서 돌린 뒤 z 만 0.8배 하면 직각이 안 남는다**(2026-09, 지적: "터렛 포드가 z 좌표 0.8배
누름하면서 옆면이 직사각형이 아니라 평행사변형으로 틀어졌어") — 회전 다음의 **비균일 배수**는 전단이다.
· 고침은 차례를 바꾸는 것이다: **높이를 먼저 누르고**(t·Z8 · 축 높이도 Z8) 그 눌린 자에서 회전을 **강체로**
  건다. 회전은 각을 지키므로 모든 낯이 직사각형으로 돌아온다.
· ⚠ 그만큼 **키가 0.8배 준다** — 정규화(BLD_NORM·MODEL_NORM)를 다시 재라(터렛 1.899 → 2.210).
· ★ 부르는 자리는 **설계 자를 그대로** 쓰게 두고, 누르는 일은 그 함수 **안에서만** 한다.

## 모드가 바뀌어도 같은 부품은 같은 크기여야 한다
★★ **잉크 상자에 '그 모드에만 있는 것'이 들면 본체가 쪼그라든다**(2026-09, 지적: "탱크
시즈모드시 크기가 작아지고 색이 어두워지는데 왜 그러지 모델이 다른가") — 시즈의 잉크
상자에는 네 귀의 **버팀다리**와 뒤로 뻗은 **포신**이 든다. 그 상자를 목표에 맞추면 두 모드가
나눠 쓰는 **궤도·차체가 8.6% 작아진다**(MODEL_NORM tankbody 0.687 vs tanksiegebody 0.628).
· engine9 에 "시즈탱크 넷은 차체 하나의 값으로 못 박는다"는 ★ 가 이미 적혀 있었는데,
  포신을 뒤로 돌린 뒤 `model-norm` 값을 그대로 실어 **그 못이 뽑혔다**. 넷을 0.687 로 되돌린다.
· ⚠ **'어두워졌다'는 색이 아니라 크기다** — 여덟 각 평균 휘도를 재면 138.9(탱크) vs
  140.6(시즈)로 시즈가 오히려 조금 밝다. 작아진 만큼 결·광택의 배율 문턱(k = 상자px/16 ×
  정규화)이 함께 내려가 덜 반짝인 것이다. **어둡다는 말을 색에서만 찾지 마라.**
· 이것은 '짐을 든 별본의 정규화'와 같은 자리다 — **자세·부속으로 달라진 잉크는 재측정으로
  덮지 않는다**. 상한(축)이 1.14 라 0.687 로도 안 잘린다(model-norm 이 그 칸을 찍는다).

## 건설 단계는 '낯 수'로 단조를 못 박아라
★★ **특례를 나중에 얹으면 그 칸만 앞 칸을 앞지른다**(2026-09, 지적: "벙커만 그런 건진 몰라도
공사중 단계가 처음에 더 많이 지어졌다가 오히려 거꾸로 줄어드는 현상 있음") — `stageFaces` 는
단조를 **넓이로 고른 stop 번호**(si9)에만 걸고, 1단의 바닥 특례("맨 아래 층은 통째로")를 그
뒤에 따로 얹고 있었다. 벙커(tombFlat)는 바닥 평면 부품이 여럿이라 그 층이 두꺼운데 2단의
넓이 몫은 0.07 뿐이라, **1단이 2단보다 더 지어진 그림**이 나왔다.
· 고침은 아홉 칸을 한 번에 풀고 **최종 낯 수를 앞 칸 값으로 죄는** 것이다 — 특례가 무엇이든
  거꾸로 갈 수가 없다.
· 🔎 **보는 자**: `node scripts/model-gl.mjs --kinds <k> --stages 1,2,3,4,5,6,7,8,9,0 --rots 45`
  — 한 줄에 열 칸이 서므로 거꾸로 가는 자리가 눈에 바로 걸린다.

## 표창(tri)은 줄기가 아니다
★★ **제 몸을 그린 갈래는 그 자리에서 `continue` 해야 한다**(2026-09, 지적: "도록에서 벌처랑 뮤탈
글레이브가 안 나오고 이상하게 나와") — `st.tri`(글레이브·파편) 갈래가 별을 그려 놓고 빠져나가지
않아, 아래 **일반 줄기**(글로우 + 몸 획)가 그 위에 덧그려지고 있었다. 획 굵기가 `w × 배율 × 3.4`
라 glave(w 2.85)는 도록 배율에서 별을 통째로 덮는 **갈색 얼룩**이 되고 frag(0.95)는 은색 알약이
된다. 지도에서는 배율이 작아 획이 가늘어 '별에 붙은 잔상'으로 읽혔을 뿐 같은 병이다.
원작의 글레이브 웜·파편 수류탄은 **날아가는 표창 하나**이지 선이 아니다 — 미사일 갈래가 제 몸을
그리고 `continue` 하는 것과 같은 자리다.
· ★ **속색이 몸과 같으면 안 보인다** — glave 의 #6b4732 은 **뮤탈의 몸과 같은 갈색**이라, 몸에서
  막 떠난 별이 통째로 묻힌다. 갈래표가 이미 든 번짐색(`st.glow`)을 1.4배 크기로 한 겹 뒤에 깔면
  그 자리가 갈린다(새 표 값을 지어내지 않는다 — 줄기 갈래가 글로우 획을 먼저 긋는 그 짜임이다).
· ★ **도록에서는 별을 키운다**(`FxPaintEnv9.triK9` · 도록 2.2) — 지도의 자 그대로면 글레이브가
  몸의 15% · 파편이 6px 라 칸에서 안 읽힌다. 스플래시를 도록에서만 0.78배로 줄이는 것과 **같은
  자리**다: 무기의 자(FX_BEAM.w)는 안 건드리고 **보여 주는 자리에서만** 키운다.
· ⚠ 도록의 공격 칸은 **총구가 칸 가장자리**라 날 자리가 13px 남짓뿐이다(실측: 뮤탈 140×104 칸에서
  총구 (46, 91) · 사격 방향이 아래-왼쪽) — 그래서 '날아가는 것'은 사실상 총구에 머문다. 칸을 넓힐
  수는 없으니(창이 곧 모델의 잉크 상자다) **크기로 읽히게** 하는 것이 이 자리의 답이다.

## 같은 몸을 두 이름으로 굽지 마라
★★ **부품 등급(autoTier)은 요잉 0 의 화면 넓이로 매기므로, 돌려 구운 쌍둥이는 더 세게 깎인다**
(2026-09, 지적: "탱크/시즈가 같은 각도에서 색이 다른 게 문제" · "색뿐만 아니라 디테일도 달라
보여 애니메이션 시 다른 모델이나 2D 그림 쓰는 거 아닌지 확인") — 2D 는 아니고 기하도 같았다.
갈린 것은 **메시를 어떻게 쪼개고 어떤 이름으로 굽느냐**다.
· 실측: 정착 시즈 `tanksiegebody` 1004부품/1387폴리 = 몸 539/655 + 다리 465/732 ·
  전환 중 `tankbody` 539/655 + 다리 홑판 465/732(310/488 + 155/244) — **합이 정확히 같다**.
· 그런데 등급 예산은 **종류마다**라, 한 덩이로 묶인 쪽이 훨씬 세게 깎인다:
  lod0 에서 tankbody 655 → 597폴리(91% 남음) · tanksiegebody 1387 → 730(53%).
· 게다가 `tanksiegebody` 는 **spin 270** 으로 굽고 몸 각에서 **−270** 을 빼고 있었다 — 둘은 서로
  지우는 짝이라 **같은 세계 각의 같은 기하**인데, 등급을 매기는 요잉 0 에서는 궤도가 끝으로 서서
  훨씬 작게 잡혀 또 한 번 깎였다(다리를 뗀 뒤에도 lod0 328 vs 597).
· 고침은 **한 판으로 모으는 것**이다: 정착 시즈의 차체도 `tankbody`(spin 0 · rotDeg = 앞)를 쓰고,
  버팀다리는 전환 때와 **같은 홑판**(attach·attach2, 자세 5 = 다 뻗음)으로 붙인다. 함께 따라오는
  세 상수: 몸 각의 −270 을 걷고 · 포탑 뒤 치우침의 시즈 갈래를 걷고(두 모드가 같은 자) ·
  쉬는 포신 각 90 → **180**(차체 rotDeg 가 hdg 로 바뀐 몫).
· ★ 유닛의 딸림 부품도 이제 **제 요잉**을 가질 수 있다(`op.attachRot` — 건물 쪽과 같은 자).
· 🔎 **재는 자**: 두 종류를 lod 0~3 으로 굽어 부품·폴리 수를 견주라(`collectMesh9` + `autoTier` +
  `lodFilter`). 수가 다르면 그림도 다르다 — 눈으로 갸웃할 일이 아니라 세면 나온다.

## 더하는 빛은 바탕 밝기를 타야 한다
★★ **절대값으로 더하면 어두운 재질이 통째로 뜬다**(2026-09, 지적: "같은 모델인데 저렇게 다를
이유가 없어 보이고 특히 탱크 모드 캐터필러 색이 검지 않아서 수상해") — GL 셰이더의 더하는 몫
셋이 다 **절대값**이었다. 궤도 레일판(#4b5158 · 휘도 0.30)에서 재 보면:
· 테두리 빛 `+0.10` → **+33%** · 윤기 바닥 `0.30 + 0.70·휘도` → 검은 낯에도 3할이 깔린다 ·
  봉우리(테란 0.24) → **+80%**.
합치면 어두운 낯이 밝은 낯보다 **더 많이** 밝아지고, 셋 다 요잉(법선 × 시선·빛)을 타므로 그
몫이 각마다 들쭉날쭉하다 — 곧 "검어야 할 궤도가 회색이고, 돌 때마다 색이 바뀐다"가 된다.
· 고침은 셋 다 **바탕 밝기를 태우는 것**이다: 테두리 `×(0.30 + 0.70·휘도)` · 윤기 바닥
  `0.30 → 0.10` · 봉우리 `×(0.45 + 0.55·휘도)`. **흰 낯(휘도 1)은 한 톨도 안 바뀐다** —
  밝은 것은 그대로 두고 어두운 것만 내리는 꼴이라 전수 표가 안 흔들린다.
· ⚠ 봉우리를 **0 으로 죄지는 않는다** — "젖은 검은 키틴에도 반사광 한 점은 맺힌다"는 규약은
  그대로다(그래서 0.45 가 바닥이다).
· 실측(tankbody 여덟 각 · 중앙 휘도): 127.7~158.2(폭 **30.5**) → 115.3~140.9(폭 **25.6**).
  몸이 고르게 어두워지고 각에 따른 흔들림도 함께 준다.
· ★ 이 규약은 '**알베도가 낮은 낯은 덜 반사한다**'는 물리 그대로다 — 새 더하기 항을 넣을 때는
  늘 휘도를 곱할지 먼저 물어라.

## 요청이 이름 붙여 뺀 것만 빼라
★★ **"총구와 탄착지 폭발 효과는 제거"는 '셋 다 제거'가 아니다**(2026-09, 지적: "트레이서가 안
나가") — 일반 탱크 모드(`cannon`)를 `MUZZLE_BURST_FX9` 에서 빼면서 그 갈래를 **아무것도 안 그리는
자리**로 보냈다(NO_BEAM_FX 라 선도 없다). 그런데 그 요청이 이름 붙여 뺀 것은 **총구 불꽃**과
**탄착지 폭발** 둘이고, 가운데의 **날아가는 포탄**은 뺀 적이 없다.
· 그래서 갈래를 하나 더 둔다: `SHELL_ONLY_FX9`(지금은 cannon 하나) — 셋 중 ②만 낸다.
· ★ 교훈: 요청이 **목록으로 말할 때는 그 목록만** 건드려라. '그 무리 전체'로 넓히면 말하지 않은
  것까지 사라지고, 그 손실은 한참 뒤에 "왜 안 나오지"로 돌아온다.
· ⚠ **폭발의 자는 두 번 곱해진다** — 엔진이 `fxPx × 1.5` 를 주고 갈래표(`FX_IMPACT.tankboom`)가
  다시 `r 1.55` 를 곱해 실제로는 **쏘는 몸 상자의 2.3배**가 깔렸다(지적: "탄착지 폭발 너무 큰 듯").
  1.0 으로 내려도 몸의 1.55배 · 포구 불꽃(0.38)의 2.6배라 '넓게'라는 뜻은 그대로다.
  **새 크기를 고를 때는 갈래표의 r 까지 곱해 보라.**
· 도록은 거기에 **0.6 을 더 건다** — 시즈는 몸이 4타일이라 그 폭발이 칸을 통째로 주황 안개로
  덮는다(무기의 자는 안 건드리고 보여 주는 자리에서만 줄이는 그 규약이다).

## 총구는 채널이 둘이다 — 지대공과 지대지는 딴 발사관이다
★★ **갈래는 이미 갈라 두었는데 구멍이 하나였다**(2026-09, 요청: "지대공 지대지 트레이서 시작점을
분리해서 가지고 있어야 해 · 레이스 대공 공격은 양쪽 포드 끝에서 나가야 하고 골리앗은 대지는 양쪽
포신 끝, 대공은 어깨 갑옷 앞쪽 면의 윗부분에서 나가 · 스카우트는 대지는 앞코, 대공은 양쪽 엔진에서
나가") — 이 셋은 원작에서 **포탑이 둘**이고 도록·지도도 이미 두 무기로 그리는데(지상 버스트 ↔ 대공
미사일), 총구 앵커는 종류마다 **한 점**이라 두 무기가 같은 자리에서 났다.
· 채널을 둘로 둔다: 빌더의 `markMuzzle9`(지상·기본) · **`markMuzzleAir9`**(대공). 표는
  `MUZZLE_GEN9` 와 **`MUZZLE_AIR_GEN9`** 로 함께 굽히고(muzzle-table), 엔진은 `muzzleAnchor9(kind, air)`
  한 문으로 고른다. **대공 표식이 없는 종류는 표에 안 들어** 종전과 똑같다(값이 안 는다).
· 채널을 고르는 자는 **표적**이다 — 지도는 `foe.air`, 도록은 칸(`DocCell9.air` → `DocTracer9 air`)이다.
  ⚠ 칸에 그 값을 안 실으면 도록만 늘 지상 무기의 구멍에서 쏜다(두 칸이 똑같아 보인다).
· ★★ **좌우 한 쌍인 발사관은 앵커 하나로 적는다** — 오른쪽(+x)만 찍고 엔진이 **모형 x 를 뒤집어**
  왼쪽을 낸다(`MUZZLE_PAIR9` · `muzzleLanes9`). 곧 두 줄기의 간격이 **모델의 실제 간격**이다.
  · 옛 자(engine9 `twin9`)는 '미사일 갈래면 잉크 폭의 절반씩 벌린다'는 **어림**이었고, 그래서
    ㉠ 지상 무기는 아무리 발사관이 둘이어도 한 줄기였고(골리앗 쌍열 기관포가 몸 한가운데에서 났다)
    ㉡ 벌리는 폭이 기하가 아니라 몸 크기에서 나왔다. 쌍 앵커가 있으면 그 자리가 이기고, 없으면
    옛 자로 물러난다(발키리처럼 손 표만 있는 종류는 그대로였다 — 아래에서 그것도 표식으로 옮겼다).
  · ⚠ 빌더가 `withModelSpin` 으로 통째로 돌아가 있으면 x 뒤집기가 거울이 아니다 — 이 넷은 안 돈다.
· 🔎 **보는 자**: `node scripts/muzzle-sheet.mjs --kinds wraith,goliath,scout --air`(하늘색 십자 ·
  쌍이면 **둘**) · `--air` 없이 돌리면 지상 채널(붉은 십자)이다.
· 적어 둔 자리: 레이스 대공 = 날개 끝 포드의 총구(⚠ 관은 제 z 보다 `tubeAxisLift` 만큼 올려 그려지므로
  표식도 올린다) · 골리앗 지상 = 오른 포신의 소염기 끝 · 골리앗 대공 = 포드 아래 마디(어깨 갑옷)의 앞
  낯 위쪽 · 스카우트 대공 = 카혼 엔진의 앞 끝 · 발키리 = 동체 양옆 포드의 앞 낯(둘 다 쌍).
· ⚠ **발키리는 포드 앞 낯보다 한 뼘 앞이다**(지적: "발키리 포드 앞쪽에서 나와야지 뒤 말고") —
  낯 위에 두면 그 점이 살에 묻혀 부감에서 미사일이 포드 **뒤**에서 비어져 나오는 것으로 읽힌다.

## 총구 표식은 '쏘는 자세'의 자리다
★★ **표는 자세 0 에서 한 번 굽는데 무기는 공격 컷에서만 난다**(2026-09, 지적: "메딕은 오른쪽 팔에
맞춰야 해") — `muzzle-table` 은 `poseSet(0)`(평상시)로 굽는다. 그런데 메딕의 주사기 팔은 평상시엔
**허리께로 내려가** 있고 치료 컷에서만 앞으로 든다 — 그래서 표에 실린 점이 **z 1.15**(허리)였고,
불빛이 오른팔이 아니라 옆구리 아래에서 났다. 치료 컷의 자리로 적으면(z 3.53) 든 팔의 바늘 끝이다.
· ★ 이 꼴을 찾는 자는 **자세로 움직이는 부품에 달린 총구**다 — 메딕 주사기 · 파이어뱃 노즐 ·
  마린 총이 그 셋이다. 그런 자리는 빌더가 **그 컷의 좌표를 손으로 적어** 표식을 찍는다
  (자세 깃발을 안 본다 — 표는 어차피 한 벌이다).
· 함께 고친 눈 값 둘(지적): **마린 +0.14**(축 위의 점은 총열 가운데인데 눈이 총구로 읽는 것은
  그 관의 위쪽 자락이다 — "살짝 낮게 잡힌 거 같아") · **파이어뱃 −0.14**("살짝 높게 잡힌 거
  같아 — 공격 모션에서 포신이 살짝 내려가거든").

## 앵커는 '그것을 든 판' 위에 찍어 봐야 한다
★★ **합본 모델 위에 찍으면 멀쩡한 표도 틀려 보인다**(2026-09, 지적: "시즈모드 트레이서가 포신
입에서 나오는지 다시 확인") — 탱크 둘은 총구 앵커를 **포탑 판**(tankgun·tanksiegegun)에서 뽑아
합본 이름(tank·tanksiege)으로 싣는다(`muzzle-table` 의 `KEY_OF`). 그 이름으로 `muzzle-sheet` 가
**합본 모델**을 굽고 그 위에 십자를 찍으면, 합본의 포탑은 **쉬는 각**으로 서 있으므로 십자가 90도
어긋난 자리에 앉는다 — 표가 틀린 것이 아니다.
· 판정표가 `MUZZLE_PLATE` 를 뒤집어 **판 이름으로도** 찾게 고쳤다:
  `node scripts/muzzle-sheet.mjs --kinds tanksiegegun,tankgun --rots 0,90,180,270`
  → 십자가 해저드 띠(포구)에 정확히 앉는다(0°는 포신이 화면 안쪽을 봐 가려지는 것이 맞다).
· ★ 지도에서 쓰는 자도 그 판의 자다: 원점은 **포탑 op 의 자리**(`turretAt9.fx/fy`)이고 각은 그
  포탑의 겨눈 각(`mzHdg9`)이다 — 곧 앵커는 '포탑 판의 +y = 겨누는 쪽' 규약 위에 있다.

## 모드가 바뀌는 유닛은 '그리는 판'과 무기를 한 벌로 움직여라
★★ **`siegeOn` 이 아니라 `siegeShow9` 다**(2026-09, 지적: "탱크모드의 트레이서가 왜 자꾸 시즈모드의
트레이서를 쓰는 거야 … 탱크모드에서 시즈 포신이 나오는 애니메이션 동안이 문제라고") — 전환 창이
열리는 순간 `siegeOn` 은 이미 1 이지만 몸은 아직 **탱크 판**이다. 그 동안 시즈 무기를 쓰면 탱크가
시즈의 큰 포탄을 쏜다. `siegeShow9`(= `siegeXf9 ? 0 : siegeOn`)는 **그리는 판이 무엇인가**를 그대로
말하므로, 무기·총구 앵커·갈래가 몸과 한 벌로 움직인다.
· ⚠ 자도 따로다 — 90mm 포와 자주포는 아예 다른 무기다: 포탄 자 **시즈 0.68 · 탱크 0.26**.

## '싸우는가'의 어림이 모드를 모르면 먼 표적이 통째로 빠진다
★★ **탱크는 모드에 따라 사거리가 7.8 ↔ 12.8 로 갈린다**(2026-09, 지적: "시즈 트레이서는 가까우면
나오고 타겟이 멀면 안 나옴") — 참값 이름은 시즈를 걸어도 "Siege Tank (Tank Mode)" 그대로이고,
`turretAim9`·`chaseAim9`(참값 상태가 ST_FIGHT 가 아닐 때 '싸우는 것으로 보는' 유일한 문)는
`siegeOn` 을 셈하기 **전**이라 모드를 모른다. 그래서 정착 시즈가 **8~12타일**을 쏘면 그 문이
7.79 에서 막혀 트레이서가 통째로 안 났다 — 가까우면 나오고 멀면 안 나오는 그 꼴이다.
· 그 자리는 **어림**이므로 두 모드 중 **큰 쪽**으로 연다(`reachMax9`) — 진짜 겨눔 문(`aimTiles9`)은
  뒤에서 `drawUnit2` 로 제 모드의 사거리를 따로 보므로 느슨하게 열어도 새지 않는다.
· ★ 교훈: **모드로 표가 갈리는 유닛은 '표를 읽는 자리'가 모드를 아는지 늘 물어라** — 이름 하나로
  두 무기를 가리키는 자리가 이 코드에 셋 있었다(갈래 · 겨눔 · 싸움 판정).

## 시즈 중이면 무기가 다른 유닛이다
★★ **참값의 이름은 시즈를 걸어도 "Siege Tank (Tank Mode)" 그대로다**(2026-09, 지적: "시즈탱크
재생기에서 트레이서 안 나옴") — 그래서 `fxUnit` 을 갈아 끼우지 않으면 `attackFxOf9` 가
**cannon**(탱크포)을 내고 시즈의 `siege` 갈래를 못 만난다. cannon 은 `NO_BEAM_FX` 라 선을 안 긋고
`MUZZLE_BURST_FX9` 에도 없으므로(요청: "일반 시즈모드에서는 총구와 탄착지 폭발 효과는 제거")
**한 톨도 안 그려진다**. 총구 앵커(fxKind)만 이 갈아 끼우기를 하고 있었다.
· 이름을 한 자리에서 갈면 **갈래·쿨다운·사거리가 한 벌로** 시즈의 것이 된다 — 그 셋이 다 이
  이름에서 나온다(시즈는 사거리 12 · 쿨다운도 다르다).
· ★ 교훈: **'무엇으로 그리나'를 갈아 끼우는 자리가 여럿이면 갈래도 함께 갈아야 한다** — 하나만
  고치면 그림은 시즈인데 무기는 탱크인 어긋남이 조용히 생긴다.

## 날아가는 것의 각은 접선이 아니라 곧은 선이다
★ **포물선의 접선을 각으로 주면 쏘는 순간이 거의 수직이다**(2026-09, 지적: "시즈탱크 포탄
세로로 긴 게 아니라 가로로 긴 타원형이어야 하고") — 전차포의 들림은 사거리의 0.12 라
발사 접선의 세로 몫이 **0.48·사거리**다. 나는 몫이 0.16초뿐이라 눈에 드는 것은 그 앞부분이고,
그래서 에너지포가 늘 세로로 선 타원이었다. 덩이의 긴 축은 **날아가는 길**을 가리켜야 하고
그 길은 총구와 표적을 잇는 선이다 — 포물선은 그 위에 얹힌 들림일 뿐이다.

## 짐을 든 별본의 정규화는 다시 재지 마라
★ **몸이 같은데 자세만 다른 별본은 본체의 배수를 쓴다**(2026-09, 요청: "자원 들었을 때 팔은 좀 모으기") —
팔을 모으면 잉크 상자가 그만큼 **작아지고**, 재측정 값을 그대로 넣으면 배수가 커져 **짐을 들 때 일꾼이
커진다**(실측: scvMin 0.793 → 0.816 · 3%). '비가 안 맞을 때 늘이는 자리'의 ⚠와 같은 자리다 — 자세로
달라진 잉크는 재측정으로 덮지 않는다. 잉크 **중심**은 앵커라 따로다(그 쪽은 재서 올린다).

## 도는 부품이 건물의 자리를 흔들면 안 된다
★★ **애니메이션 부품은 잉크 상자에서 빼고 재라**(2026-09, 지적: "포지 톱니바퀴 회전할 때 위로
들썩거림" → "포지는 톱니바퀴만이 아니라 **전체가** 들썩였는데") — 두 번째 말이 진짜 병을 짚었다.
붓은 건물을 **잉크 상자의 바닥을 바닥선에 앉혀** 놓는데(`footOf(glB9).bot`), 그 상자를 **그 칸의
메시**로 재고 있었다. 포지 톱니는 칸마다 이가 실루엣 밖으로 들고 나므로 상자 바닥이 칸마다
움직인다 — 실측(요잉 여덟 칸 × 회전 여덟 칸): 요잉 45·315 에서 **0.33 모델칸**, 90 에서 0.17.
그 몫이 그대로 건물의 y 로 실려 **건물 전체가** 오르내렸다.
· 고침은 한 줄이다: 상자는 **칸 0 의 메시**로 잰다(`bldMesh({ ...op, spin: 0 })`). 그리는 것은
  종전대로 그 칸의 메시다 — 톱니는 돌고 건물은 안 흔들린다.
· ⚠ 값은 안 든다 — 안 도는 종류는 메시 열쇠가 같아(`spinTag` "0") 같은 벌이 그대로 오고,
  도는 종류만 칸 0 한 벌을 더 쥔다(그 벌은 안 도는 프레임에서 어차피 쓰인다).
· ★ 이것은 터렛에서 이미 적어 둔 규약(**"각은 몸의 요잉으로 못 박아 재라"**)의 **회전 칸 판**이다.
  머리 각 쪽은 막아 두고 spin 쪽은 안 막혀 있었다 — 그 규약은 '도는 값 전부'에 걸어야 한다.
★★ **공사 발판도 같은 자리다**(2026-09, 지적: "테란 건설 완료 시 스캐폴드 없어지면서 건물이 살짝
내려오거든? 건설 중 조금 위로 올라가는 원인 수정") — 회전 칸은 막아 두었는데 **건설 단계
(buildStage)** 만 안 막혀 있었다. 공사 중 메시에는 `stageFaces` 가 세우는 **발판 두 채**가 함께 드는데,
그것들은 건물 **앞 모퉁이 땅에** 서므로 화면 아래로 더 내려온다. 붓은 상자 바닥을 바닥선에 앉히므로
그만큼 **건물이 통째로 떠오르고**, 완성되어 발판이 사라지는 순간 도로 내려앉는다.
· 실측(지도의 건물 각 = footOf 자로 320 · **단계 1~9 가 모두 같은 값** — 곧 범인은 자라는 몸이 아니라
  늘 서 있는 발판이다): 스타포트 **2.55** · 커맨드 1.06 · 아카데미 1.00 · 배럭 0.81 · 리파이너리 0.79 ·
  팩토리 0.22 · 서플라이 0.11 모델칸.
  ⚠ **각을 잘못 잡으면 0 이 나온다** — 요잉 40 에서는 대부분 0.00 이다. `footOf` 에 넘기는 각은
  `−op.rotDeg` 라 지도의 40 은 여기서 **320** 이다. '안 움직인다'가 나오면 부호부터 의심하라.
· 고침은 한 줄이다: 상자를 **다 지은 모델**로 잰다(`bldMesh({ ...op, spin: 0, buildStage: 0 })`).
  딸림 부품 쪽도 같은 자로 잰다(그리는 것은 종전대로 그 단계의 메시다).
· ★ 덤으로 **총구 앵커표(`BLD_INK_BOX`)가 안 오염된다** — 그 표는 종류마다 한 번만 적히는데, 그 종류를
  처음 본 프레임이 공사 중이면 **발판이 든 상자**가 그대로 굳었다.
· ⚠ 값은 거의 안 든다 — 더 굽는 그 한 벌은 완성되는 순간 어차피 쓰는 벌이라 미리 데워 두는 셈이다.
· 재는 자: 단계별 상자 바닥(`scratchpad/stagebox.mjs` 꼴 — `collectMesh9(빌더, f => stageFaces(f, stg, kind))`)
  · 눈으로는 `node scripts/scene-sheet.mjs --race 테란`(공사 중 건물이 함께 선다)을 고치기 전후로 뜬다.
· 재는 자: **`node scripts/spin-box.mjs <종류…>`** — 칸마다 메시의 화면 상자를 요잉 여덟 각으로
  재서 바닥이 얼마나 흔들리는지 찍는다. **그 값이 0 이 아닌 종류가 이 병에 걸리는 종류다**
  (상자를 칸마다 재면 그만큼 건물이 오르내린다 — 붓이 칸 0 으로 고정해 재므로 지금은 안 흔들리지만,
  그 고정을 풀거나 새 애니메이션 부품을 넣을 때 이 자로 먼저 확인하라).
  실측: 포지만 흔들린다(요잉 45·315 에서 0.327 · 90 에서 0.165) — cyber·mshop·trapezoid·sunken 은 0.
  ⚠ **정지 그림 한 장으로는 안 보인다** — 칸마다 따로 구우므로 각 장은 멀쩡하다.

## 손짓 중 '한 장이 무거웠다'로 판정하는 자는 다 같은 함정이다
★★ **실시간 원근과 배킹 죄기가 서로 싸운다 — 가장 빠른 단만 흐려졌다**(2026-09, 지적: "품질 매우높음에서만
드래그 시 모델들이 흐려지는데 왜 그럴까" → "티어 3에서 켜져 있는 실시간 원근 변화가 관련있을 수도" — 그
짐작이 맞다) ─────────────────────────────────────────────────────────────────────────────────
두 손질이 맞물려 거꾸로 섰다:
· ㉠ **실시간 원근**(PC 3단 `DEV9.live3d`)은 손짓 중 던지기 문턱을 **건너뛰고**
  (`if ((live3d9 || gap9 >= need9) && moved9)`) 손끝이 움직일 때마다 한 장을 다 칠한다.
· ㉡ **배킹 몫**(`xfBackK9`)은 직전 한 장이 **25ms** 를 넘으면 0.7(또는 0.5·0.4)로 내리고, 진동을 막으려
  그 값을 **한 손짓 동안 붙들어 둔다**. 배킹이 내려가면 캔버스는 CSS 크기 그대로 두고 화소만 줄므로
  브라우저가 늘려 그린다 — 곧 **흐려진다**.
곧 3단은 ㉠ 때문에 손짓 한 번에 훨씬 많은 장을 칠하는데, 그중 **한 장만** 25ms 를 넘겨도 ㉡ 이 걸려
남은 손짓이 통째로 흐려진다. 3단이 아닌 단은 던지기 문턱에 걸려 장 수가 적으니 그 한 장을 안 만나고
또렷하다 — '매우 높음에서만 흐리다'가 그것이다.
· 고침은 **접힐 때까지 기다리는 것**이다(`xfLive9`): 실시간 원근이 살아 있는 동안은 배킹을 1 로 둔다.
  진짜로 무거우면 연달아 세 장이 33ms 를 넘어 원근이 **접히고**, 그때 `xfLive9.on` 이 내려가 종전 규칙이
  돌아온다. 곧 문턱이 '한 장이 튀었다'에서 '이 기기가 실제로 못 버틴다'로 바뀐다.
· ⚠ 이것은 **접기 규칙에서 이미 한 번 고친 함정과 같은 자리**다("한 장 규칙은 메시를 처음 짓는 첫
  프레임에 걸려 손짓마다 접힌 채 돌았다") — 그때 접기만 고치고 배킹은 안 고쳤다. **손짓 중 한 장으로
  판정하는 자가 또 있으면 다 이 손이다**(그림자 접기 `shFold9` 는 `xfBackK9.k < 1` 을 보므로 저절로 따라온다).
· 🔎 보는 자: `#diag` 의 `[손짓]` 줄 — `배킹×1(live 붙듦) 원근 live` 가 고쳐진 모습이고,
  `배킹×0.7 원근 live` 가 나오면 이 손질이 안 걸린 것이다. `perf-check --wide --3d --drag --diag` 도 같은 줄을 낸다.
· ★ 교훈: **단을 올려 켠 기능이 다른 죔을 건드리지 않는지 물어라** — '더 좋게'가 '더 자주 칠하기'로
  구현되면, '자주 칠하면 죄는' 자와 만나 가장 빠른 기기에서만 품질이 떨어진다.

## 도는 부품의 칸은 몇이어야 하나 — 그리고 한 칸이 대칭 주기면 안 돈다
★★ **칸 수는 시간 해상도만 정한다 — 회전 속도에는 안 든다**(2026-09, 요청: "코어 디스크, 서플라이 팬,
터렛 포드 도는 애니메이션 프레임수를 늘려줘 부드럽게") — 엔진이 `floor(t·rate·N) % N` 로 칸을 세고
빌더가 `칸·2π/N`(대칭이면 `2π/(n·N)`)으로 각을 내므로 각 ≈ `t·rate·2π` 다. 곧 **N 만 올리면 속도는
그대로고 프레임만 는다**. `SPIN_ANIM9` 8 → **16**(서플라이 팬 초당 17.6 → 35.2번 갈림 · 코어·포지·
머신샵 12.8 → 25.6번).
· **무늬 칸과 가른다** — `SPIN_STEPS`(8)는 도는 각과 **핵 폭발·구름의 무늬 번호**를 겸하고 있었다.
  ①만 올리려고 `SPIN_ANIM9` 를 새로 두었다(`SPIN_SLOTS`·`STORM_STAGES` 를 이미 그렇게 가른 자리다).
· ⚠ **더 올려도 값이 없다** — 설계도가 초당 30장이라 그 위는 안 그려진다(16 에서 서플라이는 35.2 로
  이미 포화). 그런데 **메시 벌은 그대로 는다**: 실측(perf-check --wide --probe-gl, 테란 킷)
  메시 67 → **75벌** · VBO 11.14 → **11.64MB** · 서플라이 9 → 16벌 · 버림 0. 폰 상한이 240벌이라
  24·32 로 올리는 것은 값이 안 맞는다.
· ⚠ **도록의 칸 수도 같이 올려라**(`docCellsOf9` 의 `cell9`) — 안 올리면 도록만 절반 박자로 끊긴다.
· ★ **먼저 '유니폼으로 돌릴 수 있나'를 물어라** — 각이 열쇠에 안 들면 칸이 애초에 필요 없다(아래 ★★).

★★ **한 칸이 대칭 주기의 정수배면 그 부품은 한 번도 안 돈다**(같은 요청 · 눈으로 보다가 잡았다) —
머신샵 바닥 톱니는 이가 **16개**(간격 22.5도)인데 각이 `spinRad()/2` 였다. 옛 여덟 칸에서 그것은
칸마다 **정확히 22.5도** — 곧 한 칸 넘어가면 이가 **이웃 이의 자리에 겹쳐** 그림이 한 톨도 안 바뀐다.
16갈래 대칭에서 한 칸 = 한 이는 회전이 아니라 **항등**이다. 주석에는 "이 하나와 맞아 굴러가는 그림이
된다"고 적혀 있었지만 실제로는 바닥 톱니가 한 번도 안 돌았다(옆 바퀴는 `×2` 라 −90도, 이 간격 36도의
나머지가 −18도씩이어서 **두 그림만** 오갔다).
· 고침은 **대칭 몫**이다(`spinRadSym(n)` — 이 하나를 칸 수로 나눠 밟는다). 칸마다 그림이 갈리고
  마지막 칸 다음이 첫 칸과 정확히 이어진다. 포지 톱니(`spinRad()/TN`)가 이미 같은 식이었다.
· ★ **코어 디스크도 같은 병이었다** — 살 셋(120도 주기)에 `spinRad()`(360도를 칸에 나눈 것)를 태워,
  보이는 각이 0·45·90·**15**·60·105·30·75… 로 **차례가 뒤죽박죽**이었다(그림은 다 다르니 '돌기는
  하는데 떠는' 꼴). 서플라이 팬에서 한 번 고쳐 적어 둔 그 함정에 코어만 남아 있었다 → `spinRadSym(3)`.
· 🔎 **찾는 자는 `--spins` 다**(2026-09 에 model-gl 에 더했다):
  `node scripts/model-gl.mjs --kinds mshop,cyber,trapezoid,forge --spins all --rots 45`
  — 칸이 한 줄로 서므로 ⓐ **서로 다른 칸이 몇인가**(16/16 이라야 한다) ⓑ **이웃 칸 화소차가 고른가**
  (고르면 등속이다) 둘이 눈에 바로 걸린다. 실측(고친 뒤): 넷 다 16/16 · 이웃 화소차 최소/최대가
  mshop 189/241 · cyber 774/856 · trapezoid 147/163 · forge 761/897 — 최소와 최대가 붙어 있다.
  `spin-box.mjs` 도 칸 수를 표에서 읽게 고쳤다(8 로 못 박아 두면 늘린 뒤 절반만 재고 통과한다).

## 각을 유니폼으로 돌리기로 했으면 칸을 걷어라
★★ **터렛 포드는 각이 열쇠에서 빠진 뒤에도 7.5도로 접고 있었다**(2026-09, 같은 요청) — 그 48칸의
근거는 **판 굽기 시절의 바이트**였고, 포드를 딸림 부품으로 가르며(그 자리의 ★★) 각이 `attachRot`
곧 **카메라와 같은 유니폼**이 되어 메시 열쇠에서 빠졌다. 접을 까닭이 사라졌는데 값만 남아,
96°/s 에서 **초당 12.8번만** 갈리는 계단으로 돌고 있었다 — 0 으로 두면 이어서 돈다(프레임 수 무제한).
· ⚠ **포톤 캐논은 그대로 22.5도다** — 그쪽 몸(`coil`)은 `HEAD_KINDS` 에 들어 **각이 아직 열쇠**다.
  접는 것을 걷으면 프레임마다 새 메시를 굽는다. 같은 손잡이라도 **열쇠에 드는가**로 갈린다.
· ★ 규약: **각을 유니폼으로 옮겼으면 그 각을 접던 칸을 같은 커밋에서 걷어라** — 안 걷으면 값은
  안 벌면서 그림만 계단으로 남는다(이 자리는 두 달을 그렇게 있었다).

★★ **완성되는 순간 방향이 튀는 것은 '공사 중에는 안 돈다'는 문 때문이다**(2026-09, 지적: "터렛
처음 지어지면 공사중일 때랑 포드 방향이 다른데 왜 그래?") — 포탑 각을 내는 문이
`!raising && …`(`raising` = `t < doneAt`)이라, 공사 중에는 `undefined` 를 내고 붓이
`attachRot: headDeg9 ?? buildingYawOf()` 로 **건물 기본 요잉**에 포드를 세운다. 그런데 완성되는
순간 탐지 회전(`t·96 + 자리`)이 켜지는데 그 각은 건물 요잉과 아무 상관이 없다 — 곧 **한 프레임에
포드가 툭 돈다**.
· 고침은 **겨눔과 탐지 회전을 가르는 것**이다: 겨누기(표적 각)는 여태처럼 공사 중에 안 하고,
  탐지 회전은 공사 중에도 돈다. 완성 전후가 같은 함수라 저절로 이어진다.
· ⚠ 탐지 회전을 가진 것은 터렛뿐이라(포톤·성큰은 `sweep9` 가 없다) 나머지는 종전과 똑같다.
· ★ 이 꼴을 찾는 자는 **`?? 기본값` 이 붙은 애니메이션 값**이다 — 그 기본값과 켜졌을 때의 값이
  이어지지 않으면, 꺼짐 → 켜짐의 그 한 프레임이 늘 튄다.

## 도는 부품을 지면에서 자를 때
★★ **중심각이 아니라 '걸치는 구간'으로 보고, 꼭짓점을 죄라**(2026-09, 지적: "포지 톱니바퀴
회전할 때 위로 들썩거림") — 반쯤 파묻힌 톱니바퀴는 판을 현(지면)에서 깔끔히 자르는데,
**이빨은 중심각만** 땅 위인지 보고 있었다(`sin(a) > 0`). 이의 반각이 7.6도라 중심이 2도쯤인
이는 아래 절반이 현 밑으로 삐져나왔고, 그것이 칸마다 났다 사라지니 **도는 것이 아니라
위아래로 들썩이는 것**으로 읽혔다.
· 네 꼭짓점의 각을 [A0, A1] 로 죄면 땅에 닿은 이는 현 위에 납작하게 눕고(넓이 0에서 시작해
  돌면서 솟는다) 바퀴의 아랫변은 어느 칸에서나 정확히 지면선이다.
· 보임 판정도 **이가 걸치는 구간**(a−TW < A1 && a+TW > A0)으로 본다 — 중심만 보면 현에
  반쯤 걸친 이가 통째로 사라져 그 자체가 또 하나의 깜빡임이다.
· ⚠ 이런 떨림은 **정지 그림 한 장으로는 안 보인다**(칸마다 따로 구우므로 각 장은 멀쩡하다) —
  칸 0~7 을 한 줄로 늘어놓고 봐야 잡힌다(`model-shot --spin N`).

## 몸에 딸린 것은 몸이 받는 평행이동을 같이 타야 한다
★ **부양 높이 통일표(MODEL_Z_OFF9)는 표에 든 빌더만 옮긴다**(2026-09, 지적: "일꾼들 미네랄
가스 든 거 위치가 이상함" → "테란·프로토스만 이상하네" → "저그는 괜찮음") — 그 표를 먹이는
고리가 `Object.keys(MODEL_Z_OFF9)` 를 도는 자라, **딴 빌더로 갈라 둔 짐**(load…)은 제자리에
남았다. scv +2.072 · probe −2.168 · drone +0.192 이므로 SCV 는 짐이 2.07 아래로, 프로브는
2.17 위로 어긋나고 드론은 0.19 뿐이라 안 보였다 — **"저그는 괜찮다"가 곧 범인을 가리키는
말**이었다(세 종족의 차이가 그 표의 세 숫자와 그대로 맞아떨어진다).
· 고친 자리는 표가 아니라 **짐 쪽**이다: `loadZ9(몸 종류, …)` 로 몸과 같은 값을 표에서 읽어
  건다. 표를 고치면 짐도 함께 따라간다.
· ★ 부품을 **딴 빌더로 가를 때**(op.attach·load…)는 그 몸이 받는 감싸개를 전부 세어 보라 —
  z 평행이동·배수·요잉이 몸에만 걸려 있으면 딸림 부품이 조용히 어긋난다.

## 비가 안 맞을 때 늘이는 자리
★ **한 종류만 손보기 전에 '그 무리 전체가 작은가'를 먼저 물어라**(2026-09, 요청 세 번:
"마린/파뱃 비율이 이상함 높이를 1.2배해야함" → "마린 파뱃 1.2배 메딕 고스트 1.1배 다 원복
해 줘 그리고 마린 파뱃만 다리길이만 1.2배" → "마린 파뱃 다리길이 원복하고 테란 보병 전체
높이를 1.1배로") — 세 번째가 답이었다. 보병끼리의 비는 이미 서 있었고, 모자란 것은 **다른
유닛에 견준 키**였다. 그래서 넷(마린·파뱃·메딕·고스트)에 **같은 몫**을 준다.
· 자는 모형 공간의 z 배수다(`withModelScale(1, 1, k, …)`) — 꼭짓점에 굳으므로 그리기
  배수(MODEL_NORM)가 아니라 모델 제 좌표가 바뀐다. 보병은 `SUIT_TALL9` 한 자리에 모았다.
· ⚠ **MODEL_NORM 은 다시 재지 않는다** — 재면 잉크 상자가 커진 만큼 배수가 줄어 폭까지
  같이 작아진다(비만 서고 키는 제자리다). '높이를 N배'는 재측정 값으로 덮지 않는 값이다.
· 되돌린 두 길도 적어 둔다(다시 그 길로 가지 말라고):
  ㉠ **키를 통째로 1.2배** — 어깨·헬멧·배낭까지 커져 덩치만 커지고 비가 안 선다.
  ㉡ **다리만 1.2배**(suitLegs 의 zk 가 골반 1.888·무릎 1.072 에만 곱한다) — 다리는 늘지만
     그 위를 `suitBodyRatio` 로 같이 올려야 하고(안 그러면 몸통이 골반에 안 앉는다),
     정작 눈에 띄던 것은 다리가 아니었다.
· 검산: 되돌린 종류의 `model-norm` 값이 **한 자리도 안 틀리고 옛 값**이면 원복이 깨끗하다.

## 모델을 통째로 돌릴 때
`SHAPE_ROT` 은 **2D 길(resolveShapeFaces)만 탄다** — GL(bldMesh)은 `op.rotDeg` 만 보므로 그 표에 적으면 붓이 둘로 갈린다.
모델의 기본 방향을 바꿀 때는 빌더를 **`withModelSpin(도, …)` 로 감싼다**(2026-09, 요청: "파일런 −45도 요잉") — 모형 공간이
셈에 드는 모든 입구(project·depthNow·faceLight·facingRatio)가 그 회전을 지나므로 2D·GL·도록이 한 자를 쓴다.
⚠ 돌린 뒤 `bld-norm`·`ink-center` 를 다시 재라 — 파일런은 둘 다 안 변했지만(잉크폭 10.32 → 10.34) 실루엣이 길쭉한 모델은 변한다.
⚠⚠ **띠·데칼의 키 기준점은 늘 '그 몸통이 tagKey 에 준 그 자리'다**(2026-09, 지적: "앞면 양옆
건물에 창이랑 띠 어디 갔지") — 배럭 좌우 판을 줄이며 그 y 중심이 0 에서 PCY9(1.19)로 옮겨
갔는데 띠만 `depthNow(bx9, 0)` 으로 재고 있었다. 그 판의 몸통 키는 1.9·depthNow(0,1) 만큼
커졌으므로 띠·창이 제 판 **뒤로** 깔려 통째로 사라졌다(그림에서 앞면 띠가 가운데에만 남는다).
덩이의 자리를 옮길 때는 **그 덩이에 얹힌 것들의 키 기준점도 함께** 옮겨라.
이 한 자리에서 **세 번** 물렸다: 앞면 띠·창 → 옥상 갈라짐 선("양옆 건물 위의 갈라짐 선
안 보임") → 옆면 배관. 다 `depthNow(±PX, 0)` 로 굳어 있던 값이다. 덩이를 옮기면
**그 덩이의 x 를 쓰는 키를 전부 훑어라**(배럭은 `depthNow(sx9 * PX, …)` 가 다섯 자리).
얹힌 것이 뒤 기둥 위면 그 기둥의 자리(VC9)로 — 벤트 한 벌이 그렇다.
⚠ `BLD_NORM` 에 손으로 눌러 둔 값(주석에 '상자 상한에 걸림' 따위)이 있으면 **재측정 값으로 덮지 마라** — 파일런의 1.905 가
그것이고, bld-norm 은 돌리기 전에도 2.255 를 냈다(곧 그 차는 회전 탓이 아니다).

## 층 셋 — GL 캔버스는 베끼지 않고 그대로 화면에 선다
★★ **매 장 전체 화면을 한 번 베끼던 drawImage 를 걷었다**(2026-09, 요청: 다음 손 1 "합성 걷기") — 여태 GL 은
display:none 뒷캔버스에 그리고 붓이 flush 뒤 `drawImage` 로 유닛 캔버스에 **통째로** 베꼈다(폰 DPR 3 이면 한 장에
천만 화소 · 실기에서 GPU→합성 한 번 더). 이제 캔버스 셋이 DOM 차례로 쌓여 브라우저가 합성한다:
  ① 유닛 캔버스(아래 — 크립 얼룩·접지 타원·링·체력바 = 2D 가 몸 **앞에** 그리던 것) ② GL 캔버스(몸·번짐·연기)
  ③ 효과 캔버스 `.scr-motion-fx9`(위 — 트레이서·피격·캐스트 = `paintFxList9`). `#gl=0` 이면 ①만 있다.
· 세 장이 **같은 클래스**(`scr-motion-unitlayer`)라 크기·손짓 변환·감춤 규칙을 함께 받는다 — 변환을 거는 자리는
  `unitCanvases9(root)` 로 셋을 다 돈다(하나만 돌리면 손짓 중 몸과 효과가 갈린다). CSS 는 둘에 `background: transparent` 뿐.
· ⚠ **`#glblit=0`(perf-check 기본)의 뜻이 바뀌었다** — 옛날엔 '합성만 뺌'이었고 지금은 **GL 층을 display:none 으로 숨긴다**
  (문맥·그리기는 그대로 돈다). 헤드리스 SwiftShader 는 보이는 WebGL 캔버스를 합성할 때 ReadPixels 로 서서 한 장이
  3초가 된다(실측 p50 3117ms) — 옛 drawImage 가 치르던 값이 브라우저 합성기로 옮겨 갔을 뿐이라, 실기 GPU 에 없는 값을
  계측에서만 뺀다. 그림을 보는 자(scene-sheet·doc-sheet·`--shot`)는 켜 둔다(perf-check `--glblit`).
· 다크 스웜은 이 층 안에서 **스텐실**로 판다(위 '연기가 무엇을 가리고…').
· ★ **도록 아이콘 판은 칸 사이를 띄운다**(2026-09, 지적: "주변의 라이트(넥서스 보석) 효과가 다른 곳에 영향을 주고 있는 거
  같아") — 한 GL 판에 칸을 붙여 모아 굽으면 이웃 칸의 **번짐**(켠 창·보석의 블룸 — 1/4 판 반지름 BR9 = 화면 40px 남짓)과
  칸 밖으로 삐져나온 겹판(굴뚝 위로 오른 가스 덩이)이 옆·위 칸 자리로 새어 들어 그 칸을 찍을 때 **남의 빛이 얼룩**으로 실린다
  (실측: 도록 시트 리파이너리 칸 아래의 초록 반원 = 아래 칸의 덩이). `glIconFlush9` 의 `GAP9`(64px)가 그 틈이다.
  지도에서 같은 자리는 번짐 자체다 — 번짐은 '빛 상자 합집합 안의 몸을 깊이로 한 번 더 그리기'라 늘 켜진 빛(보석·캐리어 창)이
  흩어져 있으면 매 장 화면 전체를 두 번 그린다(다음 손 MRT 가 그 패스를 없앤다).
· 값: scene-sheet 테란 1M 화소 중 216 차이(옛 합성 길과 같은 그림) · 헤드리스 PC 프로필 p50 67 → 50ms(합성 몫이 빠진 만큼).

## 번짐은 둘째 기하 패스가 아니라 본 패스의 둘째 출력이다(MRT)
★★ **WebGL2 에서 몸은 캔버스가 아니라 우리 MSAA FBO 에 그린다**(2026-09, 다음 손 3 · 지적: "라이트 블루밍 추가 후 느려진 거
같고") — 옛 번짐은 '빛 상자 합집합 안의 몸을 1/4 깊이 판에 한 번 더 그리고 빛 낯을 또 그리는' **둘째 기하 패스**였다. 늘 켜진
보석·창이 흩어져 있으면 그 상자가 곧 화면이라 매 장 화면 전체를 두 번 그렸다. 이제 본 패스가 색(출력 0)과 **빛 판**(출력 1)을
함께 내고(`gl9.mrtEnsure` · 정점 `vEm` = 음영 없는 제 색 + 빛 표식 · FS 의 `emitColor`), 번짐은 그 판을 1/4 로 내려 흐려 더할
뿐이다. 색은 `blitFramebuffer` 로 캔버스에 옮긴다(MSAA 풀기).
· **가림은 본 깊이 그대로다** — 빛이 아닌 낯도 빛 판에 `(0,0,0,알파)` 를 쓰므로(합성 (ONE, 1−a)) 앞에 선 몸이 뒤 빛을 알파만큼
  덮는다. 그래서 빛 판의 **알파는 못 쓴다**(가린 몸의 알파가 쌓인 값) — 화면에 더할 때 알파를 밝기(max rgb)로 낸다(fx 셰이더 `uDown` −1).
· **셰이더는 GLSL ES 3.00 으로 올려 쓴다** — 두 출력은 ES 1.00 의 gl_FragColor 로 못 낸다. 원본(ES 1.00)을 글자로 바꾼다(`es3()`:
  attribute/varying → in/out · texture2D → texture · gl_FragColor → fragColor · `//__EMIT__` 자리 = 빛 판 한 줄). WebGL1 은 원본
  그대로 옛 길(둘째 기하 패스)이다.
· **캔버스 AA 는 끈다**(WebGL2 문맥 `antialias: false`) — MSAA → MSAA 옮기기는 막혀 있어 AA 는 FBO(표본 4)가 낸다. 번짐이 꺼진
  단도 색 하나짜리 FBO 를 거친다(AA 를 지키려고).
· ⚠ **도록 아이콘 문맥은 MRT 를 안 쓴다**(`new GlUnits9(…, mrtOk = false)`) — 4096² 판에 MSAA 두 장이면 수백 MB 다. 캔버스 AA 그대로.
· ⚠ **헤드리스는 이 길을 못 잰다** — SwiftShader 는 FBO 에 그리고 옮기는 값이 커서 p50 50 → 67ms · p95 1.5초로 튄다. perf-check 는
  기본으로 `glmrt=0`(옛 길)을 붙인다(`--mrt` 로 켬) — 번짐(glbloom=0)·합성(glblit=0)과 같은 까닭이다. 견줌: `#glmrt=0`.
· 값: 프로토스 장면 시트가 옛 길과 286px 차이(가스 위상·번짐 알파) · 스텐실 스웜 검사 그대로 통과 · 둘째 기하 패스 0.
· ⚠ **내려 찍기는 4×4 상자다**(`uDown` 1 — 2×2 선형 넷) — 온 크기 빛 판에서 1/4 로 갈 때 다섯 점만 읽으면 가는 빛줄(낮은 배율의
  창띠)이 점 사이로 빠져 깜빡인다.

## 피격 파편은 알약이 아니다 — 결마다 제 꼴이 있다
★ **둥근 끝 획 넷은 캡슐로 읽힌다**(2026-09, 지적: "이 효과들 형태가 알약 형태라 부자연스럽지? 실제 효과는 안 그럴 거 아니야
자연에서" → "얘는 파편이 아니라 불티일걸") — splash 없는 `hit` 의 낱개가 `lineCap round` 획(폭 고름·양 끝 반원)이라 어느 결이든
알약 넷이 나란히 섰다. 자연의 파편은 **머리와 꼬리가 다르고** 결마다 꼴이 다르다(paintFxList9 의 그 자리):
· **피(bio·zerg) = 물방울** — 둥근 머리(반지름 0.034~0.05·wb9) + 뒤로 가늘어지는 꼬리(점) · 머리 곁에 잔 튐 점 하나.
· **기계(mech) = 불티** — 물감이 아니라 **빛**이다: `lighter` 로 얹고 셋을 지난다(번짐 deep 0.3 · 꼬리 바늘 core 0.9 · 머리 점 flash 1).
  바늘은 아주 가늘고(0.012~0.018·wb9) 꼬리가 길다(d0 ×0.55). 처음 0.022 로 뒀더니 **당근**이었다.
· **프로토스(toss) = 플라즈마 조각** — 양 끝이 뾰족한 얇은 조각, 가장 넓은 자리를 낱개마다 비스듬히.
· 낱개마다 크기(kz 0.75~1.25)·길이를 흔든다 — 같은 꼴 넷이 나란히 서면 그것도 알약이다. 색별 한 경로에 몰아 채우는 짜임(삯)은 그대로.
· 🔎 `$S/vec2.mjs` 의 hit 줄(결 넷 × 갈래) · `doc-sheet --anim --kinds gunner --times 0.24,0.29,0.34`(곰돌이의 피).
· **죽음 파편(burst)의 기계 막대**도 같은 병이었다 — 둥근 끝 획을 걷고 한쪽이 가는 각진 네모(뜯긴 관·꺾인 부품)로 채운다.
  살점 타원·쇳조각 네모·유리 조각은 원래 채움이라 그대로.

## 효과 캔버스는 심이 없을 때만 선다
★ **GL 심이 효과를 맡으면 `.scr-motion-fx9` 는 늘 빈 장이다**(2026-09) — 폰 DPR 3 이면 캔버스 한 장이 수 MB 라 아예 안 세운다.
`UnitLayer` 의 `fxCvOn9` 가 붓의 첫 장에서 `gl9 && !gl9.vecOk`(심 못 열음 · `#glvec=0`)를 보고서야 켜진다(그 한 장은 유닛 캔버스에
그린다). 🔎 `perf-check --dockprobe` 의 `[캔버스]` 줄 — 심이 있으면 unitlayer 2장(유닛·GL), `--hash glvec=0` 이면 3장.

## 효과(트레이서·피격·캐스트)도 GL 이 그린다 — 붓은 그대로, 받는 쪽이 심이다
★★ **효과 붓(paintFxList9)은 캔버스 2D 를 말하고, `glctx9.GlCtx9` 가 그 말을 삼각형으로 옮긴다**(2026-09, 다음 손 2
"링·체력바·효과 GL로" 의 뒷 단) — 붓 코드 수천 줄(줄기·파편·구름·캐스트·상처)을 다시 쓰지 않는다. 심은 캔버스 2D 의 **부분집합**
(경로·채움·획·그러데이션·drawImage·합성 source-over/lighter·변환·대시)을 받아 `VecSink9.vert(합성, 텍스처, x, y, u, v, 미리곱 rgba)` 로
gl9 에 민다. gl9 는 그것을 합성·텍스처가 갈릴 때만 나눠(`vecRuns`) 몸·연기 **다음**에 깊이 없이 그린다(`vecPass`) — MRT 판 안이라
MSAA 를 받고 옮기기가 한 번이다. 효과 캔버스(`.scr-motion-fx9`)는 GL 이 없거나 심을 못 열 때(`vecOk` 거짓 · `#glvec=0`)만 쓴다.
· **차례가 값이다**: 심이 모은 꼭짓점을 `flush` 가 밀므로 붓은 **flush 앞에서** `paintFxList9` 를 불러야 한다(캔버스 길은 종전대로 뒤).
· **스프라이트 판은 표식으로 올린다**(`sprVer9` → `__ver9`): 한 번 그리고 안 바꾸는 판(domSpr9·woundSpr9·fxModelCv9)은 그 값이 같으면
  텍스처를 다시 안 올린다. 표식이 없는 판은 프레임마다 올린다(바뀌는 판이라고 본다). 새 스프라이트 캐시를 만들면 찍어라.
· ⚠⚠ **그러데이션은 만든 순간이 아니라 칠하는 순간의 변환을 탄다**(캔버스 규약) — 처음엔 만들 때 장치 좌표로 굳혔더니 `createRadialGradient
  (0, 0, …)` 뒤에 `translate` 하는 스플래시가 **화면 (0, 0) 의 쐐기**로 떴다. 이제 사용자 좌표로 들고 `fill/stroke` 때 CTM 과 그 역을 붙여
  (`Grad9.dev`) 색을 **장치 점 → 사용자 점**으로 되돌려 잰다 — 비균일 배수(scale(1, 0.4))의 타원 그러데이션도 그대로다.
· ⚠⚠ **획은 겹치지 않는 기하여야 한다** — 마디 네모 + 이음 원판을 얹으면 겹친 자리가 lighter 에서 두 배로 밝다(실측: 파편 획 양 끝의 붉은
  점). 안쪽 모서리는 두 마디가 안 이음점(이등분·hw/cos 반각)을 나눠 쓰고, 바깥쪽은 이음 조각(둥근 부채꼴·빗면·뾰족)만, 끝은 반원만 얹는다.
· **3단 이상 선형 그러데이션 채움은 멈춤선으로 다각형을 띠로 자른다**(`splitPolyAtStops` · 서덜랜드-호지먼) — 꼭짓점 사이를 곧게 보간하므로
  한 띠 안에서만 정확하다(실드 돔의 아래쪽이 비었던 자리). 방사는 종전대로 고리(`fillRadial`)다.
· **Path2D 는 속을 못 읽는다** — 붓이 미리 짜는 경로(실드 돔)는 `PathRec9`(명령을 적어 두는 것)로 짓고 `fillPath9(ctx, p)` 로 채운다: 심이면
  되풀이, 캔버스면 Path2D(한 번 지어 둔다). 새로 `new Path2D()` 를 효과 붓에 넣지 마라 — 심에서는 조용히 안 그려진다.
· 안 받는 것: `clip`·`filter`·글자 — 붓은 그것들을 오프스크린 판에서만 쓴다(nsmoke 의 blur 는 스프라이트 안이다).
· **다크 스웜도 MRT 판 안이다** — 깊이 판을 `DEPTH24_STENCIL8` 로 두고 `swarmPass(…, fb)` 가 그 판에 스텐실을 판다. 그동안 빛 판은 뺀다
  (`drawBuffers([ATT0])` — 안 빼면 출력 1 에 쓰레기가 남는다).
· ★ **번짐 합성은 더하기가 아니라 최댓값**(지적: "아직도 블룸 효과끼리 서로 영향을 주는 거 같아" → 소환구 둘의 후광이 겹치는 자리) —
  `blendEquation(MAX)`(WebGL1 은 `EXT_blend_minmax` 가 있을 때만 · 없으면 종전 더하기). 겹친 후광이 더 밝아지지 않고, 이미 켠 창 위에
  이웃 빛이 또 얹히지 않는다. 그린 뒤 `FUNC_ADD` 로 되돌린다.
· 🔎 견줌 하네스 `$S/vec2.mjs` — FxOp 스물넷(모든 갈래의 beam/shot + hit + 스플래시·분출·가시·실드·폭발·상처·캐스트·대시 선)을 2D 캔버스와
  심에 그려 화소를 견주고 셋째 판에 차이 마스크(붉음 색차 · 초록 있고 없음)를 낸다. `HASH=glmrt=0` · `HASH=gl2=0` 으로 길을 바꾼다.
  실측: 차이 6.4k/630k 화소(가장자리 AA뿐) · 잉크 224.8k vs 228.4k · 붓 한 장 심 3.4ms vs 캔버스 1.3ms(240 op · 헤드리스 CPU).
  `$S/vec1.mjs` 는 그리기 명령 여덟(원·방사·획·선형·lighter·변환 네모·drawImage·타원/베지에)의 낱 견줌이다.
· 진단: `#diag=draw` GL 줄의 `효과△ N`(삼각 수 · `gl9.stat.fxTris`) · perf-check 의 `[GL]` 줄(--shot 에서도 찍는다).
· ⚠ `scene-sheet`/`perf-check` 의 `readBuffer: invalid read buffer` 경고 한 줄은 이 손질 **전부터** 있던 것이다(옛 번들로도 난다).

## 바닥 도형(접지 타원·링·체력바·저배율 마커)은 GL 인스턴스 사각이다
★ **유닛 캔버스의 2D 도형을 한 드로 콜로 옮겼다**(2026-09, 다음 손 2 "링·체력바·효과 GL로" 의 앞 단) — `gl9.prim(kind, x, y,
rx, ry, 색, 알파, 선굵기, 전단)`: 0 네모 · 1 타원 채움 · 2 타원 테. 꼴은 조각 셰이더가 거리(SDF)로 낸다(PR_VS/PR_FS · 기록 12 float ·
`primPass` 가 몸보다 먼저 깊이 없이 그린다 = 유닛 캔버스가 그리던 차례 그대로). 붓은 `gl9.primOk` 면 prim, 아니면 종전 캔버스다
(인스턴싱 없는 문맥·`#gl=0`). 체력바는 `hpBarRects9(op, …, put)` 한 함수가 네모를 내고 캔버스(drawHpBar)·GL 이 같은 것을 받는다.
· 옮긴 자리 여섯: 저배율 네모·원 마커 · 뜬 건물 그림자 타원(다각형 발자국은 캔버스) · 공중/부양 그림자 타원 · 접지 타원 ·
  선택 링(입체 시점 밀림은 shear) · 상태 오라 · 체력바(바탕 + 줄 + 금).
· 타원 테의 AA 는 **기울기로 잰 거리**다(`(d−1)·d / |q/R|`) — 반지름 하나로 재면 납작한 타원(0.31)의 긴 변 테가 두 배로 흐려진다.
· ⚠ 바닥 도형 프로그램의 FS 도 빛 판(emitColor)에 0 을 써야 한다(`//__EMIT0__`) — MRT 판에 그리면서 출력 1 을 안 쓰면 쓰레기가 남는다.
· ⚠ 속성 번호가 본 프로그램의 것과 겹칠 수 있다 — 그린 뒤 divisor 를 0 으로 되돌린다(번짐 사각과 같은 자리).
· 값: 프로토스 장면 시트 7px 차이(가장자리 AA) · 진단 `#diag=draw` GL 줄의 `바닥 N`.

## 조금이라도 든 몸은 그린다
★ **가장자리 걸러내기 셋을 넓혔다**(2026-09, 지적: "화면에 조금이라도 들어온 모델은 그려야 하는데 안 그리는 거 같아") —
① 붓의 화면 밖 판정이 발자국(또는 sizePx) 한 벌 + 24px 였다: 몸은 발자국 위로 그 폭만큼 솟고 나는 몸은 뜬 높이만큼 위라, 앵커가
가장자리 밖으로 그만큼 나가면 보이는 윗몸까지 빠졌다 → 두 배 + 뜬 몫(airPx) + 64px. ② 워커 시야 여유가 폰에서 반 화면이라 16배에서
넉 타일 건물 한 채보다 좁았다 → **여덟 타일 아래로는 안 내려간다**. ③ 효과(폭풍·핵)의 여유 60px 고정 → 크기(size·len)를 더한다.

## 붓은 WebGL 2 이고 드로 콜은 개체가 아니라 메시 무리마다다
★★ **개체별 값은 유니폼이 아니라 인스턴스 속성이다**(2026-09, 요청: "gl2로 진행") — 옛 붓은 개체마다 `bind(mesh) · place(it)`(유니폼
여남은 개) · `drawArrays` 를 놓아 난전 한 장이 **드로 콜 2천 번**이었다(890기 실측: 그림자 + 몸 + 꼬리 = 1800↑). 이제 개체의
자리·요잉·배수·알파·깊이 칸·임자색·단색·그림자를 **인스턴스 기록**(22 float · 88바이트 — `aInA·aInB·aInC·aTeamC·aSolidC·aShad`,
divisor 1)에 싣고 같은 메시의 개체를 `drawArraysInstanced` 한 번으로 그린다. 실측: **890기 → 드로 콜 86**.
· **문맥은 WebGL 2 를 먼저 연다**(`gl2` 필드 · `#gl2=0` 이면 1) — 셰이더는 GLSL ES 1.00 한 벌이라 두 문맥이 같다. 1 로 떨어지면
  `ANGLE_instanced_arrays` 로 같은 길을 가고, 그것도 없으면(`#glinst=0` 도) **상수 속성**(`vertexAttrib4f`)으로 개체마다 하나씩
  그린다(`constInst`) — 셰이더·기록은 그대로다. 세 길의 그림이 **화소까지 같다**(imgdiff 0).
· **패스 넷**: ① 그림자(메시 무리 · 못 박은 깊이라 차례 무관) ② 불투명 몸(메시 무리 · 깊이가 가른다) + 그 **반투명 꼬리**(nSolid
  뒤 부품 · 메시 무리 · 깊이 쓰기만 끔) ③ 차례 몫(큐 차례 그대로 — 알파 < 1 개체·단색·발광·더하기; 같은 메시·상태가 잇달으면
  한 묶음) ④ 번짐(깊이 → 빛나는 낯 · 메시 무리).
  · ⚠ 꼬리를 큐 차례로 그리면 무리가 안 선다(실측: 760 드로 콜 — 개체마다 메시가 갈린다). 메시 무리로 두면 딴 개체의 꼬리끼리
    겹치는 자리의 섞는 차례만 갈린다(scene-sheet 테란 1M 화소 중 214 — 컴샛 접시 한 자리). 받아들였다.
· **결 옥타브 몫은 정점 셰이더가 k 에서 낸다**(옛 CPU `grainOct9` · `uGrain·uGrainA`) — varying(vG0·vGa·vGr.w)으로 화소에
  넘긴다. 개체 안에서 상수라 보간해도 그대로다.
· ⚠ 정점 속성이 14(메시 8 + 인스턴스 6)라 `MAX_VERTEX_ATTRIBS < 14` 면 붓을 못 연다(GL 없음으로 떨어진다 — 사실상 없는 기기).
· ⚠ 번짐 사각(`fxQuad`)의 속성 번호가 인스턴스 속성과 겹칠 수 있어 그 자리에서 divisor 를 0 으로 되돌린다.
· ⚠ 헤드리스(SwiftShader)는 드로 콜을 못 잰다 — 소프트웨어 래스터가 지배라 p50 이 노이즈(±16.7ms) 안에서 같았다. 이 손질의
  값은 실기 GPU 의 드라이버 몫(드로 콜당 수십 µs × 2천)이다. 진단은 `#diag=draw` GL 줄의 `on2 … 드로 N`(`gl9.stat.draws`).
· MRT 로 번짐의 둘째 기하 패스는 없앴다(위 절). 남은 다음 손: 링·체력바·효과를 GL 로 · UBO.

## 덧칠을 얹을 몸은 '마지막 몸'이 아니라 '그 그늘과 겹치는 몸'이다
★ **mesh9 의 둘째 자(byPid)를 겹침으로 고른다**(2026-09 · '그 자는 따로 고칠 일'로 남겨 둔 자리) — 한 뭉치(pid)에 몸이
여럿이면(기둥 + 뚜껑 · 벽 여러 장 · 관 토막) 제 경로를 가진 그늘은 목록의 **마지막 몸**에 접히고 있었다. 이제 그 뭉치의
몸들 중 그늘 상자와 **가장 많이 겹치는 것**(`overlapK9` — 교집합 ÷ 그늘 넓이)에 접고, 같으면 나중 것(옛 규약)이다.
· 실측: 160종에서 얹힐 몸이 바뀐 그늘 **약 600장**(시타델 52 · 팩토리 33 · 스파이어 26 · 사일로 23 · 프로브 21 …)인데
  GL 시트 16종 × 두 각의 화소차는 **403** 뿐이다(가디언 날개 한 장의 음영) — 바뀐 것은 대개 구 위의 작은 광(0.12~0.7 반지름)
  이라 어느 몸에 접혀도 넓이 몫이 같다. **값은 정확함이지 그림이 아니다** — 앞으로 '얹힐 몸의 절반보다 작을 것' 같은
  크기 견줌을 넣을 때 그 기준이 이제 옳다.
· 🔎 `node scripts/model-mesh.mjs [--kinds k] --dump` 의 `↪ 얹힐 몸 바뀜` 줄(`MESH_DBG9` — 도구만 목록을 모은다).

## 메시 층·WebGL 시제(2026-09)
- **메시 층**: 도형 헬퍼(polyPath3·관·뿔·돔·구·원통·원반·고리·곡면판, bake9.rodFaces)가 `MESH9.on` 일 때 면의 경로 문자열을 열쇠로
  **판 모형 공간 3D 폴리곤**(`Poly3`, 모델 배율·이동·회전은 먹고 요잉·카메라는 안 먹음)을 곁표 `MESH9.byD` 에 적는다. 2D 그림은
  한 톨도 안 바뀐다(`model-faces-snap --diff` 허용 0.001 로 지킨다). `mesh9.collectMesh9(builder)` 가 빌더 하나를 요잉 0 으로 굽고
  음영 덧칠 면(얕은 알파의 흰·검·종족 광택색, `isOverlay9`)을 뺀 부품 메시를 낸다. 검사·미리보기: `node scripts/model-mesh.mjs
  [--kinds a,b] [--svg out.html --rots 0,45,90,180]`(덮임 154종 100%; 미리보기는 html 캔버스 — svg 는 수천 면에서 스크린샷이 멎는다).
  새 도형 헬퍼를 만들면 `meshPut9(d, polys)` 를 함께 적는다 — 안 적으면 그 부품이 GPU 그림에서 빠진다(덮임 표가 잡는다).
  화면 자로 얹던 **광·무늬 타원**은 `shinePath3(cx, cy, cz, r, ox, oy, rr, ry?)` 로 놓는다 — 2D 경로(screenCircle·
  groundEllipse)는 종전 그대로 내면서 3D 로는 **구 표면에 붙는 고리**를 적는다(구·오브의 흰 광 · 아가리 · 눈).
- **숫자 경로**: polyPath3 가 화면 2D 좌표를 `POLY2` 에 적고 `bake9.pathOf` 가 그 숫자로 Path2D 를 짓는다(문자열 파싱 생략, 판당 약 20%).
- **WebGL 붓(어느 기기에서나 기본 켬 · `#gl=0` 이면 면을 곧장 그리는 느린 폴백)**(`gl9.ts`): 유닛·건물 몸을 **판 없이** 메시로 GPU 가 그린다.
  메시는 열쇠별 한 벌(유닛 `u:종류:자세:lod`, 건물 `b:종류:건설단계:포탑각:불빛:회전:lod`), 상한은 기기 표(`DEV9.glMeshMax`: PC 600 · 폰 240). 정점 셰이더가 `project` 와
  같은 카메라(평면 sin40/cos40 · 입체 pitchFlatNow·0.7/0.9/앞숙임 0.34/시각 밀림 tan(vq), `camOf9`)를 걸고, 자리·배수는 판 블릿과 같은 자다
  (유닛 앵커 (sx, sy−px·0.24−lift)·px/16·MODEL_NORM·원점 줄 12/12.6 · 건물 x 상자 가운데, 잉크 바닥을 바닥선−띄움에). 판이 주던 잉크 상자
  (그림자·링·체력바·BLD_INK_BOX)는 `footOf`(메시 꼭짓점을 카메라·요잉으로 돌려 잰 상자)가 같은 자로 준다.
  · **메시 기록 중(MESH9.on)엔 빌더의 시점 가지가 다르다**: `faceLight().visible` 은 늘 참(뒷면도 낸다), `facingRatio()` 는 1(마주 볼 때만 그리는
    장식도 다 든다) — 2D 굽기에는 안 걸린다(스냅샷 동일).
  · 색: 고정색은 tone9 를 지나고, 같은 경로 위의 흰·검 덧칠(topFace·sideFace·faceLight)은 정점의 aOv 로 접는다(mesh9.collectMesh9).
    접을 때 **2D 와 같은 알파 보정**(shadeBoost, 반투명 색면 ×1.25·상한 0.7)을 태우고 셰이더도 그 세기를 그대로 쓴다 — 0.7 배로 눌렀던 것을
    걷었다(그만큼 모델이 납작했다). 제 꼴을 가진 그늘을 몸에 접을 때는(같은 부품의 마지막 몸) **상자 넓이 비만큼만** 먹인다 — 통째로
    먹이면 작은 부품이 반토막으로 어두워졌다(실측: 일꾼이 든 미네랄 밝기비 0.53 → 0.86).
  · 음영은 셋이다: ① 면 덧칠(aOv, 위) ② 실루엣 빛(silhouetteLight 식, 개체 상자 안의 화면 기울기) ③ **방향광**(2026-09, 2D 에 없던 몫) —
    반쪽 램버트(**0.82 + 0.26**·(0.5+0.5·n·L))에 카메라를 스치는 낯의 **테두리 빛**(+0.10·(1−|n·cam|)⁴)을 얹는다. 효과(uFlat)는 셋 다 안 탄다.
    ★ **폭을 한 번 좁혔다**(2026-09, 지적: "정면으로 빛을 받을수록 환해지는 게 정도가 너무 심한 거 같기도 — 정면 아닌
      부분은 좀더 밝히고 정면은 좀 낮춰 보자") — 방향광과 윤기(광택)가 **둘 다 n·L 로 같은 방향**을 가리켜, 빛을 마주 본
      낯에서 두 몫이 겹쳐 쌓였다. 0.78~1.10 → **0.82~1.08**(등진 낯은 올라가고 마주 본 낯은 내려간다) + 윤기에 바닥 0.10.
      ⚠⚠ (**폐기된 자**) 여기 적혀 있던 '0.5 선'은 옛 gl-check 의 2D 견줌 값이었다 — 0.86~1.06 까지 좁혔더니
        diamond 가 0.501 로 그 선을 넘었다는 기록이다. **그 선은 이제 안 지킨다**(아래 '전수조사'의 ★★):
        2D 는 대비가 더 세므로 GL 이 좋아질수록 그 수는 오르고, 그 선을 지키려는 것이 곧 **없어진 붓에 맞춰
        새 붓을 깎는 일**이었다. 빛을 좁힐지 넓힐지는 **눈으로** 고른다(`model-gl`).
        다만 값 자체(0.82~1.08 · 윤기 바닥 0.10)는 눈으로도 골랐던 값이라 그대로 둔다.
  · 앞뒤: 깊이 칸은 **화면 겹침**으로 나눈다(개체마다 나누면 400기에서 칸이 너무 얇다) + 칸 안은 카메라 가까움 + 부품 차례(aOrd) 편향.
    ★ **개체 사이는 칸이, 개체 안은 칸 안이 가른다**(2026-09, 지적: "모델끼리 겹쳐질 때 각 부품이 별개로 앞뒤가 판단되어서
      정신없이 바뀐다 — 모델 안에서는 각 부품의 값을 비교하지만 모델 간에는 하나로 정해서 비교해야") — 그 지적이 이 자의 규약
      그대로다. `near` 는 **모델 제 좌표**라 어느 개체가 앞인지를 모른다. 곧 겹친 두 개체가 **같은 칸**에 들면 두 모델의 부품이
      서로 끼어들고, 그 짝이 프레임마다 바뀌니 흔들려 보인다.
      옛 셈은 x 로 정렬해 한 번 훑고 **완화를 두 번** 더 도는 꼴이었는데, 이것은 겹침 그래프의 **가장 긴 사슬**을 푸는 일이라
      난전처럼 사슬이 길면 수렴하지 않았다(실측 886기 한 장: 칸 75 → 제대로 풀면 **146** — 절반이 덜 갈려 있었다).
      이제 **화가 차례(i 오름차순)** 로 한 번만 돈다: 나보다 먼저 칠하는(j < i) 겹친 개체의 칸 + 1. j < i 는 이미 확정이라
      한 번에 정확하다(DAG 의 가장 긴 경로). 훑기도 셋에서 하나로 줄었다.
      ⚠ 칸이 늘면 칸 폭(2/(M+1))이 좁아지므로 상한을 둔다(`SLOT_MAX9` — 24비트 깊이 4096 · 16비트 512).
    ★ **데칼은 작아야 데칼이다**(2026-09, 지적: "배럭 큰 세 개 건물 사이 두 개 건물이 반투명하게 보이고 가려져야 할
      부분도 안 가려지는 현상") — `isDecal` 이 `if (p.alpha < 0.98) return true;` 로 **반투명이면 크기를 안 묻고** 있었다.
      그런데 `frustumFaces3` 는 벽마다 제 음영 덮개를 **한 장짜리 반투명 면**으로 낸다 — 배럭 판의 벽 덮개(대각선
      5.5칸)가 통째로 데칼로 잡혀 **1.3 모델칸** 앞으로 끌려 나왔고, 그러면 뒤 판의 덮개가 앞 판을 이기고 그 위에
      그려져 앞 판이 비쳐 보이고 가려야 할 것을 못 가린다. **몸은 멀쩡히 불투명으로 있는데도** 그렇다(실측: 배럭의
      불투명 몸 #373 폴리 5 · 합친 경로 그대로 있다). 크기 문(대각선 < 2.8)을 반투명에도 똑같이 건다 — 돔 뒤의
      초승달 그늘 같은 진짜 데칼은 작아서 그대로 남는다(실측: 18종이 좋아지고 3종이 0.005 안쪽으로 나빠졌다).
    **데칼**(한 장짜리 작은 부품: 줄무늬·창·환풍구)의 깊이 편향(**유닛 0.18 · 건물 0.25 모델칸**)을 **정점(aOrd)에** 얹는다.
    편향을 그리기 단위로 주면 같은 구간의 다른 면까지 끌려 나온다. 구간은 둘뿐이다: 불투명 | 반투명(깊이 안 씀).
    ★ **편향은 자리를 옮기는 자가 아니라 무승부를 가르는 자다**(2026-09, 지적: "배럭 건물 옆면의 띠가 가려져야 하는데
      안 가려지네") — 옛 값 1.3(유닛 0.8)은 **벽 한 장 두께보다 크다**. 배럭 옆면 띠(`bandY9`)는 벽 **표면에** 놓인 한
      장인데 그만큼 앞으로 끌려 나와 **이웃 판을 넘어** 그려졌다. 큰 값의 근거였던 '2D 는 데칼을 벽 안쪽 0.5칸쯤에
      그려 둔다'는 **메시에는 해당이 없다** — 메시가 적는 3D 자리는 진짜 자리(벽 표면)다.
      훑어 재 보면 값도 못 벌고 있었다: gl-check 154종 평균이 편향 **1.3 → 0.6 → 0.3 → 0.12 → 0** 에서
      **0.190 → 0.189 → 0.188 → 0.188 → 0.189**. 0.12~0.3 이 바닥이고 1.3 은 그냥 과했다(0 은 같은 평면의 데칼이
      z 싸움을 해 깜빡인다 — 그 몫만 남긴다). 내린 뒤 41종이 좋아지고 20종이 나빠졌다(평균 0.190 → 0.188).
      **새 편향 값을 고를 때는 `#glbias=N` 으로 훑어 재라** — 짐작으로 키우면 데칼이 이웃 부품을 넘는다.
  · ★★ **제 경로를 가진 작은 그늘은 접지 말고 남긴다**(2026-09, 지적: "배럭 벤트의 가로 줄이 없어짐") —
    접기의 문이 `k9 < 0.6`(상자 넓이 비)이었는데 `areaK9` 는 **그 pid 의 마지막 몸**과 견주는 자라, 몸이
    작으면 1 이 나온다. 그래서 벤트의 가로 살처럼 **제 경로로 또렷이 그린 줄**이 판에 통째로 접혀
    '줄'이 아니라 '조금 어두운 판'이 됐다(실측: 배럭에서 경로가 다른데 k9 ≥ 0.6 으로 접힌 그늘 37장).
    가름의 자는 **경로 + 절대 크기**다: 몸과 같은 경로의 그늘(faceLight 낯 음영)은 낯 전체의 밝기이니
    접고, **제 경로를 가진 한 장짜리 작은 것**(3D 상자 지름 < 2.8 — gl9 `isDecal` 과 같은 문)은 남긴다.
    ⚠ 크기 문을 안 걸고 다 남기면 **판이 허옇게 뜬다**(실측 배럭 0.21 → 0.26) — 벽 한 장을 덮는 큰
      그늘은 데칼 자에 안 걸려 깊이 편향을 못 받고, 몸과 같은 평면이라 깊이 싸움에서 져 아예 안 그려진다.
      곧 **접어야만 보이는** 몫이다. ⚠ '몸의 절반보다 작을 것'을 더해 봤더니 줄이 도로 죽었다 —
      `byPid` 가 가리키는 것이 그 뭉치의 **마지막** 몸이라 견줌의 기준이 엉뚱하다(그 자는 따로 고칠 일).
    값: gl-check 평균 0.188 → 0.191(배럭 0.142 → 0.170 · 시즈 다리 0.471 → 0.360) — **표가 조금
    나빠지고 빠졌던 살창이 돌아온다**. 2D 에 있는 것이 GL 에 없던 자리라 그 쪽이 옳다.
  · ★★ **화면 자로 놓은 장식은 요잉을 안 탄다 — 모형 자로 옮겨라**(2026-09, 지적: "파일런 고리 위
    동그라미 위치 흐트러짐") — 파일런의 청록 띠 여섯은 `groundEllipse(cx + cos·R, cy + sin·R·0.45, …)`
    로 **화면 좌표에** 찍혀 있었다. 화면 자로 그린 원은 3D 기록이 없어 메시가 **가장 가까운 기록점의
    높이를 빌려** 되짚는데, 그 이웃이 갈고리나 수정이면 띠가 엉뚱한 높이에 흩어진다. −45도로 돌려
    세운 뒤 더 크게 드러났다(화면 자 장식은 요잉을 안 타는데 메시는 돈다).
    링과 같은 **모형 자리**(갈고리와 같은 (sinθ, cosθ) · 높이 PY_M)에 `discPath3` 로 놓고 앞뒤도 제
    자리 깊이로 준다 — 되찾을 일이 없고 돌려도 띠가 링에 붙어 함께 돈다.
    실측: **diamond 0.490 → 0.409**(0.5 선의 여유가 처음으로 넉넉해졌다 · 최악이 tankbarrel 로 바뀌었다).
    ★ 같은 꼴이 남아 있으면(화면 자로 더한 중심) 다 이 손이다 — **자리를 옮겨라**(아래 ★★ 뒤로는
      메워 주는 자가 아예 없으므로, 안 옮기면 그 면이 GL 에서 빠진다).
  · **덧칠 면은 몸에 접거나 · 제 부품으로 남기거나 · 버린다**(mesh9). 접을 몸을 두 자로 찾는다: ① 같은 경로 ② 같은 부품 번호(pid)의
    마지막 몸 면. **통째로 얹히는 덧칠은 반드시 접어야** 한다 — 제 부품으로 남기면 모델이 통째로 비쳐 보인다(실측: 보급고 103부품 중
    54개가 반투명이었다). 남기는 것은 둘뿐이다:
      · 몸보다 **작고**(상자 넓이 비 < 0.6) 제 기하가 있는 그늘 — 접으면 부품 전체가 고르게 어두워져 결이 뭉개진다.
      · 접을 몸이 **아예 없는** 데칼(제 뭉치에 몸 면이 없다 — 지붕 살창·둔덕 얼룩) — 단 불투명 몸의 **상자 안**에 들 때만
        (여유 = 상자 지름 12% + 0.4 · 상자는 앞잡이로 한 번 다 돌아 잰다). 안 보면 자리가 틀린 데칼이 몸 밖 허공에 뜬다
        (실측: 라바의 마디 구슬이 사슬로 떴다 · 셔틀 돔의 흰 무늬). 옛날엔 '높이를 빌려 지은 자리가 아닐 것'(GUESSED9)이
        한 문 더 있었는데, 빌리는 길 자체를 걷으며 함께 없앴다(아래 ★★).
  · **개구부(격납구)는 붓마다 다르게 낸다**(2026-09): 오목한 속은 화가 차례로만 서는 2D 가 낼 수 없는 꼴이다 — 속은 앞벽보다
    뒤·뒷벽보다 앞인데 두 벽이 한 프리미티브라 차례로 가를 수가 없다. 그래서 2D 는 속을 벽 **위**에 얹고 새는 몫을 덧댐판으로
    덮는다. GL 은 진짜 깊이가 있어 그 손이 필요 없고, 오히려 덧댐판이 속보다 앞에 서서 **속을 통째로 먹는다**. 그래서 빌더가
    `MESH9.on` 으로 갈라, GL 에서는 벽에 **진짜 구멍**을 뚫고 개구부 둘레만 벽으로 메우고 덧댐판을 하나도 안 댄다(2D 는 종전 그대로 —
    스냅샷이 안 바뀐다). 프리미티브의 손: `frustumFaces3`/`boxFaces3` 의 `omit`(평면 법선으로 벽 하나 빼기)·`noTop`(위에 같은 바닥의
    덩이가 앉는 3단 몸통의 **속살** 윗면 빼기) · `spirePillar` 의 `skipFace`(낯 한가운데를 받는 술어). 쓴 자리는 팩토리·커맨드 격납구다.
    ⚠ 구멍을 뚫으면 **덩이 속의 수평면**도 함께 베어야 한다 — 수평 원반은 같은 화면 자리에서 속벽보다 카메라에 가까워 구멍을
    통째로 메운다(팩토리는 절두체 윗면을 빼고, 커맨드는 받침 테 윗면을 개구부만 베어 낸 채 손수 깐다).
    ⚠⚠ **아래를 보는 뚜껑도 마찬가지다 — 눈에 안 보이면서 깊이만 쓴다**(2026-09, 지적: "격납구 경사로가
      테두리에 가려짐 — 테두리는 저 부분은 없어야 해"). 커맨드 받침 첫 단의 **밑 뚜껑**(반지름 5.15 원반)이
      선체 밑을 통째로 덮는데, 격납구 경사로는 그 밑을 지나 앞으로 나온다. 카메라는 늘 40도 위에서
      내려다보므로 그 원반의 **그림은 한 번도 안 보이지만**, GL 은 깊이를 쓰므로 경사로가 통째로 가렸다
      (2D 는 화가 차례라 안 보이던 자리다). 밑에서 올려다보는 시점이 없으니 GL 에서는 안 깐다.
      ★ **`skipFace` 는 옆 낯에만 걸린다** — 뚜껑(caps)은 그 문을 안 지나므로 늘 따로 막아야 한다.
      ★ 이런 '안 보이는데 가리는 것'은 **떼어 보는 실험**으로 가장 빨리 잡는다(한 줄 고쳐 굽고 되돌리기).
  · GL 캔버스(`.scr-motion-gl9`)는 **화면의 한 층**이다(2026-09 — 옛날엔 display:none 뒷캔버스를 매 장 유닛 캔버스에 drawImage 로
    베꼈다. 아래 '층 셋'). 캔버스가 맡는 것은
    크립 얼룩(creepPlate9)·맨 네모·그림자 타원·링·체력바·효과뿐이다. `#gl=0` 이나 WebGL 이 안 설 때만 붓이 면을 곧장 그린다(`SPRITE_PERF.direct`).
  · ★★ **면을 내는 자가 3D 를 함께 적는다 — '승격'이라는 단계는 없앴다**(2026-09, 요청: "우회로 없애고
    설계도도 짓는 것도 3D 로 통일해서 승격하는 과정 없앨 수 있어?" → "게이트로 검사하는 것과 승격 과정
    필요없게 설계부터 바로 100% 3D 로 해서 짓는 건 안 되는 이유가 있나? 2D 로 시작할 이유가 없어 보이는데").
    걷어낸 것: `mesh9.meshFromPath9`(경로 문자열을 글자로 도로 읽어 3D 를 지어 내던 자) ·
    `shapeOblique.unproject9` · 화면점→모형점 표 `PROJ9`(project 가 점마다 적던 몫) ·
    되찾기 면 수 기준선(`scripts/model-mesh-baseline.json`).
    · **세 걸음이 두 걸음이 되었다**: 옛 길은 '화면 자로 짓기 → 3D 로 승격 → 다시 2D 로 사영'이었고,
      지금은 '**3D 로 짓기 → 2D 로 사영**'이다. 사영은 우회로가 아니라 **출력**이다(캔버스 폴백 `#gl=0`·
      도록 SVG·model-shot·`model-gl --vs2d` 가 Path2D 를 먹는다) — 없앨 수 있는 것은 '2D 가 원본인 자리'뿐이었다.
    · **왜 2D 로 시작했었나**: 설계가 아니라 내력이다. 캔버스가 유일한 붓이던 때 짠 코드고 GL 은 나중에 왔다.
    · **왜 위험했나**: 되찾기는 **경로만** 본다 — 반지름 둘이 2% 넘게 다르면 공을 땅에 누운 판때기로 읽었고
      (사이언스 베슬의 몸통이 그랬다), 관이 적어 둔 빈 표를 만나면 엉뚱한 낯을 구로 되살렸고, 한때는
      이웃의 높이를 빌려 장식을 딴 부품 위에 띄웠다(파일런 청록 띠 · 트리뷰널 돔의 광 · 탱크 옆 흰 구슬 ·
      셔틀 돔의 흰 무늬 — 네 번 물렸다).
    · **어떻게 0 으로 갔나**(786 → 0면). 순서가 값이다:
      ① **프리미티브부터** — `cylinderFaces3` 가 몸통만 적고 **윗면 타원은 화면 자**로 내고 있었다.
        그 한 줄이 299면 중 **195면**을 혼자 냈다. ★ 종류별로 뒤지지 말고 공용 헬퍼를 먼저 보라
        (`miss-sites.mjs` 가 짚어 준 줄이 죄다 그 헬퍼를 부르는 줄이면, 범인은 빌더가 아니라 안쪽이다).
      ② **새 3D 헬퍼로 갈아탄다** — 다 **경로 문자열이 글자까지 같고** 3D 만 더 적는다(2D 스냅샷 불변):
        `discPath3(x, y, z, r[, ryS][, seg])` 땅 원반(ryS 는 빌더가 눈으로 누른 **화면** 세로 반지름) ·
        `annulusPath3(x, y, z, ro, ri)` 땅 고리 · `orbPath3(x, y, z, r[, ryS])` 공 ·
        `billPath3(x, y, z, r)` **빌보드 원반**(늘 카메라를 본다 — 꼴이 없는 빛) ·
        `shinePath3` 구 표면의 광 · `shellMesh9`/`sphereSurf9` 구 껍질 위의 무늬.
      ③ **남은 손 면은 빌더가 직접 적는다** — 파일런 링(모형 자리는 PY_M 높이의 둥근 반고리) ·
        포지 돔의 띠 · 크립 얼룩의 테두리 · 스포어 아가리(기둥 축에 수직인 고리) · 패인 그릇의 초승달.
    · **관문은 이제 덮임 100% 하나다**(`node scripts/model-mesh.mjs --check`). 기준선 파일은 **없다** —
      빠진 면은 늘 0 이어야 하는 값이라 눌러 둘 자리가 아니다. 어느 줄이 3D 를 안 적었는지는
      **`node scripts/miss-sites.mjs`** 가 짚는다(화면 자 헬퍼가 SITE9 에 제 호출 줄을 적게 켜고
      빠진 면의 경로를 그 표에 묻는다 — 경로 숫자를 눈으로 뒤질 일이 없다).
    · ⚠ **새 도형 헬퍼를 만들면 `meshPut9(d, polys)` 를 함께 적어라** — 안 적으면 그 면은 되살아나지
      못하고 **그냥 빠진다**(옛날엔 되찾기가 어떻게든 메워 주었다). 그게 이 규약의 값이다: 틀린 기하로
      조용히 사는 대신, 빠져서 관문에 걸린다.
    · ⚠ **조각 수는 짝이 정한다** — `discPath3` 의 기본 12 는 원통 몸통 링(12)과 맞춘 값이다. 뚜껑만 잘게
      쪼개면 모서리를 두 낯이 안 나눠 써 '닫힌 입체' 판정(solidSigns9)이 깨진다. 짝이 없는 큰 땅 원반
      (핵 충격파·구름)에서만 `seg` 를 32 로 올린다 — 16 이면 둘레가 다각형으로 읽힌다.
    · 값: 덮임 156종 **74960/74960(100%)** · `model-faces-snap --diff` 어긋난 종류 **0/156**(2D 그림이
      한 톨도 안 바뀐다) · gl-check 평균 **0.190** 그대로(0.5 넘는 종류 0).
    ★ **빈 표는 '못 적었다'가 아니라 '적을 것이 없다'는 말이다**(2026-09, 지적: "탱크 오른쪽 좀 떨어진
      곳에 구슬이 있어 뭐지") — 관·막대 헬퍼(tubeFaces·rodFaces …)는 제 메시를 **첫 낯 하나에 몰아**
      적고 나머지 낯에는 빈 표(`meshPut9(d, [])`)를 적어 '이건 딴 낯이 낸다'고 표시한다. 셋을 가른다:
      **없음**(빠진 면 — 관문이 잡는다) · **빈 표**(그냥 둔다) · **있음**.
      ⚠ **그 빈 표가 남의 기하를 지울 수도 있다** — 곁표의 열쇠는 경로 문자열이라, 관이 제 둘째 낯에
        적는 빈 표가 **글자까지 같은 딴 부품의 경로**에 떨어지면 그 부품이 통째로 사라진다. 게다가 빈 표는
        덮임 셈의 분모에서 빠지므로 **표에도 안 걸린다**(실측: 핵 사일로의 지름 0.6 짜리 구 하나. 156종을 다
        훑어 이런 충돌은 그 하나뿐이었다 — 곧 경로 열쇠가 실제로 겹치는 일은 7만 면에 한 번꼴이다).
        meshPut9 가 **빈 표로는 있는 기하를 못 덮게** 막는다.
  · **덧칠 판정은 회색 계열만**(mesh9.isOverlay9): 얕은 알파(<0.4) + 휘도 끝(<0.2 · >0.8) + **채도 ≤ 0.25**. 채도 문턱이 없으면
    네온 몸(간헐천 가스 #80ff96, 휘도 0.86)이 덧칠로 접혀 사라졌다(실측: 간헐천 IoU 0.68 → 0.97 · 어사이밀레이터 0.73 → 0.95).
  · 빠진 부품 찾기: `node scripts/model-mesh.mjs --kinds <k> --dump` — 부품마다 색·알파·폴리·z 범위·접힌 덧칠 세기를 찍고,
    3D 가 없는 면의 경로를 `⚠ 빠진 면` 으로 낸다(줄 번호까지 짚으려면 `node scripts/miss-sites.mjs`).
    덮임 셈의 **분모는 3D 를 적어야 하는 낯만**이다: 접힌 덧칠(skipped)과 헬퍼가 **일부러 빈 표를 적어 둔 낯**
    (blank — rodFaces 는 관 하나를 첫 끝 낯에 몰아 적고 나머지에 빈 표를 적는다)은 뺀다.
    그것을 빠진 것으로 세면 표가 거짓으로 낮아지고 ⚠ 목록이 쓸모를 잃는다(실측: 배럭의 '빠진 면' 16 중 12 가 이것이었다).
  · **화면 효과 여섯(warpin·storm·nukeblast·nukecloud·archon·darchon)도 GL 이 맡는다**(`GL_CANVAS_KINDS9` 는 비었다). 발광 종류
    (`GL_GLOW_KINDS9`)의 규약: ① mesh9 glow — 반투명 흰·검 면은 덧칠이 아니라 **몸**(같은 경로에 몸이 있을 때만 접는다) ② 화면 원은
    구가 아니라 **카메라를 보는 원반**(빌보드, 정점 aBb — 요잉을 안 돌리고 가운데만 돌린다). ⚠ 그 판정을 mesh9 가
    경로를 보고 내리던 것은 걷었다 — **빌더가 `billPath3` 로 직접 놓는다**(소환구·아콘·다크아콘·게이트 보석) ③ 깊이 없이 **화가 차례**(불투명·반투명을
    안 가른다)·음영 없음(`flat`, uFlat) ④ 폭풍·핵은 **더하기 합성**(`add`, 2D 의 lighter). 폭풍·핵 폭발·핵 구름은 효과 op 이라 붓이
    GL 큐의 맨 뒤에 `glFxPush9`(판 fxModelCv9 의 fit 자와 같은 자: 모델 상자를 w×h 에 맞춰 가운데에)로 넣고 판은 안 굽는다(`glFx9`).
    효과 메시 열쇠 `f:종류:칸`. 탄두·섬광·연기(모델 아님)는 캔버스에 남는다.
  · **그림자**(2026-09): 몸은 꼭짓점을 **빛 방향으로 밀어 z 를 0 으로 눌러** 바닥에 눕힌다(정점 uShadow · 기울기 SHADOW_K9
    [0.5, −0.25] = −평면빛/높이빛). 실루엣이 그대로 바닥에 깔리므로 높은 부품이 멀리 눕는다. 알파는 `SHADOW_ALPHA9` 0.26 **한 겹**이다.
    ⚠ 무른 가장자리를 낸다고 같은 실루엣을 1.1 배로 한 번 더 깔던 것(SHADOW_BLUR9)은 걷었다(2026-09, 지적: "그림자가 연한 거·진한 거
    한 장씩 두 장") — 배수는 **앵커를 중심으로** 먹으므로 1.1 배 실루엣은 후광이 아니라 **비스듬히 밀린 복사판**이 된다.
    흐림은 그리기를 한 번 더 하는 것으로는 안 난다 — 필요하면 판(FBO)에서 흐려야 한다.
    · **캔버스 타원은 이제 대타뿐이다**(2026-09, 물음: "타원 캔버스 그림자는 이제 걷어낸 거지?") — 공중 유닛에서는
      걷었지만 **부양 지상 유닛**(일꾼·벌처·아콘류)은 문이 `op.air && …` 라, 배율이 문턱을 넘으면 메시 그림자와
      타원을 **둘 다** 받고 있었다(공중에서 한 번 고쳤던 '그림자가 둘'과 같은 자리다). 이제 그 문은 GL 바닥
      그림자의 조건과 **같다** — 타원은 그것이 안 깔리는 프레임에만 선다(덜어내기 1단↑ · 손짓 중 접힘 ·
      `shadowGroundMinZoom` 아래 배율). 그 아래에서도 몸의 자리는 읽혀야 하므로 아주 걷지는 않는다.
      ⚠⚠ **그 문을 갈래 **둘 다**에 달아야 한다**(2026-09, 지적: "프로브 같은 유닛 그림자가 사영 그림자와
        타원 그림자 두 개가 나와") — 부양 갈래에만 `!glShadow9` 를 달았는데, 막힌 몸이 떨어져 나가는 것이
        아니라 **다음 `else if`(땅에 선 몸의 접지 타원)로 굴러들었다**. 부양 지상 유닛은 `op.air` 가 거짓이라
        그 문을 다 지나므로, GL 이 깐 사영 그림자 위에 접지 타원이 한 겹 더 깔렸다.
        ★ 갈래를 `else if` 로 이을 때는 **앞 갈래를 막은 조건이 뒷 갈래의 입구가 되지 않는지** 보라.
    · **나는 몸도 같은 손이다**(공중 유닛): 꼭짓점 z 에 **나는 높이**(uShadow.w, 모형 칸)를 더해 눕히고(그만큼 빛 방향으로
      멀리 눕는다) 앵커가 들린 몫은 화면에서 도로 내린다(dy = lift). 예전에는 몸을 검게 아래로 밀어 그리고 그 아래 캔버스
      타원을 또 깔아, 밀어 놓은 **몸의 복사판** 때문에 그림자가 둘로 보였다(지적). 캔버스 타원은 공중 유닛에서 걷었다 —
      부양 지상 유닛·낮은 배율(GL 바닥 그림자가 안 깔리는 자리)에만 남는다. 이사 중인 건물은 아직 dy 다.
    · **한 화소에 한 겹만** — 그림자는 몸의 삼각형을 그대로 검게 깔므로 겹친 부품(날개 위 포탑)마다 알파가 쌓여 실루엣
      안에 얼룩이 났다. 깊이를 못 박고(uShZ 0.996) 깊이 쓰기를 켠 채 LESS 로 그리면
      같은 깊이의 두 번째 화소가 걸러져 고르게 깔린다(몸 ≤0.99 보다 뒤라 몸을 안 가린다).
  · **닫힌 입체의 뒷면은 버린다**(2026-09, mesh9.solidSigns9 · 정점 aBb 128): 뭉치(pid)의 폴리를 모아 ① 모든 모서리를 두 낯이
    나눠 쓰는 **다양체**인가 ② 이웃을 타고 번져 가며 감기 차례를 맞추고(BFS — 헬퍼마다 감기가 뒤섞여 있다) ③ 부호 있는
    부피로 안·바깥을 정한다. 반투명 뭉치는 속이 보여야 하므로 안 건다. 감기는 **폴리마다** 싣는다(flips) — 한 부품 안에서도
    감기가 섞인다. 버는 것은 **화소 채우기**(닫힌 덩이 넓이의 절반)라 폰의 GPU 몫이 그만큼 준다(부품 66755 중 12894 = 19%).
    ⚠ 이 자는 **2026-09 에 셰이더까지 이어졌다** — 처음 넣을 때(506caef) mesh9 만 고치고 gl9 를 안 건드려, 표식도 셰이더도 없이
    셈만 하고 버렸다(그래서 "그림이 안 바뀐다"는 검산도 저절로 참이었다). 이은 뒤 gl-check 평균 0.181 → 0.180(19종 좋아지고
    8종이 0.002 안쪽으로 나빠진다 — 실루엣 가장자리에서 뒷면이 덮던 한 줄 화소다).
  · **광택(스페큘러)**(2026-09, 요청: "셰이더에 스페큘러 항 넣어서 광택 되살려줘 … 금속뿐 아니라 저그 살점 윤택도 있고") —
    ⚠ 먼저 **없던 까닭**을 적어 둔다: 2D 의 광택 겹(`glowBake9`)은 **부르는 자리가 사라졌다**. 유일한 호출자가 `rasterBld9`
    인데 판 굽기 길을 걷은 뒤 그것을 부르는 것은 `creepPlate9` 하나뿐이고, 그 크립 종류는 바로 그 줄의 `DECAL_KINDS` 로
    걸러진다 — 곧 앱에서 한 번도 참이 안 되는 조건이었다(실측: `[스프라이트] 직접 0`). 그래서 `DEV9.glow`·`glowSet9`·
    폰 1단의 `glow: true` 는 **죽은 스위치**이고, `brushOn9`(결)도 두 쓰임이 모두 glowBake9 안이라 같이 죽어 있다.
    그 몫을 캔버스가 아니라 **정점 셰이더**에서 낸다:
    · **정점에서 재도 정확하다** — 이 메시의 법선은 낯마다 하나(평면 음영)라 한 삼각형 안에서 안 변한다. 화소마다
      다시 잴 몫이 한 톨도 없다.
    · **겹은 둘이다**(옛 GLOW9 와 같은 짜임): ⓐ 빛을 마주 본 낯에 고르게 깔리는 **윤기**(옛 GLOW9.flat ≒ 0.10)와
      ⓑ 한 자리에서 타는 **봉우리**(옛 GLOW9.hot ≒ 0.20). 하나만 두면 어느 쪽도 안 산다 — 봉우리만 두면 다면체라
      몇 낯만 번쩍이고(실측 밝기비 +4%: "티가 안 난다"), 윤기만 두면 그냥 밝아진다.
    · **봉우리는 pow 가 아니라 로렌츠 봉우리**다: `t = (1−n·H)·날카로움` 의 `1/(1+t²)`. pow 는 SwiftShader 에서
      exp2·log2 둘이라 정점마다 지면 비쌌다(실측 헤드리스 폰 4배 조임 p50 267 → 375ms). 곱셈 셋으로 꼴이 거의 같다
      (반값 자리를 맞추면 **날카로움 ≒ 1.44 × 옛 지수**). 반각 H 는 빛·시선이 둘 다 유니폼이라 **CPU 에서 한 번** 낸다.
    · **자는 종족이 든다**(`glossOf9`) — 낯마다 재질을 적으면 빌더 154종을 다 만져야 하고 색으로 짐작하면 틀린다.
      도록 표(`SHAPE_GALLERY`)가 154종을 이미 종족으로 갈라 두었다: 테란 [14, 0.50, 0.30, 0.16, 0.85] · 프로토스
      [18, 0.44, 0.70, 0.14, 0.35] · 저그 [12, 0.22, 0.15, 0.08, 0] · 그 밖 [14, 0.12, 0.30, 0.06, 0.15]
      (날카로움 · 봉우리 · **제 색에 물드는 몫**(금속은 제 색으로 빛나고 살점은 흰 광택이다) · 윤기 · **결**).
    · ⚠ **저그를 넓게 주면 알·고치가 허옇게 뜬다** — 처음에 날카로움 7 로 뒀더니 그것은 봉우리가 아니라 또 하나의
      환경광이었다(lurkeregg 밝기비 1.17 → 1.40, 눈으로도 하얬다). 젖은 살은 '넓게 밝은' 것이 아니라 '한 자리가
      번들거리는' 것이다 — 14 로 좁히고 세기를 내렸다(그래도 테란·토스보다 두 배 넓다).
    · ⚠ 윤기의 각 곡선은 **바닥 0.10 + smoothstep(0.15, 1.00)** 이다(위 방향광의 ★ — 등진 낯도 제 몫의 1할을 받는다).
    · ⚠ **윤기는 바탕 밝기를 태운다**(`0.30 + 0.70·휘도`) — 더하는 값이라 검은 낯에서는 그 몫이 곧 배수다
      (실측: 저그 굴 burrowhole 밝기비 1.48 → 1.79. 하늘을 보는 납작하고 어두운 낯이다). 봉우리는 안 태운다 —
      젖은 검은 키틴에도 반사광 한 점은 맺힌다.
    · **끄면 정말로 안 돈다**: `uGloss` 는 유니폼이라 `if (uGloss.y > 0.0 …)` 가지는 한 그리기 안에서 한쪽으로만 간다.
      손잡이 `#glspec=0|1|N`(표 값에 곱한다) · 기기 표 `DEV9.glSpec`(PC 켬 · **폰은 0단에서 끄고 1단부터 표가 켠다**).
      `#glshade=0` 도 CPU 에서 접어 끈다 — 정점마다 물을 일이 아니다.
    · 값(2026-09 실측): 한 장 **1.21M 삼각 = 3.6M 정점**에 곱셈 여남은 번이다. 헤드리스(소프트웨어 GL·CPU 4배 조임,
      곧 정점 셰이더가 통째로 CPU)에서 중앙값 308 → 358ms 였지만 낱 표본이 217~483ms 로 겹쳐 이 자로는 못 가른다 —
      실기 GPU 에서 정점 몫은 병목이 아니다. 새 패스·새 그리기·새 정점 바이트는 **하나도 없다**(유니폼 둘뿐).
    · ⚠ **테란은 바탕이 이미 회색**이라 봉우리를 제 색에 물들이면(물듦 0.5) 회색 위에 회색을 얹는 꼴이라 대비가
      안 산다(지적: "테란 광택 봉우리 너무 약한 듯"). 프로토스 금이 세 보이는 까닭은 세기가 아니라 **금빛이 제
      바탕과 밝기가 다르기** 때문이다 — 그래서 테란만 세기를 올리고(0.46 → 0.70) **물듦을 내려**(0.50 → 0.30)
      흰빛에 가깝게, 날카로움도 한 칸 좁혔다(23 → 28 · 넓게 퍼지면 그만큼 옅어진다).
      ★ **금속의 광택 세기는 제 바탕색과 얼마나 다른가로 정한다** — 종족마다 같은 숫자를 주면 안 된다.
    · ★★ **봉우리는 둥근 것을, 윤기는 납작한 것을 맡는다**(2026-09, 지적: "동그란 부분은 괜찮은데 테란 건물처럼
      수직 딱딱인 면들은 봉우리가 잘 안 지네") — 셈이 분명하다. 반각 H = 정규화(빛 + 시선) 은 z 성분이 **0.73**
      이라 거의 위를 본다. **세운 낯(nz = 0)이 낼 수 있는 n·H 는 최선이 0.682** 고, 날카로움 28 이면 봉우리가
      0.0087 이다(지붕도 0.0122). 곧 납작한 낯은 봉우리에 **닿을 수가 없다** — 둥근 것은 조각 중 하나가 H 와
      맞아떨어져 제값(0.70)을 받으므로 '둥근 건 괜찮고 납작한 건 안 된다'가 된다.
      그래서 납작한 낯의 광택은 **윤기**(n·L)가 내야 하고, 이것은 한 종족의 일이 아니다(요청: "테란만 올리지
      말고 공통으로") — 넷 다 **날카로움을 넓히고**(28·32·14·17 → 14·18·12·14) **윤기를 올리고**
      (0.04·0.05·0.05·0.02 → 0.16·0.14·0.08·0.06) **봉우리는 도로 내렸다**(한 번 1.45배 올렸던 몫).
      결(vSpec 에 곱한다)도 그만큼 벽에서 보이기 시작한다 — 긁힌 강철은 그 둘이 함께 있어야 읽힌다.
      ⚠ 그전에 테란만 손보며 알아낸 것도 함께 적어 둔다: **금속의 광택 세기는 제 바탕색과 얼마나 다른가로
        정한다**. 테란은 바탕이 이미 회색이라 봉우리를 제 색에 물들이면(물듦 0.5) 회색 위에 회색이라 대비가 안
        산다 — 물듦을 0.30 으로 내려 흰빛에 가깝게 한 것이 세기를 올린 것보다 크게 먹혔다.
    · gl-check 154종: 평균 나쁨 0.174 → **0.190** · 밝기비 1.044 → **1.163** · 0.5 넘는 종류 0(최악 0.498 diamond)
      (가장 밝아진 것은 tanksiegelegs 1.56 → 1.84 · burrowhole 1.48 → 1.65 — 원래도 밝던 작고 어두운 부품들이다). 2D 에 없는 빛이라
      오르는 것이 정상이다(방향광·테두리 빛과 같은 자리).
    · ★★ **봉우리 폭은 낯이 실제로 차지하는 n·H 폭에 맞춘다**(2026-09, 지적: "빛을 90도로 받는 면과 135도로
      받는 면이 있으면 90도로 받는 면은 디테일이 안 보일 정도로 너무 밝고 135도 면에는 이렇다할 봉우리가
      없어" → "아 모든 봉우리가 한 면에 집중이야?") — 그렇다. 바탕 0.45 짜리 테란 낯의 **더한 빛**을 재면:
        경사45°(빛 쪽) n·H 0.933 → 봉우리 **0.267** · 지붕 0.732 → 0.033 · 벽135° 0.659 → 0.021 ·
        벽90° 0.588 → 0.015 · 등진 벽 → 0.003.   **12.7배가 낯 하나에 몰린다.**
      셈이 분명하다: 로렌츠 봉우리의 반폭은 `1/날카로움` 이고 14 면 **0.071** 인데, 이 고정 카메라·고정 빛에서
      **실제 모델 낯이 차지하는 n·H 폭은 0.17~0.93** 이다 — 바늘 하나가 그 폭의 **8%** 만 덮으니 H 와
      맞아떨어진 낯 하나만 타고 나머지는 한 톨도 못 받는다. 다면체에서 그것은 광택이 아니라 '흰 조각 하나'다.
      ★ 그래서 **재질의 교과서 광택도가 아니라 그 폭으로** 날카로움을 정한다: ×0.29(반폭 0.25 — 벽·지붕·경사를
        한 줄로 잇는다) · 세기 ×0.26 으로 **에너지 중립 재분배**(13개 표본 평균 더한 빛 0.0746 → 0.075 로 같다):
        경사45° 0.343 → **0.196**(대비 1.81× → 1.48×) · 벽135° 0.090 → **0.113**(봉우리 몫만 0.021 → 0.046,
        **2.2배**) · 지붕 0.107 → 0.133 · 벽90° 0.033 → 0.049 · 등진 벽 0.012 → 0.013.
        표: 테란 [4.0, 0.13] · 프로토스 [5.0, 0.11] · 저그 [3.5, 0.06] · 그 밖 [4.0, 0.03] · 윤기 바닥 0.10 → 0.05.
      ⚠ **세기를 그대로 두고 넓히기만 하면 그것은 환경광이다**(옛 저그 날카로움 7 의 실패와 같은 자리) —
        넓힌 배수만큼 세기를 내려야 총량이 안 는다. 둘은 **늘 짝으로** 움직인다.
      실측: gl-check 평균 0.187 → **0.184** · 밝기비 1.166 → 1.157 · 47종이 좋아지고 25종이 0.02 안쪽으로
        나빠진다 · **파일런 0.497 → 0.494**(0.5 선의 여유가 오히려 늘었다).
  · **결(긁힌 광택)은 셰이더가 절차적으로 낸다**(2026-09, 요청: "긁힘과 함께 광도 생겨야 하는데" → 광은 위 스페큘러가
    이미 내고 **결은 그 광을 타고 올라탄다**. 옛 2D `BRUSH9` 은 캔버스에 줄을 수십 개 긋는 길이라 GL 로 못 옮긴다).
    · 곱하는 자리가 규약이다: 몸에는 아주 옅게(×0.035) · **광(vSpec)에는 그대로**(×1). 곧 옛 규약 그대로 —
      "결은 낯 전체에 있고, 광택 띠는 그중 어디가 환한가만 정한다". 긁힌 자국은 **빛이 튀는 자리가 갈리는 것**이지
      물감이 바뀌는 게 아니다.
    · **줄 눈금은 모형 좌표로 잰다**(`aNrm`·`aPos`) — 요잉이 돌아도 빛이 바뀌어도 자국이 안 흐른다(결은 낯의 성질이다).
      줄이 서는 쪽은 세계 수직이라, 가로지르는 자는 낯의 **수평 접선** h = (−ny, nx, 0) 이고 눈금은 s = P·h 다.
      낯이 누우면(지붕) h 가 사라지므로 모형 x 로 물러난다. s 는 자리의 **1차 함수**라 정점에 실어 보간해도 정확하다.
    · 무늬는 사인 둘(27·63 · 모형 칸당)에 아주 느린 사인(3.7)으로 **세기를 묶어** 굵은 무리·가는 무리를 낸다.
      ⚠ 처음 9·21·47 로 뒀더니 배율 8에서 8화소 띠가 되어 **골판지**로 읽혔다(실측) — 모형 한 칸이 그 배율에서
        10~20화소(k = 상자px/16 × 정규화)임을 먼저 셈하고 주파수를 고르라.
    · ⚠ **가까이서만 켠다**: k 6 아래 0 · 14 위 full. WebGL1 은 화소 미분(dFdx)이 없어 셰이더가 스스로 못 줄이는데,
      줄 간격이 화소보다 촘촘해지면 결이 아니라 **물결무늬**다. 페이드는 CPU 가 `uGrain` 에 접어 보내므로 낮은
      배율에서는 그 식이 **돌지도 않는다**(유니폼 가지).
    · 이 자는 광택과 달리 **화소마다**다(정점이 아니다) — 그래서 '가까이서만'이 값의 문지기이기도 하다.
      자: 테란 0.85 · 프로토스 0.35(닦은 금이라 옅게) · 저그 0 · 그 밖 0.15. 손잡이 `#glgrain=0|1|N`.
    · ★★ **긁힘은 되풀이가 아니라 드문 사건이다**(2026-09, 지적: "테란 스크래치 … 메탈 헤어라인 느낌이
      아니고 요철 느낌이야") — 처음 짜임은 **진폭이 꽉 찬 사인**(27·63)이었다. 사인은 밝음↔어둠이
      부드럽게 오가므로 그것은 긁힘이 아니라 **둥근 이랑의 음영**이고, 광택에 ×(1 ± 1) 로 걸려 밝은
      벽의 휘도가 **±24%** 흔들렸다 — 골함석이 될 만한 대비다(그림으로 확인: 배럭이 골함석, 커맨드
      돔이 골판 껍질). 헤어라인은 그 반대다: **대개 매끈하고 가는 줄만 드문드문**, 간격도 고르지 않다.
      ① **간격을 되풀이 없이** — 주파수 셋(34 · 43.1 · 26.3)을 무리수에 가까운 비로. 고른 박자가 곧
         '기계로 낸 홈'이다(비가 정수면 몇 화소마다 같은 마루가 서서 이랑이 된다).
      ② **가운데를 눌러 끝만 남긴다**(n·|n| — 부호 있는 제곱). 셋의 합은 어쩌다 한 번만 큰 값에 닿으므로,
         제곱하면 그 드문 마루만 줄로 남고 나머지 낯은 매끈해진다. 이것이 '드문 사건'의 꼴이다.
      ③ **흔들 폭을 줄인다** — 광택 ×1.0 → **×0.55** · 몸 ×0.035 → **×0.012**. 몸(확산)이 띠를 지면
         그건 표면이 울퉁불퉁한 것이다 — 브러시드 메탈의 확산은 고르고, 결은 **반사에만** 있다.
      ⚠ 63 은 k 14 에서 1.4화소로 이미 나이퀴스트 아래여서 지글거렸다 — 걷었다. 맨 위는 43.1(k 18 에서
        2.5화소)이고, 그래서 페이드를 k 6~14 → **9~18** 로 올렸다. 멀리서 헤어라인이 보이는 것은 사실도
        아니다. 실측: gl-check 156종 평균 0.184 → 0.183(움직인 종류 하나) — 결은 그 자가 재는 배율에서
        거의 안 켜지므로 이 손질은 전수 표를 안 흔든다. **눈으로 봐야 하는 값이다**
        (`model-gl --kinds cube,factory,tomb --rots 45 --cell 460`).
    · ★★ **결은 재질이지 낯이 아니다**(2026-09, 지적: "원작을 보면 건물 전체에 헤어라인이 있진 않고
      **보이는 부분이 있는** 거 같아") — 원작 판을 보면 긁힌 마감은 **큰 맨 강철 장갑판**에만 있고
      관·어두운 속·붉은 띠·발판은 맨질하다. 여태는 종족 하나로 켜고 낯을 안 가려 건물이 통째로 같은
      천을 두른 꼴이었다. 낯마다 재질을 적을 수는 없으므로(빌더 154종) **이미 있는 두 자로** 가른다:
      ① **바탕색**(정점의 base) — 채도가 있으면 페인트지 맨 강철이 아니고(임자색·붉은 띠·플라즈마·창),
         너무 어두우면 속 그늘, 너무 밝으면 빛나는 낯이다. 가운데 밝기의 무채색 회색만 남긴다.
         실측 문 값: 강철 회색(#8d94a0·#666e77·#767e87·#8b939d·#7d848d) **1.00** · 어두운 속
         (#3c3c3c·#373d47) 0.12 · 임자색 #2b62e8 · 켠 창 #ffe790 · 프로토스 금 · 붉은 띠 **0.00**.
      ⚠ **부품 차례(aOrd)로 흩는 길은 넣었다가 걷었다** — 판마다 마감을 다르게 주려던 손인데, 표면이
        **여러 부품으로 쪼개진 모델**에서 통째로 무너진다: 스타포트 착륙판은 쐐기 여럿이라 해시가
        쐐기마다 갈려 **부채살**로 떴다(`#glgrain=0` 으로 끄면 사라지는 것으로 범인을 확인했다).
        실물 사진(브러시드 알루미늄)을 보아도 **맨 강철은 판 전체가 고르게 솔질돼 있다** — '보이는
        부분'을 가르는 것은 **재질(색)이지 판 번호가 아니다**.
      결과는 varying 에 실어 넘긴다 — 낯마다 한 값이라 보간해도 안 흐른다.
    · ★★ **결은 겹이 둘이다**(2026-09, 보기로 받은 브러시드 알루미늄 판 사진) — 실물 결은 **빽빽한 잔
      줄이 낯 전체에 깔리고**(길이로 이어진다) 그 위에 **드문 기스 몇 가닥**이 얹힌 것이다. 한 겹만 두면
      둘 다 틀린다: 잔 줄만 두면 긁힌 자국이 없고, 기스만 두면 판이 맨질해서 금속으로 안 읽힌다
      (한 번 기스만 남겼다가 '빽빽한 결'이 빠져 되돌린 자리다). 둘은 **같은 줄자리**(n)를 나눠 쓴다 —
      기스도 솔질 방향을 따라 나기 때문이다.
      ⓐ 바탕 결: `n·|n| × (0.60 + 0.40·q)` — 가운데를 한 번만 눌러 골판지를 막고, 길이 자로 세기를
        느리게 흔들어 줄이 흐려졌다 진해졌다 한다(사진의 그 얼룩덜룩함).
      ⓑ 기스: `n⁵ × 2.0 × smoothstep(0.30, 0.95, q)` — 마루만 남겨 가늘게, 봉투로 양끝을 여위게.
      섞는 자: `g = 바탕·0.8 + 기스` · 몸 ×0.018 · 광택 ×0.45 · 흰 섬광은 기스의 도드라진 쪽만.
      ⓒ **긴 기스도 섞는다**(요청: "판의 크기에 따라 길이도 좀 길기도 해야 할 듯") — 파장을 3.5 배로
        늘린 둘째 봉투를 **더 높은 문턱**(0.55)으로 얹어 드물게만 켠다. 파장이 11 모형칸이라 **작은 부품은
        그 토막을 담을 수가 없고 큰 판에서만 긴 줄이 나온다** — 곧 판이 클수록 기스도 길어진다(면 크기를
        셰이더가 몰라도 되는 길이다).
      ⚠ **줄 수를 늘리면 페이드도 같이 올려야 한다**(요청: "솔질 있는 부분에 좀 더 숫자 늘리기") —
        주파수 ×1.3(34·43.1·26.3 → 44.2·56·34.2)과 함께 페이드를 k 9~18 → **11~21** 로 올렸다.
        촘촘해진 만큼 한 줄이 차지하는 화소가 줄기 때문이다(k 21 에서 맨 위 56 이 2.4화소).
        ★ 이 '한 벌의 고정 주파수 + 통짜 페이드'는 아래 ★★ 에서 **옥타브**로 바뀌었다.
    · ★★ **결은 배율을 타야 한다 — 주파수를 못 박으면 가까이서 굵어지기만 한다**(2026-09, 지적:
      "테란의 메탈 결 — 스케일을 생각하면 결이 훨씬 더 얇아야 하고 밀도도 높아야 해"). 셈이 분명하다:
      주파수가 고정이면 화소 주기가 **배율에 비례**해 커진다(맨 위 56 이 k 21 에서 2.4화소 · k 31 에서
      3.5화소 · k 47 에서 5.3화소). 실물 금속은 반대로, 다가갈수록 **더 가는 줄**이 드러난다.
      · 옥타브를 **다섯**(30·42·58·80·110 rad/모형칸)으로 늘리고, 몫을 **CPU 가 배율로 깎아** 보낸다
        (`uGrain.y` + `uGrainA` · `grainOct9`): 화소 주기가 2.1 아래면 0, 3.1 위면 제 몫.
        곧 **멀리서는 굵은 줄 몇, 가까이서는 가는 줄 여럿**이고, 화소보다 촘촘한 줄은 애초에 안 켜지므로
        물결무늬가 안 진다(WebGL1 은 dFdx 가 없어 셰이더가 스스로 못 줄인다).
      · ⚠ **몫의 합을 1 로 맞추지 마라** — 맞추면 저배율에서 가는 옥타브가 꺼진 몫까지 굵은 옥타브가
        떠안아 나이퀴스트 아래에서 **혼자 커진다**. 안 맞추면 합이 곧 '지금 보이는 결의 총량'이라
        멀어질수록 저절로 여윈다(0.34 → 1.00).
      · ⚠⚠ **거듭제곱으로 줄을 얇게 만들려 하지 마라** — n·|n| → n³ 로 날을 세워 봤더니, 줄이 얇아진 것이
        아니라 **느린 세기 봉투의 마루만 남아 굵은 띠**로 뭉쳤다(실측 렌더: 착륙판에 폭 100화소짜리 쓸린
        자국 몇 줄). **얇고 촘촘한 것은 주파수가 내고, 안 거슬리는 것은 낮은 대비가 낸다** — 그래서
        바탕 결은 사인 합 그대로 두고 광택에 **±13%** 로만 얹는다(옛 ±45%). 기스만 ±55% 로 도톰하게
        따로 얹는다(한 값으로 묶으면 잔 줄을 보이게 올릴 때 기스가 골함석이 된다).
      · ⚠ 세기 봉투도 함께 잘게 쪼갠다(3.1 → 9.0 · 흔들 폭 0.62±0.38 → 0.80±0.20) — 파장이 모형 두
        칸이면 그 마루가 가까이서 **넓은 얼룩**으로 읽힌다.
      · 켜는 문(k ≥ 11)은 이제 **값의 문지기**일 뿐이다 — 나이퀴스트는 옥타브마다 따로 보므로, 그 아래
        에서는 어느 옥타브도 몫이 0 이라 튐 없이 꺼진다.
      · 값: 화소당 사인 셋 → 다섯(결이 켜진 화소에서만 · 테란·프로토스만 · 가까운 배율만).
        눈으로 봐야 하는 값이다: `model-gl --kinds plane,cube --rots 45 --cell 760`.
    · ★★ **줄에는 자가 둘 필요하다**(2026-09, 요청: "헤어라인은 얇고 **양끝은 더욱 얇아야** 해 — 기스
      느낌" · "빛을 받는 면의 헤어라인은 유난히 밝게 빛나기도 해(거의 흰색)") — 가로지르는 자(vGr.x)
      하나만으로는 줄이 **끝이 없다**(그 자의 함수이므로 길이 방향으로 값이 안 변한다 = 낯을 가로지르는
      무한한 줄). 기스는 시작과 끝이 있는 **토막**이다. 그래서 낯 위의 **둘째 축**을 함께 싣는다:
      줄이 서는 쪽이 세계 수직이므로 세운 낯은 **높이 z**, 누운 낯(지붕)은 모형 y 다(vGr.y).
      · **길이 봉투** `smoothstep(0.30, 0.95, ½+½·sin(1.9·y + 5.3·x))` 로 토막을 내면 양끝이 여윈다.
        ⚠ 위상에 **가로 자를 섞어야**(×5.3) 한다 — 안 섞으면 이웃 줄의 토막 끝이 한 줄로 서서
        **가로 띠**가 된다(줄이 아니라 무늬가 된다).
      · **n⁵ 로 더 얇게** — n·|n| 는 아직 굵었다. 셋의 합은 어쩌다 한 번만 큰 값에 닿으므로 제곱을
        거듭할수록 그 드문 마루만 가는 줄로 남는다(이득 2.0 으로 여윈 마루를 도로 세운다).
      · **흰 섬광은 빛 받는 낯에서만** — '얼마나 빛을 받나'는 이미 광택(vSpec)이 들고 있다. 그 휘도를
        곱하면 등진 낯에서 그 항이 **저절로 0** 이 되므로 문을 따로 둘 일이 없다. 도드라진 쪽(g>0)만
        태운다 — 기스는 빛을 튕기는 자국이다(`+ w·2.4·max(g,0)·휘도(vSpec)`).
      실측: gl-check 156종 평균 0.185 · 밝기비 1.161 — **움직인 종류 0**. 이 자가 재는 배율에서는
      기스가 몇 화소라 표를 안 흔든다. **눈으로 봐야 한다**(`--kinds cube,tomb,plane --rots 45 --cell 500`).
    · ★ **광택을 내는 빛에 색을 줄 수 있다**(2026-09, 요청: "테란 건물에 빛나는 효과 … 불빛을 좀
      청색톤을 넣을 수 있나") — `uSpecTint` 를 새로 두고 광택 표의 여섯째 칸이 그 색을 든다.
      테란만 **찬 강철빛** [0.82, 1.02, 1.36] 이고 나머지는 흰빛이다. 함께 테란의 **물듦을 0.30 → 0.18**
      로 내렸다 — 회색 바탕이 섞이면 파랑이 죽는다.
      ⚠ **휘도를 1 로 맞춰 둔다**(0.299·0.82 + 0.587·1.02 + 0.114·1.36 = 1.00) — 색만 바꾸고 밝기는
        안 건드리므로 0.5 선이 안 흔들린다. 색을 더 세게 하려면 이 비를 지키며 기울여라.
      ⚠ 이것은 **일부러 2D 와 달라지는 값**이다 — gl-check 평균 0.183 → 0.185 · 테란 35종이 0.01 안팎
        나빠진다(색차를 재는 자이므로 당연하다). 0.5 넘는 종류 0 · 최악 0.494(diamond)는 그대로다.
    · ★★ **넓은 낯의 '전구 반사'는 거울 반사로는 못 낸다 — 얼룩으로 그려야 한다**(2026-09, 요청: "넓은
      면에도 봉우리가 있어서 전구나 태양빛이 반사되듯한 강한 부분이 있으면 좋겠는데 되나?") — 셈이
      분명하다. 이 카메라는 40도로 **내려다본다**(시선 c = (0, cos40, sin40)). 세운 낯(nz = 0)이 어떤
      빛을 거울로 비치려면 그 빛이 반사 방향 `r = 2(n·c)n − c` 에 있어야 하는데, 그 **z 성분은 어떤
      벽에서도 −0.643** 이다 — 곧 **지평선 아래**다. 빛을 어디에 놓아도(해든 전구든, 평행광이든
      점광원이든) 세운 벽에는 거울 반사가 안 맺힌다. **벽이 비추는 것은 하늘이 아니라 땅**이기 때문이다.
      (반각을 정점마다 재는 점광원 길도 따져 봤지만 최고값 0.68 은 그대로다 — 자리마다 조금 달라질 뿐이다.)
      그래서 이것은 **그리는 몫**이다: 빛 쪽 한 점(LIGHT×7)을 잡고 거기서 멀어질수록 여위는 밝은 얼룩을
      모형 자리에 얹는다 — 옛 2D 광택 겹(glowBake9)이 캔버스 그라디언트로 하던 바로 그 몫이고, 판 길을
      걷으며 사라졌던 것이다. 이제 모형 공간이라 **요잉을 돌려도 얼룩이 같이 돈다**.
      ⚠ 자리는 **요잉을 먹인** 좌표(rx, ry, z)라야 한다 — aPos 를 그대로 쓰면 개체가 돌 때 얼룩이 몸에
        붙어 같이 돌아 빛이 아니라 무늬가 된다. ⚠ 등진 낯에는 안 얹는다(×ndl).
      ⚠ **세기는 파일런이 정한다** — 종족 구분 없이 주면 diamond 가 **0.500** 으로 선에 닿는다(실측).
        표에 일곱째 칸을 두어 테란 1.0 · 프로토스 0.40 · 저그 0.60 · 그 밖 0.50 으로 나눴다(diamond 0.498).
    · ★ **푸른빛은 센 자리에만**(2026-09, 요청: "강하게 빛나는 곳은 푸른색을 입히고 아닌 곳은 원래색이
      나오게") — 광택 전체에 한 색을 먹이면 판이 통째로 차가워진다. **세기로 가른다**:
      `hot = smoothstep(0.08, 0.30, 윤기+봉우리+얼룩)` 으로 약한 쪽은 **제 바탕색**(물듦), 센 쪽은
      찬 강철빛. 곧 **물듦은 약한 쪽의 색이고 uSpecTint 는 센 쪽의 색**이다 — 그래서 테란 물듦을
      0.18 → **0.55** 로 도로 올렸다(약한 자리가 제 색으로 보이라고).
    · ★ **봉우리 높이는 파일런이 정한다**(2026-09, 지적: "빛나는 봉우리가 너무 약한 듯 티가 잘 안 남") —
      폭을 넓히며 에너지 중립으로 내렸던 세기를 도로 올렸다: 테란 0.13 → **0.24**(거의 두 배) ·
      저그 0.06 → 0.08 · 그 밖 0.03 → 0.04. ⚠ 프로토스는 **0.18 까지 올렸다가 0.13 으로 되물렸다** —
      그 값에서 diamond 가 **정확히 0.500** 으로 선에 닿았기 때문이다(0.13 에서 0.496).
      **봉우리를 올릴 때는 늘 diamond 를 먼저 보라** — 가장 먼저 선에 닿는 종류다.
      값(전체): 평균 0.185 → **0.194** · 밝기비 1.161 → 1.193 · 0.5 넘는 종류 0.
      ⚠⚠ **GLSL 주석에 역따옴표를 쓰지 마라** — 셰이더는 JS 템플릿 문자열 안에 있어서 역따옴표 하나가
        문자열을 끊고 `TS1005` 로 터진다. 이 자리에서 두 번 겪었다(`#glspec=0` · n·|n|).
  · **번짐(블룸)**(2026-09) — 빛나는 낯만 **1/4 크기 판**에 한 번 더 그리고(정점 셰이더가 나머지를 클립 밖으로: uEmit),
    가로·세로로 흐린 뒤(다섯 번 읽는 가우시안 · 핑퐁 판 둘) 화면에 **더한다**. 2D 판에는 없던 몫이다.
    · **밝기 문턱으로 안 고른다** — 색만 보고는 '빛'과 '진한 물감'을 못 가른다(프로토스 금 #e6d063 은 휘도 0.85·채도
      0.51 로 켠 창 #ffe790 과 거의 같다). 빛나는 자리를 **표식으로** 고른다: 발광 종류(폭풍·핵·아콘·워프인)는 밝은
      색 면만(어두운 속 몸·임자색 면은 몸이다), 그 밖은 `shapeOblique.EMIT_FILL9` 에 든 색만 — 그 표는 **켜지는 자리가
      스스로 적는다**(bake9 `winLit`·`glowLit` 이 불이 켜질 때 제 색을 넣는다. 늘 빛인 플라즈마는 미리 적혀 있다).
      ⚠ 그래서 표는 **굽는 차례를 탄다**(켜진 건물을 아직 안 구운 판에서는 그 색이 표에 없다) — 메시 열쇠가 불빛(litTag)을
      가르므로 꺼진 판이 빛나는 일은 없지만, 새 빛 색을 넣을 때는 winLit·glowLit 을 지나게 하라.
    · 정점 표식은 aBb 한 칸의 **비트**다: 1 빛 · 2 닫힌 입체 · 4 빌보드.
    · 값은 거의 다 '화면 한 겹 더하기'라 **가위(scissor)로 빛이 있는 자리만** 훑고, 화면에서 14px 보다 작은 개체는
      건너뛴다(낮은 배율에서는 그 자로 거의 다 걸러져 번짐이 저절로 꺼진다). 실측(헤드리스 폰 프로필·CPU 4배 조임):
      전체 화면 8배 → 가위·작은 것 걸러내기 뒤 1.4배. PC 프로필은 두 쪽 다 p50 67ms(헤드리스 프레임 박자)로 차이가 안 보인다.
    · 기본은 **기기 표 + 벤치 단**(`DEV9.glBloom`): PC 켬 · 폰은 **1단(보통)부터 켠다**(PHONE_TIERS9 — 요청:
      "번짐도 보통 단에 넣어줘". 가위로 빛 있는 자리만 훑으므로 폰 프로필 p50 이 같았다: 66.6 → 66.6). `#glbloom=0|1` 로 끄거나 못 박는다.
      벌의 `meshMax`·`bloomOn` 은 읽기 전용이 아니다 — 단이 오르면 `glUnits9` 가 다음 칠하기에서 그 벌에 새 값을 일러 준다.
      `perf-check --shot x.png --probe-gl` 첫 줄이 그 둘을 찍는다(`상한 360벌 · 번짐 켬`).
    · ⚠ **`model-gl` 은 기본으로 번짐을 끈다**(`--hash` 기본값 `glbloom=0`) — 옛 2D 견줌에서 온 값이다
      (2D 에 없는 빛을 얹으면 발광 종류의 색차가 통째로 뛰었다: 폭풍 0.04 → 0.59). **번짐까지 눈으로 보려면
      `--hash ""`** 로 켜라 — 이제 2D 와 안 견주므로 끌 까닭이 그림 취향 말고는 없다.
  · **폴리곤은 귀 잘라내기로 삼각화한다**(2026-09, gl9 `triIdx9` · 지적: "넥서스 … 피라미드에서 나오는 삼각발판") —
    여태 **0번 꼭짓점 부채꼴**이었다. 그것은 볼록하거나 0번에서 별꼴인 다각형에서만 옳다: 넥서스의 표창 발판 윗면은
    **네 날 별**(날 끝 ↔ 오목점이 번갈아 여덟 꼭짓점)이고 0번이 날 끝이라, 부채질이 **날 사이 오목한 자리까지 메워**
    날이 넓은 판때기로 퍼졌다(2D 는 Path2D 가 제 꼴로 채우므로 안 났다 — 붓이 다르면 드러나는 자리다).
    이제 낯의 법선에서 지배 축을 빼 2D 로 눕히고 **볼록하면 종전 부채꼴**(빠른 길)·**오목하면 귀를 하나씩** 자른다.
    삼각형 수는 늘 n−2 라 미리 잡은 버퍼가 그대로 맞고, 꼭짓점이 서른 남짓이라 O(n²)도 값이 없다(메시는 한 번 짓는다).
    실측: gl-check 평균 0.176 → 0.175 · 11종이 좋아졌다(넥서스 0.203 → 0.146 · 뮤탈 −0.035 · 벌처 −0.021 · 크립얼룩 −0.015).
  · **몸 밖을 감는 장식은 2D 에서 '걷지' 말고 키를 낮춰 뒤로 보낸다**(2026-09, 고치 밑동의 장기 파이프) — 껍질 **위**를
    타는 핏줄은 등진 것이 실제로 안 보이므로 `facingRatio` 로 걷어도 되지만, 껍질 **밖**을 감는 관은 등진 것도 옆구리로
    삐져나온다. 걷으면 2D 에만 그 몫이 없어 GL 과 어긋난다(실측: gl-check 0.315 → 0.393 · GL 화소가 28% 많았다).
    뒤쪽 것을 **몸보다 낮은 키**로 따로 묶어 화가 차례가 몸 뒤에 깔게 하면 두 붓이 같은 그림이 된다(0.393 → 0.196).
  · **평평한 네모로 흉내 낸 '이음매'는 GL 에서 판때기다** — 고치(cocoon)는 껍질 두 쪽이 맞물린 자리를 그 높이의
    **현(弦)에 깐 네모 + 옅은 덮개**로 냈다(몸 면 없는 덧칠). 2D 는 화가 차례가 돔 위에 얹어 그늘로 읽혔지만, 진짜
    깊이에서는 그 네모가 굽은 낯을 뚫고 지나가 **반투명한 직사각형 띠**로 떴다(지적) — 걷었다. 그런 결은 화면 자의
    네모가 아니라 **회전 단면을 타는 관**(고치 핏줄처럼 spirePillar)으로 지어야 한다.
  · **되찾은 원의 조각 수**(mesh9 RSEG9 32 · meshSphere9 12×20): 16 조각이면 핵 충격파·워프인의 둘레가 다각형으로 읽혔다.
  · GL 캔버스는 **미리곱한 알파**(premultipliedAlpha: true · 셰이더가 rgb·a 를 내고 합성 (ONE, 1−a)) — 예전(SRC_ALPHA 짝)은 알파 채널이
    a² 로 쌓여 반투명 빛무리가 어두웠다(밝기비 0.75 → 1.0). 더하기는 (ONE, ONE).
  · **폰도 기본 켬(2026-09)** · 정점은 **36바이트**(pos·nrm float, rgb·team·alpha·덧칠·빌보드는 정규화 바이트, 부품 차례 float — 예전 float 15개 60B)이고
    footOf 용 사본은 **겹치지 않는 꼭짓점 xyz 만**(정점 사본 통째 270KB/벌 → 수십 KB). 메시 상한은 기기 표 `DEV9.glMeshMax`(PC 600 · 폰 240;
    실측 폰 프로필 157벌 24MB → 240벌 37MB). 데우기는 rAF 당 6ms·8벌로 나뉜다. 처음 보는 종류의 메시를 지은 몫은 `glBakeMsTake9()` 가
    프레임마다 내고, 시계가 그것으로 '굽는 프레임'을 안다(옛 판 굽기의 그 문지기 자리).
  · ★ **메시 보관함은 LRU 여야 한다**(2026-09, 지적: "모바일에서 화면 이동도 안 하고 유닛 변화도 거의 없는데
    모델 굽는 중이 계속 나오는 현상") — `meshFor` 가 **찾았을 때 다시 안 넣어** 버리는 자가 Map 의 **삽입 차례**를
    보고 있었다(FIFO). 삽입 차례는 '얼마나 오래되었나'이지 '얼마나 안 쓰나'가 아니다 — 매 프레임 쓰는 메시일수록
    먼저 늙어, 상한에 닿는 순간 **가장 많이 쓰는 것부터** 버려지고 곧바로 다시 지어져 다음 것을 밀어낸다.
    · 갈리는 자리는 **열쇠 churn** 이다: 포탑 각(headTag)·건설 단계·불빛·회전 칸 때문에 새 열쇠가 끊이지 않는다.
      정책만 떼어내 흉내 낸 값(상한 240 · 늘 쓰는 메시 150벌 · 600프레임): 새 열쇠가 프레임당 둘이면 FIFO 는
      **늘 쓰는 메시를 프레임당 3.25벌** 다시 굽고, 하나뿐이어도 1.5벌이다. LRU 는 **0**이다.
      ⚠ churn 이 없으면 둘이 **똑같다**(한 번 다시 구우면 그것이 곧 맨 뒤가 되므로) — 짧은 계측으로는 못 가른다.
    · 그래서 화면이 한 톨도 안 움직여도 굽기가 영영 이어지고, '모델 굽는 중' 띠가 안 꺼진다. 게다가 시계가
      **굽는 프레임에는 시간을 안 보내므로**(BAKE_HOLD) 유닛도 거의 안 움직인다 — 그 둘이 한 뿌리였다.
    · 진단: `#diag=draw` GL 줄의 `버림 N` (`stat.evict`). **0 이 아니면 보관함이 좁다**. 상한을 손으로 바꿔 재현하려면
      `#glmesh=N`.
  · ★★ **도는 부품은 열쇠가 아니라 유니폼으로 돌린다**(2026-09, 지적: "아까 모델 굽는 중 뜨는 거 다른 원인도
    있나 봐 — **서플라이랑 터렛 많은 화면**에서 그래") — LRU 로 고친 뒤에도 남은 둘째 뿌리다.
    · 미사일 터렛은 **쉴 때도 96°/s 로 돈다**(idle 탐지 회전). 그 각을 `headTag` 가 **메시 열쇠에 박는다**.
      칸 수(7.5도)는 **판 굽기 시절**에 고른 값이고 근거는 바이트였다 — "터렛 판은 2×2 발자국의 작은 판이라
      48장이라도 몇 MB". 그런데 **메시 보관함의 상한은 바이트가 아니라 벌 수**(`DEV9.glMeshMax` · 폰 240)다.
      곧 그 근거는 붓이 바뀌며 **통째로 무효**가 되었는데 값만 남아 있었다.
      실측(`perf-check --wide --probe-gl`, 테란 킷 = 터렛 4채·서플라이 12채): 터렛 열쇠 **93벌**(각이 접힌 뒤에도
      요잉과 엇갈려 48보다 많다) · 메시 총 **157벌** · VBO **26.4MB** · 누적 굽기 **2250ms**.
    · ★ 고칠 자리는 칸 수가 아니라 **각이 열쇠에 드는 것** 자체다 — 요잉·시각 밀림이 이미 그 길을 갔다
      (카메라 유니폼이라 열쇠에 안 든다). 터렛 머리는 `withModelSpin(headYawNow)` 한 겹이 통째로 감싼
      **강체 회전**이므로, 그 겹을 떼어 **제 딸림 부품**으로 만들면 그 부품의 '모델 각'이 곧 **개체 요잉**이다.
      붓이 `op.attachRot`(딸림 부품만의 절대 요잉)로 그 각을 주면 메시는 **한 벌**로 끝난다 —
      **93 → 2벌 · 157 → 67벌 · 26.4 → 9.9MB · 2250 → 736ms**. 성큰이 혓바닥을 갈라낸 그 규약(op.attach)이다.
    · 가를 때 함께 봐야 하는 것 넷(하나라도 빠지면 조용히 어긋난다):
      ⓐ **도록 표(SHAPE_GALLERY)에 두 별본을 적는다** — 광택 표(`glossOf9`)가 그 표로 종족을 찾으므로, 빠지면
        터렛이 테란 광택을 잃고 '그 밖' 값으로 그려진다.
      ⓑ **`BLD_NORM_PAIR` 에 접는다** — 배수(`bldNormOf`)·앵커 열쇠(`bldAnchorKey`)·잉크 가운데(`bldMidK9`)가
        한 벌이라야 머리가 밑동 위에 앉는다. ⚠ `ink-center --emit` 은 SHAPE_BUILDERS 를 다 돌아 **별본의 줄까지
        찍어 낸다** — 붙일 때 그 줄은 빼라(제 줄이 있으면 접기보다 그것이 이겨 둘이 따로 논다).
      ⓒ **잉크 상자는 딸림 부품까지 아울러 잰다**(footOf 둘의 합집합) — 터렛은 몸의 절반 넘는 몫이 머리라,
        밑동만 재면 체력바·링·총구 앵커가 통째로 내려온다. 각은 **몸의 요잉으로 못 박아** 재라(도는 각으로 재면
        상자가 프레임마다 바뀐다). 덤으로 종전보다 안정적이다 — 옛 상자는 '그때 그 머리 각'의 것이었다.
      ⓓ **폴백(#gl=0)도 딸림 부품을 그리게 한다** — 여태 `op.kind` 하나만 그려 성큰 혓바닥이 통째로 빠져 있었다
        (터렛은 '머리 없는 터렛'이 될 참이었다).
    · ⚠ 깊이 검사(`model-depth-check`)는 빌더 본문을 **글자로** 훑는 자다 — 한 본문 안에 `partKey` 가 한 번이라도
      있으면 그 빌더는 통과한다. 그래서 **빌더를 가르면 가려져 있던 옛 열쇠가 드러난다**(turret 의 머리 쪽
      `20 + depthNow` 가 밑동의 partKey 에 가려 있었다). 새로 어긴 것이 아니므로 기준선에 더하되, 커밋에 까닭을 적는다.
    · 서플라이(trapezoid)는 회전 8칸 × 불빛 2 = **16벌로 이미 닫혀 있다**(`% SPIN_STEPS`) — 실측 열쇠도 여덟이다.
      곧 '애니메이션 건물'이 다 문제인 것이 아니라, **각을 칸으로 안 접고 열쇠에 싣는 것**이 문제다.
  · ★ **번짐(블룸)은 헤드리스에서 3초짜리 거짓 회귀를 낸다**(2026-09, 조사) — perf-check 에서 번짐을 켜면 한 장이 3~9초로
    튀었는데, 단을 하나씩 빼 보니(깊이 선행 · 발광 · 흐림+합성) 어느 것을 빼도 안 줄고, **번짐 판 FBO 를 바인드하고 지우기만
    해도** 3133ms 였다(판을 짓고 곧장 돌아오면 283ms). SwiftShader 가 렌더 타깃을 바꿀 때 치르는 값이지 GPU 일이 아니다.
    옛 측정에서 '번짐 켜도 같다'였던 까닭은 화면에 빛나는 개체가 없어(stat.bloom 0) 그 길을 안 지났기 때문이고, 늘 켜진 빛
    (파일런 보석·캐리어 창·부속 활성)이 EMIT_FILL9 에 든 뒤로는 매 장 지난다(stat.bloom 12). perf-check 는 이제 기본으로
    `glbloom=0` 을 붙인다(`--bloom` 으로 도로 켬). ⚠ 실기에서 번짐의 참값은 '합집합 상자 안의 몸을 깊이로 한 번 더 그리기'
    뿐이다 — 빛이 화면에 흩어져 있으면 그 상자가 곧 화면 전체다(줄이려면 상자를 무리로 가르는 것이 다음 손이다).
  · **헤드리스는 GL 을 못 잰다**: 크로뮴 헤드리스는 소프트웨어 GL(SwiftShader)이라 보이는 WebGL 캔버스의 합성(옛날엔 2D `drawImage` ·
    지금은 브라우저 합성기 — 실측 p50 3117ms)이 ReadPixels 로 서서
    1454² 한 장에 1~2초(실측, 옵션 무관)고 삼각형 채우기도 CPU 다. perf-check 는 기본으로 `#glblit=0`(합성만 뺌 — GL 은 다 돈다)을 붙여
    GL 의 CPU 몫만 잰다(`--glblit` 로 도로 붙임). PC 프로필 gl=0/gl=1 둘 다 p50 67ms(헤드리스 프레임 박자)라 차이가 안 보인다 — GPU 채우기·
    첫 진입 덜컥임은 실기에서만 안다.
  · **도록 아이콘 `DocIcon9`**(ReplayMotionPlayer): GL 이 켜져 있으면(GL_ON9 · `gl` 프롭) `<img>` 에 메시 그림, 아니면 ShapeIcon(SVG).
    손은 gl9 `glIconRequest9` — 한 프레임의 청을 모아 숨은 GL 캔버스 한 판(4096² 상한, 격자)에 그리고 **한 번 읽어** 칸마다 PNG dataURL 로
    나눠 준다(아이콘마다 문맥을 열면 상한 16에 걸리고, 헤드리스는 읽기 한 번이 1~2초). 자·원점은 gl-check 와 같다(16-상자 x = 8 + rx ·
    y = 12 + Y). 창은 ShapeIcon 의 viewBox 규약 그대로(fit 은 footOf 상자 + 짧은 변 12%·fitBox·wide·16-상자). 평면(flat)만 — 입체 보기는
    SVG 로 떨어진다. 색은 요소의 currentColor(--scr-doc-own).
  · 진단: `#diag=draw` 'GL' 줄(개체·삼각·메시/상한(굽기 ms·VBO MB·**버림**)·깊이칸/bit·번짐 개체 수·판으로 떨어진 종류) · `#glmesh=N` · `#glspec=0|N` · `#glgrain=0|N` · `#glshade=0|1|2` · `#gldepth=0` · `#glbias=N` · `#gllod=N` · `#glbloom=0|1` ·
    `#glwarm=0` · `#glblit=0`. 계측 `perf-check --hash gl=0`(캔버스 비교) `--shot x.png --probe-gl`(메시 표: 삼각·색·KB) `--warm 0`. 메시만 따로 보려면 `scripts/model-mesh.mjs`.
  · **전수조사 `node scripts/model-gl.mjs [--kinds a,b] [--rots 45,225] [--rows 40] [--out <scratch>/gl.png] [--json x.json] [--vs2d]`** —
    156종을 **GL 로만** 굽고 눈으로 보는 시트다(5초). 재는 것은 하나뿐이다: **칸마다 실루엣이 있나**
    (빈 칸 = 빌더가 터졌거나 통째로 안 그려졌다 — 그것은 여전히 자동으로 잡을 값어치가 있다).
    ★★ **2D 와 견주는 자는 걷었다**(2026-09, 물음: "이제 gl-check 필요없지 않아? 이전의 2D 캔버스 붓은
      더 이상 기준이 아니고 새로운 세계가 시작됐어") — 옛 `gl-check` 의 '나쁨'(1−IoU + 색차)은 **틀린 쪽을
      가리키게 되었다**:
        · 사이언스 베슬의 몸통이 GL 에서 **땅에 누운 판때기**였던 진짜 버그를 고치자 점수가 **나빠졌다**
          (0.14 → 0.21) — 2D 가 그리는 것이 납작한 타원이기 때문이다.
        · 방향광·테두리 빛·광택·결·푸른빛·번짐은 **2D 에 없는 몫**이라 GL 이 좋아질수록 평균이 오른다
          (0.174 → 0.190). 그 0.5 선 때문에 광택 세기를 여러 번 되물렸다 — **없어진 붓에 맞춰 새 붓을 깎은 셈**이다.
        · '빠진 부품이 없나'는 이제 `model-mesh --check`(덮임 100%)가 **정확히·1초에** 본다.
      그래서 이 자는 **눈으로 보는 시트 + 빈 그림 관문**만 남는다. 2D 폴백(`#gl=0`·도록 SVG)을 감사할 때만
      `--vs2d` 로 옛 견줌을 켜고, 그때도 그 수는 '**2D 폴백과의 차이**'이지 '나쁨'이 아니다 — 낮추려 들지 마라.
    ⚠ 자세 0 에서 비는 것이 맞는 종류는 명단으로 뺀다(`EMPTY_OK9` — 시즈 전환의 뒤 포신은 나온 몫이
      `poseNow / 5` 라 자세 0 에서 길이가 0 이다).
    · 아래·위의 옛 숫자들(평균 0.17~0.19 · 밝기비 · '0.5 넘는 종류')은 **그 옛 자로 잰 기록**이다 — 자취로 남겨 둔다.
    ★ **마주 봄 판정(facingRatio)은 GL 에 없다** — 2D 는 '뒤로 돌아간 장식'(돔 둘레의 구리띠 따위)을 아예 안 그렸지만,
      메시는 요잉을 안 싣고 셰이더가 돌리므로 기록 때 그 문을 닫을 수가 없다. 되살리는 두 길을 다 재 보고 접었다(2026-09):
      ① 빌더가 물어 온 법선을 tagKey 도장으로 부품에 딸려 보내기 — 프리미티브도 제 안에서 facingRatio 를 부르는 탓에
         도장이 엉뚱한 부품에 붙고, 꼴·크기로 걸러 내면 전체 부품의 2.2%(12종)에만 남는다(정작 커맨드는 0).
      ② 꼴로 판정해 한 장짜리 세운 낯을 등졌으면 걷기 — 저그 막·아비터 날개처럼 **진짜 몸**인 한 장짜리 판이 걷혀
         47종이 나빠졌다(0.179 → 0.188 · 하이브 23292 → 20015 화소).
      부감 카메라에서 돔의 먼 쪽 벽은 실제로 보이므로 그 위의 장식도 보이는 것이 옳다 — 2D 의 '안 그리기'가 속임수였다.
      붐비는 모델이 있으면 그 빌더에서 줄인다.
